$ PowerShell: writing text into a notepad window with SendMessage

The following example uses the two → Windows/development/WinAPI functions → Windows/dirs/Windows/System32/user32_dll/SendMessage[`SendMessage()`] and `FindWindowEx()` to write text into two
→ Windows/dirs/Windows/System32/notepad_exe[`notepad.exe`] instances.

gh|about-PowerShell|/examples/WinAPI/SendMessage.ps1||

sa:
  Other → Windows/PowerShell/examples
