$ PowerShell command line parameter -Command

`-Command` (which can be abbreviated with `-c`) specifes a → Windows/PowerShell/language/command[command] to be executed in PowerShell.
-
After executing the command, PowerShell exits.

sa:
  → Windows/PowerShell#ps-cmd-line-opts[Other command line parameters]
