$ PowerShell parameter attribute: alias

{ Question mark as parameter name

  The Parameter attribute `alias` comes in handy to define a question mark → Windows/PowerShell/language/statement/function/parameters[parameters]:
  gh|about-PowerShell|/language/statement/function/parameters/attributes/alias/qm.ps1||

code {
PS:> .\qm
doing stuff

PS:> .\qm -?
help message
code }

  Unfortunately, this technique does not seem to work when trying to alias an exclamation mark to a parameter.

}
