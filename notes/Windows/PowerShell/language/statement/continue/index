$ PowerShell: continue statement

The `continue` statement allows to → Windows/PowerShell/language/statement/foreach#ps-foreach-continue[skip specific iterations] in loops. While → Windows/PowerShell/language/statement/break[`break`] terminates a loop immediatly, `continue` resumes the loop
with its next element.

sa:
  → Windows/PowerShell/language/statement[Other statements]
