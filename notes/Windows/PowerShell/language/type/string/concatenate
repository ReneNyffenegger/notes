$ Concatenate strings in PowerShell

code {
'foo', 'bar', 'baz' → Windows/PowerShell/language/operator/string-manipulation#ps-split-join[-join] ''
→ Windows/PowerShell/language/operator/string-manipulation#ps-split-join[-join]('foo', 'bar', 'baz')
code }

code {
$('foo' + 'bar' + 'baz')  
code }
