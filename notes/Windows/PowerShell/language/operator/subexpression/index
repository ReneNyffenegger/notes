$ PowerShell: subexpression operator

The subexpression → Windows/PowerShell/language/operator[operator] (`$(…)`) is primarily used in strings. It evaluates the expression within its parantheses and «merges» the result into the string.

gh|about-PowerShell|/language/operator/subexpression/dayOfYear.ps1||

sa:
  → Windows/PowerShell/language/operator
