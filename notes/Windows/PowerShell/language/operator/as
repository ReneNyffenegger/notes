$ PowerShell: -as operator

The `-as` → Windows/PowerShell/language/operator[operator] casts a value into a different → Windows/PowerShell/language/type[type].

gh|about-PowerShell|/language/operator/as.ps1||

{ Casting strings to types

code {
'→ Microsoft/dot-net/namespaces-classes/System/Int32[system.int32]' -as [→ Microsoft/dot-net/namespaces-classes/System/RuntimeType[type]]
code }

}

sa:

  A value can also be cast into another type by prepending the type before it:
code {
[int]0x40
code }

  → Windows/PowerShell/language/operator[operators]
