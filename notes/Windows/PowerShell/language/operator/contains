$ PowerShell: -contains operator

{ Check for existence of value in array

  The following example tries to demonstrate how `-contains` can be used to check if a given value exists in an → Windows/PowerShell/language/type/array.
  gh|about-PowerShell|/language/operator/contains/value-in-array.ps1||

}
{ -contains vs -like

  The `-contains` → Windows/PowerShell/language/operator[operator] must not be confused with the → Windows/PowerShell/language/operator/comparison[`-like` operator]. `-like` compares strings while
 `-contains` checks for the existence of an object in a collection.
  gh|about-PowerShell|/language/operator/contains/like-vs-contains.ps1||

}
{ Misc  

  `-contains` returns `$True` if every property of the compared object match.

  `-contains` has an opposite: `-notContains`.

}

sa:
  → Windows/PowerShell/language/operator/in[`-in`]
