$ PowerShell: Enum
@ enum

gh|about-PowerShell|/language/enum/play-rock-paper-scissors.ps1||

{ An enum derives from System.Enum

  An `enum` derives from → Microsoft/dot-net/namespaces-classes/System/Enum[`System.Enum`]:
  gh|about-PowerShell|/language/enum/base-class.ps1||

}
{ Enums with flag characteristics (bit-fields) #ps-enum-flag

  gh|about-PowerShell|/language/enum/flags.ps1||

  Testing for bits in an enum with the → Windows/PowerShell/language/operator/bitwise#ps-band-test-bits-enum[`-band`] operator.

}
