$ PowerShell: the automatic variable $executionContext

The type of `$executionContext` is → Microsoft/dot-net/namespaces-classes/System/Management/Automation/EngineIntrinsics[`System.Management.Automation.EngineIntrinsics`]. Such an object represents the
*execution context* of the → Windows/PowerShell/host[PowerShell host].

sa:

  Typically, `$executionContext.SessionState.Path.CurrentLocation` has the same value like → Windows/PowerShell/language/variable/automatic/pwd[`$pwd`], but is read-only, while `$pwd` is writable.

 `$psCmdLet`

  Other → Windows/PowerShell/language/variable/automatic[automatic variables]

  The → Microsoft/dot-net class → Microsoft/dot-net/namespaces-classes/System/Threading/ExecutionContext[`System.Threading.ExecutionContext`] is not related to `$executionContext`.
