$ PowerShell: the automatic variable $psEdition

In PowerShell version 5.1 or higher, the value of `$psEdition` is either `Core` or `Desktop`. Below that version, this variable is inexistant.

`Desktop` indicates that PowerShell uses → Microsoft/dot-net/implementations/Framework, `Core` indicates → Microsoft/dot-net/implementations/Core.

