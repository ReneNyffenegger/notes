$ PowerShell: the automatic variable $matches

`$matches` is a collection that is populated when using → development/regular-expressions[regular expressions] with the
→ Windows/PowerShell/language/operator/comparison[`-match`] operator.

The type of `$matches` is `System.Collections.Hashtable`.

{ Simple example

  gh|about-PowerShell|/language/variable/automatic/matches/simple.ps1||

  Note: this example does not set `$matches[1]`.

}
sa:
  Other → Windows/PowerShell/language/variable/automatic[automatic variables]
