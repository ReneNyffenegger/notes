$ PowerShell: the preference variable $warningPreference
@ $warningPreference

The value of the `$warningPreference` → Windows/PowerShell/language/variable/preference[preference variable] determines what happens when the
`→ Windows/PowerShell/command-inventory/noun/warning/write` cmdlet is invoked.
