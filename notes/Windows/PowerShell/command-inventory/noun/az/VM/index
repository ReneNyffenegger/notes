$ Powershell command noun: azVM

The → Windows/PowerShell cmdLet `azVM` manages *virtual machines*.

Commands releated to the → Windows/PowerShell command noun `azVM`:
  • `enter-azVM`
  • `get-azVM`
  • `new-azVM`
  • `remove-azVM`
  • `restart-azVM`
  • `set-azVM`
  • `start-azVM`
  • `stop-azVM`
  • `update-azVM`

sa:
  → Windows/PowerShell/command-inventory/noun
