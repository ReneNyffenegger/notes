$ Powershell command noun: cliXML

The → Windows/PowerShell command noun `cliXML` allows to serialize (or persist) → Windows/PowerShell/language/object[objects] to an → development/XML based representation of the object. The persisted object can later be re-instantiated (or deserialized).

Commands related to the → Windows/PowerShell[PowerShell] command noun `cliXML`:
  • → Windows/PowerShell/command-inventory/noun/cliXML/export[`export-cliXML`]
  • `import-cliXML`

sa:

  → Windows/PowerShell/command-inventory/noun
