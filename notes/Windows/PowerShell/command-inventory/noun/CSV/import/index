$ PowerShell cmdLet Import-CSV
@ import-CSV

`import-CSV` reads a → development/Data/exchange/formats/CSV file and returns an → Windows/PowerShell/language/type/array[array] of elements whose type is
→ Microsoft/dot-net/namespaces-classes/System/Management/Automation/PSCustomObject[`System.Management.Automation.PSCustomObject`]. Each of these elements corresponds to a data (= non-header) row of the imported CSV file.

sa:

  `import-CSV` belongs to the → Windows/PowerShell/language/cmdlet[cmdlets] with the → Windows/PowerShell/command-inventory/parameter/encoding[`-encoding` parameter].

  → Windows/PowerShell/command-inventory/noun/CSV

  → Windows/PowerShell/language/object[Object creation in PowerShell]
