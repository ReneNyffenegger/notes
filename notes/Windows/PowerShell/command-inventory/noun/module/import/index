$ PowerShell cmdLet import-module

Import a → Windows/PowerShell/language/module that is not located in one of the directories listed in → Windows/development/environment-variables#win-envvar-PSModulePath[`$env:PSModulePath`]:
code {
import-module p:\ath\to\module
code }

{ -disableNameChecking #import-module-disableNameChecking

  Suppresses the warning that would be caused if a → Windows/PowerShell/language/module uses → Windows/PowerShell/language/command[commands] with
  → Windows/PowerShell/command-inventory/verb#ps-approved-verb[unapproved verbs].

}
{ -prefix #import-module-prefix

  TODO: Compare with the  entry `DefaultCommandPrefix` in a → Windows/PowerShell/language/module/files/manifest[PowerShell module manifest file].

}

sa:

  → Windows/PowerShell/command-inventory/noun/module
