$ PowerShell Cmdlet: Start-Process
@ start-process

{ -ArgumentList

 `-argumentList` is followed by an → Windows/PowerShell/language/type/array[array] that is passed as command line arguments to the executable being started:
code {
start-process → Microsoft/Office/Excel[excel] -argumentList → Microsoft/Office/Excel#excel-command-line-parameters[/e, /p], "→ Windows/PowerShell/language/variable/automatic/pwd[$pwd]"
code }

  It's also possible to embed the arguments into a string:
code {
start-process powershell -argumentList "-noExit -noProfile -command echo 'Hello world'"
code }

}
{ Start process with administrator privileges

  With `-verb runas`, a process can be started with administrator privileges:
code {
start-process powershell -verb runas
code }

}

sa:

  → Windows/PowerShell/command-inventory/noun/job/start[`start-job`]

  The → Windows/PowerShell/language/command parameter → Windows/PowerShell/command-inventory/parameter/credential[`-credential`].

  → Windows/PowerShell/command-inventory/noun/process
