$ PowerShell cmdLet Get-Uptime
@ get-uptime

`get-uptime` returns a `→ Microsoft/dot-net/namespaces-classes/System/TimeSpan` object which corresponds to the amount of time that elapsed since the last system boot.

The `→ Microsoft/dot-net/namespaces-classes/System/DateTime` object that corresponds to the boot time is returned by using the `-since` argument:
code {
PS C:\> get-uptime -since
code }

sa:
  → Windows/booting#windows-last-boot-time[Determine the last boot time in Windows]
