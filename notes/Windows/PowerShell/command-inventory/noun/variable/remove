$ PowerShell cmdLet Remove-Variable
@ remove-variable

`remove-variable` removes a → Windows/PowerShell/language/variable.

sa:
  → Windows/PowerShell/command-inventory/noun/variable

  Use the `-force` option to remove read-only variables (such as `→ Windows/PowerShell/language/variable/automatic/home`).

  When using → Windows/development/COM automation for → Microsoft/Office, a variable apparently needs to be removed to → Windows/PowerShell/language/object/COM/Office-process-running-even-after-calling-Quit[kill a process].
