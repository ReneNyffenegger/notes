$ PowerShell cmdLet Set-Clipboard
@ set-clipboard

Setting the content of the → Windows/clipboard to the value of a → Windows/PowerShell/language/variable[variable]:
code {
$var = 'foo bar baz'
set-clipboard $var
code }

Copy the output of a → Windows/PowerShell/language/command into the clipboard.
code {
set-clipboard ( → Windows/PowerShell/command-inventory/noun/childItem/get[get-childItem] )
code }

{ Linux

  On Linux, `set-clipboard` requires the `→ Linux/X/clipboard/xclip` utility to be referenced in the `PATH` environment variable.

}

sa:
  → Windows/PowerShell/command-inventory/noun/clipboard

  The simple PowerShell Script `→ development/tools/scripts/personal/pc_ps1`
