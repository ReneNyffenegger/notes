$ Powershell command noun: clipboard

Commands related to the → Windows/PowerShell[PowerShell] command noun `clipboard`:
  • `get-clipboard`
  • `set-clipboard`

{ clipboard not available on PowerShell Core

  The noun `clipboard` is not available on → Windows/PowerShell Core. It can be replaced with `clipboardText`:
code {
→ Windows/PowerShell/command-inventory/noun/module/install[install-module] -name → https://www.powershellgallery.com/packages/ClipboardText[ClipboardText]
code }

}

sa:

  → Windows/clipboard

  → Windows/PowerShell/command-inventory/noun
