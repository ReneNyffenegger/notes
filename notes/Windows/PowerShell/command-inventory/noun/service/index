$ Powershell command noun: service

Commands related to the → Windows/PowerShell[PowerShell] command noun `service`:
  • → Windows/PowerShell/command-inventory/noun/service/get[`get-service`]
  • `new-service`
  • `restart-service`
  • `resume-service`
  • → Windows/PowerShell/command-inventory/noun/service/set[`set-service`]
  • `start-service`
  • → Windows/PowerShell/command-inventory/noun/service/stop[`stop-service`]
  • `suspend-service`

{ No cmdlet to uninstall a service

  There seems to be no → Windows/PowerShell/language/cmdlet[cmdLet] to remove and/or uninstall a service; it can be done however by resorting to → Windows/dirs/Windows/System32/sc_exe[`sc.exe`]:
code {
PS C:\> sc.exe delete $svc
code }

  Note that in → Windows/PowerShell, `sc` is an → Windows/PowerShell/language/alias for → Windows/PowerShell/command-inventory/noun/content/set#sc-alias[`set-content`], so the `.exe` suffix is required in this case.

}

sa:

  → Windows/services

  → Windows/PowerShell/command-inventory/noun
