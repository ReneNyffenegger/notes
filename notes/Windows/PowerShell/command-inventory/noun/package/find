$ PowerShell cmdLet find-package

`find-package` searches (and occasionally finds) packages in → Windows/PowerShell/command-inventory/noun/packageSource[package sources].

If it finds a package that matches the criterias, it returns a → Microsoft/dot-net/namespaces-classes/Microsoft/PackageManagement/Packaging/SoftwareIdentity[`Microsoft.PackageManagement.Packaging.SoftwareIdentity`] object.

sa:
  → Windows/PowerShell/command-inventory/noun/package
