$ PowerShell cmdLet set-psRepository

{ Setting installation policy

  `set-psRepository` can be used to set the installation policy of a repository to *trusted* (see → Windows/PowerShell/command-inventory/noun/module/install#cmdLet-install-module-untrusted):
code {
PS C:\> set-psRepository -name PSGallery -installationPolicy trusted
code }


}

sa:
  → Windows/PowerShell/command-inventory/noun/psRepository
