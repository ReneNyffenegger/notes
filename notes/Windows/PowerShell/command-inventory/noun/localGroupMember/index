$ Powershell command noun: localGroupMember

Commands related to the → Windows/PowerShell command noun `localGroupMember`:
  • `add-LocalGroupMember`
  • `get-LocalGroupMember`

{ Find users with administrative privileges #ps-get-localGroupMember-admin

code {
PS C:\> get-localGroupMember -name → Windows/user-account/group/Administrators[Administrators]

ObjectClass Name                    PrincipalSource
----------- ----                    ---------------
User        THISPC\Administrator    Local
User        THISPC\admpc            Local
User        THISPC\rene             Local
User        DOM\admrny              ActiveDirectory
User        DOM\admpak              ActiveDirectory
Group       DOM\Domain Admins       ActiveDirectory
code }

}

sa:

  The command noun → Windows/PowerShell/command-inventory/noun/localGroup[localGroup]

  → Windows/PowerShell/command-inventory/noun
