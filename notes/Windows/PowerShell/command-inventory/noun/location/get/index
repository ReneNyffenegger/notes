$ PowerShell cmdLet get-location

{ Alias

  An → Windows/PowerShell/language/alias[alias] for `get-location` is `pwd` (which is also a → Linux/shell/commands/pwd[Unix shell command]). *pwd* stands for *print working directory*.

}

sa
  → Windows/PowerShell/command-inventory/noun/location
