$ Powershell command noun: location

The noun `Location` refers to the *current working directory* (or its equivalent when using a non-filesystem → Windows/PowerShell/language/provider/drive[drive]).


Commands related to the → Windows/PowerShell[PowerShell] command noun `object`:
  • `get-location`
  • `pop-location`
  • `push-location`
  • → Windows/PowerShell/command-inventory/noun/location/set[`set-location`]

{ Aliases

  table { ll
    → Windows/PowerShell/language/cmdlet[cmdLet] ☰ → Windows/PowerShell/language/alias[alias]
    `set-location`  ☰ `cd`
    `push-location` ☰ `pushd`
    `pop-location`  ☰ `popd`

  }

}

sa:
 → Windows/PowerShell/command-inventory/noun
