$ PowerShell cmdLet Set-TimeZone
@ set-timeZone

code {
set-timeZone 'W. Europe Standard Time'
code }

? When executed in a → Companies-Products/Oracle/VM-VirtualBox[VirtualBox] guest installation, `set-timeZone` seems to set the → development/date-time/time-zone of the host as well.

sa:
  → Windows/PowerShell/command-inventory/noun/timeZone
