$ PowerShell cmdLet Get-WindowsCapability
@ get-windowsCapability

`get-windowsCapability` returns the *Windows capabilities* for an image (parameter `-path …`) or the local (running) Windows installation (parameter `-online`).

sa:
  → Windows/PowerShell/command-inventory/noun/windowsCapability

