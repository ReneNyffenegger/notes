$ Powershell command noun: item

? An *item* is somehting that is stored in a → Windows/PowerShell/command-inventory/noun/location[location].

Commands related to the → Windows/PowerShell[PowerShell] command noun `item`:
  • `clear-item`
  • `copy-item`
  • `get-item`
  • `invoke-item`
  • `move-item`
  • → Windows/PowerShell/command-inventory/noun/item/new[`new-item`]
  • `remove-item`
  • `rename-item`
  • `set-item`

{ Registry

  Although the hierarchy of the → Windows/registry[registry] is similar to that of a → Windows/file-system[file system], the values under a registry key are not `items`. Instead, in order to access these,
  the → Windows/PowerShell/language/cmdlet[cmdLets] related to → Windows/PowerShell/command-inventory/noun/itemProperty[`itemProperty` noun] need to be used.

}

sa:
  → Windows/PowerShell/command-inventory/noun/itemProperty/[The command noun `itemProperty`]

  → Windows/PowerShell/command-inventory/noun
