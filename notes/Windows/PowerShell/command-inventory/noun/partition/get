$ PowerShell cmdLet Get-Partition
@ get-partition

code {
get-partition |
  select-object `
      DriveLetter,
      isSystem,
      gptType,
      guid,
      isHidden,
      @{name='SizeGB'; expression={[math]::Round($_.Size / 1gB)}} |
  format-table
code }

sa:

  → hardware/harddisk/partition
