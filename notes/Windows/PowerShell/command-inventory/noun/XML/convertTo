$ PowerShell cmdLet convertTo-XML

sa:

  → Windows/PowerShell/command-inventory/noun/cliXML/export[`export-cliXML`] is similar to `convertTo-XML`, but writes the produced → development/XML to a file rather than returning it as a string.

  → Windows/PowerShell/command-inventory/noun/XML
