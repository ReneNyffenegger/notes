$ Powershell command noun: acl

The → Windows/PowerShell cmdlet-noun `acl` can be used to manipulate
→ Windows/security/descriptor[security descriptors] of certain → Windows/PowerShell/command-inventory/noun/item[items] (files, directories, → Windows/registry/tree-structure/key[registry keys]).

Commands related to the → Windows/PowerShell[PowerShell] command noun `acl`:
  • → Windows/PowerShell/command-inventory/noun/acl[`get-acl`]
  • `set-acl`

{ Adding a rule

  The following simple example tries to demonstrate how a combination of `get-acl` and `set-acl` can be used to add an identity to the set of users that are allowed access to a directory:
  gh|about-PowerShell|/cmdlets/acl/add-rule.ps1||

}

sa:

  → Windows/dirs/Windows/System32/cacls_exe[`cacls.exe`],
  → Windows/dirs/Windows/System32/icacls_exe[`icacls.exe`]

  → Windows/PowerShell/command-inventory/noun
