$ Powershell command noun: authenticodeSignature

The `authenticodeSignature` cmdlets manages *authenticodes* (Microsoft's code signing technology).

Commands related to the → Windows/PowerShell command noun `authenticodeSignature`:
  • `get-authenticodeSignature`
  • `set-authenticodeSignature`


sa:
  → Windows/PowerShell/command-inventory/noun
