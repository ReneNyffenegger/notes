$ PowerShell cmdLet get-odbcDriver

The → Windows/PowerShell/language/cmdlet[PowerShell cmdLet] `get-odbcDriver` returns a list of installed → development/databases/ODBC drivers.

Apparently, the returned array contains objects whose types are `Microsoft.Management.Infrastructure.CimInstance`.
