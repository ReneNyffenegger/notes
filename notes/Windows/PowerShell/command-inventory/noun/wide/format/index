$ PowerShell cmdLet Format-Wide

`format-wide` only shows a single property of an object. If not specified with the `-property` parameter, it displays an object's default property.

sa:

  → Windows/PowerShell/command-inventory/verb/format

  → Windows/PowerShell/command-inventory/noun/wide
