$ PowerShell command verb: set

{ -passThru

  In many `set-*` commands, the `-passThru` parameter can be used to return information about the result of the command if the command does not have a default output.

}
