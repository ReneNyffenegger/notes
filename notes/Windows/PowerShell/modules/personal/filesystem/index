$ Powershell module: filesystem

{ Functions

  table { ll
    `initialize-emptyDirectory $dir` ☰ Creates an empty directory named `$dir`. If `$dir` already exists, it is removed (including content)
  table }

}

{ filesystem.psm1

  gh|ps-modules-filesystem|/filesystem.psm1||

}
{ filesystem.psd1

  gh|ps-modules-filesystem|/filesystem.psd1||

}
