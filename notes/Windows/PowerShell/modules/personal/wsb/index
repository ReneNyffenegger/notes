$ Powershell module: Rene.wsb
@ Rene.wsb

`Rene.wsb` is a → Windows/PowerShell/language/module[Powershell module] for → Windows/optional-features/Windows-Sandbox

{ Functions

  table { ll
    `start-wsb` ☰ Find a → Windows/PowerShell/modules/personal/wsb/examples[few examples for using this function here].
    `start-wsbWithNotepad` ☰  Copies the host's `→ Windows/dirs/Windows/System32/notepad_exe` to the sandbox's `→ Windows/dirs/Windows/System32[C:\Windows\System32]` directory so that `notepad` can be run in the sandbox.
  table }

}

{ Source code

  { wsb.psm1
  
    gh|ps-module-wsb|/Rene.wsb.psm1||
  
  }
  { wsb.psd1
  
    gh|ps-module-wsb|/Rene.wsb.psd1||
  
  }
}

sa:

  The source code on → https://github.com/ReneNyffenegger/ps-module-wsb[github].

  Because of a nameclash, the module is published under the name → https://www.powershellgallery.com/packages/Rene.wsb[`Rene.wsb`].

  → Windows/PowerShell/modules/personal
