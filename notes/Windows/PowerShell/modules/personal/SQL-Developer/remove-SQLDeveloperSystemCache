$ PowerShell module SQLDeveloper - remove-SQLDeveloperSystemCache
@ remove-SQLDeveloperSystemCache

`remove-SQLDeveloperSystemCache` removes the `system_cache` directory.

Removing the `system_cache` is necessary if starting → development/Software/Oracle-SQL-Developer results in the error message *disable modules and continue*, see
→ https://stackoverflow.com/a/61330713/180275[this Stackoverflow answer].

sa:
  The Powershell module → Windows/PowerShell/modules/personal/SQL-Developer[`SQL-Developer`]

