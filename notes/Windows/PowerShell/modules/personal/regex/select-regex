$ PowerShell module regex, function select-regex

Examples for the function `select-regex` of the → Windows/PowerShell/modules/personal/regex[PowerShell module `regex`].

code {
'foo19', 'bar', 'baz', 'akjdlkjf 20109 !', '', "foo '4' baz" | select-regex \d+

get-childItem -recurse -include *.sql | get-content  | foreach-object { select-regex 'insert +into +\S+' $_ }
code }

sa:
  Some → Windows/PowerShell/modules/personal[(mostly simple or lightweight) PowerShell modules I have written].
