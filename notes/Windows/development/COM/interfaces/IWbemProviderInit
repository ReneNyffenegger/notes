

All Windows Management(?) providers are required to implement the `IWbemProviderInit` interface which is used to initialize a provider.

sa:
  `→ Windows/development/COM/interfaces/IWbemServices`
