$ Windows: user objects

User objects include
  • (Keyboard?) Accelerator table 
  • Caret
  • Cursor
  • DDE conversation
  • Hook
  • Icon
  • Menu
  • Window
  • Window position

{ Handles

  User object handles can be accessed and used by all → Windows/development/process[processes] *if* the process has security access to it.

  The number of user object handles per → Windows/development/session[session] is limited by the value of `USERProcessHandleQuota` under the
  → Windows/registry/tree-structure/key[registry key] → Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows-NT/CurrentVersion/Windows[`HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Windows`].

}

sa:
  → Windows/development/objects
