$ Windows CRT: wmain

`wmain(int argc, wchar_t *argv[], wchar_t *envp[])` is Window's *wide character* version of → development/languages/C-C-plus-plus/C/main[`main()`].

{ Linking with MinGW

  If a program is linked with → Windows/development/MinGW[MinGW], the linker should be given the → development/languages/C-C-plus-plus/GCC/options/m_lowercase/unicode[`-municode`] linker option.

}

sa:
  → Windows/development/CRT/source/crt0_c[`crt0.c`]
