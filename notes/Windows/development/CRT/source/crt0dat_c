$ Windows CRT: crt0dat.c

`crt0dat.c` implements c run-time *initialization* and *termination* functions: `_cinit`, `exit`, `_exit`

-`cinit` and `exit` are called from `_astart` which is defined in `crt0.asm`.

sa:
  → Windows/development/CRT/source[Some CRT sources]
