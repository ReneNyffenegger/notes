$ Windows CRT: dllcrt0.c

{ __DllMainCRTStartup

  `dlcrt0.c` contains `__DllMainCRTStartup` which calls `DllMain` (of which a dummy version is provided in → Windows/development/CRT/source/dllmain_c[`dllmain.c`].

  __DllMainCRTStartup returns an `int` (`long`?) that is interpreted as a boolean) and is declared as → development/languages/C-C-plus-plus/calling-convention#cdecl[cdecl]) :
code {
__declspec(noinline) BOOL __cdecl __DllMainCRTStartup(
        HANDLE  hDllHandle,
        DWORD   dwReason,
        LPVOID  lpreserved
        ) {
        …
}
code }

}

sa:
  → Windows/development/CRT/source[Some CRT sources]
