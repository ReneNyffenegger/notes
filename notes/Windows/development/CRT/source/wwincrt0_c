$ Windows CRT: wwincrt0.c

The entry point for Windows applications that use the → Windows/development/CRT with wide characters.

`#define`s `WPRFLAG` to `1` and `_UNICODE` and `_WINMAIN_`. Undefines `_MBCS`. Then includes → Windows/development/CRT/source/crt0_c[`crt0.c`].

