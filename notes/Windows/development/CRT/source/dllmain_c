$ Windows CRT: dllmain.c

`dllmain.c` provides a default («dummy») implementation of `DllMain`.

`DllMain()` is called from `__DllMainCRTStartup` (which is defined in → Windows/development/CRT/source/dllcrt0_c[`dllcrt0.c`].

