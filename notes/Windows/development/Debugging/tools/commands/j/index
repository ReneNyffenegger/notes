
`j` is the *if-then-else* → Windows/development/Debugging/tools/commands[command]: it executes one or the other command depending on the value of an expression:
code {
j Expression Command1 ; Command2 
j Expression 'Command1' ; 'Command2' 
code }
