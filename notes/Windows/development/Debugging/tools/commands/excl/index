$ !

{ Invocation of extensions

code {
!extension
!extension arguments…
!module.extension
!module.extension arguments…
code }

 `module` is a → Windows/development/Portable-Executable/DLL (but invoked without the `.dll` → development/filename-extensions[suffix]), `extension` is a function in that DLL that is loaded
  with `GetProcAddress()`.

}
