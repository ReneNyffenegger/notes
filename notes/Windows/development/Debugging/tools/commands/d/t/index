$ dt

`dt` (display type) shows information about a local or global variable or a data type.

Signed numbers are displayed base 10, unsigned numbers in hexadecimal.

Short summary
code {
dt -h
code }

code {
dt → Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows-NT/CurrentVersion/AeDebug#JIT_DEBUG_INFO[JIT_DEBUG_INFO]
code }

sa:
  The `!list` extension executes a command for each element of a list.
