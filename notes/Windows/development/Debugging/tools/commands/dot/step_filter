$ .step_filter

`.step_filter` allows to specify a list of functions (even with wild cards) that are stepped over when stepping or tracing over function (see the
→ Windows/development/Debugging/tools/commands/p[`p`] and → Windows/development/Debugging/tools/commands/t[`t`] commands).

code {
.step_filter "msvcrt!*" 
code }

