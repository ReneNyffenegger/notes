$ $

The `$` commands execute → Windows/development/Debugging/tools/commands[commands] that are stored in a script file. But compare with the `$$` command that introduces a comment.

code {
$<scriptFile 
$><scriptFile 
$$<scriptFile 
$$><scriptFile 
$$>a<scriptFile [arg1 arg2 arg3 ...] 
code }
