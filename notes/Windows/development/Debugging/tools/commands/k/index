
`k` shows a stack backtrace, which includes
  • base pointer for the stack frame
  • return address
  • function names.

In order to specify a thread for which a backtrace is requsted, `k` can be combined with the → Windows/development/Debugging/tools/commands/tilde[tilde command]:
code {
~2 k
code }
