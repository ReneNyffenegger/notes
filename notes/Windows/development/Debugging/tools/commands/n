
`n` sets the radix (number base) to octal, decimal or hexadecimal.
code {
n 8
n 10
n 16
code }

Setting the radix affects the evaluation of *MASM* expressions (→ Windows/development/Debugging/tools/commands/question-mark[`?`]) but not the evaluation of C++ expressions (`??`).
