
MinGW is designed to build native → Windows code (rathern than being → Linux/POSIX compliant). One effect of this is that it links with MSVCRT.

→ development/Cygwin uses MinGW to build its DLL.

sa:
  → Windows/development
