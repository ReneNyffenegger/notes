
MinGW is designed to build native → Windows code (rather than being → Linux/POSIX compliant). One effect of this is that it links with MSVCRT.

→ development/Cygwin uses MinGW to build its DLL.

sa:
  → Windows/development

  → development/languages/C-C-plus-plus/GCC/options/Cygwin-MinGW[GCC options for Cygwin and MinGW]
