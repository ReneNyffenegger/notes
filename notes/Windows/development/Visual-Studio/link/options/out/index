$ link /OUT

The `/OUT:…` specifies the name of the file that the linker produces. The option requires a colon and no spaces are permited. This is of course confusing because
the `cl` option → Windows/development/Visual-Studio/cl/options/F/o[`/Fo`] does not permit a colon.

