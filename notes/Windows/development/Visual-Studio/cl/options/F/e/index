$ cl: /Fe

The `/Fe` option allows to specify then name of the exe file to be produced.

Apparently, `cl` expects the name of the exe to be followed *immediatly* the option, that is: without spaces!
code {
c:\> cl /FeProg.exe foo.obj bar.obj baz.obj
code }

sa:
  → Windows/development/Visual-Studio/cl/options/F[`cl /F…`]
