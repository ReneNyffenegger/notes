$ cl /Zl

Don't place the default name of the default → Windows/development/CRT[CRT] library into the created *object file*.

Compiling with `/Zl` defines the `_VC_NODEFAULT` macro.

sa:

  The `/MD`, `/MT` and `/LD` → Windows/development/Visual-Studio/cl/options[compiler options].

 The → Windows/development/Visual-Studio/link/options/nodefaultlib[`/nodefaultlib`] linker option
