$ cl: __cpuid() / __cpuidex()

`__cpuid()` and `__cpuidex()` are wrappers around the → development/languages/assembler/x86/instructions/cpuid[`cpuid`] instruction.

sa:
  The → Windows/development/Visual-Studio/cl/intrinsic[cl intrinsic] functions → Windows/development/Visual-Studio/cl/intrinsic/__cpuid[`__cpuid()` and `__cpuidex()`].
