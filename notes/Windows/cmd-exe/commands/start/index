$ cmd.exe - start
@ start

Run another program or command

{ Quickly opening folders below a directory in %PATH%

  Directories immediatly below a directory in `%PATH%`  can be opened by typing `start` plus the name of the directory. For example, to open the `fonts` folder (that usually is below
→ Windows/dirs/Windows[`C:\Windows`], the following command might do:
code {
C:\> start fonts
code }

}
sa:

  `start` is affected by whether → Windows/cmd-exe/command-extension[command extensions] are enabled or disabled.

  → Windows/cmd-exe/commands

