$ cmd.exe - popd
@ popd

`popd` returns the the directory that was pushed onto the directory stack with → Windows/cmd-exe/commands/pushd[pushd].

`popd` is affected by whether → Windows/cmd-exe/command-extension[command extensions] are enabled or disabled.

sa:
  → Windows/cmd-exe/commands
