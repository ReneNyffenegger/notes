$ cmd.exe: line continuation

A long command can be spread out over multiple lines if each line's last character is the caret (`^`) character.

gh|about-cmd.exe|/lineContinuation.bat||

sa:

  The → Windows/cmd-exe/parse-command-line/exe#caret[role of the caret] in → Windows/cmd-exe/parse-command-line/exe[command line parsing].

  → Windows/cmd-exe
