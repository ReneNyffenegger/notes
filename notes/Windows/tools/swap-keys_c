$ swap-keys.c

This small C program swaps the *Esc* and the *Caps Lock* keys and assigns the *right ctrl* key to the *right alt* key.

gh|swap-keys|/swap_keys.c||

{ Compiling as DLL

code {
$ gcc -DAS_DLL -c swap_keys.c
$ gcc -shared -o swap_keys.dll swap_keys.o
code }

}

sa:
  → https://github.com/susam/uncap
