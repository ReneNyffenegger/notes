$ WMI Namespace ROOT/cimv2
@ ROOT/cimv2

`ROOT/cimv2` is the default → Windows/Management-Instrumentation/namespaces[WMI namespace].

Because `ROOT/cimv2` is the default namespace, the following two  commands are equivalent:
code {
→ Windows/PowerShell/command-inventory/noun/cimClass/get -namespace root\cimv2
→ Windows/PowerShell/command-inventory/noun/cimClass/get
code }
