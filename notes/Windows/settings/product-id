$ Windows product id
@ product id

The *product id* is needed for Windows support.

The product id changes when updates are installed.

The format of the product id is `aaaaa-bbb-ccccccc-ddeee`
-
`aaaaa` corresponds to the *Windows Product Code* (Platform, build and → Windows/versions[version])
-
`bbb` is the *Channel ID* (type of license)


In the → Windows/registry[registry], the *product id* is stored under
→ Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows-NT/CurrentVersion[`HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows`].

The product id can be shown, for example, with → Windows/dirs/Windows/System32/systeminfo_exe[`systeminfo.exe`] or by pressing *Windows-key + Pause/break*.

sa:
  → Windows/settings/product-key[product key]
