$ become-system (Tool to become System in Windows)

`become-system.c` is a C source files that, when compiled, can be run with elevated privilages to start a `cmd.exe` process as → Windows/security/SID/S-1-5/18[NT Authority\System].

{ become-system.c

  gh|windows-become-system|/become-system.c||

}
{ Makefile

  gh|windows-become-system|/Makefile||

}
{ run-in-sandbox.wsb #win-become-system-wsb

  This → Windows/optional-features/Windows-Sandbox#win-sandbox-wsb[`.wsb`] file runs `become-system.exe` in a → Windows/optional-features/Windows-Sandbox.

  gh|windows-become-system|/run-in-sandbox.wsb||

}
{ embed-exe.ps1

 `embed-exe.ps1` is a PowerShell script that embeds the compressed content of `become-system.exe` in Base64 encoding in `create-become-system.ps1`.

  gh|windows-become-system|/embed-exe.ps1||

}
{ create-become-system.ps1

 `create-become-system.ps1` is the script created by `embed-exe.ps1`.
  -
  When run, it re-creates `become-system.exe`.

  The idea of this script is to be able to clone the github repository and create `become-system.exe` in environments with tools such as Cortex XDR and without C compiler.

  gh|windows-become-system|/create-become-system.ps1||

}

sa:

  The source code is hosted in → https://github.com/ReneNyffenegger/windows-become-system[this github repository].

  I have taken → https://github.com/ryanalieh/NT-privilege-escalation/blob/main/main.cpp[this source file] and adapted it.
