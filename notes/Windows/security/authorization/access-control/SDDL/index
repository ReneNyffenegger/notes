$ Security Descriptor Definition Language (SDDL)
@ SDDL

rem {
The security descriptor definition language (SDDL) uses ACE strings in the DACL and SACL components of a security descriptor string.

As shown in the Security Descriptor String Format examples, each ACE in a security descriptor string is enclosed in parentheses. The fields of the ACE are in the following order and are separated by semicolons (;).
rem }

SDDL defines a format with which
→ Windows/security/descriptor can be textually described.

sa:

  The PowerShell command noun → Windows/PowerShell/command-inventory/noun/sddlString[`sddlString`].

  The → Windows/development/WinAPI functions `ConvertSecurityDescriptorToStringSecurityDescriptor` and `ConvertStringSecurityDescriptorToSecurityDescriptor`.

 `→ Windows/dirs/Windows/System32/sechost_dll`

  → Windows/security[Windows Security]

links:
  → https://docs.microsoft.com/en-us/openspecs/windows_protocols/ms-dtyp/f4296d69-1c0f-491f-9587-a960b292d070[ABNF of SDDL]
