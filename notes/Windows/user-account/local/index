$ Windows: local user accounts

A *local* → Windows/user-account[user account] exists on one particular computer only.

{ Default local user accounts #windows-default-local-user-accounts

  When → Windows is installed, a few local user accounts are created, for example:
    • *Administrator* (The very first account created). This account's → Windows/security/SID[SID] is `S-1-5-*domain*-500`)
    • *Guest* account (disabled by default)
    • *HelpAssistant* account
  


}
