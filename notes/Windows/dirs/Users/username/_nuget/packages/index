$ %userprofile%\.nuget\packages

`→ Windows/dirs/Users/username[%userprofile%]\.nuget\packages` is the default *global packages* folder for nuget. In → Linux, the corresponding path is `~/.nuget/packages`.

The → Windows/development/environment-variables[environment variable] `DOTNET_PACKAGES` allows to use another package directory.
