$ %APPDATA%\Microsoft\Windows\PowerShell\PSReadline\ConsoleHost_history.txt

This file seems to be the default location of the file that stores the → Windows/PowerShell/language/command/history[PowerShell command history].

This location can be queried like so
code {
(→ Windows/PowerShell/command-inventory/noun/psReadLineOption/get[get-psReadLineOption]).historySavePath
code }
