$ %APPDATA%\Microsoft\Windows\Start Menu

This directory contains a → Windows/user-account[user's] (non-shared) Entries in his → Windows/configuration/Start-menu.

{ Determining location with PowerShell

  The → Windows/PowerShell[PowerShell] prints the location of this directory with
code {
[environment]::getFolderPath('StartMenu')
code }

}

The entries that are shared among all users are found in → Windows/dirs/ProgramData/Microsoft/Windows/Start-Menu[`%programdata%\Microsoft\Windows\Start Menu`].

