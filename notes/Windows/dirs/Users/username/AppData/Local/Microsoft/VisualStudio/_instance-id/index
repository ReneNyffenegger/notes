$ %LOCALAPPDATA%\Microsoft\VisualStudio\_instance-id_

{ privateregistry.bin

 `privateregistry.bin` is a → /Windows/registry/tree-structure/hive.

  Each installation (instance) of Visual Studio stores its setting in such a hive which allows multiple installations on a machine to have independent settings.

 `privateregistry.bin` can be shown in `→ Windows/dirs/Windows/regedit_exe` (for example by selecting `→ Windows/registry/tree/HKEY_LOCAL_MACHINE` then using the menu File -> Load Hive).

}
