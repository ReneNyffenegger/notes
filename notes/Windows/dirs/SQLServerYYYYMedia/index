$ C:\SQLServeryyyyMedia

*yyyy* might be 2017, so, the directory name is `C:\SQLServer2017Media`. A *media* directory is used for the *SQL-Server installation center*.

The → development/databases/SQL-Server developer edition stores its data in the sub-directory `Developer_ENU`.
