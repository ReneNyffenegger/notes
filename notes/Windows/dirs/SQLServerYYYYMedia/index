$ C:\SQLServeryyyyMedia

*yyyy* might be 2017, so, the directory name is `C:\SQLServer2017Media`. A *media* directory is used for the *SQL-Server installation center* (which es started
with `setup.exe` which is located under → Windows/dirs/Program-Files/Microsoft-SQL-Server/version/Setup-Bootstrap/SQLyyyy[`%programfiles%\Microsoft SQL Server\version\Setup Bootstrap\SQLyyyy`]).

The → development/databases/SQL-Server developer edition stores its data in the sub-directory `Developer_ENU`.
