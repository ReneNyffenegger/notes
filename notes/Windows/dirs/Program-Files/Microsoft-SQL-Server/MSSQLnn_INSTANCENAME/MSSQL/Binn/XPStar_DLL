$ XPStar.DLL

`XPStar.DLL` seems to contain the functionality for some (all?) `xp_` procedures: executing `→ development/databases/SQL-Server/administration/schemas/sys/objects/stored-procedures/sp_helptext N'xp_regwrite'` returns `xpstar.dll`.
