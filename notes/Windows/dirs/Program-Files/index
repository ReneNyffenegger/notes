$ C:\Program Files

Apparently, this is the default directory for the directory that → development/environment-variable[environment variable] `%PROGRAMFILES%` points to.

sa:
  → Windows/dirs/Program-Files-x86

  `ProgramFilesDir`,`ProgramFilesPath` and `ProgramW6432Dir` under the → Windows/registry[registry] key
  → Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows/CurrentVersion[`HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion`]
