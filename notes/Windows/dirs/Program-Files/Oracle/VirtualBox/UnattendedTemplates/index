$ C:\Program Files\Oracle\VirtualBox\UnattendedTemplates

table { ll
   `debian_postinstall.sh` ☰
   `debian_preseed.cfg` ☰
   `fedora_ks.cfg` ☰
   `lgw_ks.cfg` ☰
   `lgw_postinstall.sh` ☰
   `ol_ks.cfg` ☰
   `redhat67_ks.cfg` ☰
   `redhat_postinstall.sh` ☰
   `rhel3_ks.cfg` ☰
   `rhel4_ks.cfg` ☰
   `rhel5_ks.cfg` ☰
   `ubuntu_preseed.cfg` ☰
   `win_nt5_unattended.sif` ☰
   `win_nt6_unattended.xml` ☰
   `win_postinstall.cmd` ☰ Post installation script template for Windows - runs after the target system has been booted, typically as part of the first login.
table }

sa:
  `→ Windows/dirs/Program-Files/Oracle/VirtualBox`
