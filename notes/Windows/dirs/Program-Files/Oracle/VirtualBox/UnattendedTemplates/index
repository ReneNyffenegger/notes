$ C:\Program Files\Oracle\VirtualBox\UnattendedTemplates

table { ll

   `debian_postinstall.sh` ☰
   `debian_preseed.cfg` ☰
   `fedora_ks.cfg` ☰
   `lgw_ks.cfg` ☰
   `lgw_postinstall.sh` ☰
   `ol8_ks.cfg` ☰
   `ol9_ks.cfg` ☰
   `ol_ks.cfg` ☰
   `ol_postinstall.sh` ☰
   `os2_cid_install.cmd` ☰
   `os2_response_files.rsp` ☰
   `os2_util.exe` ☰
   `redhat67_ks.cfg` ☰
   `redhat_postinstall.sh` ☰
   `rhel3_ks.cfg` ☰
   `rhel4_ks.cfg` ☰
   `rhel5_ks.cfg` ☰
   `ubuntu_autoinstall_user_data` ☰
   `ubuntu_preseed.cfg` ☰
   `win_nt5_unattended.sif` ☰
   → Windows/dirs/Program-Files/Oracle/VirtualBox/UnattendedTemplates/win_nt6_unattended_xml[`win_nt6_unattended.xml`] ☰
   → Windows/dirs/Program-Files/Oracle/VirtualBox/UnattendedTemplates/win_postinstall_cmd[`win_postinstall.cmd`] ☰ Post installation script template for Windows - runs after the target system has been booted, typically as part of the first login.

table }

sa:
  `→ Windows/dirs/Program-Files/Oracle/VirtualBox`
