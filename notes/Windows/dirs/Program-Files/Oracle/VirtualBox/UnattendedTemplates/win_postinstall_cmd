$ C:\Program Files\Oracle\VirtualBox\UnattendedTemplates\win_postinstall.cmd

`C:\Program Files\Oracle\VirtualBox\UnattendedTemplates\win_postinstall.cmd` is the *ost installation script template for Windows* which runs after the target system has been booted, typically as part of the first login.

sa:

  The step → Companies-Products/Oracle/VM-VirtualBox/command-line/PowerShell/unattended-os-installation#vbox-unattended-specify[Specfy an unattended installation] in
  → Companies-Products/Oracle/VM-VirtualBox/command-line/PowerShell/unattended-os-installation[Unattended OS installation with Powershell]

  `→ Windows/dirs/Program-Files/Oracle/VirtualBox/UnattendedTemplates`
