$ VBoxHeadless.exe

`VBoxHeadless.exe` is a → Companies-Products/Oracle/VM-VirtualBox/virtual-machine frontend that enables redirection through the *VirtualBox Remote Desktop extension*.
-
However, it does not provide video output or keyboard and mouse input enables 
