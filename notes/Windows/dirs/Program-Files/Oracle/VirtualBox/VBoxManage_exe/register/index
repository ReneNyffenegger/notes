$ VBoxManage registervm | unregistervm

{ Unregistering a VM #unregistervm

  Combine with `--delete` to also remove associated
    • Hard disk images
    • Saved state files
    • The machine XML file and its backups
    • Machine log files
    • The machine directory (if it is empty after deleting the above files)
    

code {
$vmName = …
→ Windows/dirs/Program-Files/Oracle/VirtualBox/VBoxManage_exe/controlvm $vmName poweroff
VBoxManage unregistervm $vmName --delete
code }

}


sa:

  `→ Windows/dirs/Program-Files/Oracle/VirtualBox/VBoxManage_exe/clonevm … --register`
