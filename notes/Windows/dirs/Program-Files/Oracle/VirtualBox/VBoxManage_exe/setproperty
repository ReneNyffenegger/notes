$ VBoxManage setproperty

table { lll

   `machinefolder` ☰ `default|<folder>` ☰ Sets the location of the → Companies-Products/Oracle/VM-VirtualBox/virtual-machine#VirtualBox-machine-folder[machine folder]
   `hwvirtexclusive` ☰ `on|off` ☰ Specifies if VirtualBox exclusively uses the host's hardware virtualization extensions VT-x (Intel) or AMD-V (AMD). Must be set to `off` if these extensions are shared with other hypervisors running at the same time. Setting it to `off` has negactive performance implications.
   `vrdeauthlibrary` ☰ `default|<library>` ☰
   `websrvauthlibrary` ☰ `default|null|<library>` ☰
   `vrdeextpack` ☰ `null|<library>` ☰
   `autostartdbpath` ☰ `null|<folder>` ☰
   `loghistorycount` ☰ `<value>` ☰
   `defaultfrontend` ☰ `default|<name>` ☰
   `logginglevel` ☰ `<log setting>` ☰
   `proxymode` ☰ `system|noproxy|manual` ☰
   `proxyurl` ☰ `<url>` ☰

table }


sa:
  `→ Windows/dirs/Program-Files/Oracle/VirtualBox/VBoxManage_exe`
