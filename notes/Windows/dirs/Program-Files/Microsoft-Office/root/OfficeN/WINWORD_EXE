$ WINWORD.EXE

`WINWORD.EXE` is the → Windows/development/Portable-Executable/EXE[executable] for → Microsoft/Office/Word[Microsoft Word].

{ Open a word document from the command line and start a macro

  When starting `winword.exe` from the command line, it can be given the `/m` command line option which specifies the name of
  a macro to be executed.

code {
C:\user\rene\> winword.exe /mMacroName someDocument.docm
code }

  Note: no space between `/m` and the name of the macro.


}

sa:
  → Windows/dirs/Program-Files/windows-nt/accessories/wordpad_exe[`wordpad.exe`]
