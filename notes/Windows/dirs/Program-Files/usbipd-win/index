$ C:\Program Files\usbipd-win

{ usbipd.exe #usbipd-exe

 `usbipd.exe` is a *deamon* like program that allows to share locally connected USB devices to other machines, including → Windows/optional-features/Hyper-V guests and → Windows/Subsystem-for-Linux[WSL] 2.

  On the other machine, `→ Linux/shell/commands/usbip` is required to connect to the daemon.

  See also → Windows/Subsystem-for-Linux#wsl-usb[accessing an USB device] from → Windows/Subsystem-for-Linux.

}

