$ %ALLUSERSPROFILE%

The exact location of this directory is stored in the → Windows/registry[registry] under
→ Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows-NT/CurrentVersion/ProfileList[`HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\ProfileList`] in the
value → Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows-NT/CurrentVersion/ProfileList#reg-profile-programdata[ProgramData].
-
The default seems to be `%SystemDrive%\ProgramData` (at which also the variable `%ProgramData%` points).

Some possible subdirectories might include
  • AMD
  • ATI
  • → Windows/dirs/ProgramData/Docker[Docker]
  • EPSON
  • Foxit ContentPlatform
  • Foxit Software
  • McAfee
  • → Windows/dirs/ProgramData/Microsoft[Microsoft]
  • Microsoft OneDrive
  • Mozilla
  • → Windows/dirs/ProgramData/MySQL[MySQL]
  • Package Cache
  • Packages
  • SoftwareDistribution
  • ssh
  • USOPrivate
  • → Windows/dirs/ProgramData/USOShared[USOShared]
  • → Windows/dirs/ProgramData/chocolatey[chocolatey]
  • WindowsHolographicDevices

{ TODO

  The → Windows/development/environment-variables[environment variables] `%ALLUSERSPROFILE%` and `%PROGRAMDATA%` have the same value, at least on → Windows/versions/10.

}
sa:
  The → Microsoft/dot-net enumeration → Microsoft/dot-net/namespaces-classes/System/Environment/SpecialFolder[`System.Environment+SpecialFolder`].
