$ MsMpEng.exe

`MsMpEng.exe` is the *Antimalware Service Executable*

sa:

  → Windows/security/Antimalware-Scan-Interface

 `MsMpEng.exe` might → Windows/PowerShell/examples/Problem-Ejecting-USB-Mass-Storage-Device[prevent an USB drive from being ejected]

  The → Windows/dirs/ProgramData/Microsoft/Windows-Defender/Platform/_version_[directory] where `MsMpEng.exe` is located.
