$ %ProgramFiles(x86)%\IBM\IDM\Cache

? The Cache directory contains *cached* documents. Each *page* (not document!) occupies its own sub directory.
-
? The subdirectory contains a `.tif` and a `.fob` file.

sa:
  The parent directory `→ Windows/dirs/Program-Files-x86/IBM/IDM`

