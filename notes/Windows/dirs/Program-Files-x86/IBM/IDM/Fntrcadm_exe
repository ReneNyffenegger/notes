$ Fntrcadm.exe

Enabling tracing with Record to *IBM FileNet Log* seemed to create the directory `C:\Program Files (x86)\IBM\IDM\Data` and populate it with
the two MS-Access files `FNLOG.MDB` and `FNLOG.ldb`.

`FNLOG.MDB` contained two tables:
  • `NP_AUDITLOG` and
  • `NP_TRACLOG`


sa:
 `→ Windows/dirs/Program-Files-x86/IBM/IDM/Fntrcadm_exe`

