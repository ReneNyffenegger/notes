$ oleview.exe

`oleview.exe` is an → Windows/development/COM/object[OLE/COM object] viewer.

{ IVIEWERS.DLL

  In order to use the *interface viewers*, `oleview.exe` requires a `IVIEWERS.DLL`.

  Without this → Windows/development/Portable-Executable/DLL, `oleview.exe` will run, yet without the mentioned *interface viewers* feature.

}

sa:
  → Windows/development/COM/type-library[Type libraries]
