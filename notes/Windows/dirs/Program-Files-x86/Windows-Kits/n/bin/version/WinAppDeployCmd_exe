$ WinAppDeployCmd.exe

`WinAppDeployCmd.exe` is a  a command line tool that can use to deploy a → Windows/development/Universal-Windows-Platform/[Universal Windows Platform (UWP)] app from a Windows 10 PC to any Windows 10 device. 

code {
WinAppDeployCmd install -file "P:\ath\to\some\file.appx" -ip 192.168.0.1
code }
