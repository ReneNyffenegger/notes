$ winnt.h

32- and 64-bit Windows types and constants that are defined by NT, but exposed through the Win32 API.

{ IMAGE_LOAD_CONFIG_DIRECTORY #IMAGE_LOAD_CONFIG_DIRECTORY

 `IMAGE_LOAD_CONFIG_DIRECTORY` comes in a 32-bit and a 64-bit version (`IMAGE_LOAD_CONFIG_DIRECTORY32` and `IMAGE_LOAD_CONFIG_DIRECTORY64`).

  In a → Windows/development/Portable-Executable, the `IMAGE_LOAD_CONFIG_DIRECTORY` structure can be dumped with
 `→ Windows/dirs/Program-Files-x86/Microsoft-Visual-Studio/version/edition/VC/Tools/MSVC/version/bin/dumpbin_exe[dumpbin] /loadconfig`.
 

}
