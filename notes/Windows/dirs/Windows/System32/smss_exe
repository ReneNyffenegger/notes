$ smss.exe

The *Windows Session Manager*.

`smss.exe` is the first *user mode* → Windows/development/process[process] that is started by the Windows kernel.

The session managers starts → Windows/development/Portable-Executable/subsystem[subsystems].
