$ RecoveryDrive.exe

*Recovery Media Creator*

`RecoveryDrive.exe` requires a → hardware/USB drive.

When the *recovery drive* is created on the USB drive, the content of the USB drive will be deleted.

sa:
  → Windows/dirs/Windows/System32/recdisc_exe[`recdisc.exe`]
