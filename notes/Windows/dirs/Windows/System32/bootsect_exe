$ bootsect.exe

`bootsect.exe` is a *Boot Sector Manipulation Tool*.

This tool can be used to
  • update the master boot code for hard disk partitions in order to switch between *bootmgr* and *ntldr*
  • restore a *boot sector* (the → hardware/harddisk/MBR[MBR]?

sa:
 `→ Windows/dirs/Windows/System32/bootcfg_exe`
