$ gpedit.msc

*Local group policy editor*.

gh|Windows-Pics|/Windows/System32/gpedit.msc.png||

Note that `gpedit.msc` is not installed in the *Home edition* (at lest in → Windows/versions/10).

sa:

  → Windows/Group-Policy

  → Windows/dirs/Windows/System32/GroupPolicy[`%SYSTEMROOT%\System32\GroupPolicy`]

  → Windows/dirs/Windows/System32/rsop_msc[`rsop.msc`] (resultant set of policy)
