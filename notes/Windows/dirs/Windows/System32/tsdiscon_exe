$ tsdiscon.exe

*Session disconnection utility*. The *ts* probably stands for *terminal server*.

When executed without parameters, `tsdiscon` locks the workstation (as though the → Windows/keyboard-shortcuts[keyboard shortcut `Win-L`] had been pressed).

sa:
  → Windows/dirs/Windows/System32/tscon_exe, the session connection utility
