$ tsdiscon.exe

*Session disconnection utility*. The *ts* probably stands for → Windows/Remote-Desktop-Services[Terminal Service] or Terminal Server.

When executed without parameters, `tsdiscon` locks the workstation (as though the → Windows/keyboard-shortcuts[keyboard shortcut `Win-L`] had been pressed).

sa:
  → Windows/dirs/Windows/System32/tscon_exe[`tscon.exe`], the session connection utility, which allows to → Windows/dirs/Windows/System32/tscon_exe#tscon-switch-between-sessions[switch between sessions].
