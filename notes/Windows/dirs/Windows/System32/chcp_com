$ chcp.com

*Change CodePage Utility*

`chcp` displays or changes the → Windows/development/code-page[code page number].

The following command changes the current code page to → development/Unicode/UTF-8 (whose codepage number is 65001):
code {
c:\> chcp 65001
code }


sa:

  Compare with → Windows/dirs/Windows/System32/mode_com[`mode con: cp …`]
