$ subst.exe

`subst.exe` allows to associate a file-system path with a drive letter.

With `/d`, the association is ended.

Such an association is not persistent accross logons.

code {
C:\> subst X: p:\ath\to\a\given\directory
C:\> X:
X:\> …
X:\> C:
C:\> subst X: /d
code }

