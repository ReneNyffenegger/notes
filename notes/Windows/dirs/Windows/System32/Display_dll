$ Display.dll

`Display.dll` implements the → Windows/Appearance/display[Display] → Windows/control-panel.

On the command line (→ Windows/dirs/Windows/System32/cmd_exe, → Windows/PowerShell), this control panel can be opened like so:
code {
control /name microsoft.display
code }
