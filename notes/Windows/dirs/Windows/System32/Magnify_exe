$ Magnify.exe

Screen Magnifier

sa:

  The → Windows/keyboard-shortcuts[keyboard shortcuts] `win` + `+` and `win` + `-` zoom in and out, `win`+ `esc` turns off the magnifier.

  → Windows/dirs/Windows/System32/SnippingTool_exe[`SnippingTool.exe`]
