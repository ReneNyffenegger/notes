$ http.sys


`http.sys` listens for → development/web/HTTP#http-request[HTTP requests] from the network, passes the requests to → Windows/optional-features/IIS[IIS] for processing and then returns processed responses to client browsers.

sa:
  The → Windows/services[Windows service] → Windows/services/installed/W3SVC.
