
*Display Switch*

`DisplaySwitch.exe` and the → Windows/keyboard-shortcuts[shortcut] `windows+p` seem to open the same window (namely the *presentation* (projection) sidebar).

{ Command line options

Options
code {
displayswitch /internal
displayswitch /clone
displayswitch /extend
displayswitch /external
code }

}
