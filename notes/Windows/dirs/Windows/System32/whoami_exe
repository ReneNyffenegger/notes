$ whoami.exe


code {
whoami /priv
code }

{ /logonid #win-whoami-logonid

  Displays the → Windows/security/SID[SID] for the *logon session* which always starts with `S-1-5-5`. The final two values of the SID are different for each logon session and are recycled when
  → Windows is restarted.

code {
whoami /logonid
code }

}
{ /user #win-whoami-user

  Displays the username (`Domain\Username` format) and the → Windows/user-account[user's] → Windows/security/SID[SID].

code {
whoami /user
code }

}
{ /groups

  `whoami.exe /groups` shows the memberships in → Windows/user-account/group[groups].

}
