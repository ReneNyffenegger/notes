$ sihost.exe

`sihost.exe` is the *Shell infrastructure host*.

→ https://www.technewstoday.com/shell-infrastructure-host-high-cpu-usage/[Apparently, there is a memory leak issue in the default photo viewer] which causes
`sihost.exe` to continuously query the → Windows/registry which leads to high CPU usage.
