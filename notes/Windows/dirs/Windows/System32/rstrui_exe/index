$ rstrui.exe

`rstrui.exe` is the *→ Windows System Restore* utiltity. It allows to restore a computer to a restore point (for example after an installation went wrong).

gh|Windows-Pics|/Windows/System32/rstrui.exe/restore-point.png||
