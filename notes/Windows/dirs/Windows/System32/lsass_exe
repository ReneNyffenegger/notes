$ lsass.exe

The *Local Security Authority Subsystem*.

When an account logs on, `lsass.exe` creates a logon session and a *token* for the sesion that contains the accoun's → Windows/security/SID[SID] (and more).
