$ wbemtest.exe

`wbemtest.exe` is a GUI *WMI Test Tool* that was designed primarily as a diagnostic tool.

It is able to
  • enumerate objects
  • perform → Windows/Management-Instrumentation/WQL[queries]
  • register events
  • modify → Windows/Management-Instrumentation[WMI] objects and classes
  • invoke methods

The interface is not user friendly.
