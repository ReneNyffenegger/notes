$ mofcomp.exe

`mofcomp.exe` is the *Managed Object Format* (MOF) compiler.
-
MOF is a lanaguage with which → development/Enterprise/Architecture/CIM classes are described.

sa:

  `mofcomp.exe` is typically(?) located in `→ Windows/dirs/Windows/System32/wbem`.

  `→ Windows/dirs/Windows/System32/wbem/MOF`
