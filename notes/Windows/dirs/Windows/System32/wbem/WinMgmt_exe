$ WinMgmt.exe

→ Windows/Management-Instrumentation[WMI] Service Control Utility

code {
winmgmt [/backup <filename>] [/restore <filename> <flag>]
        [/resyncperf] [/standalonehost [<level>]] [/sharedhost]
        [/verifyrepository [<path>]] [/salvagerepository]
        [/resetrepository]
code }

sa:
  `WinMgmt.exe` is typically(?) located in `→ Windows/dirs/Windows/System32/wbem`.
