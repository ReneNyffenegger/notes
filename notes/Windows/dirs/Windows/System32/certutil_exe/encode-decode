$ certutil.exe -encode / -decode

The two command line options `-encode` and `-decode` of `→ Windows/dirs/Windows/System32/certutil_exe` provide a possibilty to encode or decode a file in/from → development/Base64
when only native tools of → Windows can be usedr:
code {
C:\> → Windows/dirs/Windows/System32/certutil_exe[certutil] -encode file.txt file.64
C:\> → Windows/dirs/Windows/System32/certutil_exe[certutil] -decode file.64  file.txt
code }
