$ schtasks.exe

`schtasks.exe` is the → Windows/services/installed/Schedule[Task Scheduler] Configuration Tool and is used to schedule tasks so that they run
automatically. It replaces the deprecated → Windows/dirs/Windows/System32/at_exe[`at.exe`] command line interface.

`schtasks` does not have all options that the → Windows/dirs/Windows/System32/taskschd_msc[Task Scheduler] in → Windows/control-panel (under *Adminsitrative Tools*) provides. For
example, it does not allow to influence power (battery) related settings (see → https://stackoverflow.com/a/9075595/180275[this Stackoverflow answer).


gh|about-cmd.exe|/commands/schtasks/schedule-a-task.bat||

gh|about-cmd.exe|/commands/schtasks/the-task-to-be-scheduled.bat||


sa:

  → development/tools/scripts/personal/notify_me_at_bat[`notify_me_at.bat`]

  → Windows/dirs/Windows/System32/Tasks

  → Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows-NT/CurrentVersion/Schedule/TaskCache/Tree[`HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Schedule\TaskCache\Tree`]

  → Windows/dirs/Windows/System32/cmd_exe/commands
