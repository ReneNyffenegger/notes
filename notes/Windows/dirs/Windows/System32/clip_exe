$ clip.exe

*Copy data into the → Windows/clipboard[clipboard]*

The output of a → Windows/cmd-exe/commands[cmd.exe command] can be redirected into the clipboard like so:
code {
C:\> → Windows/dirs/Windows/System32/findstr_exe[findstr] foo xyz.txt | clip
code }

The content of a file can be put into the clipoard with
code {
C:\> clip < someFile.txt
code }
