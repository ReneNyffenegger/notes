$ clip.exe

`clip.exe` allows to manipulate the → Windows/clipboard[clipboard] from a command line application such as
→ Windows/dirs/Windows/System32/cmd_exe[`cmd.exe`] or → Windows/PowerShell[PowerShell].

The output of a → Windows/dirs/Windows/System32/cmd_exe/commands[cmd.exe command] can be redirected into the clipboard like so:
code {
C:\> → Windows/dirs/Windows/System32/findstr_exe[findstr] foo xyz.txt | clip
code }

The content of a file can be put into the clipboard with
code {
C:\> clip < someFile.txt
code }
