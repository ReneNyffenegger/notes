$ cmd.exe - rem
@ rem

{ Usage of rem

  `rem` can be used to add comment lines to → Windows/dirs/Windows/System32/cmd_exe/batch-files[batch files].

   gh|about-cmd.exe|/commands/basic.bat||

}
{ rem is a command

 `rem` is a «real» built-in command.

  This can be demonstrated with the following simple batch file. The `/?` is not interpreted as comment but rather as a request for help on `rem`.
  gh|about-cmd.exe|/commands/built-in.bat||

  When executed, the batch file prints:
code {
Records comments (remarks) in a batch file or CONFIG.SYS.

REM [comment]
code }

}
sa:
  → Windows/dirs/Windows/System32/cmd_exe/commands
