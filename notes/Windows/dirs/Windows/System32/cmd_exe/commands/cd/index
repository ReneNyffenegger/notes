$ cmd.exe - cd
@ cd

`cd` stands for *change directory*.

`cd` is affected by whether → Windows/dirs/Windows/System32/cmd_exe/command-extension[command extensions] are enabled or disabled.

sa:

  The equivalent → Windows/PowerShell/language/cmdlet[PowerShell cmdLet] of `cd` in → Windows/PowerShell is → Windows/PowerShell/command-inventory/noun/location/set[`set-location`] (which in fact has an
  → Windows/PowerShell/language/alias[alias] named `cd`).

  → Windows/dirs/Windows/System32/cmd_exe/commands
