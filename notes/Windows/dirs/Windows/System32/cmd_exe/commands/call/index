$ cmd.exe - call
@ call

`call` is used to execute a → Windows/dirs/Windows/System32/cmd_exe/batch-files[batch file] and wait until the batch file is finished with the execution.

code {
call [drive:][/path/to/]batch-file.bat [parameters] [:label [arguments]]
code }

`call` is affected by whether → Windows/dirs/Windows/System32/cmd_exe/command-extension[command extensions] are enabled or disabled.

sa:

  To call a batch file (or any command, for that matter) and not wait for it to complete, → Windows/dirs/Windows/System32/cmd_exe/commands/start[`start`] can be used. `start` executes the given program/batch file in a new
  → Windows/development/process.

  → Windows/dirs/Windows/System32/cmd_exe/commands
