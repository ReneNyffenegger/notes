$ wusa.exe

`wusa.exe` is the → Windows/update Standalone Installer. It uses the *Windows Update Agent API* to install update packages (which are identified by their → development/filename-extensions#msu[`.msu`] suffix.

sa:
  → Windows/update
