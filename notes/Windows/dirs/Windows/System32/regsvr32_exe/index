$ regsvr32.exe

*Microsoft© Register Server*

code {
regsvr32 [/u] [/s] [/n] [/i[:cmdline]] <name-of-→ Windows/development/Portable-Executable/DLL[DLL]>
code }

{ Command line options

  table { ll

   `/u` ☰ Unregisters server.
   `/s` ☰ Silent operation: prevents displaying messages.
   `/i:<cmdline>` ☰ Passes an optional command-line string (cmdline) to `DllInstall`. If you use this parameter with the `/u` parameter, it calls `DllUninstall`.
   `/n` ☰ Prevents calling `DllRegisterServer`. This parameter requires you to also use the `/i` parameter.
   `/?` ☰ Displays help at the command prompt.

  table }

}
{ TODO

  The module C:\‥ was loaded but the entry-point `DllRegisterServer` was not found.
  -
  Make sure that C:\‥ is a valid DLL or OCX file and then try again.

  The module C:\‥ was loaded but the call to `DllRegisterServer` failed with error code 0x8002801c.
  

}

