$ usoClient.exe

*USO* stands for → Windows/update#windows-update-uso[Update Session Orchestrator].

? `usoClient.exe` has more options than → Windows/dirs/Windows/System32/wuauclt_exe[`wuauclt.exe`]

Options:
  • `startScan` to check for updates
  • `startDownload` to download the updates
  • `startInstall` to install the downloaded updates
  • `restartDevice` to restart → Windows when installation is finished

sa:
   → Windows/update
