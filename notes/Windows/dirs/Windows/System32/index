$ %SYSTEMROOT%\System32

`%SYSTEMROOT%` usually is → Windows/dirs/Windows[`C:\Windows`].

{ TODO

  Extract information about files with → Windows/PowerShell:
code {
foreach ($file in get-childItem "$env:SystemRoot\System32") {
    $file.name + "     " + $file.versionInfo.fileDescription
}
code }

}
