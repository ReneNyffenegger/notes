$ wuauclt.exe

`wuauclt.exe` is the *Windows Update Client*: it automatically checks with → Microsoft[Microsoft's] website for Windows updates.

? → Windows/dirs/Windows/System32/UsoClient_exe[`usoClient.exe`] has more options than `wuauclt.exe`.

Options:
  • `/detectnow` to check for new updates
  • `/updatenow` to install the updates

sa:
  → Windows/update
