$ Taskmgr.exe

*Task manager*: show running → Windows/development/process[processes]. The task manager can also be opened with `ctrl`+`shift`+`esc`.

A better alternative for the task manager is probably the Sysinternals *Process Explorer*.
