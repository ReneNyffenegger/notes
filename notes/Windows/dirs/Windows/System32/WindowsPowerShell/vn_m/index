$ %SYSTEMROOT%\System32\WindowsPowerShell\vN.M

The installation directory for → Windows/PowerShell. (`N.M` currently seems to be always `1.0`).

The name of this directory is stored in the → Windows/PowerShell/language/variable/automatic[automatic Powershell variable] `$psHome`.

