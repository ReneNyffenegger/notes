$ tree.com

`tree.com` is a *tree Walk Utility that graphically displays the nested directory structure of a drive or a path*.

The program can be given two optional arguments: `/a` and `/f`.
-
`/a` prints the tree with → development/Unicode/ASCII characters rather than extended characters.
-
`/f` prints files. (The default is to print directories only)


sa:

  The → Linux/shell/commands[Linux shell command] with the → Linux/shell/commands/tree[same name].
