$ %SystemRoot%\debug\UserMode\Userenv.log

`Userenv.log` is the diagnostic → development/log-files[log file] that records details on the processing of the → Windows/Group-Policy Engine.

In order to enable verbose logging, the value of `UserenvDebugLevel` in the registry key
→ Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows-NT/CurrentVersion/Winlogon[`HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon`] must be set
to `0x10002`.
