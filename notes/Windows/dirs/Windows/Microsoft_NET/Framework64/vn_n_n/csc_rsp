$ csc.rsp

The `csc.rsp` file contains a list of command line options that will be used when `csc.exe` is invoked without the `-noconfig` option.

sa:
  The → development/languages/C-Sharp/compiler/options/link[`-link` compiler option]
