$ Windows registry structure: key
@ key

The nodes (but not the leaves aka → Windows/registry/tree-structure/value[values]) in a given → Windows/registry[registry] tree are referred to as *keys*.

sa:
  → Windows/registry/tree-structure
