$ Registry: HKLM|HKCU\Software\Policies\Microsoft\Internet Explorer

{ DisableFirstRunCustomize #reg-pol-ie-DisableFirstRunCustomize

  The value of `DisableFirstRunCustomize` seems to correspond to the → Windows/Group-Policy#win-gpo[Group Policy Object]
  *Computer|User Configuration -> Administrative Templates -> Windows Components -> Internet Explorer -> Prevent running First Run Wizard*.

  This value seems to influence the behaviour of the → Windows/PowerShell/command-inventory/noun/webRequest/invoke[`invoke-webRequest` cmdLet], see also
  → Windows/after-installing#after-inst-win_activate-webRequest[activating webRequest].

}
