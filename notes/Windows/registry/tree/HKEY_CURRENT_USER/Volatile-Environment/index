$Registry: HKEY_CURRENT_USER\Volatile Environment

{ Powershell

  With → Windows/PowerShell[Powershell] and the → Windows/development/COM[COM] object *wscript.shell*, it's possible to access the values of volatile
  environment variables like so
  gh|about-Windows-Registry|/HKEY_CURRENT_USER/Volatile Environment/printVars.ps1||

}
