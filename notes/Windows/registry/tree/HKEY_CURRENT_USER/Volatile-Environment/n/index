$ Registry: HKEY_CURRENT_USER\Volatile Environment\n

The `n` in this registry key seems to correspond to the → Windows/development/session[session] id.

Two → Windows/development/environment-variables[environment variables] are found under this → Windows/registry[registry] key:
  • CLIENTNAME
  • SESSIONNAME

{ SESSIONNAME #reg-vol-env-sessionname

  The value of `SESSIONNAME` (for example: `Console`) seems to correspond to the output of
  → Windows/dirs/Windows/System32/query_exe[`query session`].

}
