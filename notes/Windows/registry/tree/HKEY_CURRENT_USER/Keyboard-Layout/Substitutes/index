$ Registry: HKEY_CURRENT_USER\Keyboard Layout\Substitutes


The Substitutes subkey allows to assign alternative keyboard layouts preferred by the user.
-
This key is empty unless the user has selected a substitute keyboard layout.

This key is checked when a keyboard layout is loaded.

sa:
  → Windows/registry/tree/HKEY_CURRENT_USER/Keyboard-Layout[`HKEY_CURRENT_USER\Keyboard Layout`]
