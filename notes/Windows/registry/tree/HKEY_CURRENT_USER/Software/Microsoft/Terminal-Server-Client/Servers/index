$ Registry: HKEY_CURRENT_USER\Software\Microsoft\Terminal Server Client\Servers

{ Subkeys

  This key seems to store the known *Terminal Servers* in subkeys that are named like the Terminal Server.

  These subkeys contain the values
    • `UsernameHine`
    • … (?)

}

sa:
  → Windows/registry[Registry] key → Windows/registry/tree/HKEY_CURRENT_USER/Software/Microsoft/Terminal-Server-Client[`HKEY_CURRENT_USER\Software\Microsoft\Terminal Server Client`]

  → Windows/Terminal-Server
