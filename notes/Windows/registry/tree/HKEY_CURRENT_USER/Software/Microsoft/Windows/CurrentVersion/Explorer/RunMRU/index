$ Registry: HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Explorer\RunMRU

The values under this → Windows/registry[registry] key show which commands a → Windows/user-account[user] entered into the *run box* (`Win`+`R`).

