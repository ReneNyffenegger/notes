$ Registry: HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Internet Settings

{ TODO

  As per → http://clienthelp.dealogic.com/help/html/InternetExplorerRegistry.htm[this link], the settings under `HKEY_CURRENT_USER\…\Internet Settings` can be rendered ineffective in that it forces to use the
  settings under `HKEY_LOCAL_MACHINE\…\Internet Settings` by setting the value of `Security_HKLM_only` to `1`.


}
