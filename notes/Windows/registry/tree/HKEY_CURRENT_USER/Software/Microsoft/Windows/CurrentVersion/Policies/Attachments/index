$ Registry: HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Policies\Attachments
@ HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Policies\Attachments

The settings under `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Policies\Attachments` seem to correspond to those that are configurable with the group policy editor under
*User Configuration -> Administrative Templates -> Windows Components -> Attachment Manager*.

{ Values

  table { ll

    `HideZoneInfoOnProperties` ☰
    `SaveZoneInformation` ☰
    `ScanWithAntiVirus` ☰
    `UseTrustedHandlers` ☰ 

  table }

}

sa:

  File type extensions for Low, moderate and high riks file types are stored under the registry key `→ Windows/registry/tree/HKEY_CURRENT_USER/Software/Microsoft/Windows/CurrentVersion/Policies/Associations`.

  `HKEY_LOCAL_MACHINE\Software\Microsoft\Windows\CurrentVersion\Policies\Attachments`

