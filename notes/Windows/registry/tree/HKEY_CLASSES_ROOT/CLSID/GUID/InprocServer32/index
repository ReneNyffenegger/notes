$ Registry: HKEY_CLASSES_ROOT\CLSID\{GUID}\InprocServer32

{ Values

  Values that are found under `KEY_CLASSES_ROOT\CLSID\{GUID}\InprocServer32` include
table { ll
  `Assembly` ☰
  `Class` ☰
  `RuntimeVersion` ☰
  `ThreadingModel` ☰ This value seems to be always present and specifies the *type of threading environment* is one of `Both`, `Free`, `Apartment`, `Single` or `Neutral`. See also → Windows/development/COM/Apartments
table }

}

sa:
  → Windows/PowerShell/command-inventory/noun/object/new/COM
