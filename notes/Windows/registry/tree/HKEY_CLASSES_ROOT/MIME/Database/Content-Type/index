$ Registry: HKEY_CLASSES_ROOT\MIME\Database\Content Type

This registry key contains a list of sub keys each of which is a content type (such as `text/html` or `image/png`).
-
These subkeys contain an entry whose name is `Extension` and whose value is a → development/filename-extensions[filename extension].
