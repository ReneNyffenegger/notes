$ Registry: HKEY_CLASSES_ROOT\MIME\Database\Content Type

This registry key contains a list of sub keys that each name a content type (such as `text/html` or `image/png`). These subkeys contain an entry whose name is `Extension` and hosw value is a
→ development/filename-extensions[filename extension].
