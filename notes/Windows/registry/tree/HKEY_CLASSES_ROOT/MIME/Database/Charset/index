$ Registry: HKEY_CLASSES_ROOT\MIME\Database\Charset

This → Windows/registry[registry] key contains a list of subkeys each of which seems to identify a → development/Unicode/character-set[character set].
