$ Registry: HKEY_USERS

The → Windows/registry/tree-structure/key[keys] under the → Windows/registry/tree-structure/hive[registry hive] `HKEY_USERS` correspond to the
→ Windows/security/SID/S-1-5-21_user-account[user account SIDs] of → Windows/user-account[user accounts] (starting with `S-1-5-21-…`) or
a → Windows/services[serivce) (starting with `S-1-5-80-…`)

The currently logged in user's hive → Windows/registry/tree/HKEY_CURRENT_USER[`HKEY_CURRENT_USER`] is a symbolic link to the respective key under `HKEY_USERS`.

Additionally, there is also a `.DEFAULT` subkey.
