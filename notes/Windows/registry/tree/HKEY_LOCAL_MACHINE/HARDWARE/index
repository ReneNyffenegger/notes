$ Registry: HKEY_LOCAL_MACHINE\HARDWARE

`HKLM\HARDWARE` and its subkeys contains and makes accessible data about hardware that was gathered during the booting up of the system (startup).
-
This data is used for information purposes, changing it does not have an effect on the → hardware[hardware].
