$ Registry: HKEY_LOCAL_MACHINE\System

The following table lists some → Windows/registry/tree-structure/key[sub keys] that might be found belo
`HKEY_LOCAL_MACHINE\System`. A tick (✓) indicates that the key was already present after a fresh installation of → Windows/versions/10.
table { ll
  `ActivationBroker` ☰ ✓
  `ControlSet001` ☰ ✓
  `DriverDatabase` ☰ ✓
  `HardwareConfig` ☰ ✓
  `Input` ☰ ✓
  `Keyboard Layout` ☰ ✓
  `Maps` ☰ ✓
  → Windows/registry/tree/HKEY_LOCAL_MACHINE/System/MountedDevices[`MountedDevices`] ☰ ✓
  `ResourceManager` ☰ ✓
  `ResourcePolicyStore` ☰ ✓
  `RNG` ☰ ✓
  → Windows/registry/tree/HKEY_LOCAL_MACHINE/System/Select[`Select`] ☰ ✓
  → Windows/registry/tree/HKEY_LOCAL_MACHINE/System/Setup[`Setup`]☰ ✓
  `Software` ☰ ✓
  `WaaS` ☰ ✓
  `WPA` ☰ ✓
 → Windows/registry/tree/HKEY_LOCAL_MACHINE/System/CurrentControlSet[`CurrentControlSet`] ☰ ✓
table }
