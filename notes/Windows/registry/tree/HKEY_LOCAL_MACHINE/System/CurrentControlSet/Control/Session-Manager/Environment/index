$ Registry: HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Session Manager\Environment

{ TODO

  Apparently, after changing the value of an → development/environment-variable[environment variable], the system should be notified with sending `WM_SETTINGCHANGE`, having `lParam` set to the
  string `"Environment"`.

}

sa:
  → Windows/registry/tree/HKEY_CURRENT_USER/Environment[`HKEY_CURRENT_USER\Environment`]

