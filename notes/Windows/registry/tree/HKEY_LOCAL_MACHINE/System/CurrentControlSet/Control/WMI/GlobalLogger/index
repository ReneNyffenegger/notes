$ Registry: HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\WMI\GlobalLogger

The registry key `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\WMI\GlobalLogger` configures the *Global Logger*, in particular, the value of `Start` determines if
the logger is started (`1`) or not (`0`).

