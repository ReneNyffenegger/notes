$ Registry: HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\SecurityProviders\SCHANNEL\Protocols

→ Windows/registry/tree-structure/key[Subkeys] that are possible under the → Windows/registry tree → `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\SecurityProviders\SCHANNEL\Protocols` are
  • `SSL 2.0`
  • `SSL 3.0`
  • `→ development/security/cryptography/TLS 1.0`
  • `TLS 1.1`
  • `TLS 1.2`

Each of these subkeys can have the two sub-subkeys `Client` and `Server` with a value `DisabledByDefault`.
