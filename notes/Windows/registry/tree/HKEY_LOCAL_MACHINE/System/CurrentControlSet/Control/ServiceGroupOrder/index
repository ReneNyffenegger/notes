$ Registry: HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\ServiceGroupOrder

{ List

  The value of `List` specifies the order in which *kernel device drivers* are loaded during the boot process by the boot manager.

}
