$ Registry: HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Nls\Locale

TODO: Apparently, this key can have multiple *default values* or values whose name is `(default)`.

sa:
  → Windows/development/Internationalization/locale[Windows locale]
