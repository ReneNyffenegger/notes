$ Registry: HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Session Manager\Memory Management

{ System virtual address space quatas

? *system virtual address space limits* can be configured with the values of
  • PagedPoolQuota
  • NonPagedPoolQuota
  • PagingFileQuota
  • WorkingSetPagesQuota

}

{ TrackPtes

  Setting this value to 1 allows to track *system PTE* with → Windows/development/WinAPI/Debugging[debuggers].

  If enabled, the debugging command is `!sysptes 4`.

}
