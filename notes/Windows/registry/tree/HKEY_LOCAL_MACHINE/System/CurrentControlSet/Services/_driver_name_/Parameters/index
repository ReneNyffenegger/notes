$ Registry: HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\driverName\Parameters

The *Parameters* subkey of a driver's (or → Windows/services[service's]) subkey stores *driver *specific data*.

The values under this key are added using the `AddReg` directive of a driver's `.inf` file.
