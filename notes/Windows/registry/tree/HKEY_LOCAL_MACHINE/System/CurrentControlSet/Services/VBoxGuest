$ Registry: HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\VBoxGuest
@ HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\VBoxGuest

The → Windows/registry/tree-structure/key[registry key] `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\VBoxGuest` controls the *VBoxGuest* → Windows/services which is installed on
→ Companies-Products/Oracle/VM-VirtualBox guests running Windows.

→ https://www.virtualbox.org/wiki/AdditionsLogging[This page] explains that some additional logging on the guest side can be enabled by setting `LoggingEnabled` to 255 (0xFF) - but more steps need to be taken.

sa:
 `→ Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/ORACLE/VirtualBox-Guest-Additions`
