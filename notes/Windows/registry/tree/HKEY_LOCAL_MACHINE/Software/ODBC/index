$ Registry: HKEY_LOCAL_MACHINE\SOFTWARE\ODBC

After a fresh installation of → Windows/versions/10, the → Windows/registry key `HKEY_LOCAL_MACHINE\SOFTWARE\ODBC` stores the following two → Windows/registry/tree-structure/key[sub keys]:
  • `ODBC.INI` (which, at least after the installation of Windows, is empty)
  • → Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/ODBC/ODBCINST_INI[`ODBCINST.INI`]

sa:
  → development/databases/ODBC
