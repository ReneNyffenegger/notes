$ HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Microsoft SQL Server

{ InstalledInstances #reg-ms-sql-server_InstalledInstances

  The → Windows/registry/tree-structure/value of `InstalledInstances` is string that contains the *→ development/databases/SQL-Server/architecture/instance names* (not instance IDs) of the
  installed → development/databases/SQL-Server/architecture/instance[instances] on a server.

  The values are stored in a *REG_MULTI_SZ* string, so each instance is stored in its own line.

}

sa:

  Compare with the *other* → Windows/registry/tree-structure/key[registry key]
  → Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/MSSQLServer[`HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\MSSQLServer`].

  → development/databases/SQL-Server
