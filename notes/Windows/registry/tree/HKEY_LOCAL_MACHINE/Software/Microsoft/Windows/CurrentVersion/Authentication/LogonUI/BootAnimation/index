$ Registry: HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Authentication\LogonUI\BootAnimation

It seems that this key contains the value `DisableStartupSound` which determines if the annoying startup sound is enabled or disabled.
