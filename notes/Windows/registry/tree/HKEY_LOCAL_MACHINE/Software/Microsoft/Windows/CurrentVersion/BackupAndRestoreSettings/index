$ Registry: HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\BackupAndRestoreSettings

After a fresh installation of → Windows/versions/10, `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\BackupAndRestoreSettings` has one sub key only: `Providers` which
itself has only two subkeys:
  • `Windows.Internal.BackupRestore.SettingsProviders.LocalBackupSettingsProvider`
  • `Windows.Internal.BackupRestore.SettingsProviders.Win7BackupSettingsProvider`
