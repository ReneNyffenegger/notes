$ Registry: HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows Defender\Features

Values found in `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows Defender\Features` include
table { ll
  `DlpAppEnlightenmentSettings` ☰
  `MpCapability` ☰
  `MpPlatformKillbitsFromEngine` ☰
  `TamperProtection` ☰ As per → https://superuser.com/a/1585355[superuser answer], setting `TamperProtection` to 0 turns tamper protection off, setting it to 5 turns it on. After an initial installation of Windows, I found this value to be 1. It seems that administrator privileges are not sufficient to change this value.
  `TamperProtectionSource` ☰
  `TPExclusions` ☰
table }


sa:
  The parent key `→ Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows-Defender`
