$ Registry: HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\CurrentVersion\Image File Execution Options\progName.exe

This → Windows/registry[registry] relate to the executable with the name `progName.exe`.

Values that are often seen in the → Windows/registry/tree-structure/key is `MitigationOptions` and `CFGOptions`.

{ GlobalFlag

  The value of `GlobalFlag` can be set with → Windows/dirs/Program-Files-x86/Windows-Kits/n/Debuggers/arch/gflags_exe[`gflags.exe`].

  See also the value of `GlobalFlag` under the registry key → Windows/registry/tree/HKEY_LOCAL_MACHINE/System/CurrentControlSet/Control/Session-Manager[`HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control\Session Manager`].

}

sa:
  → Windows/development/Portable-Executable/EXE[PE format for exes].
