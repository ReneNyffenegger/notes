$ Registry: HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\FontLink\SystemLink

`HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\FontLink\SystemLink` defines *linked fonts*.
Font linking is used to display → design/font/glyph[glyphs] that are not available for a particular font.

Each entry under this key corresponds to the name of a *base font*. An entry's value is an array of strings that specifies the font files and fonts
that are linked to that base font.
