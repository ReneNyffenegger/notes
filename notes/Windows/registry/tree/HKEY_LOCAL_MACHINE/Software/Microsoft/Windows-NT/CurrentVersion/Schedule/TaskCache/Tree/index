$ Registry: HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Schedule\TaskCache\Tree
@ HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Schedule\TaskCache\Tree

The → Windows/registry[registry] key structure under this key (ideally) corresponds to the directory structure under `→ Windows/dirs/Windows/System32/Tasks` (however, I was able to corrupt this by manually deleting a folder in the registry).
-
In some task related PowerShell cmdlets, a path in this tree is referred to with the `-TaskPath` option.

{ Creating keys in PowerShell / COM

code {
$scSvc = new-object -comObject schedule.service
$scSvc.connect()
$scSvc.getFolder('\').createFolder('IGS\Synergy')
code }

}

sa:
 `→ Windows/dirs/Windows/System32/schtasks_exe`
