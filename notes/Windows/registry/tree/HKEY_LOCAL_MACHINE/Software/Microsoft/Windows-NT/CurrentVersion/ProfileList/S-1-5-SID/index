$ Registry: HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\ProfileList\S-1-5-SID
@ HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\ProfileList\S-1-5-SID

Some values that are found under `→ Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows-NT/CurrentVersion/ProfileList[HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\ProfileList]\S-1-5-SID` include:
table { ll
  `ProfileImagePath` ☰ The path to the user's profile directory (for example `→ Windows/dirs/Users/username[C:\Users\Rene]`).
  `Flags` ☰
  `FullProfile` ☰
  `State` ☰
  `Sid` ☰ The User's → Windows/security/SID[SID] (the same value as the last part of this registry key)
  `LocalProfileLoadTimeLow` ☰
  `LocalProfileLoadTimeHigh` ☰
  `ProfileAttemptedProfileDownloadTimeLow` ☰
  `ProfileAttemptedProfileDownloadTimeHigh` ☰
  `ProfileLoadTimeLow` ☰
  `ProfileLoadTimeHigh` ☰
  `RunLogonScriptSync` ☰
  `LocalProfileUnloadTimeLow` ☰
  `LocalProfileUnloadTimeHigh` ☰
table }
