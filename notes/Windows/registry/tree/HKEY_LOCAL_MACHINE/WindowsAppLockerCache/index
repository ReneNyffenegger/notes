$ Registry: HKEY_LOCAL_MACHINE\WindowsAppLockerCache
@ HKEY_LOCAL_MACHINE\WindowsAppLockerCache

I found the registry key `HKEY_LOCAL_MACHINE\WindowsAppLockerCache` in a → Windows/optional-features/Windows-Sandbox guest with System privileges. I'm not sure if this key is present in non Sandboxes too.

