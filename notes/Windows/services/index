$ Windows services


`services.msc`

`→ Windows/dirs/Windows[%systemroot%]\system32\svchost.exe`

Communicate with the *sercie control manager* with `sc`.

Information about each service on a system is stored in the → Windows/registry[registry] under
→ Windows/registry/tree/HKEY_LOCAL_MACHINE/System/CurrentControlSet/services[`HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\services`]

{ svchost.exe #svchost-exe

  svchost.exe runs services.

  When Windows starts up, svchost.exe starts the services it finds in the → Windows/registry[registry] under the key
  → Windows/registry/tree/HKEY_LOCAL_MACHINE/Software/Microsoft/Windows-NT/CurrentVersion/Svchost[`HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Svchost`].

  See → https://blogs.technet.microsoft.com/askperf/2008/01/11/getting-started-with-svchost-exe-troubleshooting/[here].




}

sa:
  The *well known* → Windows/security/SID[SID] `S-1-5-80`.
