$ Android SDK: system-images/android-apiLevel/variant/arch

`system-images/→ development/Android/SDK/dirs/system-images/android-apiLevel[android-apiLevel]/→ development/Android/SDK/dirs/system-images/android-apiLevel/variant[variant]/arch` is
the *→ development/Android/AVD system directory*.

{ arch

  The value of arch identifies the target architecture and assumes values such as `x86`.

}

{ Files

  table { ll

    → development/Android/SDK/dirs/system-images/android-apiLevel/variant/arch/advancedFeatures_ini[`advancedFeatures.ini`] ☰ 
    `build.prop` ☰
    `encryptionkey.img` ☰
    `kernel-ranchu-64` ☰ The binary → Linux/kernel image. (Alternative names seem to be `kernel-qemu` and `kernel-ranchu`)
    `NOTICE.txt` ☰
    `package.xml` ☰
    `ramdisk.img` ☰ The *boot partition* image. It is a subset of of `system.img`.
    `source.properties` ☰
    `system.img` ☰ The *read-only* initial version of the system image that contains the *system libraries* and data corresponding the API level and variant. A subset of this file is `ramdisk.img`.
    `userdata.img` ☰ The initial version of the data partition that contains all *writable* data for the AVD. This file is used when a new AVD is created or the `-wipe-data` option is used. See also the `userdata-qemu.img` file in the → development/Android/AVD/dirs/data.
    `vendor.img` ☰ The initial vendor image.
    `VerifiedBootParams.textproto` ☰

  table }

  Besides these files, there is also the subdirectory `data`.

}

sa:

  The `-sysdir` option allows to specify a different system directory.
  

  The parent directory → development/Android/SDK/dirs/system-images/android-apiLevel/variant[variant]
