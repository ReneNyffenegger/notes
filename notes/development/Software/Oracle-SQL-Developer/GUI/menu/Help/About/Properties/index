$ SQL Developer: Help -> About -> Properties

Some interesting properties that are listed (imho):
table { ll
  `ide.conf`        ☰ → development/Software/Oracle-SQL-Developer/directories/installation/sqldeveloper/bin/sqldeveloper_conf[`sqldeveloper.conf` path]?
  `ide.config_path` ☰ → development/Software/Oracle-SQL-Developer/directories/installation/sqldeveloper/bin/sqldeveloper_conf[`sqldeveloper.conf` path]?
  `ide.extension.search.path` ☰
  `ide.pref.dir` ☰
  `idepref.dir.base` ☰
  `ide.startingArg0` ☰
  `ide.startingcwd` ☰
  `ide.system.dir` ☰
  `ide.user.dir` ☰
  `ide.user.dir.var` ☰ Default value being `SQLDEVELOPER_USER_DIR`?  (see also → development/Software/Oracle-SQL-Developer/directories/user-information).
  `ide.work.dir` ☰
  `ide.work.dir.base` ☰
  `java.class.path` ☰
  `java.home` ☰
  `oracle.ide.startup.features` ☰ For example: `sqldeveloper`
  `oracle.jdbc.mapDateToTimestamp` ☰
  `oracle.jdbc.Trace` ☰
  `oracle.jdbc.useFetchSizeWithLongColumn` ☰
  `path.separator` ☰ Whose default value seems to be `;`
  `sqldev.debug` ☰
  `sqldeveloper.oci.available` ☰
  `tool.user.conf` ☰ → development/Software/Oracle-SQL-Developer/directories/installation/sqldeveloper/bin/sqldeveloper_conf[`sqldeveloper.conf` path]?
  `user.conf` ☰ Path to `product.conf` ?
  `user.dir` ☰
  `user.home` ☰
  `user.script` ☰

table }

