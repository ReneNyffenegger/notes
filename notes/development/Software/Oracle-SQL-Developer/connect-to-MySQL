$ Connect to MySQL with Oracle's SQL Developer

The required jar file can be downloaded from → https://dev.mysql.com/downloads/connector/j/[dev.mysql.com/downloads/connector/j].

As operating system, *platform independent* needs to be chosen.

The downloaded zip file will contain a file with a version number similar to `mysql-connector-java-8.0.15.jar`.
-
This jar file needs to be added as outlined in → development/Software/Oracle-SQL-Developer/connect-to-SQL-Server.

sa:
  → development/Software/Oracle-SQL-Developer[SQL Developer]
