$ SQL Developer installation directory: sqldeveloper/bin/sql

`sql` or `sql.exe` are the executables for → development/databases/Oracle/SQLcl.

Unfortunately, `sql.exe` opens in a new console which makes it virtually unusable on Windows.
