$ SQL Extensions for Oracle SQL Developer and SQLcl

In SQL Developer, `F5`, not `ctrl+enter` must be used to turn the extensions on.

code {
select /*insert/* * from ...;
code }

code {
select /*xml/* * from ...;
code }

TODO: compare with → development/databases/Oracle/SQLcl#sqlcl-sqldev-select-hints[SQL select hints].
