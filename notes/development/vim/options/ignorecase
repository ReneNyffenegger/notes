$ VIM option: ignorecase
@ ignorecase

`ignorecase` has no influence on `cinwords`.
-
In → development/vim/script/9[VIM Script 9], `ignorecase` also has no influence on the behavior of string-comperators.

sa:
  → development/vim/options 
