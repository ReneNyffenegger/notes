$ vim option: indentkeys

Defines a list of keys that, when typed in insert mode and when → development/vim/options/indentexpr[indentexpr] is set, cause the current line to be → development/vim/editing/indent[re-indented].

The format is the same as that of → development/vim/options/cinkeys[cinkeys].

sa:
  → development/vim/options
