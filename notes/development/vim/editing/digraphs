$ Vim digraphs

Fractions: `nm` (for example, n=2, m=5 produces ⅖)

Superscript and subscript: `nS` and `ns` (n= 0 … 9, +, -, =, ( and ) )

Arrows: `-n` (`n` = !, v, >, <)

Roman numbers: `nr` and `nR` (`n` = 1 .. 9, a, b, c)

Greek letters: `n*` (n = A, B …, a, b ..)

Hebrew letters: `n+` (n =A, B …)


{ Misc

  *digraphs* are somehow related to → development/web/RFC[RFC] → development/web/RFC#rfc-1345.

}
sa:
  → development/vim/editing
