$ Vim: command-line mode
@ command-line mode

*Command-line mode* (aka Cmdline mode) is used to enter → development/vim/commands[ex commands], pattern search commands (`/`, `?`) and the filter command (`!`).
