$ VIM runtime: compilier/

The `compiler/` directory contains Vim scripts to be used with a specific compiler whose main purpose is to set
→ development/vim/options[options] such as `errorformat`.

sa:
  The `:compiler` command.

  `:help write-compiler-plugin`
