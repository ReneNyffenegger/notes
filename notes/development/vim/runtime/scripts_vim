$ VIMRUNTIME/scripts.vim

This file is called by an autocommand for every file that has just been
loaded into a → development/vim/editing/buffers[buffer].  It checks if the type of file can be recognized by
the file contents.  The autocommand is in → development/vim/runtime/filetype_vim[$VIMRUNTIME/filetype.vim].
