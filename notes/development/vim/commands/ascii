$ Vim ex command: ascii
@ ascii

Print the value of the character under the cursor in decimal, hexadecimal and octal representation.
Typing `ga` does the same thing.

sa:
  → development/vim/commands[Ex commands]
