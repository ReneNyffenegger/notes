$ Vim ex command: d
@ d

The `d` command deletes line in a → development/vim/editing/buffers[buffer].

Delete current line:
code {
:d
code }

Delete lines in a given *range*:
code {
:range d
code }

Copy deleted lines into register `r`:
code {
:range d → development/vim/registers[r]
code }

sa:
  → development/vim/commands[Ex commands]
