$ Vim ex command: simalt
@ simalt

code {
:simalt c
:simalt xyz
code }

Simulate pressing alt + character (`c`) or alt + characters (`xyz`).

The tilde `~` opens the system menu:
code {
:simalt ~
code }

sa:
  `:simalt` is implemented in → development/vim/sources/src/gui_w32_c#vim-src-gui_w32-ex_simalt[`ex_simalt()`].
