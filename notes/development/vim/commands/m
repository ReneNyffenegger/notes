$ Vim ex commands: m, t and co
@ m, t and co

`:m` is an abbreviation for `:move`, `:t` and `:co` are abbreviations for `:copy`.


Move/copy current line to the end of the → development/vim/editing/buffers[buffer]: `:m$` / `:t$`.

Move the line after two lines below cursor to end of the buffer: `:+2m$`

Copy three lines, starting after two lines, to beginning of buffer: `:+2,:+4 t 0` (spaces not required, here included for readabilty)
sa:
  → development/vim/commands[Ex commands]
