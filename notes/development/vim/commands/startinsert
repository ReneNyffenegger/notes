
When `startinsert` is used within → development/vim/script/vimscript: when the function ends, vim is then in *insert mode*.

→ development/vim does not have a `startappend` command. Instead, `startinsert!` (with an exclamation mark) can be used.
