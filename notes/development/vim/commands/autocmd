$ Vim ex command: autocmd
@ autocmd

What is executed when creating a new `*.sql` file:
code {
:autocmd BufNewFile *.sql
code }

In order to determine *where* an autocmd was defined, the → development/vim/commands/verbose[`:verbose`] command can be used:
code {
:verbose autocmd BufNewFile *.sql
code }

sa:
  → development/vim/commands/augroup

  → development/vim/events
