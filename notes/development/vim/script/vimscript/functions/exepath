$ vim script: exepath

`execpath("foo")` returns the path of the executable `foo`.

sa:
 → development/vim/script/vimscript/functions/executable[`executable("foo")`] checks if `foo` is an executable in PATH.
