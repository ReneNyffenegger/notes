$ src/os_w32exe.c

Contains `wWinMain` which is called as entry point in an Windows GUI (→ development/vim/features[feature] `FEAT_GUI`) environment.
-
`WinMain` eventually calls `VimMain()`, found in → development/vim/src/main_c
