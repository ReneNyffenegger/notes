$ src/main.c

{ main

  `main.c` defines → development/languages/C-C-plus-plus/C/main[`main`].

   In Windows environments, `main` is  called from `WinMain`, found in → development/vim/src/os_w32exe_c[`os_w32exe.c`].

}
{ main_loop

 `main_loop` executes normal mode commands until vim is terminated.

 `main_loop` is alos used in the command-line window until that window is closed.

}
