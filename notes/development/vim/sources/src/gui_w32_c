$ VIM: src/gui_w32

GUI support for Microsoft Windows, aka Win32.

Among others, this file handles Windows messages such as `WM_KEYDOWN`, `WM_SYSKEYDOWN`.

{ gui_mch_init()

 `gui_mch_init()` initializes the GUI.

}
{ _WndProc()

 `_WndProc()` seems to be the Window procedure for gvim's main window.

}
{ _OnChar() #vim-src-gui_w32-_OnChar

  `_OnChar` seems to be (one of the?) functions that calls → development/vim/sources/src/ui_c#vim-src-ui-add_to_input_buf[`add_to_input_buf()`].

}
