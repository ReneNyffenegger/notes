$ vim - src/os_dos.h


Common → development/languages/C-C-plus-plus/preprocessor/macros[preprocessor macros] for a → Windows environment.

These macros define the path of files (exrc, help etc) and temporary files.

Some of these may have been defined in the makefile or → development/vim/sources/src/feature_h[`feature.h`].

Among others, the `SYS_VIMRC_FILE` macro is defined for a → Windows environment (see → development/vim/vimrc) to the value `$VIM\vimrc`.
