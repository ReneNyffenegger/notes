$ VIM: src/os_w32exe.c
@ src/os_w32exe.c

{ wWinMain()

  `wWinMain` is the entry point for VIM in an Windows (with GUI? → development/vim/features[feature] `FEAT_GUI`) environment.

 `WinMain` eventually calls → development/vim/sources/src/main_c#vim-src-main-main[`VimMain()`], found in → development/vim/sources/src/main_c

}

sa:
  → development/vim/src/os_mswin_c

  → development/vim/sources/src/os_w32dll_c[`src/os_w32dll.c`]
