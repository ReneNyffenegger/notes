$ VIM: src/drawscreen.c

Functions to update all → development/vim/editing/windows[windows] on the screen.

{ update_screen()

 `update_screen()` is the functino that updates all windows and status lines.

  It is calle from the *main loop* when `must_redraw` is non-zero.

  It may be called from other places when an immediate screen update is needed.

}
