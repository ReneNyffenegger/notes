$ VIM: src/option.c

`option.c` contains code to handle user-settable → development/vim/options[options].

{ vimrc_found()

  `vimrc_found()` is called when a `.vimrc` or `"VIMINIT"` has been found.

}
{ set_init_1()

 …

}

sa:
  → development/vim/sources/src/option_h[option.h]
