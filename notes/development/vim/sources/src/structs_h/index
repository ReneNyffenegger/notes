$ vim - src/structs.h

`structs.h` defines some  → development/languages/C-C-plus-plus/C/language/struct[structs] that are used in vim such as → development/vim/sources/src/structs_h/window_S[`window_S`] (`win_T`) that contains all information
that belongs to a → development/vim/editing/windows[window].

→ development/vim/sources/src/structs_h/buffblock_T[`buffblock_T`] is used to store one block of the suff/redo/recording buffers.

→ development/vim/sources/src/structs_h/buffheader_T[`buffheader_T`] is used for the *stuff buffer* and the *redo buffer*.

`dictitem_S` / `dictitem_T` is able to store an *dictionary item* (such as a → development/vim/script/vimscript/variables[variables]).

→ development/vim/sources/src/structs_h/foldinfo_T[`foldinfo_T`] used to pass info about a fold frmo the fold-detectino code to the code that displays the foldcolumn.

→ development/vim/sources/src/structs_h/garray_T[`garray_T`] is used for *growing arrays*.

→ development/vim/sources/src/structs_h/sctx_T[`sctx_T`] stores a *script context* (basically a line number in a script).

→ development/vim/sources/src/structs_h/tasave_T[`tasave_T`] holds the saved *typeahead* for `save_typeahead()`.

→ development/vim/sources/src/structs_h/typebuf_T[`typebuf_T`] the type-ahead buffer.

→ development/vim/sources/src/structs_h/match_T[`match_T`] is used for highlighting

→ development/vim/sources/src/structs_h/mparm_T[`mparm_T`] is a struct to pass parameters between → development/vim/sources/src/main_c[`main()`] and other functions.
-
An interesting member is `use_vimrc` which contains the → development/vim/vimrc file from the → development/vim/editing/start-up/command-line-options[`-u` command line option].
