$ vim - src/structs.h

`structs.h` defines some  → development/languages/C-C-plus-plus/C/language/struct[structs] that are used in vim such as `window_S` (`win_T`) that contains all information
that belongs to a window.

`dictitem_S` / `dictitem_T` is able to store an *dictionary item* (such as a → development/vim/script/vimscript/variables[variables].

`mparm_T` is a struct to pass parameters between → development/vim/src/main_c[`main`] and other functions.
-
An interesting member is `use_vimrc` which contains the → development/vim/vimrc file from the → development/vim/editing/start-up/command-line-options[`-u` command line option].
