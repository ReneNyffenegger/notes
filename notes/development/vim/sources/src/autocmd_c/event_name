$ VIM struct: event_name

`event_name` is a static struct that assigns a name to an event number (→ development/vim/sources/src/vim_h/auto_event[enum `auto_event`, typedef `event_T`]).

code {
static struct event_name
{
    char	*name;	// event name
    event_T	event;	// event number
} event_names[] =
{
    {"BufAdd",		EVENT_BUFADD},
    {"BufCreate",	EVENT_BUFADD},
    {"BufDelete",	EVENT_BUFDELETE},
      …
};
code }
