$ vim - src/feature.h

`feature.h` allows to define some preferences for → development/vim. In order to change these preferences, it should be changed
before → development/vim/compiling[compiling vim].
