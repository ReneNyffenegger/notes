$ vim: src/normal.c

 `normal.c` contains the main routine for processing characters in command mode (aka → development/vim/editing/modes/normal?).
 -
 Communicates closely with the code in ops.c to handle the operators.

`nv_cmd` is the list that stores every → development/vim/editing/modes/normal[normal] or
→ development/vim/editing/modes/visual[visual mode] command.
