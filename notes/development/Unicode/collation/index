-

The collation specifies how text is sorted. Thus, it is used, for example, in a → development/databases/SQL/select/order-by[`order by`] clause
of → development/databases/SQL.

Apparently, in Sweden, `z` is ordered before `ö` while in Germany, the `ö` is ordered before the `z`.
