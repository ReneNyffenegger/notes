$ openssl asn1parse
@ asn1parse

code {
$ openssl → development/security/cryptography/SSL-TLS/OpenSSL/executable/req -new -config csr.conf -keyout server.key -out server.csr
$ openssl asn1parse -in ./server.csr -inform PEM -i
code }
