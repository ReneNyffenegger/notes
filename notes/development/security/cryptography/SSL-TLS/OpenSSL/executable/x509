$ openssl x509
@ x509

Display, sign, convert → development/security/cryptography/public-key/certificate[certificates] etc.

Show the content of a → development/security/cryptography/public-key/certificate:
code {
$ openssl x509 -in xyz.crt -noout -text
code }


{ TODO

code {
$ → development/security/cryptography/SSL-TLS/OpenSSL/executable req -new -out xyz.csr
$ openssl → development/security/cryptography/SSL-TLS/OpenSSL/executable/rsa       -in server.key -out priv.key
$ openssl x509 -in server.csr -out server.crt -req -signkey server.key -days 365
code }

}
