$ SSH configuration

{ Configuration file #ssh-config-file

  The system wide configuration file is located in `→ Linux/fhs/etc/ssh/ssh_config`.
  -
  A user's configuration file is located in `→ Linux/fhs/home/username/_ssh/config`.

}

sa:
  `ssh -G`
  -
  `ssh -F …`

