
PAM is a standard library for performing user → development/security/authentication[authentication] - but not identification.

With modules (libraries), it's possible to access different authentication forms.

sa:
 `→ Linux/fhs/etc/pam_d`
