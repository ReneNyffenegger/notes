
PAM is a standard library for performing user ahtentication - but not identification.

With modules (libraries), it's possible to access different authentication forms.

sa:
  → Linux/fhs/etc/pam_d
