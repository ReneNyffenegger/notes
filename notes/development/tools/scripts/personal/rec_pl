$ Script: rec.pl

`rec.pl` is a → development/languages/Perl[Perl] script that recursively does *stuff* that I frequently use. *stuff* is determined by the command line options to the script.

gh|scripts-and-utilities|/rec.pl||

sa:
  → development/tools/scripts/personal/[Scripts]
