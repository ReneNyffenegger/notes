$ goWnd.ps1

`goWnd.ps` is a → Windows/PowerShell script that takes as its only argument the name of a → Windows/development/process[process] and then calls the
→ Windows/development/WinAPI function `SetForegroundWindow()` with the process's main window handle.

gh|scripts-and-utilities|/goWnd.ps1||

sa:
  The → Windows/PowerShell/command-inventory/noun/process/get[`get-process`] command let.

  → development/tools/scripts/personal[Scripts]
