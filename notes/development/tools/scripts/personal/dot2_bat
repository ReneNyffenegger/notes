$ Script: dot2.bat

`dot2.bat` processes a → tools/Graphviz[Graphviz / dot] file and opens the result.

The script is invoked like so:
code {
C:\users\rene> dot2 <out-format> graph.dot
code }

The value of `<out-format>` must be one that is accepted by the → https://graphviz.org/doc/info/command.html#-T[`-T` argument] of `dot`, for example
`pdf`, `png`, `svg` etc. (See also → https://graphviz.org/docs/outputs/[this link]).

{ The script

  gh|scripts-and-utilities|/dot2.bat||

}

sa:
  → development/tools/scripts/personal[Scripts]
