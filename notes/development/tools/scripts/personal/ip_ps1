$ ip.ps1 - Print IP Addresses of local non-loopback network interfaces.

`ip.ps1` is a → Windows/PowerShell/script[PowerShell script] that prints the → development/networking/IP#determine-machines-ip-addr[IP Address of local non-loopback network interfaces].

gh|scripts-and-utilities|/ip.ps1||

sa:

  Other → development/tools/scripts/personal[Scripts]

links:
  This script was inspired by → https://github.com/Treer/ip4
  
