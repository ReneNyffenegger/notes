$ Docker: registries
@ registry

{ Default registry: docker.io

? The default registry is Docker hub (`docker.io`). This default → https://stackoverflow.com/a/33054440/180275[cannot be changed] with the genuine Docker CE or EE version, but
  it is possible with → https://stackoverflow.com/a/41578728/180275[Red Hat's fork] and its `--add-registry` flag.
  

}

sa:
  `→ development/tools/Docker/commands/login` and `docker logout` logs a user in to or out from a registry.

  `→ development/tools/Docker/commands/pull` pull an image or a repository from a registry
