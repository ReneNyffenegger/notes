$ docker port

`docker port` lists a container's port mappings or specific mapping.

code {
docker port CONTAINER [PRIVATE_PORT[/PROTO]]
code }

code {
$ docker port ora23
1521/tcp -> 0.0.0.0:1521
1521/tcp -> :::1521
code }

sa:

  The `-p` (or `--publish`) option of `→ development/tools/Docker/commands/run`

  → development/tools/Docker/commands
