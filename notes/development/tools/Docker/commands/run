$ docker run

`docker run` creates a → development/tools/Docker/container[container] and runs a command in it.

{ -p / -P


  TODO: see the → development/tools/Docker/Dockerfile#dockerfie-expose[Dockerfile `EXPOSE` command].

}
