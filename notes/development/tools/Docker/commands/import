$ docker import

`docker import` creates a *filesytem* → development/tools/Docker/image from a tar archive.

The following command creates the most minimal image possible
code {
→ Linux/shell/commands/tar#tar-minimal cv --files-from → Linux/fhs/dev/null | docker import - minimal-image
code }

sa:

  → development/tools/Docker/commands
