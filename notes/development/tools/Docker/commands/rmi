$ docker rmi

`docker rmi` removes one or more → development/tools/Docker/image[images].

code {
docker rmi [OPTIONS] IMAGE [IMAGE…]
code }

{ Options

  table { lll
  `-f` ☰ `--force`    ☰ Force removal of the image
       ☰ `--no-prune` ☰ Do not delete untagged parents
  table }

}

sa:

 `→ development/tools/Docker/commands/rm` removes → development/tools/Docker/container[containers].

  → development/tools/Docker/commands.

  `→ development/tools/Docker/commands/image/rm` is an alias for `docker rmi`
