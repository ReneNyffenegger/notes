$ docker container prune

`docker container prune` removes all stopped containers.

The optional `--filter` argument allows to specify criteria which must be met for c container to be removed.

sa:

  `→ development/tools/Docker/commands/rm`

  `→ development/tools/Docker/commands/image[docker image] prune`

  `→ development/tools/Docker/commands/container`

  → development/tools/Docker/commands[Docker commands]
