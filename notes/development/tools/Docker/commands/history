$ docker history

`docker history` shows an → development/tools/Docker/image[image's] history.

Apparently, the image must be available locally to inspect its history:
code {
docker pull    $image
docker history $image
docker rmi     $image
code }


Don't truncate column widths (gets very wide possibly):
code {
docker history --no-trunc $image
code }
