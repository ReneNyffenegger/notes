$ src/addrdb.h

Contains `CAddrDB` which accesses the address database stored in `peers.dat` in the
→ development/Crypto-Currencies/Bitcoin/clients/Bitcoin-Core/Data-directory.

Also contains `CBanDB` which access es the banlist database stored in `banlist.dat` (also in the → development/Crypto-Currencies/Bitcoin/clients/Bitcoin-Core/Data-directory).
