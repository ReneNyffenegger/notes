abbr: Media Transfer Protocol

The *Media Transfer Protocol* allows to transfer media files between mobiles and PCs.

{ TODO

code {
$ → Linux/shell/commands/sudo → Linux/distributions/arch/pacman -Sy mtpfs
code }

  PTP = Picture Transfer Protocol

  → Linux/filesystem/GVfs[gvfs] (→ Linux/GUI/GNOME[gnome] Virtual File System)

  When I connected an Android mobile to PC running → Windows, it first didn't recognize the device. However, when I changed the cable, Windows was able to recognize the device without problems.

}
