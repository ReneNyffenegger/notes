
→ development/tools/CMake

→ development/GNU/toolchain/Build-System[GNU Build System] (aka autotools).

→ development/build-systems/Meson

SCons

{ Misc

  Meson and → development/tools/CMake don't build software directly, but rather use a backend build system: *ninja* on → Linux, *MSBuild* on → Windows and *XCode* on macOS.


}
