$ .gitignore


{ Ignore every file except css files

code {
*
!*.css
code }

}

{ «empty» directory

code {
*
!.gitignore
code }

}

sa:
  → development/version-control-systems/git/_git/info/exclude[`$GIT_DIR/info/exclude`]

  The → development/version-control-systems/git/options[option] `core.excludesfile`
