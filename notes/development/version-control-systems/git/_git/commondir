$ .git/commondir

Sets the value of → development/environment-variable#git_common_dir[$GIT_COMMON_DIR] (if $GIT_COMMON_DIR is not explicitly set).

sa:
  → development/version-control-systems/git/_git#_git-dir-structure[`.git` directory structure]
