$ .git/refs/heads

This directory stores *heads* (except when using *packed refs*). That is: it contains a file for each local → development/version-control-systems/git/branch. The name of the file
matches the name of the branch. The file contains the hash of the hash of the latest → development/version-control-systems/git/objects/commit[commit] for the corresponding branch.

Heads are a named reference to the → development/version-control-systems/git/objects/commit[commit] at
the → development/version-control-systems/git/branch#tip-of-the-branch[tip of a branch].

This directory contains files that are → development/version-control-systems/git/_git/refs/heads/branch_name[named after branch names].

sa:
  → development/version-control-systems/git/_git/refs
