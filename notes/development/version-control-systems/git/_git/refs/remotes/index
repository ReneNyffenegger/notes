$ .git/refs/remotes

This directory lists all → development/version-control-systems/git/repository#remote[remote repositories] that were created with → development/version-control-systems/git/commands/remote[`git remote`] plus
the repository named `origin` that points to the repository from which the local repository was → development/version-control-systems/git/commands/clone[cloned] from (*if* it was cloned, of course).

sa:

  *Local branches* are listed below → development/version-control-systems/git/_git/refs/heads[`./git/refs/heads`]

  → development/version-control-systems/git/_git#_git-dir-structure[`.git` directory structure]

  → development/version-control-systems/git/_git/refs
