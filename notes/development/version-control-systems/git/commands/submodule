$ git submodule
@ submodule

Initialize, update or inspect submodules.

After → development/version-control-systems/git/commands/clone[cloning](?):
code {
git submodule update --init --force --remote
code }

sa:
  → development/version-control-systems/git/commands
