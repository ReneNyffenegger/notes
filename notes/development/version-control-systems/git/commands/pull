$ git pull
@ pull

The pull command performs actually two operations: 1) it fetches changes from a remote branch and
2) merges (»integrates«) them into the → development/version-control-systems/git/branch#current[current branch].

rem {
  Fetch from and integrate with another → development/version-control-systems/git/repository[repository]
  or a local → development/version-control-systems/git/branch[branch].
rem }

sa:
  `→ development/version-control-systems/git/commands/push`
