$ git gui

`git gui` opens a → development/GUI graphical interface (GUI)] which allows to perform some operations such as
  • staging
  • committing
  • adding
  • pushing
  • viewing blames
  • browsing a tree
  • making exactly one commit before exiting (used as a *citool*)
  • etc.

{ Viewing a repository's content of another branch #git-gui-browser

 `git gui browser <branch>` allows to view («to browse») the tree and content of the files of another → development/version-control-systems/git/branch without
  changing the branch.

code {
git gui browser V1.1
code }

  Apparently, `gui gui browser` must be executed in a checked out directory's top directory, i.e. the directory where → development/version-control-systems/git/_git[`./.git`] is located.

}

sa:

  → development/version-control-systems/git/commands

links:
  The → https://github.com/prati0100/git-gui/[git gui repository] on github.
