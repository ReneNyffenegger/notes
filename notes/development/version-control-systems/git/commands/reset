$ git reset

Reset current → development/version-control-systems/git/HEAD to the specified state.

`git reset` is useful, for example, if a → development/version-control-systems/git/merge resulted in conflicts. With `git reset`, the
merge can then be started over.

{ git reset --hard

  `git reset --hard` is a synonym for `git reset --hard HEAD`.

}

sa:
  → development/version-control-systems/git/commands
