$ git add

`git add` adds files (or more accuratly their content) to the → development/version-control-systems/git/index_[index].

After adding files to the index, they can be put permanently to a repository's history with `→ development/version-control-systems/git/commands/commit`.

sa:


  → development/version-control-systems/git/commands
