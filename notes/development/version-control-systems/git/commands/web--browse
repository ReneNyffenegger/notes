$ git web--browse

A helper script to launch a → development/web/browser and display the specified file (or URL) as a → development/web/HTML page.
-
The shown page is opened in a new tab.

code {
git web--browse .\readme.txt
code }


sa:

  `→ development/version-control-systems/git/commands/instaweb`

  → development/Cygwin#cygstart[`cygstart`]
