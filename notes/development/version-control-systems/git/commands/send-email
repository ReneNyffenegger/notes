$ git send-email

`→ development/version-control-systems/git[git] send-email` is mostly used by developers to send patches (for example when developping for Linux).

Maintainers receiving the patches typically use `→ development/version-control-systems/git/commands/am` to apply patches.

{ git: 'send-email' is not a git command. See 'git --help'.

  Ubuntu/Debian:
code {
$ sudo apt install -y git-email
code }

}

sa:

 `→ development/version-control-systems/git/commands/format-patch`

 `→ development/version-control-systems/git/commands/am`

  → development/version-control-systems/git/commands
