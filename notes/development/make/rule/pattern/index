$ make: pattern rules
@ pattern rules

{ Example

  The following Makefile builds `executable` if the current directory contains the four → development/languages/C-C-plus-plus/C[c] files `prog.c`, `foo.c`, `bar.c` and `baz.c`.

  gh|about-Makefile|/rule/pattern/example-1/Makefile||

}

sa:
  → development/make
