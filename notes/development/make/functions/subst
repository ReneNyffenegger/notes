$ GNU make: function $(subst …)

`$(subst what, with, text)` substituts `$(what)` in `$(text)` with `$(with)`.

gh|about-Makefile|/functions/subst/Makefile||

sa:
  → development/make/functions[Make functions]
