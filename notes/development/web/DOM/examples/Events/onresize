$ DOM example: onresize

The `window.onresize` property calls the assigned function when the window is resized.

gh|about-Document-Object-Model|/Events/onresize.html||

sa:
  → development/web/DOM/examples/[DOM examples]
