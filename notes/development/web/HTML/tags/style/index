$ HTML element: style
@ style

code {
<style>

  #foo { background-color: yellow }

  …etc…

</style>
code }


{ Attributes

  Besides the *global attributes*, the `<style>` tag also allows the following attributes
    • `media` (a valid media query list, default: `all`)
    • `title`

  The `type` attribute is not required anymore since HTML 5.

}



sa:

  The → development/web/HTML/attributes#html-attr-style[`style` attribute]

  → development/web/CSS/inline-vs-external

  → development/web/HTML/tags
