$ HTML element: video

{ Attributes specific to the <video> tag

  Besides the → development/web/HTML/attributes#html-global-attributes[global attributes], the `<video>` element has these specific attributes:
  table { ll
    `autoplay` ☰
    `controls` ☰
    `controlslist` ☰
    `crossorigin` ☰
    `disablepictureinpicture` ☰
    `disableremoteplayback` ☰
    `height`, `width` ☰
    `loop` ☰
    `muted` ☰
    `playsinline` ☰
    `poster` ☰
    `src` ☰
  table }
}

sa:
  Render a → development/web/HTML/tags/video/webcam[webcam stream] in a HTML page.

  Other → development/web/HTML/tags such as `<source>` or → development/web/HTML/tags/audio[`audio>`]
