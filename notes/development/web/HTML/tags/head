$ HTML element: head

A `<head>` element contains the → development/Data/metadata about the *document*.

The `<head>` element is the first child element of the → development/web/HTML/tags/html[`<html>`] element.

sa:

  → development/web/HTML/tags
