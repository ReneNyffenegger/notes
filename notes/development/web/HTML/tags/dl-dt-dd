$ HTML tags: dl, dt and dd

`<dl>` starts a list of definitions.

`<dt>` is the term being defined.

`<dd>` is the definition of the term.

gh|about-html|/tags/dl-dt-dd.html||

sa:
  → development/web/HTML/tags
