$ HTML element: blockquote

`<blockquote>` represents a section that is quoted from another source.

Besides the → development/web/HTML/attributes#html-global-attributes[global attributes], the only valid other attribute is `cite`.

sa:

  → development/web/HTML/tags/cite[`<cite>`]

  → development/web/HTML/tags/q[`<q>`]

  → development/web/HTML/tags
