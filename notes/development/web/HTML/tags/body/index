$ HTML element: body

? The `<body>` element is the second and last child element of the → development/web/HTML/tags/head[`<head>`] element.

With → development/languages/JavaScript, the `<body>` element can be accessed via
→ development/web/DOM/examples/Node/Document/body[`document.body`].

sa:

  CSS:
    • → development/web/CSS/tags/html-and-body[CSS: the `<html>` and `<body>` tag]
    • Root element/canvas: → development/web/CSS/root-element/canvas/body-with-and-without-html[Styles for `body` with/without styles for `html` element]

  → development/web/HTML/tags

