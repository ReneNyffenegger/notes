$ HTML element: body

? The `<body>` element is the second and last child element of the → development/web/HTML/tags/head[`<head>`] element.

With → development/languages/JavaScript, the `<body>` element can be accessed via `document.body`.

sa:
  → development/web/CSS/tags/html-and-body[CSS: the `<html>` and `<body>` tag]

  → development/web/HTML/tags
