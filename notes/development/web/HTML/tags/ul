$ HTML element: ul

`<ul>` is used to create an *unordered list*.

The individual list items are specified with `<li>`.

sa:

  An ordered list is created with `<ol><li>…<li>…</ol>`.

  → development/web/HTML/tags
