$ HTML elements: table, tr, td, thead, tbody

gh|about-html|/tags/table.html||

See result on → http://htmlpreview.github.io/?https://github.com/ReneNyffenegger/about-html/blob/master/tags/table.html[htmlpreview.github.io]

{ td

  { vertical-align #td-vertical-align

    Setting the value of the → development/web/CSS/properties/vertical-align[`vertical-align`] to top makes a table look nicer, imho.

    gh|about-html|/tags/table/td/vertical-align.html||

    gh|about-html|/tags/table/td/img/vertical-align.png||

  }

}
{ Misc


  If present, the (optional) `<caption>` element must be the first element of the parant `<table>` element. 
  -
  If the parent `<table>` element is the only descendent of a `<figure>` element, `<figcaption>` rather than `<caption>` should be used.

  If present, the (optional?) `<colgroup>` element must be between the preceding (potentially absent/optional) `<caption>` and following `<thead>`, `<th>`, `<tbody>`, `<tfoot>` and `<tr>` elements.

  The DOM interface for `<caption>` is `HTMLTableCaptionElement` (not `HTMLCaptionElement`, see → development/web/HTML/DOM/API/interfaces-classes/HTMLElement#HTMLElement-inheritance[`HTMLElement` inheritance]).

  The CSS style `→ development/web/CSS/properties/background/color`, when applied to a `<table>`, does not color the `<caption>` area.

}

sa:

  → development/web/CSS/properties/border

  → development/web/HTML/tags/table/create-with-js

  → development/web/CSS examples:
     • → development/web/CSS/tables/responsive
     • → development/web/CSS/tables/fixed-header
     • → development/web/CSS/tables/vertical-header

  → development/web/HTML/tags

  → development/web/CSS/properties/display/table-elements


  The CSS property → development/web/CSS/properties/border/collapse[`border-collapse`] controls whether cells (`<td>` elements?) inside a `<table>`
  share their borders with the adjacent cell or if the borders are drawn separately.

  The → Windows/PowerShell cmdLet → Windows/PowerShell/command-inventory/noun/HTML/convertTo[`ConvertTo-HTML`].
