$ HTML element: cite

`<cite>` represents a title of work.

sa:

  The → development/web/HTML/tags/figure[`<figure>`] and
  → development/web/HTML/tags/blockquote[`<blockquote>`] elements.

  → development/web/HTML/tags
