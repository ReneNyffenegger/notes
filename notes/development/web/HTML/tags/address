$ HTML element: address

The `<address>` element is used to indicate *contact information* for is nearest `<article>` or `<body>` ancestor.

sa:

  In` → development/web/php, → development/web/php/snippets/_SERVER[$_SERVER]['SERVER_SIGNATURE']` returns its information in an `<address>` tag.

  → development/web/HTML/tags
