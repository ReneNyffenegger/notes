$ HTML elements: ul, ol and li

`<ul>…</ul>` is used to create an *unordered*, `<ol>…</ol>` to create an *ordered list*.

The individual list items within such lists are specified with `<li>`.

`<li>` is the only (default) → development/web/HTML/tags[element] whose `→ development/web/CSS/properties/display` property value is `list-item`.

{ Influence of margin-top #html-ul-ol-margin-top

  The following example tries to demonstrate the influence of the CSS property → development/web/CSS/properties/margin[`margin-top`] on the
  rendering of the `<ul>` element:

  gh|about-HTML|/tags/ul-ol-li/margin-top.html||

  gh|about-HTML|/tags/ul-ol-li/img/margin-top.png||

}
{ TODO

  { Reducing intendation of bullets

    The indentation of bullets can be reduced by setting `margin-inline-start`:
code {
   margin: 0
   margin-inline-start:-2.5ch;
}
code }
 
    But is there a more official way for this?

    → https://css-tricks.com/everything-you-need-to-know-about-the-gap-after-the-list-marker/[Šime Vidas] mentions that browsers apply a default `padding-inline-start` of `40px` to `<ul>` and `<ol>` elements.

  }
}

sa:

  The `@list-style` → development/web/CSS at-rule.

  The → development/web/CSS/selectors/pseudo-classes/marker[`::marker`] CSS pseudo class.

  → development/web/HTML/tags
