$ HTML: root element
@ root element

{ CSS relative lengths based on font sizes

  In → development/web/CSS, → development/web/CSS#css-relative-font-lengths[relative length units based on a font] can be prefixed by an `r` (e. g. `1rem`) in which
  case they're relative to the computed size of the root element.

}

sa:
  The CSS pseudo class selector `→ development/web/CSS/selectors/pseudo-classes/root`.

  `→ development/web/DOM/examples/Node/Document/documentElement`
