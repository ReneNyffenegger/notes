$ HTML tag: legend

The `<legend>` specifies the caption (i.e. the *legend*) of a → development/web/HTML/tags/fieldset[`<fieldset>`].

gh|about-html|/tags/fieldset-legend.html||

The html above is rendered like so:
→ development/web/HTML/tags/fieldset-legend.png

Note the legend (»Enter your data«).

sa:
  → development/web/HTML/tags
