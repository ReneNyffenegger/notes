$ CSS: translate properties

? The values of the → development/web/CSS/properties/top-right-bottom-left[`top`, `right`, `bottom` and `left`] are not influenced (tranformed) by the `translate` properties.

sa:

  The `transform` property.

  → development/web/CSS/properties
