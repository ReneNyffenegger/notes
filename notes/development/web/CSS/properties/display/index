$ CSS: display
@ display

Controls how an element is displayed. Compare with → development/web/CSS/properties/visibility[`visibility`].
The property can be set to one of the following values:
  • none
  • inline
  • block
  • inline-block

sa:

  → development/web/CSS/properties/display/table-elements[Display properties to simulate a table].

  → development/web/CSS/properties
