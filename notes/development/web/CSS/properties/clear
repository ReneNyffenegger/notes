$ CSS property: clear
@ clear

The `clear` → development/web/CSS/properties[property] can be set to one of the following values:
table { ll
  `none` ☰
  `left` or `right` ☰
  `both` ☰
table }

sa:

  The `→ development/web/CSS/properties/float` property.

  → development/web/CSS/properties
