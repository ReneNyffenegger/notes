$ CSS: width and height properties

{ No effect in inline elements

  Assigning a value to the → development/web/CSS/properties[CSS properties] `width` or `height` for elements whose
  → development/web/CSS/properties/display[`display`] values is `inline` (such as the
  → development/web/HTML/tags/span[`<span>`] tag)) has
  → development/web/CSS/properties/display#display-inline-width-height[no effect].

}

