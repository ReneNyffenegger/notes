$ CSS property: border-spacing

The value of `border-spacing` controls the distance between the borders of adjacent cells.

In order for this property to take effect, → development/web/CSS/properties/border/collapse[`border-collapse`] must be set to `separate`.

gh|about-css|/properties/border/img/spacing.png||

gh|about-css|/properties/border/spacing.html||

sa:

  Other → development/web/CSS/properties/border[border related CSS properties]

  → development/web/HTML/tags/table[HTML elements related to `<table>`]
