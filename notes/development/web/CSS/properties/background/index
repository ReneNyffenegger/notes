$ CSS: background-* properties

table { ll

 `background-attachment` ☰
 `background-clip` ☰
 `background-color` ☰ 
 `background-image` ☰
 `background-origin` ☰
 `background-position` ☰
 `background-repeat` ☰
 `background-size` ☰

table }

`background` is a *shorthand* property to set all `background-*` properties at once.

sa:

  The `rgb()`, `rgba()`, `url()` and `linear-gradient()` functions.

  → development/web/CSS/properties
