$ CSS: background-color
@ background-color

sa:

  The `background-color` value, when applied to a → development/web/HTML/tags/table[`<table>`], does not color the area covered by the table's `<caption>`.

  → development/web/CSS/properties/background[`background-*`] properties
