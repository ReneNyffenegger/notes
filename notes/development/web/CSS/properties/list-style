$ CSS properties: list-style, list-style-image, list-style-position, list-style-type

table { ll
   `list-style-image` ☰
   `list-style-position` ☰
   `list-style-type` ☰
table }

`list-style` is a *shorthand* to specify all `list-style-*` properties at once

`list-style-*` properties are applied all elements whose `→ development/web/CSS/properties/display` value is `list-item` (which by default is → development/web/HTML/tags/ul-ol-li[`<li>`])

sa:

  The `@counter-style` → development/web/CSS at-rule.

  → development/web/CSS/properties
