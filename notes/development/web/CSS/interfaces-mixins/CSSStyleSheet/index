$ CSSStyleSheet (interface)
@ CSSStyleSheet

The `CSSStyleSheet` interface represents a single → development/web/CSS stylesheet.

{ Methods and properties

  table { ll
   `addRule()` ☰ *Deprecated*
   `cssRules` ☰ A list (`CSSRuleList`) of `→ development/web/CSS/interfaces-mixins/CSSRule` objects.
   `deleteRule()` ☰
   `insertRule()` ☰
   `Instance` methods ☰
   `ownerRule` ☰
   `removeRule()` ☰ *Deprecated*
   `replace()` ☰
   `replaceSync()` ☰
   `rules` ☰ `rules` is *deprecated* in favor of `cssRules`.
  table }

}
