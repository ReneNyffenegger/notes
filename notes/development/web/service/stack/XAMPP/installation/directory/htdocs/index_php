$ XAMPP installation directory: htdocs/index.php

This is the file that is executed after a fresh installation of → development/web/service/stack/XAMPP when pointing a → development/web/browser to `http://localhost` or `https://localhost`.
-
The script basically makes a HTTP redirect to `dashboard`.
code {
→ development/web/php/snippets/HTTP/header[header]('Location: '.$uri.'/dashboard/');
code }
