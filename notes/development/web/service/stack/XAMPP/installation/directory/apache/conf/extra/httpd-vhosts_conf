$ XAMPP installation directory: apache/conf/extra/httpd-vhosts.conf

`apache/conf/extra/httpd-vhosts.conf` is used to maintain multiple domains/hostnames on a machine.

The virtual host configuration can be verified with the `-S` command line option (`httpd.exe -S` on Windows, `apachectl -S` on UNIX).
