$ NTP

NTP is the *Network Time Protocol*. It is used to synchronize the → Linux/Clocks#system-clock[system clock] over a packet-switched, variable-latency data networks.

sa:
  → Linux/timezone

  → Linux/fhs/etc/ntp_conf
