$ Browser Object Model: window.scrollTo
@ window.scrollTo

The function `window.scrollTo(x, y)` moves the document so that the coordinates x,y are located in the top left corner of the browser.

gh|Browser-Object-Model|/window/scrollTo.html||
