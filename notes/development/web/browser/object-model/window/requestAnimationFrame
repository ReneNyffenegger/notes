$ Browser Object Model: window.requestAnimationFrame
@ requestAnimationFrame

{ Stopping the animation cycle

  `requestAnimationFrame` returns an id which can be used in `cancelAnimationFrame` to stop the animation cycle.

}
{ Example

  gh|Browser-Object-Model|/window/requestAnimationFrame.html||

}

sa:

  → development/web/browser/object-model/window/setTimeout

  → development/web/browser/object-model/window[The window object]

  → development/web/HTML/tags/canvas/snippets#canvas-requestAnimationFrame[requestAnimationFrame with the <canvas> tag]
