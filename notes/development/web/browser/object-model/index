$ Browser Object Model

{ anchors
}
{ console

  The → development/web/browser/object-model/console[console] object provides an API to a → development/web/browser[browser's] debugging console (aka *Web Console* in
  → development/web/browser/Firefox[Firefox]).

}
{ document

  See → development/web/browser/object-model/document[here].

}
{ forms

   gh|Browser-Object-Model|/forms/action.html||

}
{ frames
}
{ history

  gh|Browser-Object-Model|/history/length_1.html||

  gh|Browser-Object-Model|/history/length_2.html||

}
{ images
}
{ links
}
{ navigator

    → development/web/browser/object-model/navigator/userAgent[`navigator.userAgent`] returns the → development/web/browser[browser's] → development/web/HTTP/User-Agent[HTTP User Agent] string.
  
}
{ screen

  → development/web/browser/object-model/screen
}
{ window #bom-window

  { window.location #bom-window-location

    table { ll
      `window.location.href` ☰ The → development/web/URI-URL-URN-IRI-URC#url[URL] of the content that is displaed in the window
      `window.location.hostname` ☰ The domain of `href`
      `window.location.pathname` ☰ The path component of `href`. Determine → development/web/HTML document name in → development/languages/JavaScript with `window.location.pathname.split('/').pop());`.
      `window.location.protocol` ☰ The protocol that was used to transmit the content to the browser, for example → development/web/HTTP[http] or → development/web/HTTPS[https].
      `window.location.assign(newUrl)` ☰ load `newUrl` as document into `window`.
    table }

  }
  { window.toString

    gh|Browser-Object-Model|/window/toString.html||

  }
  { See also

    → development/web/browser/object-model/window/requestAnimationFrame[`window.requestAnimationFrame`]

    → development/web/browser/object-model/window/scrollTo[`window.scrollTo`]


    `window.WebGLRenderingContext`: → development/web/WebGL

  }

}

sa:
  → development/web/browser
