$ Browser Object Model: console.log()

`console.log(…)` allows to write messages into the debugging console. Thus, it is a handy and cheap way to write debug messages.

If the message text is prefixed with `%c`, the second parameter of `console.log` can be given a set of → development/web/CSS formatting rules that change
the appearance of the written log messages.
 
gh|Browser-Object-Model|/console/log.html||

This → development/web/HTML document writes:
gh|Browser-Object-Model|/console/img/log.png||

sa:
  The → development/web/browser/object-model/console[console] object of the
  → development/web/browser/object-model[browser object model]
