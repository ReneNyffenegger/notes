$ Browser Object Model: console.time() / timeEnd()

`console.time(timerName)` starts a timer that is named by `timerName`. The timer ends with a call to `console.timeEnd(timerName)` and the duration will be written
into the console window.

gh|Browser-Object-Model|/console/time.html||

{ Output into console window

  gh|Browser-Object-Model|/console/time.png||

}

sa:
  The → development/web/browser/object-model/console[console] object of the
  → development/web/browser/object-model[browser object model]
