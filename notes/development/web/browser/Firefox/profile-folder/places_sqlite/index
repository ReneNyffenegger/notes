$ places.sqlite

An → development/databases/SQLite[SQLite] database with the hisotry of visited sites.

{ Tables

  Some interesting tables in this database seem to be:
    • moz_places (which contains interesting statistics such as *visit count* or *frecency*
    • moz_historyvisits
    • moz_inputhistory
    • moz_hosts
    • moz_bookmarks
    • moz_keywords
    • moz_anno_attributes
    • moz_annos (Seems to store details about downloaded resources. The columns `flags`, `expiration`, `type`, `dateAdded` and `lastModified` seem to be deprecated)
    • moz_items_annos
    • moz_bookmarks_deleted
    • moz_meta
    • moz_origins

}

sa:
  → development/web/browser/Firefox/profile-folder[Firefox profile directory]
