$ Chrome user data directory: Local State

`Local State` is a → development/languages/JavaScript/JSON file that is located directly under
→ development/web/browser/Chrome/user-data-directory[Chrome's user data directory].

? This file allows to find the list of known profiles under the key `profile.info_cache` (which itself is a JSON-dictionary whose key names correspond to the
profile names.
