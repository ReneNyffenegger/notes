
WebDAV stands for *Web Distributed Authoring and Versioning*.

WebDAV is an extension of → development/web/HTTP which allows clients to create, change and move documents on a server.

{ Checking if a webserver supports WebDAV

  A → web/webserver that supports WebDAV returns a DAV header when the Options method is requested.

  Thus, in → Windows/PowerShell, the existence of such a header can be checked with:
code {
(→ Windows/PowerShell/command-inventory/noun/webRequest/invoke[invoke-webRequest] http://live.sysinternals.com -method Options).Headers.DAV
code }

}

sa:

  → development/web/RFC#rfc-5689[RFC 5689: Extended MKCOL for Web Distributed Authoring and Versioning (WebDAV) 

  Accessing → Windows/tools/Sysinternals tools → Windows/tools/Sysinternals#sysinternals-webdav[via WebDAV].
