$ PHP: ksort

`ksort($ary)` sort an → development/web/php/snippets/array/associative[associative] by the value of its keys.

gh|about-php|/array/associative/ksort.php||

`ksort()` does not have a noticable effect on an ordinary array:
gh|about-php|/array/associative/ksort-array.php||

sa:
  `krsort()` sorts an array by key in descending order.

  `sort()`

  → development/web/php/snippets/array[Arrays]
