$ PHP code snippets: list()

`list($foo, $bar, $baz) = $someArray;` assigns elements of an array to individual variables.

`list(…)` (like → development/web/php/snippets/array[`array(…)`]) is not a function but rather a language construct.

gh|about-php|/list.html||

sa:
  → development/web/php/snippets[Other PHP snippets]
