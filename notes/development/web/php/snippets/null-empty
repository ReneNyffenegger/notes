$ PHP code snippets: null, empty …

{ Comparing values to NULL

  gh|about-php|/null.html||

}
{ The empty() function #php-empty

  This example is similar to the one above, but uses the `empty()` function rather than a comparison with `NULL`:

  gh|about-php|/empty.html||

  Compare with → development/web/php/snippets/variable#php-isset[`isset()`]

}

sa:

  → development/web/php/snippets[Other PHP snippets].
