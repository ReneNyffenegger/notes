$ PHP code snippets: header()

`header()` sends a specific → development/web/HTTP/header.

`header()` must be called *before any other output* is sent to the browser!

code {
header('Content-Type: text/plain');
code }

sa:
  → development/web/php/snippets[Other PHP snippets].
