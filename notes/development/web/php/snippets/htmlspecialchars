$ PHP code snippets: the htmlspecialchars function

`htmlspecialchars` converts characters that have a meaning in → development/web/HTML to → development/web/HTML/entities.

For example, a `&` is turned into a `&amp;`, or a `<` into a `&lt;`.

gh|about-php|/htmlspecialchars.html||

sa:

  Compare with
    • `htmlentities()`
    • → development/web/php/snippets/urldecode[`urldecode()`], `urlencode()`
    • `addslashes()`, `addcslashes()`

  → development/web/php/snippets[Other PHP snippets].
