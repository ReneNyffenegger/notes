$ PHP: htmlspecialchars

`htmlspecialchars` converts characters that have a meaning in → development/web/HTML to → development/web/HTML/entities, for example, a `&` is turned into a `&amp;`, or a `<` into a `&lt;`.

gh|about-php|/htmlspecialchars.html||

sa:

  → development/web/php/snippets[Other PHP snippets].
