$ PHP: getenv()

`getenv('xyz')` returns the value of the → development/environment-variable[environment variable] `xyz`.

gh|about-php|/getenv.html||

sa:

  → development/web/CGI

  → development/web/php/snippets[Other PHP snippets]
