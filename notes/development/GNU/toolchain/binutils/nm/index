-

{ TODO

   → development/GNU/toolchain/binutils/objdump[`objdump`] can provide more useful information than `nm` but comes only as part of the GCC toolset. Thus `objdump`, in contrast to `nm` might not
   be installed on a particular system.

}
