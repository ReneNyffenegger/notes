-

`readelf -w` dumps all DWARF data (compare with → development/languages/C-C-plus-plus/GCC/options/g[`gcc -g`]).

`readelf -p .comment prog` can be used to determine with which gcc compiler version a program was compiled with.
