$ config.cache

`config.cache` is a shell script that caches the results of → development/GNU/toolchain/Build-System/configure[`./configure`] when run with the `-C` flag.

The purpose of `config.cache` is to speed up reconfigurations.
