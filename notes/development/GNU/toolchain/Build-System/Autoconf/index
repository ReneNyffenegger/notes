$ GNU Autoconf
@ Autoconf

Autoconf is an extensible package of M4 (at least version 1.4.6) macros that produce shell scripts to automatically configure software source code packages. 

The creator of a piece of software specifies the requirements in → development/GNU/toolchain/Build-System/Makefile_am. Autoconf turns this `Makefile.am` into
a portable → development/GNU/toolchain/Build-System/Makefile_in.

Autoconf tests for available features rather than for installed versions.

Based on the input of a → development/GNU/toolchain/Build-System/configure_ac file, Autoconf generates a shell script: → development/GNU/toolchain/Build-System/configure.

{ autom4te

  autom4te is the heart of Autoconf.
  
rem {
  It drives M4 and implements the features used by most of the above tools. Useful for
  creating more than just configure files
rem }

}

sa:
  → development/GNU/toolchain/Build-System[GNU Build System]

  → Linux/fhs/usr/share/autoconf

links:
  → http://savannah.gnu.org/projects/autoconf/, → https://www.gnu.org/software/autoconf/autoconf.html,
  → http://git.savannah.gnu.org/cgit/autoconf.git/
