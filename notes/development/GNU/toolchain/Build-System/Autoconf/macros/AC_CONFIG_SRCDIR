
The given file should be a distributed source file.

It's a safety check to make sure that → development/GNU/toolchain/Build-System/configure[`./configure`] is not run from »outer space«.

code {
AC_CONFIG_SRCDIR([src/main.c])
code }
