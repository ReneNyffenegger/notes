
`autoscan` is a perl script that was automatically created from `autoscan.in`.

`autoscan` might help creates → development/GNU/toolchain/Build-System/configure_scan[`configure.scan`] which servers as
a starting point to create → development/GNU/toolchain/Build-System/configure_ac[`configure.ac`]
