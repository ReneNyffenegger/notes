$ config.status

`config.status` is a shell script that is produced by → development/GNU/toolchain/Build-System/configure[`./configure`].

When `./config.status` is run, it recreates the same configuration with the options that were active when `.configure` was run.
