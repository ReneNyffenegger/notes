$ notepad++

{ Turning off line wrap

  go to Menu ~view~ and untick ~word wrap~.

}
{ Searching »wrap around«

  `ctrl+f`, check `wrap around` (`alt+p`).

}
{ Default file type

  Settings -> Preferences -> (left side) New Document -> Default languagre -> for example: SQL

}
