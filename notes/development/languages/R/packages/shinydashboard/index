$ R package: shinydashboard

`shinydashboard` uses → https://github.com/ColorlibHQ/AdminLTE[AdminLTE] which in turn uses → https://getbootstrap.com/[Bootstrap 3].


{ dashboardPage
code {
ui <- dashboardPage (
       dashboardHeader (),
       dashboardSidebar(),
       dashboardBody   ()
);

…

shinyApp(ui = ui, …);
code }


}

sa:
  → development/languages/R/packages/shiny
