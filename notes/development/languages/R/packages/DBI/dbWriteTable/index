$ R: DBI::dbWriteTable

The → development/languages/R/packages/DBI[`DBI`] function `dbWriteTable` can be used to write a → development/languages/R/data-structures/data-frame[data frame] into a → development/databases[database].

The following example uses the  → development/languages/R/packages/RSQLite[`RSQLite` package] to create a table in a → development/databases/SQLite database and write the data of the data frame `df` into it.
The name of the table to be created is specified with the second parameter (here: `r_table`):
gh|about-r|/packages/DBI/dbWriteTable/insert-data-frame-into-SQLite.R||
