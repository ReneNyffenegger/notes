$ R: search path

The search path lists → development/languages/R/packages[packages] that are currently loaded. Additionally, it also
displays *environments* (such as *.GlobalEnv*) and → development/languages/R/datasets[datasets].

It can be displayed with → development/languages/R/functions/search[search].

The search path is used to resolve function names.
