$ R: concatenate strings with spaces in between: function paste
@paste

`paste` concatenates the given strings.

By default, a space is put between the concatenated strings. This can be changed using the named argument `sep`.
-
→ development/languages/R/functions/paste0[`paste0()`] is quite similar to `paste`, except that by default the strings are concatenated with nothing being put between them.


gh|about-r|/functions/paste.R||

sa:


  → development/languages/R/functions/index 

  → Linux/shell/commands/paste[Shell command: paste]

  → development/languages/Perl/functions/join[Perl function join].
