$ R function: is.integer

`is.integer()` tests if an object is of type *integer*. The returned value is particularly interesting for numerical literals like `42`. They seem to be integers when
in fact they're doubles. (However, if they were suffixed by an `L`, they'd be integers).

gh|about-r|/functions/is/integer/doubles-vs-integers.R||

sa:
  → development/languages/R/functions
