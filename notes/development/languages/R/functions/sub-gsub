$ R functions: sub and gsub

The `sub` and `gsub` functions replace → development/languages/R/functions/regular-expressions[regular expressions] in character strings with a replacement text.

code {
sub (regexp, txtReplace, vec); # Replace first occurence
gsub(regexp, txtReplace, vec); # Replace all occurences
code }

gh|about-r|/functions/sub.R||

gh|about-r|/functions/gsub.R||

sa:

  → development/languages/R/functions
