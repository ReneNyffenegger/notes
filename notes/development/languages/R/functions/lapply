$ R function: lapply
@ lapply

`lapply` applies a function to each element of a → development/languages/R/data-structures/vector.

In the following example, the vector is `x` and the function is `func`. `lapply` thus creates a new vector that subsequently
is plotted for verification:
gh|about-r|/functions/lapply.R||

gh|about-r|/functions/images/lapply.png||


sa:
  → development/languages/R/functions/_apply[Other apply functions]

  The → development/languages/R/packages/foreach[`foreach` package]

  → development/languages/R/packages/shiny#shiny-html-tags[This `shiny` example] uses `lapply` to produce a → development/web/HTML/tags/ul[HTML unordered list] of HTML tags.

  → development/languages/R/functions
