$ R function: with

In the following example, `with()` specifies a → development/languages/R/data-structures/data-frame[data frame] on which an expression
is executed. Thus, the need to prefix the column-names of the data frame with the name of the data frame is omitted:
gh|about-r|/functions/with/sum.R||

sa:

  → development/languages/R/functions/plot#r-plot-with[Plotting a subset] uses `with()` to select specific values from a data frame and then plot these.

  → development/languages/R/functions/attach[`attach()`]
  -
  `within()`

  → development/languages/R/functions
