$ R function: emptyenv

`emptyenv()` returns the root of all → development/languages/R/environment[environments]. This root environment is the (direct) parent environment of the *base package* (which
is available through → development/languages/R/functions/baseenv[`baseenv()`].

sa:

  → development/languages/R/functions
