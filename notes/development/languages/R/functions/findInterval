$ R function: findInterval

`findInterval` searches in a → development/languages/R/data-structures/vector[vector] of »ranges« for the interval that a given value belongs to.

gh|about-r|/functions/findInterval.R||

The `findInterval` function can be used in → development/languages/R/statements/switch#switch-range[`switch` statement] to test for ranges.

sa:

  → development/languages/R/functions
