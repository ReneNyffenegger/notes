$ R function: as.Date
@ as.Date

`as.Date` converts from a character representation of a date to an → development/languages/R/object[object] whose
→ development/languages/R/object/class[`class`] is `Date`.

gh|about-r|/functions/as.Date/as.Date.R||

{ origin

  The `origin` parameter specifies a starting date from which the number is added:

  gh|about-r|/functions/as.Date/origin.R||

}

sa:

  → development/languages/R/functions/index 

  → development/languages/R/classes/Date[Date class]
