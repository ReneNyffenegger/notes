$ R function: rbind
@ rbind

{ Creating a matrix

  Each argument to `rbind` becomes a row.

  gh|about-r|/functions/rbind.R||

}
{ union all

 `rbind` can be thought of as the → development/databases/SQL[SQL] `union all`.

  gh|about-r|/functions/rbind/union-all.R||

}

sa:

  → development/languages/R/functions/cbind

  → development/languages/R/functions
