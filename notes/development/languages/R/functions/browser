$ R function: browser
@ browser

`browser()` can be used for debugging purposes. When the execution of code reaches `browser()`, execution is halted and the debugger is entered.

A call to `browser()` can be added anywhere where an R expression is valid.



sa:
  → development/languages/R/functions
