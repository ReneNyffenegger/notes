$ R function: colorRampPalette

`colorRampPalette` returns a function. The function can then be called with a positive integer argument `n` and the function will return a palette with `n` entries.
code {
paletteFunc <- colorRampPalette(…); # Create function
palette     <- paletteFunc(8);      # invoke function to create a palette with eight entries.
code }

{ Create a palette from red to blue

  The following example creates a palette from red to blue:

  gh|about-r|/functions/colorRampPalette/red-to-blue.R||

  gh|about-r|/functions/colorRampPalette/img/red-to-blue.png||

}


{ TODO

  gh|about-r|/functions/colorRampPalette.R||

}



sa:

  The → development/languages/R/packages/RColorBrewer[`RColorBrewer`] package.

  → development/languages/R/functions/colors_

  → development/languages/R/functions
