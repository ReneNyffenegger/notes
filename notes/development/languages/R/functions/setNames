$ R function: setNames

`setNames(v, n)` is a shorthand form for `→ development/languages/R/functions/names[names](v) <- n`.

gh|about-r|/functions/setNames.R||

sa:
  `setNames()` is used on objects where the → development/languages/R/object/attributes[`names` attribute] makes sense.
