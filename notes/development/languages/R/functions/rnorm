$ R function: rnorm

`rnorm` generates a → development/languages/R/data-structures/vector[vector] of normally distributed random numbers.

`rnorm` can take up to three arguments:
  • `n`: the number of random variables to generate
  • `mean`, if not specified defaults to 0.
  • `sd`: the standard deviation. If not specified, defaults to 1.

→ development/languages/R/functions/set_seed[`set.seed(…)`] can be used to set a seed for the random number generator.

gh|about-r|/functions/rnorm.R||


sa:
  → development/languages/R/functions/distributions#random-numbers[Random numbers (distribution)]

  → development/languages/R/functions/sd[`sd(…)`] and
  → development/languages/R/functions/var[`var(…)`]

  → development/languages/R/functions/index 
