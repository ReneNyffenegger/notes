$ R function: set.seed

Use `set.seed` to create the same set of »random« numbers each time a script is invoked, for example → development/languages/R/functions/rnorm[`rnorm(…)`.

gh|about-r|/functions/set.seed.R||


sa:
  → development/languages/R/functions/distributions#random-numbers[Random numbers (distribution)]

  → development/languages/R/functions/index 
