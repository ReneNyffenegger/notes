$ R: working directory

The *working directory* specifies the location where file operations (wrting and reading) take place by default (that is: without specifying an
absolute path).

The working directory is set with development/languages/R/functions/setwd[setwd] and inquired about with → development/languages/R/functions/getwd[getwd].
