$ R: formula class
@ formula

A formula captures two things: 
  • An unevaluated expression.
  • The context (environment) in which the expression was created.


Apparently, an object whose class is `formula` can be created with the tilde operator.

sa:
  → development/languages/R/classes
