$ R: ts class
@ ts

A time series (ts) object is a → development/languages/R/data-structures/vector[vector] (univariate) or
→ development/languages/R/data-structures/matrices[matrix] (multivariate) with additional attributes: 
  • time indices for each observation
  • sampling frequency
  • time increment between observations
  • cycle length for periodic data

Usually, ts objects are used for data that was observed at equallly spaced time points.

sa:

  The → development/languages/R/functions/ts[`ts`] function which produces time series.

  The → development/languages/R/classes/timeSeries[timeSeries] class which is specificially geared towards finances. It differs from *ts* in that
  it allows for irregular timeseries.

  → development/languages/R/classes
