$ Perl regular expressions; non capturing groups

`(?:pattern)`: match `(pattern)` but don't capture it.

gh|about-perl|/regular-expressions/non-capturing-group.pl||

sa:
  → development/languages/Perl/regular-expressions/[Perl regular expressions]
