$ Perl cheat sheet

{ pwd

Using → development/languages/Perl/modules/Cwd/index[Cwd] and `getcwd`:
code {
use Cwd;
my $cwd = getcwd();
code }

}
{ Files

  { Extract suffix  fro

    Use → development/languages/Perl/modules/File/Basename/index and `fileparse`.

code {
     use File::Basename;
     my ($file_without_suffix, $directory, $suffix) = fileparse ('/foo/bar/baz/file.pl', '.pl');
code }

  }

}
