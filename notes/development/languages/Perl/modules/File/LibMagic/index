$ Perl module File::LibMagic

gh|PerlModules|/File/LibMagic/script.pl||

{ Installing lib magic

  The module needs lib magic.

  It can be installed with `apt-get`
code {
sudo apt-get install libmagic-dev
code }
or `yum`
code {
yum -y install file-devel
code }

}

sa:

  → development/languages/Perl/modules/File/Type

  → development/languages/Perl/modules/index[Perl modules].
