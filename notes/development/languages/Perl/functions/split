$ Perl function: split

gh|about-perl|/functions/split.pl|one-to-one|

The »opposite« of `split` is → development/languages/Perl/functions/join[`join`].

sa:

  → development/languages/VBA[Visual Basic for Applications] has also a function named → development/languages/VBA/functions#vba-split[`split`] that does quite the same thing.

  → development/languages/Perl/functions

  The → Windows/PowerShell operators → Windows/PowerShell/language/operator/string-manipulation#ps-split-join[`-split` and `-join`].
