$ Perl function: map

`map` can be used to apply a function to each element in a list.

The function is simialar in spirit like the → development/languages/Python/functions/map[equally named Python function].

gh|about-perl|/functions/map.pl||


sa:

  → development/languages/Perl/functions

  `map` seems to roughly correspond to the → development/languages/R[R] function → development/languages/R/functions/sapply[`sapply()`].

   → science/computer/Programming-paradigm/Functional-programming/First-class-function/map
