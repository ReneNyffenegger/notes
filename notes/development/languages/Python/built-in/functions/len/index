$ Python: len
@ len

`len(obj)` returns the numbers of items in a → development/languages/Python/iterable/sequence or collection.

The → development/languages/Python/objects on which `len()` is executed needs to implement  → development/languages/Python/dunders/__len__[`__len__()`].

sa:
  → development/languages/Python/built-in-functions
