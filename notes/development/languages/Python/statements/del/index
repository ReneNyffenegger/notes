$ Python: statement del
-
@ del

`del foo` removes the name `foo` from the namespace that is associated with the *local scope*.

sa:

  The → development/languages/Python/built-in-functions/delattr[`delattr` built-in function]

  → development/languages/Python/statements
