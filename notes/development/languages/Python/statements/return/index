$ Python: statement return
@ return

The `return` statement can be used in a → development/languages/Python/functions to return an → development/languages/Python/objects.
-
If a function does → development/languages/Python/functions#py-func-no-return[not explicitely return an object], the function implicitely retunrs `None`.

sa:

  → development/languages/Python/statements
