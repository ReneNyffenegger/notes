$ Python: statement nonlocal
@ nonlocal

code {
def outer():
    x = 10
    def inner():
        print(x)   # Prints 10
    inner()

outer()
code }

code {
def outer():
    x = 10
    def inner():
        x = 20

    inner()
    print(x)   # Prints 10

outer()
code }

code {
def outer():
    x = 10
    def inner():
        nonlocal x
        x = 20

    inner()
    print(x)   # Prints 20

outer()
code }

sa:

  The `→ development/languages/Python/statements/global` statement

  → development/languages/Python/statements

