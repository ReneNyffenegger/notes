$ Python: is operator

The `is` operator compares the identity of two → development/languages/Python/objects[object] references and returns `True` if both identities are equal and `False` otherwise.

The identies are equal if and only if both references refer to exactly the same object.

sa:

  The → development/languages/Python/built-in-functions[built-in] function → development/languages/Python/built-in-functions/isinstance[`isinstance()`].

  → development/languages/Python/variables#py-var-same-object

  → development/languages/Python/operators
