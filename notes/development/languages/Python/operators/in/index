$ Python (membership-) operator in

{ Testing for a substring

 `x in txt` can be used to test if a → development/languages/Python/types/string[string] (`x`) is a substring of another string (`txt`).

  The test is performed case-sensitively (`'BAR' in 'foo bar baz'` returns `False`).
  gh|about-python|/operators/in/substr-in-str.py||

}

sa:
  → development/languages/Python/operators
