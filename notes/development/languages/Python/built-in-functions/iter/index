$ Python: iter()

{ Passing an iterable to iter()

  If `i` is an → development/languages/Python/iterable, `iter(i)` returns an → development/languages/Python/types/iterator for `i`.

}

sa:

  → development/languages/Python/iterable/iterator[iterators]

  → development/languages/Python/built-in-functions/open/iter[Using `iter()` and `open()`]

  → development/languages/Python/built-in-functions
