$ Python: str

`str(obj)` returns a → development/languages/Python/types/string[string] representation of the → development/languages/Python/objects `obj`.

code {
str(obj)
str(object=b'', encoding='utf-8', errors='strict')
code }

sa:
  → development/languages/Python/built-in-functions
