$ Python: eval

sa:

  `eval(str)` returnes an object that is equal to the one whose `str` was found like so → development/languages/Python/built-in-functions/repr[`str=repr(obj)`].

  → development/languages/Python/built-in-functions
