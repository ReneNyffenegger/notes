$ Python: eval

? `eval` is applied to an *expression* while → development/languages/Python/built-in-functions/exec[`exec`] is applied to *statements*.

sa:

  `eval(str)` returns an object that is equal to the one whose `str` was found like so → development/languages/Python/built-in-functions/repr[`str=repr(obj)`].

  → development/languages/Python/built-in-functions
