$ Python: int
@ int

`int(s)` can be used to convert a string (`s`) that represents a number into an integer.

The second, optional, paramter specifies the base of the string to be converted number. For example, if the second parameter is set to 16, it converts a hexadecimal string into an integer.

gh|about-Python|/builtin-functions/int.py||

sa:

  `float()` and `str()`

  → development/languages/Python/built-in-functions
