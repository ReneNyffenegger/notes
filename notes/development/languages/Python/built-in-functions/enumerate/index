$ Python: enumerate
@ enumerate

`enumerate(sequence)` returns an iterator that provides → development/languages/Python/types/tuple[tuples] with two elements: the position of an element in the `sequence` and the corresponding value:

gh|about-python|/builtin-functions/enumerate/demo.py||

sa:
  → development/languages/Python/built-in-functions
