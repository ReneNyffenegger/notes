
Keras is the high-level API of the → development/languages/Python/libraries/TensorFlow[TensorFlow] API.

The core data structures of Keras are
  • layers, and
  • models (of which the simplest type is the *sequential model*).

sa:
  → development/languages/Python/libraries/TensorFlow/Keras/datasets
