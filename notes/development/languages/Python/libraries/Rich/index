$ Python library: Rich


{ inspect #py-Rich-inspect

 `inspect` creates a nice report on any → development/languages/Python/objects[Python object].

code {
from rich import inspect
import sqlite3
print(inspect(sqlite3))
print(inspect(sqlite3.Connection))
code }

}
{ Print text no wider than n characters

code {
import lipsum
from rich.console import Console
console = Console(width=80)
console.print(lipsum.generate_sentences(5))
code }

}

