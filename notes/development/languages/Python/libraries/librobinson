$ Python library: librobinson
@ librobinson

Install *librobinson* directly from github.
code {
$ git clone → https://github.com/byztxt/librobinson
$ cd librobinson
$ sudo python3 ./setup.py install
code }

Then, in Python:
code {
from robinson import convert
trie = convert.beta2unicodeTrie()
beta = "LO/GOS\n"
unicode, remainder = trie.convert(beta)
code }

sa:
  The PowerShell module → Windows/PowerShell/modules/personal/betacode
