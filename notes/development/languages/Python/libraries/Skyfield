$ Python library: Skyfield
@ Skyfield

code {
pip install skyfield
code }

{ Calculate the position of mars

code {
#!/usr/bin/env python3

from skyfield.api import load

planets = load('de421.bsp')
earth   = planets['earth' ]
mars    = planets['mars'  ]

t_now  = load.timescale().now()

ra, dec, distance = earth.at(t_now).observe(mars).radec()

print(ra, dec, distance)
code }

  Note that load('de421.bsp') creates a local file with this name.

}

sa:
  → development/languages/Python/libraries/Astropy,
  → development/languages/Python/libraries/PyEphem

links:
  → https://rhodesmill.org/skyfield[Skyfield python library]
