$ Python library: matplotlib
@ matplotlib

{ Plotting a simple graph

  gh|about-python|/libraries/matplotlib/sin.py||

  gh|about-python|/libraries/matplotlib/img/sin.png||

}

{ Matplotlib is currently using agg, which is a non-GUI backend, so cannot show the figure.

  When I experienced this error on Debian, installing `→ development/languages/Python/standard-library/tkinter` solved the problem.

}
{ Module members

  table { ll

   `_add_data_doc()` ☰ Function
   `_api` ☰ Module
   `atexit` ☰ Module
   `bezier` ☰ Module
   `cbook` ☰ Module
   `checkdep_usetex()` ☰ Function
   `_check_versions()` ☰ Function
   `_c_internal_utils` ☰ Module
   `_cm` ☰ Module
   `cm` ☰ Module
   `_cm_listed` ☰ Module
   `_color_data` ☰ Module
   `colormaps` ☰ ?
   `colors` ☰ Module
   `color_sequences` ☰ ?
   `contextlib` ☰ Module
   `cycler()` ☰ Function
   `defaultParams` ☰ `dict` object
   `default_test_modules` ☰ `list` object
   `_deprecated_ignore_map` ☰ `dict` object
   `_deprecated_map` ☰ `dict` object
   `_deprecated_remain_as_none` ☰ `dict` object
   `_docstring` ☰ Module
   `_ensure_handler` ☰ ?
   `_enums` ☰ Module
   `_ExecInfo` ☰ `matplotlib._ExecInfo` class
   `ExecutableNotFoundError` ☰ `matplotlib.ExecutableNotFoundError` class
   `_fontconfig_pattern` ☰ Module
   `ft2font` ☰ Module
   `functools` ☰ Module
   `get_backend()` ☰ Function
   `get_cachedir()` ☰ Function
   `get_configdir()` ☰ Function
   `_get_config_or_cache_dir()` ☰ Function
   `get_data_path()` ☰ Function
   `_get_executable_info` ☰ ?
   `_get_ssl_context` ☰ ?
   `_get_version()` ☰ Function
   `_get_xdg_cache_dir()` ☰ Function
   `_get_xdg_config_dir()` ☰ Function
   `importlib` ☰ Module
   `_init_tests()` ☰ Function
   `inspect` ☰ Module
   `interactive()` ☰ Function
   `is_interactive()` ☰ Function
   `is_url()` ☰ Function
   `_label_from_arg()` ☰ Function
   `locale` ☰ Module
   `_log` ☰ ?
   `_logged_cached()` ☰ Function
   `logging` ☰ Module
   `MatplotlibDeprecationWarning` ☰ `matplotlib._api.deprecation.MatplotlibDeprecationWarning` class
   `matplotlib_fname()` ☰ Function
   `MutableMapping` ☰ ?
   `namedtuple()` ☰ Function
   `numpy` ☰ Module
   `_open_file_or_url()` ☰ Function
   `os` ☰ Module
   `Parameter` ☰ `inspect.Parameter` class
   `_parse_to_version_info()` ☰ Function
   `parse_version()` ☰ Function
   `Path` ☰ `pathlib.Path` class
   `_path` ☰ Module
   `path` ☰ Module
   `pprint` ☰ Module
   `_preprocess_data()` ☰ Function
   `rc()` ☰ Function
   `rc_context()` ☰ Function
   `rcdefaults()` ☰ Function
   `rc_file()` ☰ Function
   `rc_file_defaults()` ☰ Function
   `RcParams` ☰ ?
   `rcParams` ☰ ?
   `rc_params()` ☰ Function
   `rcParamsDefault` ☰ ?
   `rc_params_from_file()` ☰ Function
   `_rc_params_in_file()` ☰ Function
   `rcParamsOrig` ☰ ?
   `rcsetup` ☰ Module
   `re` ☰ Module
   `_replacer()` ☰ Function
   `sanitize_sequence()` ☰ Function
   `scale` ☰ Module
   `set_loglevel()` ☰ Function
   `shutil` ☰ Module
   `subprocess` ☰ Module
   `sys` ☰ Module
   `tempfile` ☰ Module
   `test()` ☰ Function
   `ticker` ☰ Module
   `transforms` ☰ Module
   `use()` ☰ Function
   `validate_backend()` ☰ Function
   `_version` ☰ Module
   `_VersionInfo` ☰ `matplotlib._VersionInfo` class
   `warnings` ☰ Module

  table }

}

{ TODO

 `matplotlib.pyplot` is a collection of functions that make matplotlib work like MATLAB.

}

sa:

  The → development/Notebook-interface/Jupyter magic function `%matplotlib`

  → https://seaborn.pydata.org/[Seaborn] is a (Python) → development/Data/visualization library based on matplotlib.
