$ matplotlib.style

{ Members

  table { ll

  `available` ☰ A `→ development/languages/Python/types/list` object that contains the names of available styles. `use()` can be used to select one of these styles.
  `context()` ☰
  `core` ☰ Module
  `library` ☰ ?
  `reload_library()` ☰
  `use()` ☰ Use the given set of styles.

 table }

}
