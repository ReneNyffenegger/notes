$ matplotlib.patches.Patch

A `matchplotlib.patches.Patchs` object is a 2D artist with a face color and an edge color.

? A `Patch` object is used (for example?) by
  • → development/languages/Python/libraries/matplotlib/figure/Figure[`Figure`], or
  • → development/languages/Python/libraries/matplotlib/axes[`Axes`] (to determine the shape, background and border of the plotting region)
