$ Python library: Setuptools
@ setuptools

setuptools enhances `distutils` so as to make it easier to build and distribute Python packages.

? setuptools is not part of the → development/languages/Python/standard-library, but comes with pip (and is found quite ubiquitously).

