$ torch.nn.Embedding

`torch.nn.Embedding` is a lookup table that stores embeddings of a fixed dictionary and size.

`torch.nn.Embedding` inherits from `→ development/languages/Python/libraries/torch/types/nn/Module`.
