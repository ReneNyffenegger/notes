$ torch.save() / torch.load()

code {
torch.save(model.state_dict(), "data/model.pth")
code }

code {
model = NeuralNetwork()
model.load_state_dict(torch.load('data/model.pth'))
…
model.eval()
code }

sa:

  → development/languages/Python/libraries/torch/types/nn/Module#torch-module-state_dict[`model.load_state_dict()`]
