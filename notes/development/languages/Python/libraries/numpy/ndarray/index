$ numpy.ndarray
@ ndarray

`numpy.ndarray` is a class that represents a multidimensional array of items of the same data type.

ndarrays should be created with `numpy.array(…)`, `numpy.zeros(…)` or `numpy.empty(…)`.
