$ Python library: youtube-transcript-api

`youtube-transcript-api` gets the transcript or subtitles of a given YouTube video.

code {
from youtube_transcript_api import YouTubeTranscriptApi

transcript = YouTubeTranscriptApi.get_transcript('qEO6DUyzgTA')

for item in transcript:

    print(f"{item['start']:>6.2f}: {item['text']}")
code }

sa:

  The → development/languages/Python/libraries/pytube[`pytube`] library.

 → tools/youtube-dl
