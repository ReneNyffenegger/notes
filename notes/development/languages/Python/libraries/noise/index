$ Python library: noise

`noise` generates *Perlin noise*.

code {
sudo pip3 install noise
code }

{ Members of noise

  table { ll
     `pnoise1()`, `pnoise2()`, `pnoise3()` ☰ 1, 2 and 3 dimensional *improved* noise functions.
     `snoise2()`, `snoise3()`, `snoise4()` ☰
     `_perlin` ☰ Module
     `_simplex` ☰ Module
  table }

}

sa:

  The Python libraries:
    • → https://pynoise.readthedocs.io/en/latest/[pynoise]
    • → https://pypi.org/project/perlin-noise/[perlin-noise] 
