$ Python library: request.auth

{ HTTPBasicAuth

code {
#!/usr/bin/env python3

from requests.auth import HTTPBasicAuth

basicAuth = HTTPBasicAuth('myself', 'thePassword')

print(basicAuth.username)
print(basicAuth.password)
code }

}
