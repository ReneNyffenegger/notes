$ Python: slice (type)
@ slice

`slice()` → development/languages/Python/types/slice[`slice`] returns → development/languages/Python/types/slice[`slice` object].

code {
sl = slice(stop)
sl = slice(start, stop)
sl = slice(start, stop, step)
code }

sa:

  `itertools.isslice()`

  Other → development/languages/Python/types[Python types]
