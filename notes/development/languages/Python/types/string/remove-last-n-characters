$ Python: remove last n characters from string

The `str[:-n]` removes the last n characters from the → development/languages/Python/types/string[string] `str`.

The following example removes the final character from the string `abc def ghij`, thus, `abc def ghi<` is → development/languages/Python/built-in-functions/print[printed].
gh|about-python|/built-in/types/str/remove-final-character.py||

sa:
  → development/languages/Python/types/string/rstrip[`str.rstrip()`] and
  → development/languages/Python/types/string/substring[extracting substrings from strings]

