$ Python: string.replace()

→ `str.replace(old, new)` replaces the text sequence `old` with `new` in `str`.

gh|about-python|/built-in/types/str/replace.py||

sa:

  → development/languages/Python/types/string/translate[`str.translate()`]

  → development/languages/Python/types/string
