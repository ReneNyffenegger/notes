$ Python: map
@ map

`map` can be used to apply a function to each element in a list. It's probably the closest equivalent to the equally named
→ development/languages/Perl/functions/map[Perl function].

{ Square numbers

  The following snippet uses a lambda expression to square the numbers in a list:

  gh|about-Python|/functions/map/square-items.py||

}

sa:
  → development/languages/Python/functions
