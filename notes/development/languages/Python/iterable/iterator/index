$ Python: iterator

An iterator might be created by a → development/languages/Python/generators[generator], or functions like `map`, `filter` or
→ development/languages/Python/built-in-functions/iter[`iter`] and functions that use `yield`.


{ Relation to iterables

  An iterator has an `__iter__` and a `__next__` method.

  The presence of the `__iter__` method makes the iterator also an an → development/languages/Python/iterable[iterable]. However, an iterable is not necessarily an iterator.

}

sa:
  → development/languages/Python/iterable/iterator/fibonacci[Example: create an arbitrarty number of fibonacci numbers]

  The built in function → development/languages/Python/built-in-functions/iter[`iter`] and
  The built in function → development/languages/Python/built-in-functions/next[`next`].
