$ Python: __lt__
@ __lt__

`__lt__` stands for *less than* and defines how the `<` operator behaves when applied between to → development/languages/Python/objects[objects].

`<` (or `__lt__`, respectivey) is required in order to sort objects, see for example → development/languages/Python/built-in-functions/sorted#py-sorted-udt[sorting user defined types with `sorted()`].

sa:
  → development/languages/Python/dunders[Other dunders]
