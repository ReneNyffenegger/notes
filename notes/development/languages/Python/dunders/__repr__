$ Python: __repr__
@ __repr__

An → development/languages/Python/objects[object's]  `__repr__()`, if defined, returns a represenation of the object's data as a string.
-
The returned string is supposed to be in a form that allows to re-create an → development/languages/Python/objects with the same data.

`__repr__` is called by the built-in function → development/languages/Python/built-in-functions/repr[`repr()`].

sa:

  The concept of `__repr__()` is extended in → development/languages/Python/IPython with so-called → development/languages/Python/IPython#ipython-rich-display[Rich Display].

  → development/languages/Python/dunders[Other dunders]
