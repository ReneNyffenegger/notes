$ Python: sqlite3.sqlite_version

`sqlite_version` returns the version of the currently running *→ development/databases/SQLite library*.

gh|about-python|/standard-library/sqlite3/sqlite_version.py||

sa:

 `sqlite3.version_info` stores the version of the SQLite *database adapter*, `sqlite_version_info` the version of the SQLite library.

  The → development/databases/SQLite function → development/databases/SQLite/functions/sqlite_version[`sqlite_version()`]
