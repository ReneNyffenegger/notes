$ Python: sys.getrefcount(obj)

`sys.getrefcount(obj)` returns the → development/languages/Python/objects/reference-counter[reference counter] of the → development/languages/Python/objects `obj`.

Because calling `sys.getrefcount(obj)` also increases the reference counter of `obj` for the duration of the call, `sys.getrefcount(obj)` returns a number that is at least `2`.

gh|about-Python|/standard-library/sys/getrefcount.py||

sa:

  → development/languages/Python/standard-library/ctypes/ob_refcnt[Using `ctypes` to get an object's reference counter]

  → development/languages/Python/standard-library/sys[`sys`] 
