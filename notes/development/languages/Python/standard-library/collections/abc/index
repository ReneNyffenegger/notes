$ Python: collections.abc
@ collections.abc

`collections.abc` adds some useful *abstract base classes* such as `Iterable`, `Container` and `MutableMapping`.

sa:

  The → development/languages/Python/standard-library/abc[`abc`] modules allows to define *abstract base classes*.

  → development/languages/Python/standard-library
