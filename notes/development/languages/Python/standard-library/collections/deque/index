$ Python: collections.deque 

A `deque` object is similar to a → development/languages/Python/types/list[`list`] object. The main difference is that operations at a deque's end are faster but operations in between are slower.

gh|about-Python|/standard-library/collections/deque/demo.py||

sa:
  → development/languages/Python/types/list
