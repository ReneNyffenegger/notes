$ python standard library: urllib.request
@ urllib.request

{ Simple demo

 `urllib.request.urlopen returns a `→ development/languages/Python/standard-library/http/client/HTTPResponse` object.

 `urllib.requests.urlretrieve` downloads the content of a URL to a file.

  gh|about-python|/standard-library/urllib/request/demo.py||

}
{ Exception for urlopen

 `urlopen` raises `urllib.error.URLError` if something goes wrong:
code {
try:
   res = urllib.request.urlopen('https://domain-does-not-exi.st/')
except urllib.error.URLError as e:
   print(e.reason)
code }

  `urllib.error.HTTPError` allows to filter for more specific errors;
code {
try:
   res = urllib.request.urlopen('https://renenyffenegger.ch/does-not-exist')
except urllib.error.HTTPError as e:
   print(e.reason)
code }


}

sa:

  The `→ development/languages/Python/libraries/requests` library.

  → development/languages/Python/standard-library
