$ Python standard library: curses
@ curses

{ Example: Print /proc/diskstats in regular intervals #py-curses-proc-diskstats

  The following example uses `cursos` to print the stats found in `→ Linux/fhs/proc/diskstats` in regular intervals to the console:
  gh|about-Python|/standard-library/curses/parse-proc-diskstats.py||

}

sa:

? Apparently, there is no `curses` module for Windows. Use `consolelib` instead.

  → development/languages/Python/standard-library
