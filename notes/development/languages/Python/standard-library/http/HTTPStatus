$ Python standard library: http.HTTPStatus
@ http.HTTPStatus

The type of `http.HTTPStatus` is `enum.EnumType`.

code {
from http import HTTPStatus

for s in HTTPStatus.__members__.values():
    print(f'{s}: {s.phrase:<40} {s.description}')
code }

sa:
  → development/web/HTTP/status-code[HTTP status codes]
