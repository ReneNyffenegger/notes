$ Python: http.client.HTTPResponse

A `http.client.HTTPResponse` object is returned, for example, by `urlib.requst.urlopen()`:
code {
import urllib.request

res = urllib.request.urlopen('https://renenyffenegger.ch')

type(res)  # -> <class 'http.client.HTTPResponse'>
res.read() # return byte array with URL's body
code }

sa:
  `→ development/languages/Python/standard-library/http/client`
