$ Python standard library: xml.etree.ElementTree

{ Module members

  table { ll

   `C14NWriterTarget` ☰ `xml.etree.ElementTree.C14NWriterTarget` class
   `canonicalize()` ☰ Function
   `collections` ☰ Module
   `Comment()` ☰ Function
   `contextlib` ☰ Module
   `dump()` ☰ Function
   `Element` ☰ `xml.etree.ElementTree.Element` class
   `ElementPath` ☰ Module
   `_Element_Py` ☰ `xml.etree.ElementTree.Element` class
   `ElementTree` ☰ `xml.etree.ElementTree.ElementTree` class
   `_escape_attrib()` ☰ Function
   `_escape_attrib_c14n()` ☰ Function
   `_escape_attrib_html()` ☰ Function
   `_escape_cdata()` ☰ Function
   `_escape_cdata_c14n()` ☰ Function
   `fromstring()` ☰ Returns an `xml.etree.ElementTree.Element` object. See also `parse()`.
   `fromstringlist()` ☰ Function
   `_get_writer()` ☰ Function
   `HTML_EMPTY` ☰ `set` object
   `indent()` ☰ Function
   `io` ☰ Module
   `iselement()` ☰ Function
   `iterparse()` ☰ Function
   `_ListDataStream` ☰ ?
   `_looks_like_prefix_name()` ☰ Built-in function
   `_namespace_map` ☰ `dict` object
   `_namespaces()` ☰ Function
   `parse()` ☰ Parses XML stored in a file (filename or filehandle) and returns an `xml.etree.ElementTree.ElementTree` object.  The 2nd (optional) parameter is the parser-object (`XMLParser`) by default. Returns an `xml.etree.ElementTree.Element` object. See also `fromstring()`.
   `ParseError` ☰ `xml.etree.ElementTree.ParseError` class
   `PI()` ☰ Function
   `ProcessingInstruction()` ☰ Function
   `QName` ☰ `xml.etree.ElementTree.QName` class
   `_raise_serialization_error()` ☰ Function
   `re` ☰ Module
   `register_namespace()` ☰ Function
   `_serialize` ☰ `dict` object
   `_serialize_html()` ☰ Function
   `_serialize_text()` ☰ Function
   `_serialize_xml()` ☰ Function
   `_set_factories()` ☰ Built-in function
   `SubElement()` ☰ Built-in function
   `sys` ☰ Module
   `tostring()` ☰ Function
   `tostringlist()` ☰ Function
   `TreeBuilder` ☰ `xml.etree.ElementTree.TreeBuilder` class
   `VERSION` ☰ `str` object
   `warnings` ☰ Module
   `XML()` ☰ Function
   `XMLID()` ☰ Function
   `XMLParser` ☰ `xml.etree.ElementTree.XMLParser` class
   `XMLPullParser` ☰ `xml.etree.ElementTree.XMLPullParser` class

  table }

}
{ xml.etree.ElementTree.ElementTree object

 `ElementTree` represents the tree structure of an → development/XML/DOM/nodes/document.

  { Members

    table { ll

     `find()` ☰ Class method
     `findall()` ☰ Class method
     `findtext()` ☰ Class method
     `getroot()` ☰ Returns the tree's root element (an `Element` object)
     `iter()` ☰ Class method
     `iterfind()` ☰ Class method
     `parse()` ☰ Class method
     `_root` ☰ ?
     `_setroot()` ☰ Class method
     `write()` ☰ Class method
     `write_c14n()` ☰ Class method

   table }

  }

}
{ xml.etree.ElementTree.Element object

 `Element` represents a single node in a tree represented by `ElementTree`.

  { Members

    table { ll

    `append()` ☰ Built-in function
    `attrib` ☰ `dict` object
    `clear()` ☰ Built-in function
    `extend()` ☰ Built-in function
    `find()` ☰ Built-in function
    `findall()` ☰ Built-in function
    `findtext()` ☰ Built-in function
    `get()` ☰ Built-in function
    `insert()` ☰ Built-in function
    `items()` ☰ Built-in function
    `iter()` ☰ Built-in function
    `iterfind()` ☰ Built-in function
    `itertext()` ☰ Built-in function
    `keys()` ☰ Built-in function
    `makeelement()` ☰ Built-in function
    `remove()` ☰ Built-in function
    `set()` ☰ Built-in function
    `tag` ☰ `str` object
    `tail` ☰ `NoneType` object
    `text` ☰ `NoneType` object

   table }

  }

}

sa:

  → development/languages/Python/standard-library
