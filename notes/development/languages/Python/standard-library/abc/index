$ Python - abc
@ abc

The module `abc` allows to define *abstract base classes*.

The → development/languages/Python/standard-library/collections/abc[`collections.abc`] defines some useful abstract base classes such as `Iterable`, `Container` and `MutableMapping`.

sa:

  → development/languages/Python/standard-library
