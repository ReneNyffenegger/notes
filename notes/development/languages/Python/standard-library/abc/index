$ Python standard library: abc
@ abc

The module `abc` allows to define *abstract base classes*.

Examples of built-in abstract base classes that come with → development/languages/Python include:
  • The → development/languages/Python/standard-library/collections/abc[`collections.abc`] defines some useful abstract base classes such as `Iterable`, `Container` and `MutableMapping`.
  • *numbers*, found in the `numbers` → development/languages/Python/module
  • *streams*, found in the → development/languages/Python/standard-library/io[`io`] module

sa:

  → development/languages/Python/standard-library
