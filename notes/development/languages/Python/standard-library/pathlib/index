$ Python standard library: pathlib
@ pathlib

sa:

  `pathlib` → development/languages/Python/operators/overlading[overloads] the division by (slash) → development/languages/Python/standard-library/operator[operator] (`/`).

  Some functions found in `pathlib` have a corresponding function in `→ development/languages/Python/standard-library/os` or → development/languages/Python/standard-library/os/path[`os.path`] (`Path.exists()` - `os.path.exists()`, `Path.cwd()` - `os.getcwd()` etc.)

  → development/languages/Python/standard-library
