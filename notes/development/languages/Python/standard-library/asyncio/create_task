$ Python: asyncio.create_task

`→ development/languages/Python/standard-library/asyncio[asyncio].create_task(coro, ‥ )` wraps the given coroutine into a `Task`, schedules its execution and returns the `Task` object.

code {
import asyncio

async def tsk(n):
      ‥


async def main():
      
      tsk_1 = asyncio.create_task(tsk(42))
      tsk_2 = asyncio.create_task(tsk(99))

      await tsk_1
      await tsk_2

asyncio.run(main())   
code }
