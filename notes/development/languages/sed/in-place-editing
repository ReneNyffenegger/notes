$ sed: in place editing

Without backup:
`sed -i s/PAT/REPL/ /path/to/file`

With backup:
`sed -i.bak s/PAT/REPL/ /path/to/file
-
Creates the backup file `/path/to/file.bak` and changes `/path/to/file`.

sa:
  → development/languages/Perl/in-place-editing

  → development/languages/sed
