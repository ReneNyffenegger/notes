$ JavaScript: eval function

`eval(expr_or_stmt)` evaluates the → development/languages/JavaScript/objects/String[string] `expr_or_stmt` (which probably should be an expression or a sequence of statements). 
-
It returns the value of the expression or the last statement.

The `eval` function is function that is defined in the → development/languages/JavaScript/objects/global[global object].

gh|about-javascript|/objects/Global-Object/function-properties/eval.html||

sa:
  `geval(…`) to evaluate an expression in the global context.
