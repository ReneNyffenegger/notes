$ JavaScript: replace a regular expression with a value

The → development/languages/JavaScript/objects/String[string method] `replace(regexp, value)` replaces the matched → development/regular-expressions[regular expression] (`regexp`)
in a string with `value`.


sa:

  → development/languages/JavaScript/objects/String/replace[`String.replace(…)`]

  → development/languages/JavaScript/objects/String/regular-expression[String methods] operating on
  → development/regular-expressions[regular expressions].
