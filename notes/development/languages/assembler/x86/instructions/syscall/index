
`syscall` is not available on 32 bit → hardware/CPUs/Intel[Intel CPUs] but is the standard way to enter *kernel mode* on *x86-64* (both → hardware/CPUs/Intel[Intel] and AMD).

The `syscall` instruction is decoded in two bytes: `0f 05`.

sa:
  → development/languages/assembler/x86/instructions/sysenter[`sysenter`]
