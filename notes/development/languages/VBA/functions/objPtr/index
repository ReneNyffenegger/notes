$ VBA function: objPtr

`objPtr(obj)` returns a pointer to the → development/languages/VBA/IUnknown[IUnknown] interface of the → development/languages/VBA/language/datatypes/object[object] `obj`.

→ development/languages/VBA/functions/xPtr[`varPtr`] applied to an → development/languages/VBA/language/datatypes/object[object] returns the address of the memory location that → development/languages/VBA/functions/objPtr[`objPtr`] returns:

 gh|about-VBA|/functions/objPtr/varPtr-objPtr.bas||

 If `objPtr` of an object variable returns `0`, the object variable is considered to be → development/languages/VBA/language/null-and-nothing-etc#vba-nothing[nothing].

sa:

  → development/languages/VBA/functions/xPtr[`varPtr` and `strPtr`]

  Classes: → development/languages/VBA/language/classes/reference-counting[reference counting]
