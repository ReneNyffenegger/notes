$ VBA function: mid

`mid()` is the equivalent of the function which is called → development/languages/Perl/functions/substr[`substr`], `substring` or similar in other → development/languages[programming languages].

code {
dim result as string
result = mid("The foo bar", 5, 3) ' Assign "foo" to result
code }

sa:
  → development/languages/VBA/functions
