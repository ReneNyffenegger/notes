$ VBA function: dateDiff

`dateDiff(interval, dateFrom, dateTo)` calculates the number of *intervals* (for example: `"d"` = days) between `dateFrom` and `dateTo`.

gh|about-VBA|/functions/dateDiff.bas||

sa:
  → development/languages/VBA/functions/dateAdd[`dateAdd(…)`]

  The → development/languages/VBA/language/datatypes/date[date] datetype.

  → development/languages/VBA/functions[VBA functions]
