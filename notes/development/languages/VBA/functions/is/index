$ VBA: isx() functions

→ development/languages/VBA has the following `isX(expr)` → development/languages/VBA/functions[functions] that check if the expression `expr` can be recognized as a particular
→ development/languages/VBA/language/datatypes[data type] or falls in a set of data types with similar characteristics:
  • `isArray()`
  • `isDate()`
  • `isEmpty()`
  • `isError()`
  • `isMissing()`
  • `isNull()` (see also → development/languages/VBA/language/null-and-nothing-etc[null and nothing etc.]
  • `isNumeric()`
  • `isObject()`

