$ VBA function: chr

`chr(n)` converts the → development/Unicode/ASCII[ascii] code `n` to a (single character) → development/languages/VBA/language/datatypes/string[string]. The opposite function is `asc("x")`.

gh|about-VBA|/functions/chr/asc_chr.bas||
  
sa:
  → development/languages/VBA/functions[VBA functions]
