$ VBA function: chr / chrW

{ chr #vba-chr

 `chr(n)` converts the → development/Unicode/ASCII[ascii] code `n` to a (single character) → development/languages/VBA/language/datatypes/string[string]. The opposite function is → development/languages/VBA/functions/asc-ascW[`asc("x")`].

  gh|about-VBA|/functions/chr/asc_chr.bas||

  The → Microsoft/Office/Excel[Excel] equivalent of `chr` seems to be `=char(…)` (in German: `=zeichen(…)`)

}
{ chrW #vba-chrW

  `chrW(u)` is similar to `chr`. However, it accepts the range 0 - 65535 as input parameter and produces a → development/Unicode[Unicode] character.

  The → Microsoft/Office/Excel[Excel] equivalent of `chrW` seems to be `=uniChar(…)` (In German: `=uniZeichen(…)`)

}
  
sa:

  The Excel worksheet functions → Microsoft/Office/Excel/functions/char-unichar-code-unicode[`char`, `unichar`, `code` and `unicode`].

  → development/languages/VBA/functions[VBA functions]
