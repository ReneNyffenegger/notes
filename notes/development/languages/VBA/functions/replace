$ VBA function: replace

`replace(str, search, repl)` replaces the substring `search` with `repl` in the → development/languages/VBA/language/datatypes/string `str`.

gh|about-VBA|/functions/replace.bas||

sa:

  For more complex search/replace operations, → development/languages/VBA/regular-expressions[regular expressions] might be used.

  → development/languages/VBA/functions
