$ VBA function: asc / ascW

`asc` and `ascW` are the opposite functions of → development/languages/VBA/functions/chr[`chr` and `chrW`].

{ Related (und unrelated- ?) Excel functions

  The → Microsoft/Office/Excel[Excel] equivalent of `asc` seems to be → Microsoft/Office/Excel/functions/char-unichar-code-unicode[`=code(…)`] (even in German!).
  The equivalent of `ascW` seems to be → Microsoft/Office/Excel/functions/char-unichar-code-unicode[`=uniCode(…)`] (also in German).

}

sa:

  → development/languages/VBA/functions[VBA functions]
