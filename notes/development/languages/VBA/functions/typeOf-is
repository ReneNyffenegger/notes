$ VBA: function typeOf … is

  The `typeOf var is classname` construct allows to check if a given variable is of a particular type.

{ Two classes

  First, we define two (simple) → development/languages/VBA/language/classes[classes]:

  gh|about-VBA|/functions/typeOf-is/classOne.cls||

  gh|about-VBA|/functions/typeOf-is/classTwo.cls||

}
{ Using typeOf

  Now, we're ready to use `typeOf … is` on instances (→ development/languages/VBA/language/datatypes/object[objects]) of these classes:

  gh|about-VBA|/functions/typeOf-is/main.bas||

}

{ Interna

  It can be strongly *assumed* that `typeOf()` calls → Windows/development/COM/IUnknown#iunknown-queryInterface[`QueryInterface()`] to determine if `var` is of the given type.

}

sa:

  → development/languages/VBA/functions/typeName[`typeName()`]

  → development/languages/VBA/functions[VBA functions]

  The → development/languages/JavaScript/operators/typeof[JavaScript operator `typeof`]
