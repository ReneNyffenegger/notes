$ VBA function: lenB

{ lenB vs len

  For most datatypes, `lenB()` returns the same number as → development/languages/VBA/functions#vba-len`[len()`] does. However, for
  → development/languages/VBA/language/datatypes/string[strings], `len(str)` returns the number of characters in the string while
  `lenB(str)` returns the number of bytes that the string occupies. Because Visual Basic strings are internally stored as BSTRs, `lenB(str) = 2 * len(str)`.

  gh|about-VBA|/functions/lenB/lenB-vs-len.bas||

}

sa:
  → development/languages/VBA/functions[VBA functions]
