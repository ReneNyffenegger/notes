$ VBA function: inStr

code {
inStr(posStart, strSearchedIn, strSearchedFor)
inStr(posStart, strSearchedIn, strSearchedFor, compare)
code }

`posStart` assumes the first character to be at position 1, not 0.

The value of `compare` is optional and can be one of
  • `vbUseCompareOption` (-1)
  • `vbBinaryCompare` (0)
  • `vbTextCompare` (1)
  • `vbDatabaseCompare` (2, → development/databases/Access only)

If `strSearchedFor` is found in `strSearchedIn`, the function returns the position at which `strSearchedFor` was found. Otherwise, depending on the parameter values, the function returns
→ development/languages/VBA/language/null-and-nothing-etc[`null`] or `0`.
