$ Examples for Visual Basic for Application accessing the Windows API: GetComputerName

The Windows API function `GetComputerName` determines the *Net Bios Name* of the computer.

The following example needs the VBA declarations of the Windows API which can be found
→ development/languages/VBA/Win-API[here].

gh|WinAPI-4-VBA|/examples/GetComputerName.bas||


sa:
  → development/languages/VBA/Win-API/examples[Other examples]
