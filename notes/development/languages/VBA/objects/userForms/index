$ VBA: UserForms

`UserForms` is a global intrinsic variable that is provided by → development/languages/VBA.
This variable is a collection that contains all *loaded* → development/languages/VBA/Useful-object-libraries/Microsoft-Forms#ms-forms-UserForm[`MSForms.UserForm`] objects (of the → development/languages/VBA/Useful-object-libraries/Microsoft-Forms[forms library]) of an application
(See → development/languages/VBA/language/statements/load-unload[`load` and `unload`]).

`UserForms` is technically a property of the `global` object. Its data type is `→ development/languages/VBA/language/datatypes/object`.
code {
debug.print (typename(vba.global.userForms))
debug.print (typename(vba.userForms))
debug.print (typename(userForms))
code }


sa:

  The → development/languages/VBA/language/statements/load-unload[VBA statements `load` and `unload`].
