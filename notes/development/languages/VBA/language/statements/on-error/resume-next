$ VBA statement: on error resume next

With `on error resume next`, statements that cause an error are just skipped. The `resume next` indicates to execute the next statement.

gh|about-VBA|/language/statements/on-error/resume-next.bas||

sa:
  → development/languages/VBA/language/statements/on-error[`on error`].
