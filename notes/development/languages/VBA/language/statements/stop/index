$ VBA statement: Stop
@ stop

The `stop` statement stops execution (but does not → development/languages/VBA/language/statements/end[`end`] it).

sa:

  The `SingleStep` method of the Access Object → Microsoft/Office/Access/Object-Model/DoCmd[`DoCmd`].

  → development/languages/VBA/language/statements
