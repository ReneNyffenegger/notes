$ VBA: data type currency
@ currency

The `currency` data type uses 8 bytes to store a number with up to four decimal places.

sa:

  The → development/languages/VBA/language/datatypes#vba-type-declaration-character[type declaration character] for a `currency` is `@`.

  → development/languages/VBA/language/datatypes[datatypes]
