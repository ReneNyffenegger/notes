$ VBA: data type double

→ development/languages/VBA/language/datatypes/date/internal[Internally], a → development/languages/VBA/language/datatypes/date[date] is stored as a double.

{ Floor

  → development/languages/VBA[VBA] does not have a function named `floor`.

  In order to calculate the floor of a double with VBA, either
  → development/languages/VBA/functions#vba-int[`int(num)`] or
  → development/languages/VBA/functions#vba-fix[`fix(num)`] is needed.

  TODO: In → Microsoft/Office/Excel[Excel], apperently, there is also a
  `→ Microsoft/Office/Excel/Object-Model/WorksheetFunction[worksheetFunction].floor_math` or
  `worksheetFunction.floor_precise()` function.

}

sa:

  The → development/languages/VBA/language/datatypes/single[single] and → development/languages/VBA/language/datatypes[other datatypes].
