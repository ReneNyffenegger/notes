$ VBA: Null and nothing etc.

code {
if obj is nothing then
   …
end if
code }

Then, there is also the `isNull` and → development/languages/VBA/functions#vba-isEmpty[`isEmpty`] → development/languages/VBA/functions[function] and the special values `null`, `vbNull`, `empty` and `vbEmpty`.
-
There is even a `vbNullString` and a `vbNullChar`

{ nothing

  `nothing` is only applicable to variables that are declared as *objects* or → development/languages/VBA/language/datatypes/variant[variant].


{ null

  gh|about-VBA|/language/null-and-nothing/null.bas||

}
