$ VBA Module RunSQLScripts [Access]

`runSQLScript` runs the SQL statements found in a file on an → development/databases/Access[Access] Database. It relies on the function `sqlStatementsOfFile` which is found in
the → development/languages/VBA/modules/Database/SQL[SQL module].

gh|VBAModules|/Access/RunSQLScripts.bas||

sa:

  → development/languages/VBA/modules/index
