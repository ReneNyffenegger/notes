$ VBA classes for ADO

{ adoStatement

  A class that encapsulates an → development/databases/SQL/statement and allows to bind variables (which I believe → development/databases/ADO[ADO] calls parameters).

  table { lll
    `init` ☰ ☰
    `sql` ☰ Set SQL text for statement ☰ 
    `sqlFromFile` ☰ Load SQL text from a file ☰
    `addXYZParameter` ☰ `XYZ`: `integer`, `Varchar`, `Data` ☰ This method needs to be worked on (or even discared)
    `exec` ☰ Execute the statement ☰
    `record` ☰ Select statement: moves to the next record, returns true as long as data is available ☰ 
    `col` ☰ ☰
  table }

  { Source code

    gh|VBAModules|/Database/ado/adoStatement.cls||

  }

}

sa:

  The test cases for this module are → development/languages/VBA/modules/_test/ado[here].

  → development/languages/VBA/modules/Database/ADOHelpers
