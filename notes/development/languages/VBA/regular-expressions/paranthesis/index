$ VBA regular expressions: paranthesis

Paranthesis allow to extract *submatches* from a regular expression

{ Match after bar

  The following pattern tries to match a number (the submatch) after the text `bar`.

  If the pattern matches, it is returns the submatch via the `matchCollection` property.

  gh|about-VBA|/regular-expressions/paranthesis/match-after-bar.bas||

}
{ Match words

  The following example tries to demonstrate two approaches on how to match words in a string.

  gh|about-VBA|/regular-expressions/paranthesis/match-words.bas||

}
sa:
  → development/languages/VBA/regular-expressions[Regular expressions in VBA]
