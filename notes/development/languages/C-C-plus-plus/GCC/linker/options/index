$ GCC linker: options

With → development/languages/C-C-plus-plus/GCC[gcc], linker specific options can be passed to the linker with → development/languages/C-C-plus-plus/GCC/options/Wl[gcc -Wl,-option].

→ development/languages/C-C-plus-plus/GCC/linker/options/add-stdcall-alias[`ld --add-stdcall-alias`]

→ development/languages/C-C-plus-plus/GCC/linker/options/kill-at[`ld --kill-at`]

`ld --entry` or `ld -e` specifies the binary's *entry point (for example `ld -start`) (see also → Windows/development/Portable-Executable/EXE/entry-point[entry point of a Windows executable).
