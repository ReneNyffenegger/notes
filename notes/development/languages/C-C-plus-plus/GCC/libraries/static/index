$ static libraries

A *static library* is a set of → development/languages/C-C-plus-plus/GCC/object-file[object files] (`*.o`).

The default suffix of static libraries is `.a`.

Static libraries are created with → development/GNU/toolchain/binutils/ar[`ar`], the archiver.
