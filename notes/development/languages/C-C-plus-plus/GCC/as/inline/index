$ GNU inline assembly

The compiler does parse the passed assembler instructions.

{ Operands

  An operand is described by an *operand-constraint string* followed by a c expression in paranthesis.

  *Output operands* are indicated by an `=` sign. Of course, the must be lvalues.

}
