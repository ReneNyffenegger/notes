$ gcc -v

`gcc -v` prints the commands executed to run the stages of compilation
-
In adition, it reports the version number of
  • the compiler driver program,
  • the preprocessor and
  • the compiler proper

The output of `-v` goes to → development/stdin-stdout-stderr[stderr].

{ main.c

  gh|about-gcc|/options/v/main.c||

}
{ Invoking the compiler with -v

  gh|about-gcc|/options/v/invoke_gcc_with_-v||

}

{ TODO

 `gcc -###` is similar, but does not execute the commands.

}

sa:
  → development/languages/C-C-plus-plus/GCC/options
