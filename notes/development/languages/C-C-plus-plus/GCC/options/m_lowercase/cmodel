$ gcc -mcmodel

{ Print known code models

code {
$ gcc → development/languages/C-C-plus-plus/GCC/options/help[--help]=target | → development/languages/awk '/Known code models/{getline; print}' | → Linux/shell/commands/xargs -n1
32
kernel
large
medium
small
code }

}

{ X86

  The available models on x86 are
  table { ll
    `small` ☰
    `medium` ☰
    `large` ☰
    `kernel` ☰ This one must be used when → Linux/kernel/compilation[compiling the Linux kernel].
  table }

}
