$ gcc -masm

`-masm=dialect` selects the dialect in which assembly instructions are recognized or output.

{ Print known dialects

code {
$ gcc → development/languages/C-C-plus-plus/GCC/options/help[--help]=target | → development/languages/awk '/Known assembler dialects/{getline; print}' | → Linux/shell/commands/xargs -n1
att
intel
code }

}

