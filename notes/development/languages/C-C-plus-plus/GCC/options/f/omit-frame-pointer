$ gcc -fomit-frame-pointer

Don't maintain the (stupid?) frame pointer maintanance which usually makes the executable smaller and sometimes faster.

An executable without such frame pointers is not as easily debugged with → development/tools/gdb[gdb].


sa:
  → development/languages/C-C-plus-plus/GCC/options
