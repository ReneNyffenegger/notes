$ gcc -masm
@ -masm

Select the (dis-)assembly dialect.

Apparently, on → development/languages/assembler/x86[x86] architectures, `att` (default, AT&T) and → hardware/CPUs/Intel[`intel`] are supported.

code {
gcc -masm=intel -S input.c
code }

{ Misc

 `-masm` is a GNU → development/languages/C-C-plus-plus/GCC/options/[option] and has nothing to do with → Windows/development/Visual-Studio/masm[Microsoft's Macro Assembler] (masm).

}

sa:
  → development/languages/C-C-plus-plus/GCC/options/S_uppercase

  → development/languages/C-C-plus-plus/GCC/options

  → development/languages/C-C-plus-plus/GCC/as

