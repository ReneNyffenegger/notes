$ Creating libraries with GCC

{ TODO


  Linker option `-soname` (`-Wl,-soname,libtq84.so.1`)

  A *SONAME* apparently is `lib${LIBNAME}${API_VERSION}.so.${MAJOR_VERSION}`. Usually, `${API_VERSION}` is empty. So, it becomes something like `libTQ84.so.0`.

  `objdump -p $EXECUTABLE | grep NEEDED`

  `readelf`

}
