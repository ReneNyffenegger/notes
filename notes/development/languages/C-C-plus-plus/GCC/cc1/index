$ GCC: cc1

`cc1` is also referred to as the *compiler proper*.

`cc1` takes a preprocessed c file and creates assembly code. The assembly code is converted into an → development/languages/C-C-plus-plus/GCC/object-file[object file] with
the → development/languages/C-C-plus-plus/GCC/as[assembler].

sa:
  → development/languages/C-C-plus-plus/GCC
