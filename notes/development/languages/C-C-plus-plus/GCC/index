abbr: GNU Compiler Collection

GCC requires → development/misc/GMP to be built.

{ Some command line options

  { -d Debugging dumps

    `-dM` (along with `-E`): generates a list of `#define` directives for all macros defined during the execution of the preprocessor:
    code {
gcc -dM -E - < /dev/null | sort
    code }
    or on Windows:
    code {
gcc -dM -E - < NUL | sort
#define __ATOMIC_ACQ_REL 4
#define __ATOMIC_ACQUIRE 2
#define __ATOMIC_CONSUME 1
#define __ATOMIC_HLE_ACQUIRE 65536
#define __ATOMIC_HLE_RELEASE 131072
…
#define _INTEGRAL_MAX_BITS 64
#define _REENTRANT 1
#define _stdcall __attribute__((__stdcall__))
#define _thiscall __attribute__((__thiscall__))
#define _WIN32 1
#define _X86_ 1
#define i386 1
#define WIN32 1
#define WINNT 1
    code }


  }
  { -dumpmachine #dumpmachine

    prints the → development/misc/target-triplet.

  }
  { -E

    Only → development/languages/C-C-plus-plus/CPP/preprocessor[preprocess] file (don't compile or link). Preprocessed output is sent to stdout.

  }

}

sa:
  → development/languages/C-C-plus-plus/GCC/optimization,
  → development/languages/C-C-plus-plus/GCC/options

  → development/languages/C-C-plus-plus/GCC/create-libraries
