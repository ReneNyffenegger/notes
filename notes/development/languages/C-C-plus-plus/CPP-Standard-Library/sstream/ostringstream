$ C++ Standard Library: ostringstream - write formatted text into a char array in memory

`ostringstream` is to be the C++ replacement for → development/languages/C-C-plus-plus/C/libc/index#printf-family[`sprintf`].

`ostringstream` is the `char` specialization of the template class basic_ostringstream (that is, it is: `std::basic_ostringstream<char>`).

gh|about-cpp-standard-library|/sstream/ostringstream.cpp||

sa:
  → development/languages/C-C-plus-plus/CPP-Standard-Library
