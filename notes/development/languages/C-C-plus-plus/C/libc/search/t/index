$ Standard C library: search tree

The `tsearch()` and `tfind()` can be used to implement an *associative array* in C. The `t` stands for tree.

In the following example, the `t…()` functions are renamed to `tq84_…()` so as to be possible to be used with  → development/languages/C-C-plus-plus/C/libc/search/t/tq84-tsearch[`tq84-tsearch.c`].

gh|about-libc|/search/t/prog.c||

{ TODO

  The `c` subdirectory of a Strawberry Perl installation contains a `tsearch` directory that might have a search tree implementation.

}

sa:

  → development/languages/C-C-plus-plus/C/libc

links:
  → https://www.prevanders.net/tsearch.html
