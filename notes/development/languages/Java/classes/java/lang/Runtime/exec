$ Java class java.lang.Runtime - exec

The method `exec` of `java.lang.Runtime` starts a new → Windows/development/process[process] and returns
a → development/languages/Java/classes/java/lang/Process[`java.lang.Process`]

The following example creates a → Windows/dirs/Windows/System32/cmd_exe[`cmd.exe`] process and executes → Windows/dirs/Windows/System32/cmd_exe/commands/dir[`dir /s /b ..\..`] to list all files beneath the directory
that is two levels higher than the current one. It then reads the process' → development/stdin-stdout-stderr[stdout] and repeats it to the console:
gh|JavaClasses|/java/lang/Runtime/exec.java||

sa:

  → development/languages/Java/classes
