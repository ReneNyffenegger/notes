$ C# - struct

`struct` defines a value type (as opposed to a reference type which is defined with `class`). Because they're value types, these types derive from
→ Microsoft/dot-net/namespaces-classes/System/ValueType[`System.ValueType`].
