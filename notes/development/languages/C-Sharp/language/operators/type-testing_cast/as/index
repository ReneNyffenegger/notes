$ C# - operator as

`V as T` casts the variable `V` to the type `T`. If the cast is not possible, it evaluates to `null`. This is different from the *cast operator* which trhows an excepction in such a case.

gh|about-C-Sharp|/language/operators/type-testing_cast/as/cast-types.cs||
