abbr: Windows Script Host

A →COM based environment for scripts.

WSH itself does not run scripts. They're run by → development/languages/VBScript/index, → development/languages/JScript/index

{ WScript object

  The WScript object is the root object in the Windows Script Host hierarchy.

}

{ Disabling Windows Script Host

  → Windows/registry/index: Setting the value of `HKCU\Software\Microsoft\Windows Script Host\Settings\Enabled` or
                                 `HKLM\Software\Microsoft\Windows Script Host\Settings\Enabled` to 0.

}
