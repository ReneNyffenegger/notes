$ Data lake

According to *Dull*, a → development/Data[data] lake is a stroage repository that holds a vast amount of raw data in its native format, including strctured, semi-structured and
unstructered data.
-
This implies that *any* and *all* data can be stored in a data lake.

The data lake stores data until it is needed.
