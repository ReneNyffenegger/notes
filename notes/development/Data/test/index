$ Test data

In → development/languages/R[R], test data that somewhat resembles stock exchange prices can be constructed with → development/languages/R/functions/cumsum#r-cumsum-test-data[`cumsum` and `rnorm`].

sa:

  → development/databases/SQL-Server/sql/select/values

  → development/databases/Oracle/SQL-Plus/sqlpath/insert_statement_creator[`insert_statement_creator.sql`] is an Oracle SQL script that creates insert statements from an
  existing table. The idea is that these insert statements can then manually be changed as required.

  → development/Data
