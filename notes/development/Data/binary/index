$ Binary data
@ binary data

sa:

  Binary data is sometimes encoded with → development/Base64, especially when transmitting over mutliple networks (see for example the → development/security/Privacy-Enhanced-Mail format).

  → https://github.com/wader/fq[fq] is inspired by `→ development/languages/JavaScript/JSON/tools/jq` (a `→ development/languages/sed` like tool for→ development/languages/JavaScript/JSON),
  but works with binary data formats.

  The Python module `→ development/languages/Python/standard-library/struct` allows reading and writing binary data.

  The → development/vim/options/binary[VIM option `binary`]
