$ Binary data
@ binary data

sa:

  Binary data is sometimes encoded with → development/Base64, especially when transmitting over mutliple networks (see for example the → development/security/Privacy-Enhanced-Mail format).

  → https://github.com/wader/fq[fq] is inspired by → https://stedolan.github.io/jq/[jq] (a `→ development/languages/sed` like tool for→ development/languages/JavaScript/JSON),
  but works with binary data formats.

  The → development/vim/options/binary[VIM option `binary`]
