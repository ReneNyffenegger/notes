$ SQLite: WITH clause

code {
with W as (
   select 1, 2
)
select *
from
   W;
code }

{ insert

  gh|about-sqlite|/sql/with/insert.sql||

}

sa:

  → development/databases/SQLite/sql/with/recursive[Recursive queries]
