$ SQLite: order by

{ Relevance of the data type in an order by clause

  In SQLite, the `order by` clause first sorts according to a value's
  → development/databases/SQLite/datatypes[data type] and only then according to the value:
  -
  First is the null data type, then the numerical data types integer and real, then text and finally
  → development/databases/SQLite/datatypes/blob[blobs]:
  gh|about-sqlite|/sql/select/order-by/data-types.sql||

}

sa:
  → development/databases/SQLite/sql/select[`select` statement]
