$ SQLite: group by

{ group_concat #sqlite-group_by-group_concat

`group_concat`: concatenates values within a group. It is SQLite's implementation of
the → misc/ISO/9075/features/T625[SQL standard feature T625 (`listagg`)].

gh|about-sqlite|/sql/select/group_by/group_concat.sql||

}

sa:
  → development/databases/SQLite/sql[SQL statement]
