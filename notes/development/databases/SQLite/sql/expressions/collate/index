$ SQLite: collate
@ collate

{ Case sensitive and case insenstive queries

  With `collate binary` and `collate nocase`, a comparison can be instructed to either be made case insensitive or case sensitive.

  Note, however, that → development/databases/SQLite/sql/expressions/like[`like`] expressions are not affected by `collate binary`.

  gh|about-sqlite|/sql/expressions/collate/select.sql||

}


sa:
  → development/databases/SQLite/sql/expressions
