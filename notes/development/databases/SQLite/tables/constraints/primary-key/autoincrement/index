$ SQLite: autoincrement

`autoincrement` columns are only allowed on `→ development/databases/SQLite/datatypes/integer → development/databases/SQLite/tables/constraints/primary-key`
→ development/databases/SQLite/tables/columns[columns].

{ autoincrement rowids are not reused

  The following example tries to demonstrate that *autoincrement* → development/databases/SQLite/tables/rowid[rowids] are not reused:

  gh|about-SQLite|/tables/constraints/primary-key/autoincrement/vs-rowid.sql||

}
sa:

  The maximum used autoincrement value per → development/databases/SQLite/tables is stored in the schema object → development/databases/SQLite/internals/schema-objects/sqlite_sequence[`sqlite_sequence`].
