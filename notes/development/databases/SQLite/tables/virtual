$ SQLite: virtual tables

It's not possible to create → development/databases/SQLite/indices[indices] or → development/databases/SQLite/triggers[triggers] on virtual tables in SQLite.

An example of virtual tables are FTS3 and FTS 4 tables.

sa:
  → development/databases/SQLite/tables
