$ SQLite: rowid

Every SQLite → development/databases/SQLite/tables[table] (except → development/databases/SQLite/tables#without-rowid[without rowid]) table has a `rowid` column.

This rowid column is a 64 bit integer value.
