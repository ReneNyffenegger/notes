$ SQLite: internal schema objects

The names of internal schema objects start with `sqlite_`.

→ development/databases/SQLite/internals/schema-objects/sqlite_master[`sqlite_master`]

→ development/databases/SQLite/internals/schema-objects/sqlite_sequence[`sqlite_sequence`] is used for *autoincrement` columns*.

`sqlite_stat1` is created by the (first?) → development/databases/SQLite/sql/analyze[`analyze`] statement.

`sqlite_stat3`

`sqlite_stat4`
