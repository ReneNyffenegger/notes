$ SQLite: force using a specific index with INDEXED BY

SQLite allows to use the *hint* `indexed by` to force the query planner to use a specific → development/databases/SQLite/indices[index]:
gh|about-sqlite|/indices/indexed-by.sql||
