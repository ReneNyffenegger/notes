$ SQLite function: typeof
@ typeof

  `typeof(val)` returns the → development/databases/SQLite/datatypes[data type] of a value. The type of `typeof` itselfs is text. The returned
  strings are one of `integer`, `real`, `text`, `null` or
  → development/databases/SQLite/datatypes/blob[`blob`].

  gh|about-sqlite|/functions/typeof.sql||

sa:
  → development/databases/SQLite/functions
