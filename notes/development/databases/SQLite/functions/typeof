$ SQLite function: typeof
@ typeof

  `typeof` returns the → development/databases/SQLite/datatypes[data type] of a stored value which can be one of `integer`, `real`, `text`, `null` or `blob`:

  gh|about-sqlite|/functions/typeof.sql||

sa:
  → development/databases/SQLite/functions
