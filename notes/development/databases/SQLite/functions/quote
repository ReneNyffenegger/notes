$ SQLite function: quote
@ quote

`quote(val)` returns a string that, as expression, evaluates to the same value as was quoted.

For example, → development/databases/SQLite/datatypes/blob[blobs] are returned as «blob literals» (`x'…'`).

gh|about-sqlite|/functions/quote.sql||

sa:
  → development/databases/SQLite/functions/hex[`hex(val)`]
  -
  → development/databases/SQLite/functions
