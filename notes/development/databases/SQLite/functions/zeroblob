$ SQLite function: zeroblob
@ zeroblob

`zeroblob(n)` returns a → development/databases/SQLite/datatypes/blob[blob] with `n` bytes, each being `0`.

sa:
  → development/databases/SQLite/functions/randomblob[`randomblob()`]

  → development/databases/SQLite/functions

