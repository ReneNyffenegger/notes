$ SQLite function: upper / lower
@ upper / lower

`upper(t)` and `lower(t)` returns the the → development/Unicode/ASCII characters of `t` in upper or lowercase. The non ASCII characters (such as
the `ä`, `é`) are returned unchanged. This is demonstrated in the following simple example:

gh|about-sqlite|/functions/upper-lower.sql||

sa:
  → development/databases/SQLite/functions
