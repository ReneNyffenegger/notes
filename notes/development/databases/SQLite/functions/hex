$ SQLite function: hex
@ hex

`hex(val)` returns a hexadecimal representation of the internally stored bytes for `val`.

gh|about-sqlite|/functions/hex.sql||

sa:

  → development/databases/SQLite/functions/quote[`quote(val)`]
  -
  → development/databases/SQLite/functions

  The → development/databases/SQLite/datatypes/blob[blob datatype].

