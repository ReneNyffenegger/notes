$ SQLite shell: tabular output

With the dot commands `.mode column` and the column width specifier `.width …`, it is possible to print a result in tabular form:

code {
sqlite3> .mode column
sqlite3> .width 10 25 54 …
sqlite3> select foo, bar, baz, … from tData;
code }

sa:
  → development/databases/SQLite/shell[SQLite shell]
