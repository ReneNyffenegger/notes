$ SQLite shell: .load

`.load` loads a → Windows/development/Portable-Executable/DLL[DLL] (→ Windows) or a *shared object* (→ Linux) in order to add more functionality to
→ development/databases/SQLite.

An example of such a DLL/shared object is *pcre.so*, which allows to
→ development/databases/SQLite/functions/regexp[query with regular expressions].

sa:
  → development/databases/SQLite/shell[SQLite shell]
