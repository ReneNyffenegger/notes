$ ADODB examples for Oracle: output parameters

This is an example that tries to demonstrate how output parameters from a stored procedure can be accessed with ADO and → development/languages/VBA[Visual Basic for Applications].

First, we have de create a package:
  gh|about-adodb|/Oracle/output-parameters/in-out-parameters.sql||

Then, we can access them:
  gh|about-adodb|/Oracle/output-parameters/in-out-parameters.bas||

sa:
  → development/databases/ADO/ADODB-examples/Oracle

