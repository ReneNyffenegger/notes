$ ADODB.Parameters

`Parameters` is a collection of → development/databases/ADO/objects/parameter[`ADODB.Parameter`] objects.
-
The objects are indexed with a base of 0 (i. e. the first object is `params(0)`).

The `parameters` collection is the default member of the `→ development/databases/ADO/objects/command` object.

{ Methods and properties

  table { ll
    `append()` ☰
    `count` ☰
    `delete()` ☰
    `item` ☰
    `refresh()` ☰
  table }

}


sa:
 → development/databases/ADO[ADO]
