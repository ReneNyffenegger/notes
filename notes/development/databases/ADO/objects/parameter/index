$ ADODB.Parameter
@ parameter

{ Properties and methods

  table { ll

   `appendChunk()` ☰
   `attributes` ☰
   `direction` ☰ `adParamInput` (default), `adParamInputOutput`, `adParamOutput`, `adParamReturnValue` or `adParamUnknown`
   `name` ☰
   `numericScale` ☰
   `precision` ☰
   `properties` ☰
   `size` ☰ Sets or gets the maximum size in bytes or characters of a value (data type is `long`).
   `type` ☰
   `value` ☰

  table }

}

sa:

 The `→ development/databases/ADO/objects/command/createParameter` method of the `→ development/databases/ADO/objects/command` object.

 The `parameters` property of the `→ development/databases/ADO/objects/command` object.

  → development/databases/ADO/ADODB-examples/Oracle/named-parameters

  → development/databases/ADO/objects/parameters[ADODB.Parameters]

  A `parameter` has a → development/databases/ADO/data-types[data type]

  → development/databases/ADO[ADO]

  → development/databases/Oracle/SQL/statement/bind-variables[Bind variables] in Oracle
