$ ADODB.Connection: openSchema

The `openSchema()` method of → development/databases/ADO/objects/connection[`ADODB.connection`] returns a → development/databases/ADO/objects/recordSet[RecordSet] that provides information about a database.

{ Example (VBA / MS-Access)

  The following simple → development/languages/VBA example tries to demonstrate how information about a → development/databases/SQL/table/constraint/primary-key and a
  → development/databases/SQL/table/constraint/foreign-key can be extracted from → development/databases/Access[MS Access] tables:
  gh|about-adodb|/objects/connection/openSchema/ms-access.bas||

}

sa:
 → development/databases/ADO[ADO]
