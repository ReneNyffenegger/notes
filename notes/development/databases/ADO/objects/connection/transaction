$ ADODB.connection: transaction management

The → development/databases/ADO/objects/connection[ADODB `connection`] object allows to manage → development/databases/SQL/statement/transaction[transactions] with the following methods:
  • `beginTrans`
  • `commitTrans`
  • `rollbackTrans`
 
sa:


 → development/databases/ADO[ADO]
