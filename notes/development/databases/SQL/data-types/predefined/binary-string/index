$ SQL data type: binary string

The *binary string* → development/databases/SQL/data-types[data type] stores a sequence of bytes (octets). Its length might be fixed or variable.

sa:

  BLOBs: binary large objects

  → development/databases/SQL/data-types
