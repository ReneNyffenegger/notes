$ SQL data type: datetime

The *datetime* → development/databases/SQL/data-types[data type] comes in three variants:
  • `timestamp`
  • `time` (to specify a time, but not a date - hours, minutes, seconds)
  • `date` (used for dates of the *Gregorian calendar*: year, month, day)

sa:
  → development/databases/SQL/data-types
