$ SQL: order by

{ Collations

  When specifying an order by, it should be possible to specify a → development/Unicode/collation.

}

sa:

  An example that demonstrates the → development/databases/SQL/operation-order[order of SQL operations] with `order by` and `union all` is → development/databases/SQL/operation-order[here].
