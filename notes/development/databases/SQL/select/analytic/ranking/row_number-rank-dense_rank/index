$ SQL: ROW_NUMBER, RANK and DENSE_RANK

table { ll
  `row_number()` ☰ No duplicates, no gaps
  `rank()` ☰ Duplicates with gap possible
  `dense_rank()` ☰ Duplicates possible, no gaps
table }

sa:

  → development/databases/Oracle/SQL/select/analytic/ranking/row_number-rank-dense_rank

  → development/databases/SQL/select/analytic/ranking[Ranking analytic functions]
