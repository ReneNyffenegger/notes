$ SQL: select statement


sa:

  The query result of a `select` statement can be → development/databases/SQL/DML/insert#sql-insert-query[inserted into a table] with an
  → development/databases/SQL/DML/insert[`insert` statement].

  → development/databases/SQL/select/group-by/[`group by`]

  → development/databases/SQL/select/order-by[`order by`]

  → misc/ISO/9075/features[SQL standard feature] E051 (Basic query specification)

  → development/databases/SQL/select/limiting-resultset[Selecting the first row or first n rows only]
