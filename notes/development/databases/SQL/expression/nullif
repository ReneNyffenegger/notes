$ SQL: NULLIF

The expression `nullif(p₁, p₂)` evaluates to → development/databases/SQL/data-types/null[`null`] if `p₁ == p₂`, otherwise to `p₁`.

`nullif(p₁, p₂)` is equivalent to the following `case` expression:

code {
case
   when p₁ = p₂ then null
   else p₁
end
code }
