$ SQL: where

sa:
  Much as the SQL `where` clause is used to specify on which records an SQL operation is peformed, the → Windows/PowerShell/command-inventory/nomen/object/select[`select-object cmdLet`] is used to specify which
  PowerShell-objects are kept in a → Windows/PowerShell/pipeline[PowerShell pipeline].
