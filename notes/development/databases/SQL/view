$ SQL: view

A view is a named → development/databases/SQL/select[`select` statement]. This allows to re-use the same query multiple times. The *name of the view* is then used in lieu of a
→ development/databases/SQL/table name.
