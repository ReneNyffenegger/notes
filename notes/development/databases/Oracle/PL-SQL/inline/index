$ Inlining PL/SQL code

The following → development/databases/Oracle/PL-SQL language elements and statements might be inlined.
   • Assignment
   • `call`
   • Conditional
   • `case`
   • `continue when`
   • → development/databases/Oracle/PL-SQL/execute-immediate/index[`execute immediate`]
   • `exit when`
   • → development/databases/Oracle/PL-SQL/statements/loop[`loop`]
   • `return`

sa:
  Setting → development/databases/Oracle/adminstration/init-parameters/plsql/optimize_level#plsql_optimize_level-inline[`plsql_optimize_level`] to 3 causes the compiler to seek opportunities to
  inline code.

  → development/databases/Oracle/PL-SQL/pragma/inline[`pragma inline`]
