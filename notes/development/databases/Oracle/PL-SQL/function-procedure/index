$ PL/SQL functions and/or procedures

sa:
  → development/databases/Oracle/PL-SQL/function-procedure/deterministic[Deterministic] and
  → development/databases/Oracle/PL-SQL/function-procedure/pipelined[pipelined] and
  → development/databases/Oracle/PL-SQL/function-procedure/polymorphic-table[polymorphic table (PTF)] functions.

  Enabling PL/SQL functions for → development/databases/Oracle/SQL/statement/execution/parallel[parallel execution] using the → development/databases/Oracle/PL-SQL/function-procedure/parallel[`parallel_enable`] flag.

  → development/databases/Oracle/PL-SQL/function-procedure/overhead-in-SQL[Oeverhead when using functions] in SQL.

  → development/databases/Oracle/PL-SQL/function-procedure/sql_macro[SQL macros]

  A `select` statement in a PL/SQL function which is executed from a `select` statement, does not throw a
  → development/databases/Oracle/errors/ORA-01403_no-data-found#ora-1403-plsql-func[ORA-01403: no data found] error.

  Unlike variables, a parameter that is declared to be a `→ development/databases/Oracle/SQL/data-types/varchar2` does not need to have a maximum length specification,
  see the error message → development/databases/Oracle/PL-SQL/errors-warnings/PLS-00215_String-length-constraints-must-be-in-range-1-32767.
