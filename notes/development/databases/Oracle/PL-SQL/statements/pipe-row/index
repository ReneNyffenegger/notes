$ PL/SQL: PIPE ROW

The `pipe row` statement is used (and can only be used) in → development/databases/Oracle/PL-SQL/function-procedure/pipelined[Pipelined functions] to return a row to the selector.

`pipe row(…)` requires parantheses.

gh|Oracle-Patterns|/PL-SQL/statements/pipe_row/do.sql||

sa:

  Do not confuse `pipe row` with → development/databases/Oracle/installed/packages/dbms/pipe[`dbms_pipe`]
