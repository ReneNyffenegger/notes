$ Oracle: PL/SQL package TIM: function S_AGO

The function `→ development/databases/Oracle/PL-SQL/libs/personal/tim[tim].s_ago(dt)` returns the number of seconds that `dt` is in the past in relation to `sysdate`.

sa:

  The function `s_ago` is for example used in the → development/databases/Oracle/PL-SQL/libs/personal/log#ora-pkg-log-log_v[`log_v` view] of the
  → development/databases/Oracle/PL-SQL/libs/personal/log[`log` package].

  → development/databases/Oracle/PL-SQL/libs/personal/tim[The `TIM` package].
