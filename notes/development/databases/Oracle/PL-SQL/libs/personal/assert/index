$ Oracle: PL/SQL package ASSERT

table { ll
  `is_true(val)` ☰ Makes sure that the → development/databases/Oracle/SQL/data-types/boolean  value `val` is `true`.
  `usr('RENE')` ☰ Makes sure that `user` evaluates to `RENE`. Used in conjunction with → development/databases/Oracle/PL-SQL/authid[`AUTHID CURRENT_USER`]
table }

{ Specification

  gh|PL-SQL-pkg-assert|/spec.sql||

}
{ Body

  gh|PL-SQL-pkg-assert|/body.sql||

}
{ History

  table { ll
     V0.2 ☰ Add `is_true` (2021-07-16)
  table }

}
