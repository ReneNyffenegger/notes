$ Oracle: PL/SQL package ZIPPER to create ZIP files

`The PL/SQL package `zipper` allows to create → development/archive-files/zip[zip files].

This package depends on → development/databases/Oracle/PL-SQL/libs/personal/blob_wrapper[`blob_wrapper`].

gh|PL-SQL-pkg-zipper|/spec.plsql||

gh|PL-SQL-pkg-zipper|/body.plsql||

gh|PL-SQL-pkg-zipper|/test.plsql||

sa:
  `→ development/databases/Oracle/installed/packages/utl/compress`
