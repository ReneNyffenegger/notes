$ Oracle 19c
@ 19c

19c, is available
  • on-prem,
  • on-Cloud, or
  • in a hybrid-Cloud environment.

19c may also be deployed on Oracle Engineered Systems (e.g. Exadata) on-prem, on Oracle (public) Cloud or (private) Cloud at Customer.

{ Some (imho) interesting new features

 `json_mergepatch` applies one or more changes to multiple → development/languages/JavaScript/JSON documents with a single statement.

 `distinct` for → development/databases/Oracle/SQL/select/aggregate/listagg[`listagg`]

  SQL Quarantine

? *Hybrid partitioned tables* which extend Oracle partitioning.

? Automated installation, configuration and patching

  → development/databases/Oracle/tools/Database-Configuration-Assistant commands `createDuplicateDB`, `relocatePDB`, `createFromRemotePDB`.

  → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/rdbms/admin/catpcat_sql[`catpcat.sql`] runs → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/rdbms/admin/catalog_sql[`catalog.sql`] and
  → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/rdbms/admin/catproc_sql[`catproc.sql`] in parallel to speed up the creation of a → development/databases/Oracle/architecture/database[database].

}
