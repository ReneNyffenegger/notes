$ RMAN: target database
@ target database

The *target database* is the database to which → development/databases/Oracle/tools/RMAN is connected (`$ rman target usr@db` or `RMAN> connect target usr@db`).

→ development/Data/metadata[Metadata] about these operations is stored in the → development/databases/Oracle/tools/RMAN/repository.
