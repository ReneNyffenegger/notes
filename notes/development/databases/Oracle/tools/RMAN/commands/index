$ RMAN commands

table { ll

  `backup archivelog all`                    ☰ 
  `backup database`                          ☰ 
  `backup database plus archivelog`          ☰ 
  `backup incremental`                       ☰ performs → development/databases/Oracle/adminstration/backup-recovery/backup/incremental[incremental backups]
  `backup spfile`                            ☰
  `backup pluggable database pdbOne, pdbTwo` ☰ → development/databases/Oracle/adminstration/backup-recovery/backup the two PDBs `pdbOne` and `pdbTwo`
  `connect target`                           ☰ to make a connection to a → development/databases/Oracle/tools/RMAN/target-database[target database]
  `duplicate … from active database`         ☰ (Note: a connection to an auxiliary instance is required)
  `exit`                                     ☰ → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/bin/rman#rman-client-exit[ends an RMAN session]
  `flashback database`                       ☰ revert → development/databases/Oracle/architecture/database/files/data[data files] to a content of a specific point in time
  `recover block`                            ☰ performs → development/databases/Oracle/adminstration/backup-recovery/recovery/media/block
  `run { … }`                                ☰ run the enclosed commands sequentially

table }

→ development/databases/Oracle/adminstration/backup-recovery/backup[Backup] commands:
  • `backup`
  • `convert`

Restore commands:
  • `advise`
  • `duplicate`
  • `flashback`
  • `recover`
  • `repair`
  • `restore`
  • `switch` (file?)
  • `transport tablespace`

Maintenance commands:
  • `catalog`
  • `change`
  • `configure`
  • `crosscheck`
  • `delete`
  • `report`
  • `show`
  • `validate`

Miscellaneous commands
  • `allocate`
  • `describe`
  • `execute script`
  • `host`
  • Quoted SQL command
  • `release`
  • `resync`
  • `send`
  • `set`
  • `shutdown`
  •  SQL command
  • `startup`


sa:
  → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/bin/rman#rman-client-start[Starting an RMAN session]
