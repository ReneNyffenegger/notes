$ RMAN command: allocate
@ allocate

`allocate` allocates a → development/databases/Oracle/tools/RMAN/channel.

`allocate` has the same options as → development/databases/Oracle/tools/RMAN/commands/configure#rman-configure-channel[`configure channel`].

sa:
  → development/databases/Oracle/tools/RMAN/commands
