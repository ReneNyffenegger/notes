$GOracle: SQL Loader Example: skip CSV columns

This example loads a → development/Data/exchange/formats/CSV[CSV] file five columns (or values per record). However, the destination
table only contains three columns. This is possible by using the `filler` keyword for the values that should not be loaded.

  gh|Oracle-Patterns|/SQLLoader/ex_12_skip-columns/create_table.sql||

  gh|Oracle-Patterns|/SQLLoader/ex_12_skip-columns/load.ctl||

  gh|Oracle-Patterns|/SQLLoader/ex_12_skip-columns/data.csv||

  gh|Oracle-Patterns|/SQLLoader/ex_12_skip-columns/all.sql||

sa:

  → development/databases/Oracle/tools/sql-loader/examples[Other SQL Loader examples]

  → development/databases/Oracle/tools/sql-loader
