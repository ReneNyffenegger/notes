$ SQL*Loader: FOR INSERT option, table must be empty.  Error on table …

The → development/databases/Oracle/tools/sql-loader[SQL*Loader's] `insert` option expects the target table. If there is already data in the target table, this error is thrown.

Either truncate/delete the table before running SQL*Loader or use the `append` or `replace` option instead of the `insert` option.
