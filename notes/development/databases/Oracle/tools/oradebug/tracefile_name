$ oradebug tracefile_name

`oradebug tracefile_name` prints the name of the → development/databases/Oracle/architecture/database/system-files/Automatic-Diagnostic-Repository/trace-file[trace files] that is written to.

code {
oradebug → development/databases/Oracle/tools/oradebug/setmypid[setmypid]
oradebug tracefile_name
code }

sa:

  → development/databases/Oracle/SQL-Plus/sqlpath/tracefile[`tracefile.sql`] is a SQL script which displays the current name of the tracfile. 

  → development/databases/Oracle/tools/oradebug/unlimit[`oradebug unlimit`] removes a limit of the maximum tracefile size.

  → development/databases/Oracle/tools/oradebug
