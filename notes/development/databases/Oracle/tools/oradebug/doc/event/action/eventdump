$ oradebug action: eventdump
@ eventdump

code {
SQL> → development/databases/Oracle/tools/oradebug/doc/event/action[oradebug doc event action] eventdump

eventdump
        - list events that are set in the group
Usage
-------
eventdump( group           < system | process | session >)
code }
