$ oradebug action: varaddr
@ varaddr

`varaddr` returns the address of a fixed PGA, SGA or UGA variables.

sa:
  → development/databases/Oracle/architecture/instance/fixed-tables/k/s/m/fs/v[`x$ksmfsv`].
