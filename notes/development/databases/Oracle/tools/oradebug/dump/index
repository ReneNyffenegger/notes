$ oradebug dump

code {
→ development/databases/Oracle/tools/oradebug[oradebug] dump DUMP_NAME lvl [addr] [arguments, …]
code }

`oradebug dump` dumps information pertaining to `DUMP_NAME`. A list of available *dump names* can be found with
→ development/databases/Oracle/tools/oradebug/dumplist[`oradebug dumplist`].

{ List of parameters

  A list of parameters for dump names and their meanings can be optained with:
code {
oradebug → development/databases/Oracle/tools/oradebug/doc/event/action[doc event action]
code }

  For a specific dump name like so:
code {
oradebug doc event action → development/databases/Oracle/tools/oradebug/dump/buffers[buffers]
code }

}

{ TODO

  → development/databases/Oracle/tools/oradebug/dump/systemstate[`oradebug dump systemstate`]

}
