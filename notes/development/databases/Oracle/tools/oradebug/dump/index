$ oradebug dump

code {
oradebug dump DUMP_NAME lvl [addr] [arguments, …]
code }

`oradebug dump` dumps information pertaining to `DUMP_NAME`. A list of available *dump names* can be found with
→ development/databases/Oracle/tools/oradebug/dumplist[`oradebug dumplist`].
