$ oradebug eventdump

`→ development/databases/Oracle/tools/oradebug eventdump` lists the events that are set in the specified group (session, process or system).

`oradebug eventdump` is not reported with → development/databases/Oracle/tools/oradebug/help[`oradebug help`].

code {
oradebug eventdump session
oradebug eventdump process
oradebug eventdump system
code }

Compare with

code {
alter session set events 'immediate eventdump(session )';
alter session set events 'immediate eventdump(proceess)';
alter session set events 'immediate eventdump(system  )';
code }


sa:
  → development/databases/Oracle/tools/oradebug/doc/event/action/eventdump[`oradebug doc event action eventdump`]

  MOS Note 436036.1
