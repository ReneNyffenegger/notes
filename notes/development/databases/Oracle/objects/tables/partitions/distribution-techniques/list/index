$ Oracle: list partitioned tables

gh|Oracle-Patterns|/DatabaseObjects/Tables/Partitions/distribution-techniques/list/create-table.sql||

{ Execution plan: Accessing a single partition #ora-list-part-single

  If the criteria of a select stement permit it, the optimizer is able to figure out that only one partition needs to be selected from. The
  The corresponding → development/databases/Oracle/SQL/statement/execution/plan/operations[execution plan operation] is `PARTITION LIST` with the option `SINGLE`:
  gh|Oracle-Patterns|/DatabaseObjects/Tables/Partitions/distribution-techniques/list/explain-plan.sql||

}

sa:
  → development/databases/Oracle/objects/tables/partitions[Partitioned tables]
