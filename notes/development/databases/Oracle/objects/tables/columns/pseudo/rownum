$ Oracle pseudo column ROWNUM
@ rownum

`rownum` is a → development/databases/Oracle/objects/tables/columns/pseudo that assigns a row number, starting with 1 and increasing by 1, to each record returned from a
→ development/databases/Oracle/SQL/select[`select` statement].

sa:

 Compare with
   • the `row_number()` SQL function and
   • the *row limiting clause* of `select` statements.

 The → development/databases/Oracle/SQL/statement/execution/plan/operations/count[plan operation `COUNT`].

`rownum` is frequenty used to limit the number or returned records, see → development/databases/Oracle/SQL/_examples/generate-rows#ora-gen-data-rownum[generate date with SQL].
