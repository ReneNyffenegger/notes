$ Oracle: global temporary table (GGT)

{ rowid #ora-ggt-rowid

  An experiment with → development/databases/Oracle/SQL/data-types/rowid[rowids] of GGTs.

  gh|Oracle-Patterns|/DatabaseObjects/Tables/GlobalTables/rowid.sql||

}

sa:
  The → development/databases/Oracle/SQL/hints[`/*+ materialize */` SQL hint] forces the data that is returned by a
  → development/databases/Oracle/SQL/clauses/with[`with` clause] to be stored in a global temporary table.
