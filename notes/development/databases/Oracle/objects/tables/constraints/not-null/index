$ Oracle: NOT NULL constraint
@ not null

The not `→ development/databases/Oracle/SQL/null` → development/databases/Oracle/objects/tables/constraints prevents a column value from being `→ development/databases/Oracle/SQL/null`. Thus, the `not null` constraint is a special
→ development/databases/Oracle/objects/tables/constraints/check[check constraint].

Columns that are part of a → development/databases/Oracle/objects/tables/constraints/primary-key or → development/databases/Oracle/objects/tables/columns/identity[identity columns] are always `not null`.

sa:

  The → development/databases/Oracle/errors[error messages]:
    • → development/databases/Oracle/errors/ORA-01400_cannot-insert-null-into
    • → development/databases/Oracle/errors/ORA-01407_cannot-update_to-NULL
    • → development/databases/Oracle/errors/ORA-01442_column-to-be-modified-to-NOT-NULL-is-already-NOT-NULL
