$ Oracle: SQL Translation Profile

An *SQL Translation Profile* is a → development/databases/Oracle/objects[Oracle object].

SQL Translation Profiles are editionable.

{ TODO

code {
select
   name,
   sql_translation_profile
from
   → development/databases/Oracle/installed/dynamic-performance-views/services;
code }

}

sa:

 `→ development/databases/Oracle/installed/packages/dbms/sql_translator`

  → development/databases/Oracle/objects[Database objects]
