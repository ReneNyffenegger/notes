$ ORA-00990: missing or invalid privilege

The error message *ORA-00990: missing or invalid privilege* is thrown if a user tries to grant an inexisting → development/databases/Oracle/security/privileges[privilege] such as
→ development/databases/Oracle/objects/directories#ora-00990-directory[`grant create directory to …`].

sa:
  → development/databases/Oracle/errors[Other Oracle error messages]
