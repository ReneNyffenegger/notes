$ ORA-00700: soft internal error, arguments …

The ORA-00700 error message is raised if a process encounters an unexpected condition which does not negatively impact the process from continuing doing its task.
-
Because expected conditions are *asserted*, this error is also referred to as *soft asserts*.
-
In contrast,  a condition that causes the process to terminate throws the error message  → development/databases/Oracle/errors/ORA-00600_internal-error-code.

sa:


  → development/databases/Oracle/errors/ORA-00942_table-or-view-does-not-exist#ora-942-tracefile[Using a trace file to reveal the name of a missing table] when hitting a
  → development/databases/Oracle/errors/ORA-00942_table-or-view-does-not-exist[ORA-00942 table or view does not exist] error.

 `→ development/databases/Oracle/tools/oradebug/unit_test dbke_test dde_flow_kge_soft …`
  
  → development/databases/Oracle/errors[Other Oracle error messages]

  MOS Note 737878
