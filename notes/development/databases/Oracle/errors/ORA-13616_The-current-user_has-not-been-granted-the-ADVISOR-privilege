$ ORA-13616: The current user  has not been granted the ADVISOR privilege.

The `advisor` privilege is required for
  • `dbms_advisor`
  • the tuning interface (`*_tuning_task`) of `dbms_sqltune`
  • the optimizer statistics subprograms of `dbms_stats`


sa:
  → development/databases/Oracle/errors[Other Oracle error messages]
