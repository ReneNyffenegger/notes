$ ORA-28367: wallet does not exist

? Either a wallet is not created or its location is not recorded in the `sqlnet.ora` file (parameters `wallet_location` or `encryption_wallet_location`).

sa:
  → development/databases/Oracle/errors[Other Oracle error messages]
