$ ORA-01740: missing double quote in identifier

code {
select
   42   "the number
from
   → development/databases/Oracle/SQL/dual;
code }

sa:
  → development/databases/Oracle/errors[Other Oracle error messages] such as → development/databases/Oracle/errors/ORA-00972_identifier-is-too-long
