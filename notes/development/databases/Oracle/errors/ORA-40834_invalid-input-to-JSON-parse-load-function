$ ORA-40834: invalid input to JSON parse/load function

{ JSON object types

  → development/databases/Oracle/installed/types/json[JSON object types] don't accept `development/databases/Oracle/SQL/null` when they're initialized:
code {
select
   json_array_t(cast null as number)
from
   → development/databases/Oracle/SQL/dual;
code }

}

sa:

  → development/databases/Oracle/errors[Other Oracle error messages]
