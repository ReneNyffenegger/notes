$ ORA-40238: invalid linear algebra shared library 

{ ORA-40238: invalid linear algebra shared library libora_netlib.so #ora-40238-libora_netlib

  As per MOS note 2509842.1, this error indicates that the *Fortran Runtime* `libora_netlib.so` was not installed in the OS.

}

sa:

  The init parameter `→ development/databases/Oracle/adminstration/init-parameters/dmm/blas_library`

 *ORA-15180: could not open dynamic library …, error …*

  Oracle Data Mining (ODM)

  → development/databases/Oracle/errors[Other Oracle error messages]
