$ Oracle: Init parameter MAX_IDLE_BLOCKER_TIME
@ max_idle_blocker_time

The value of `max_idle_blocker_time` specifies the number of minutes that a blocking → development/databases/Oracle/architecture/session can remain idle until it is → development/databases/Oracle/architecture/session#ora-session-terminate[terminated].

sa:

 `max_idle_time`

  → development/databases/Oracle/adminstration/init-parameters
