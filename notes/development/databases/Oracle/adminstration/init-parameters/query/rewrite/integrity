$ Oracle: Init parameter QUERY_REWRITE_INTEGRITY
@ query_rewrite_integrity

The value of `query_rewrite_integrity` can be set to one of the following three values:
table { ll
  `enforced` ☰ This is the default.
  `trusted` ☰ Relationsships that are declared but not enforced are taken into consideration when trying to rewrite a query.
  `stale_tolerated` ☰
table }

sa:
  → development/databases/Oracle/adminstration/init-parameters[Other init parameters], such as `→ development/databases/Oracle/adminstration/init-parameters/query/rewrite/enabled`
