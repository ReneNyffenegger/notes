$ Oracle init parameters
@ init parameters

Init parameters are *configuration parameterss* that control the operation of an → development/databases/Oracle/architecture/instance.

The values for the init parameters are stored in a text-based initialization parameter file or in the → development/databases/Oracle/architecture/database/files/server-parameter-file.

The actual values of the initialization parameters can be queried from → development/databases/Oracle/installed/dynamic-performance-views/parameter/index[`v$database`].

→ development/databases/Oracle/adminstration/init-parameters/log_archive_dest_n

{ control_files #control_files

  Specifies the location of the → development/databases/Oracle/architecture/database/files/control[control files].

}
{ db_create_file_dest

  Oracle recommends to set the value of `db_create_file_dest` to a different value than that of → #db_recovery_file_dest[`db_recovery_file_dest`].

}
{ db_filename_convert #db_file_name_convert

  Specifies how file names are converted from a → development/databases/Oracle/High-Availability/Data-Guard[data guard's] → development/databases/Oracle/High-Availability/Data-Guard#dg-primary-db[primary database]
  to a → development/databases/Oracle/High-Availability/Data-Guard#dg-standby-db[standby database].

  Compare with → #log_file_name_convert

}
{ db_flashback_retention_target #db_flashback_retention_target

  See → development/databases/Oracle/adminstration/backup-recovery/recovery/fast-recovery-area

}
{ db_name #db_name

  Compare with `db_unique_name`

}
{ db_recovery_file_dest #db_recovery_file_dest

  Defines the location of the → development/databases/Oracle/adminstration/backup-recovery/recovery/fast-recovery-area
  (→ development/databases/Oracle/architecture/database/files/Automatic-Storage-Management, → development/databases/Oracle/architecture/database/files/managed[OMF], file system)

  Oracle recommends to set the value of `db_recovery_file_dest` to a different value than that of `db_create_file_dest`.

}
{ db_recovery_file_dest_size #db_recovery_file_dest_size

  Sets the → development/databases/Oracle/adminstration/backup-recovery/recovery/fast-recovery-area/disk-quota which limits the maximum size of the
  → development/databases/Oracle/adminstration/backup-recovery/recovery/fast-recovery-area.
}
{ db_unique_name #db_unique_name

  Compare with `db_name`
 
}
{ db_writer_processes #db_writer_processes

  `db_writer_processes` specifies the number of → development/databases/Oracle/architecture/processes/background/DBWn[Database Writer processes]. The possible range is 1…100.

}
{ fal_client #fal_client
}
{ fal_server #fal_server

  Defines the *oracle net service name* of the *FAL* server.

  In a → development/databases/Oracle/High-Availability/Data-Guard[data guard] environment, the FAL server is typically the → development/databases/Oracle/High-Availability/Data-Guard#dg-primary-db[primary database].
  
}
{ inmemory_size #inmemory_size

  See → development/databases/Oracle/architecture/instance/SGA/In-Memory-Area

}
{ job_queue_processes #job_queue_processes

  `job_queue_processes` controls the maximum number of processes that will be created for job execution, see → development/databases/Oracle/architecture/processes/background/CJQ0.

}
{ log_archive_config #log_archive_config
}
{ log_archive_dest_N #log_archive_dest_n

  → development/databases/Oracle/adminstration/init-parameters/log_archive_dest_n

}
{ log_archive_dest_state_n

  → development/databases/Oracle/adminstration/init-parameters/log_archive_dest_state_n

}
{ log_archive_trace #log_archive_trace

  Enable/disable tracing → development/databases/Oracle/adminstration/redo-log-files[redo] transport progress.

}
{ log_file_name_convert #log_file_name_convert

  Compare with → #db_file_name_convert

}
{ memory_target #memory_target

  See → development/databases/Oracle/adminstration/memory-management

}
{ memory_max_target #memory_max_target

  See → development/databases/Oracle/adminstration/memory-management

}
{ memoptimize_pool_size #memoptimize_pool_size

  See → development/databases/Oracle/architecture/instance/SGA/memoptimize-pool

}
{ pga_aggregate_limit #pga_aggregate_limit

 `pga_aggregate_target` specifies a *hard limit* for development/databases/Oracle/architecture/instance/PGA[PGA] memory.

  If not set, `pga_aggregate_target` defaults to the largets value of the following three:
    • 2 MB
    • 2 * `pga_aggregate_target`
    • 3 * `processes`

  See → development/databases/Oracle/architecture/processes/background/CKPT[CKPT] and → development/databases/Oracle/adminstration/memory-management

}
{ pga_aggregate_target #pga_aggregate_target

  See → development/databases/Oracle/adminstration/memory-management

}
{ remote_login_passwordfile #remote_login_passwordfile
}
{ standby_file_management #standby_file_management
}

sa:
  → development/databases/Oracle/adminstration

