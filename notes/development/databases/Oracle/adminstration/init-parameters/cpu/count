$ Oracle: Init parameter CPU_COUNT
@ cpu_count

`cpu_count` specifies an → development/databases/Oracle/architecture/instance[instance']s number of CPUs.

On *Oracle Database Free*, the value of `cpu_count` cannot be higher than 2 (see also error message
→ development/databases/Oracle/errors/ORA-12797_The_parameter-cannot-be-greater-than_in-Oracle-Database-Free-version[ORA-12797: The 'cpu_count' parameter cannot be greater than '2' in Oracle Database Free version]).

sa:

 `→ development/databases/Oracle/installed/dynamic-performance-views/osstat`

  Other → development/databases/Oracle/adminstration/init-parameters such as `→ development/databases/Oracle/adminstration/init-parameters/parallel/threads_per_cpu`
