$ Oracle: init parameter PLSQL_OPTIMIZE_LEVEL

The value of the → development/databases/Oracle/adminstration/init-parameters[init paramter] `plsql_optimize_level` constrols how much effort the
→ development/databases/Oracle/PL-SQL[PL/SQL] compiler invests into optimizing PL/SQL code. This level can be set to a value between 1 and 3. The default is 2.

{ Inlining code #plsql_optimize_level-inline

  Setting `plsql_optimize_level` to 3 causes the compiler to seek oportunities to → development/databases/Oracle/PL-SQL/inline[inline] code.

  Code can be forced to be inlined with the → development/databases/Oracle/PL-SQL/pragma/inline[`inline` pragma].

}

sa:

  `ALL_PLSQL_OBJECT_SETTINGS`

  → development/databases/Oracle/adminstration/init-parameters
