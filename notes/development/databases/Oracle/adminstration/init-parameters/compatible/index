$ Oracle: Init parameter COMPATIBLE
@ compatible

The value of `compatible` consists of at least three decimal numbers, separated by periods (and Oracle recommends to only use three decimal numbers).

The value of `compatible` cannot be changed to a lower value.

The minimum possible value for `compatible` on → development/databases/Oracle/Releases-and-versions/21c is `12.2.0`.

sa:
  → development/databases/Oracle/adminstration/init-parameters
