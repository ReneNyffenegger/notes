$ Oracle: Init parameter _OPTIM_PEEK_USER_BINDS
@ _optim_peek_user_binds

? Setting the value of `_optim_peek_user_binds` to `false` turns off *bind variable peeking*.

sa:

  → development/databases/Oracle/adminstration/init-parameters
