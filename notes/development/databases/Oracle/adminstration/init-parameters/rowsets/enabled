$ Oracle: Init parameter _ROWSETS_ENABLED
@ _rowsets_enabled

? Setting the value of `_rowsets_enabled` to `false` forces a
→ development/databases/Oracle/SQL/statement/execution/plan/operations[row source (execution plan operator)] to pass *one* row at a time to its parent. With the default value (`true`), some rowsources may pass multiple rows to the parent.

sa:
  → development/databases/Oracle/adminstration/init-parameters
