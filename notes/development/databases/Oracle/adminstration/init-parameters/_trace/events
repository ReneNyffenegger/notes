$ Oracle: init parameter _TRACE_EVENTS
@ _trace_events

`_trace_events` has three components separated by a colon:
   • event number (ranges can be specified with hyphen),
   • level (0 … 255) and
   • Oracle PID (`ALL` for all processes)

code {
alter system  set "_trace_events"='10000-10999:1:ALL';
code }

Turn off:
code {
alter system  set "_trace_events"=''';
code }

sa:

  The paramter `_oracle_trace_events`.

  → development/databases/Oracle/adminstration
