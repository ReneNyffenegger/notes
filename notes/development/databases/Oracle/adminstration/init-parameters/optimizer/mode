$ Oracle: Init parameter OPTIMIZER_MODE
@ optimizer_mode

The value of `optimizer_mode` can be set to
  • `first_rows` or `first_rows_n`, where `n` is `1`, `10`, `100` or `1000`
  • `all_rows` (the default)

sa:

  The `→ development/databases/Oracle/SQL/hints/list/first_rows` hint.

  → development/databases/Oracle/adminstration/init-parameters
