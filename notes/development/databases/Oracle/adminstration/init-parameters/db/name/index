$ Oracle: Init parameter DB_NAME
@ db_name

`db_name` is the only(?) → development/databases/Oracle/adminstration/init-parameters[init parameter] without a default value. When creating a database, the
→ development/databases/Oracle/architecture/database#ora-db-name[name of the database] must match that of this parameter.

The value of `db_name` must not exceed 8 characters and must start with an alphabetic character.

When the database is created, the database name is recorded in the data files, redo log files and control files of the database.

sa:

  Other → development/databases/Oracle/adminstration/init-parameters (such as `→ development/databases/Oracle/adminstration/init-parameters/db/unique_name`)
