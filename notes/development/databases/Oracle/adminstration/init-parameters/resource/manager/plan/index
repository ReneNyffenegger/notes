$ Oracle: Init parameter RESOURCE_MANAGER_PLAN
@ resource_manager_plan

The value of `resource_manager_plan` specifies the top0-level *resource plan* for an instance in a non-CDB or a CDB resource plan in a CDB.

sa:

  → development/databases/Oracle/Resource-Manager

 `→ development/databases/Oracle/installed/packages/dbms/resource_manager[dbms_resource_manager].create_plan`, `.create_plan_directive`, `.create_cdb_plan`, `.create_cdb_plan_directive` etc.

  → development/databases/Oracle/adminstration/init-parameters
