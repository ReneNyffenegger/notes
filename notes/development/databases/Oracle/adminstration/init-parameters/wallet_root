$ Oracle: Init parameters WALLET_ROOT
@ wallet_root

`wallet_root` overwrites the `sqlnet.ora` parameter `encryption_wallet_location` (MOS note 2882397.1).

sa:

  → development/databases/Oracle/adminstration/init-parameters[Init parameters] (such as `_allow_system_wallet`, `_backup_max_wallet_session`, `_gws_catalog_wallet`, `_implicit_ssl_wallet` and `ssl_wallet`).
