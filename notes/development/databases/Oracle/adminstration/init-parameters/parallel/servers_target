$ Oracle: init parameter PARALLEL_SERVERS_TARGET
@ parallel_servers_target

The value of `parallel_servers_target` indicates the threshold of the number of parallel server processes before an SQL statement is → development/databases/Oracle/SQL/statement/execution/parallel/queuing[queued for parallel processing].
-
Thus, this value is *not* a hard limit for the number of parallel server processes.

sa:

  MOS Note 2608210.1

  → development/databases/Oracle/adminstration/init-parameters
