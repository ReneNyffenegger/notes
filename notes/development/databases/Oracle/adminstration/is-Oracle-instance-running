$ Oracle: checking if an instance is already running
@ check if an instance is already running

code {
if [ "$(→ Linux/shell/commands/ps -ef | → Linux/shell/commands/grep ora_pmon | → Linux/shell/commands/wc -l)" -gt "1" ]; then
  echo "An Oracle instance is already running"
else
  echo "No running Oracle instance found"
fi
code }

sa:
  → development/databases/Oracle/adminstration/

