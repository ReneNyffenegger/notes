$ Oracle: media recovery
@ media recovery

{ Data Guard

  ? In a → development/databases/Oracle/High-Availability/Data-Guard environment,
  a *physica* standby database receives → development/databases/Oracle/architecture/database/files/archived-redo-log[archived redo log files] from the
  primary database. The standby database uses media recovery to apply the received redo and thus is being synchronized with the primary database.

}

sa:
  → development/databases/Oracle/adminstration/backup-recovery/recovery
