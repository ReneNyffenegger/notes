$ Oracle: Default roles of a user


{ Querying a user's default roles

code {
select
   rpr.default_role,
   rpr.granted_role   
from
   sys.→ development/databases/Oracle/installed/data-dictionary/role/privs rpr
where
   rpr.grantee = 'RENE'
order by
   rpr.default_role,
   rpr.granted_role;
code }

}
