$ ASM file templates

An *ASM file template* is a collection of atttribute values. Such a template can be assigned to a → development/databases/Oracle/architecture/database/files/Automatic-Storage-Management/file[ASM file]
when it is created. The template defines characteristics such as
  • disk regions
  • file mirroring
  • striping attributes.

A default file template is provided for every file type.
