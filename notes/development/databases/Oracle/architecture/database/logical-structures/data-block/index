$ Oracle logical database structure: data block
@ data block

{ Block corruption

  A data block that is not in a recognized Oracle format or that is internally incosistent is *currupt*.

  If only a few data blocks are found to be corrupt, it might be advisable to repair them with *block recovery* rather than *data file recovery*.

}

sa:
  → development/databases/Oracle/architecture/database/logical-structures[Logical structures]

  → development/databases/Oracle/adminstration/backup-recovery/backup/incremental
