$ Oracle Job Queue Coordinator (CJQ0) background process 

The *Job Queue Coordinator process* checks the → development/databases/Oracle/installed/data-dictionary for jobs to be scheduled. When necessary, CJQ0 creates a
→ development/databases/Oracle/architecture/processes/background/Jnnn[*Job Queue Slave Process*] (Jnnn) which execute the job.

The → development/databases/Oracle/adminstration/init-parameters#job_queue_processes[`job_queue_processes` init parameter] controls the maximum number of processes that will be created for job execution.

CJQ0 itself is started and stopped by *Oracle Scheduler*.

CJQ0 is a → development/databases/Oracle/architecture/processes/background[background process].
