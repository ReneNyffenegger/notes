$ Oracle Revoverer (RECO) background process

The *Recoverer Process* finds pending distributed transactions and resolves them. Such transactions are pending because of network or system failure.

RECO is a → development/databases/Oracle/architecture/processes/background[background process]
