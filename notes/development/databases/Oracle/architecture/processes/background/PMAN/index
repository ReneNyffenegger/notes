$ Oracle Process manager (PMAN) background process

PMAN starts, monitors and stops the following → development/databases/Oracle/architecture/processes[processes] as needed
  • Dispatcher and shared server processes
  • Connection broker and pooled server processes for database resident connection pools
  • Job queue processes
  • Restartable background processes

PMAN is not only used in → development/databases/Oracle/architecture/instance[instances] but also in ASM and ASM Proxy instances.

PMAN is a → development/databases/Oracle/architecture/processes/background[background process].
