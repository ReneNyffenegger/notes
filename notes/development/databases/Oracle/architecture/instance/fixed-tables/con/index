$ Oracle: X$CON
@ x$con

rem {
  `state`  = 0 if PDB is mounted?
}

{ Misc

 `x$pdb` was renamed to `x$con` (See `catom.pm`).

}

sa:
  → development/databases/Oracle/architecture/instance/fixed-tables[fixed tables]
