$ Oracle: X$KSMSD
@ x$ksmsd

`x$ksmsd` lists the sizes of the four main components of the SGA.

`→ development/databases/Oracle/installed/dynamic-performance-views/sga` (indirectly, via `gv$sga`) selects from `x$ksmsd`.

sa:
  → development/databases/Oracle/architecture/instance/fixed-tables[fixed tables]
