$ Oracle: View merging

rem {

  In view merging, the optimizer merges the query block representing a view into the query block that contains it.

  The optimizer represents each nested subquery or unmerged view by a separate query block.

rem }

? View merging makes → development/databases/Oracle/SQL/statement/query-block[query blocks] disappear.

{ TODO
 
  The privileges
    • `merge any view`
    • `merge view`

  The init parameter `optimizer_secure_view_merging`
 
}

sa:
  → development/databases/Oracle/optimizer/features/view-merging/simple[Simple] and
  → development/databases/Oracle/optimizer/features/view-merging/complex[complex] view merging.
