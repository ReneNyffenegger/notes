$ SQLPATH - kill
@ kill.sql

`kill.sql` terminates a session that is identified by its `sid`, without to also specify the session's `serial#`.

gh|Oracle-SQLPATH|/kill.sql||

sa:

  A list of sessions is returned by → development/databases/Oracle/SQL-Plus/sqlpath/ses[`ses.sql`].

  → development/databases/Oracle/SQL-Plus/sqlpath
