$ SQLPATH - ver
@ ver.sql

`ver.sql` prints the version (`banner_full`) of the Oracle the session is connected to.

gh|Oracle-SQLPATH|/ver.sql||

sa:
  → development/databases/Oracle/SQL-Plus/sqlpath
