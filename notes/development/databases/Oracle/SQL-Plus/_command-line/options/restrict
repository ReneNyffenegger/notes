$ SQL*Plus: command line option RESTRICT

When → development/databases/Oracle/SQL-Plus#sql-plus-start-options[starting SQL*Plus], the command line option `-r` or `-restrict` can be specified.
-
This option accepts an integer value between 1 and 3. Depending on the value, SQL*Plus disables commands that interact with the file system (such as for example the `→ development/databases/Oracle/SQL-Plus/host` command).

sa:
  The SQL*Plus *product user profile (PUP) tables* (see → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/sqlplus/admin/pupbld_sql[`$ORACLE_HOME/sqlplus/admin/pupbld.sql`]).
