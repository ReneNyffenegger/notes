$ SQL*Plus: SET STATEMENTCACHE
@ set statementcache

sa:

  The default value of `statementchache` is changed to 20 when using SQL*Plus's → development/databases/Oracle/SQL-Plus/_command-line/options/fast[command line option `-fast`].

  → development/databases/Oracle/SQL-Plus/set[`set`]

  → development/databases/Oracle/SQL-Plus
