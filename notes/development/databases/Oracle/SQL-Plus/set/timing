$ SQL*Plus: SET TIMING
@ set timing

With `timing on`, → development/databases/Oracle/SQL-Plus prints the elapsed time for each statement it encounters.

code {
set timinig on

create table abc …

insert into abc select …

update abc …

set timing off
code }

sa:

  → development/databases/Oracle/SQL-Plus/timing[`timing start` … `timing end`]

  → development/databases/Oracle/SQL-Plus/set/autotrace[`set autotrace …`]

  → development/databases/Oracle/SQL-Plus
