$ SQL*Plus: EXECUTE
@ execute

`execute` executes a *single* → development/databases/Oracle/PL-SQL statement by embedding it into a → development/databases/Oracle/PL-SQL/block/[`begin … end;`] block.

Using `execute` does not change SQL*Plus' buffer.

sa:

  The *slash* «command».

  → development/databases/Oracle/SQL-Plus
