$ SQL*Plus: EXIT
@ exit

`exit` terminates an → development/databases/Oracle/SQL-Plus[SQL*Plus] session.

Using → development/databases/Oracle/SQL-Plus/set/exitcommit[`set exitcommit`] to `on` or `off` specifies if the current → development/databases/Oracle/architecture/transaction will be
→ development/databases/Oracle/SQL/TCL/commit[commited] or rolled back.

sa:
  `→ development/databases/Oracle/SQL-Plus/whenever/sqlerror[whenever sqlerror] exit …`
