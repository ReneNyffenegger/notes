$ Oracle: V$DISPATCHER
@ v$dispatcher

code {
select
   dis.accept,
   dis.breaks,
   dis.busy,
   dis.bytes,
   dis.conf_indx,
   dis.cpu,
   dis.created,
   dis.idle,
   dis.listener,
   dis.messages,
   dis.name,
   dis.network,
   dis.owned,
-- dis.paddr,
   dis.status,
   que.paddr,
   que.queued,
   que.totalq,
   que.type,
   que.wait,
   que.con_id
-- dis.con_id,
from
   v$dispatcher dis                             left join
   v$queue      que on que.paddr = dis.paddr    
;
code }

sa:
  → development/databases/Oracle/installed/dynamic-performance-views[Oracle Dynamic Performance Views] such as `→ development/databases/Oracle/installed/dynamic-performance-views/queue`.
