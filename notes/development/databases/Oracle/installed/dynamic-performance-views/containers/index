$ Oracle: V$CONTAINERS
@ v$containers

{ Select a PDB's containers

  The following query lists all containers in a CDB, including the seed and the CDB root:
code {
select
   name,
   con_id,
   dbid,
   con_uid,
   guid
from
   v$containers
order by
   con_id;
code }

}

sa:
 
 `→ development/databases/Oracle/installed/dynamic-performance-views/database`,
 `→ development/databases/Oracle/installed/dynamic-performance-views/pdbs`,
 -
 → development/databases/Oracle/installed/dynamic-performance-views/containers/columns[Columns in `v$containers` and `v$pdb`]

  → development/databases/Oracle/architecture/multitenant

  → development/databases/Oracle/installed/dynamic-performance-views
