$ Oracle: V$QUEUE
@ v$queue

code {
select
   que.paddr,
   que.queued,
   que.totalq,
   que.type,
   que.wait,
   que.con_id
from
   v$queue      que                           left join
   v$dispatcher dis on que.paddr = dis.paddr;
code }

sa:
  → development/databases/Oracle/installed/dynamic-performance-views[Oracle Dynamic Performance Views], such as `→ development/databases/Oracle/installed/dynamic-performance-views/dispatcher`.
