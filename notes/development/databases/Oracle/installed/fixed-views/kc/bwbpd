$ Oracle fixed view: x$kcbwbpd
@ x$kcbwbpd

Buffer pool descriptor, the base table for → development/databases/Oracle/installed/dynamic-performance-views/buffer_pool

It shows how the buffer cache is split between the default, the recycle and the keep buffer pool. 

sa:
  → development/databases/Oracle/installed/fixed-views
