$ Oracle: DBMS_RLS
@ dbms_rls

`dbms_rls` is used to create *Virtual Private Database* (VPD) policies.

{ Procedures

  table { ll
   `add_grouped_policy` ☰
   `add_policy` ☰
   `add_policy_context` ☰
   `alter_grouped_policy` ☰
   `alter_policy` ☰
   `create_policy_group` ☰
   `delete_policy_group` ☰
   `disable_grouped_policy` ☰
   `drop_grouped_policy` ☰
   `drop_policy` ☰
   `drop_policy_context` ☰
   `enable_grouped_policy` ☰
   `enable_policy` ☰
   `refresh_grouped_policy` ☰
   `refresh_policy` ☰
  table }

}

sa:

  `→ development/databases/Oracle/installed/packages/dbms/fga`

  → development/databases/Oracle/installed/packages/dbms
