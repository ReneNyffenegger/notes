$ Oracle: DBMS_XMLGEN
@ dbms_xmlgen

Convert → development/databases/Oracle/SQL/select[SQL query] results to → development/XML

{ Procedures and functions

  table { ll
   `clearbindvalues` ☰
   `closecontext` ☰
   `convert` ☰ Encodes or decodes XML entities. Compare with → development/databases/Oracle/installed/packages/htf#htf-escape_sc[`htf.escape_sc`].
   `getnumrowsprocessed` ☰
   → development/databases/Oracle/installed/packages/dbms/xmlgen[`getxml`] ☰
   → development/databases/Oracle/installed/packages/dbms/xmlgen/api/get/xml/type[`getxmltype`] ☰
   `newcontext` ☰
   `newcontextfromhierarchy` ☰
   `removexsltparam` ☰
   `restartquery` ☰
   `setbindvalue` ☰
   `setcheckinvalidchars` ☰
   `setconvertspecialchars` ☰
   `setindentationwidth` ☰
   `setmaxrows` ☰
   `setnullhandling` ☰
   `setprettyprinting` ☰
   `setrowsettag` ☰
   `setrowtag` ☰
   `setskiprows` ☰
   `setxslt` ☰
   `setxsltparam` ☰
   `useitemtagsforcoll` ☰
   `usenullattributeindicator` ☰
  table }

}


sa:
  → development/databases/Oracle/installed/packages/dbms/xmlquery[`dbms_xmlquery`]

  → development/databases/Oracle/SQL/functions/XML[XML related SQL functions]

  → development/databases/Oracle/XML-DB
 
  → development/databases/Oracle/installed/packages/dbms
