$ DBMS_SCHEDULER.CREATE_FILE_WATCHER
@ create_file_watcher

{ Parameter STEADY_STATE_DURATION

  The value of `steady_state_duration` specifies the minimum amount of time in which the file (its modification date?) remains unchanged before the
  → development/databases/Oracle/objects/file/watchers considers the file to be new/found.

  The maximum value is one hour.

}

sa:
 `→ development/databases/Oracle/installed/packages/dbms/scheduler`
