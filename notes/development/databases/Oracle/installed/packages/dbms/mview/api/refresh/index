$ Oracle: DBMS_MVIEW.REFRESH

Because → development/databases/Oracle/installed/packages/dbms/mview[`dbms_mview`] is a → development/databases/Oracle/objects/synonyms for `dbms_snapshot`, `dbms_snapshot.refresh` is equivalent
to `dbms_snapshot.refresh`.

{ Calling refresh commits the current transaction

  The following example demonstrates that calling `dbms_mview.refresh` commits the current → development/databases/Oracle/architecture/transaction.

  gh|Oracle-Patterns|/Installed/dbms/snapshot/refresh_commit.sql||

}
