$ dbms_xplan

`dbms_xplan` provides five table functions to format the results of a previoulsy run `explain plan` SQL statement. These functions are:
  • `display`
  • `display_awr`
  • `display_cursor`
  • `display_sql_plan_baseline`
  • `display_sqlset`

sa:

  The SQL scripts → development/databases/Oracle/SQL-Plus/sqlpath/exps-expe[`exps.sql` and `expe.sql`].

  → development/databases/Oracle/installed/packages/dbms
