$ Oracle: DBMS_SESSION
@ dbms_session

{ Procedures and functions

  table { ll
   `clear_all_context` ☰ Clears → development/databases/Oracle/objects/contexts[context] information in a given namespace.
   `clear_context` ☰
   `clear_identifier` ☰
   `close_database_link` ☰ Closes a *database link*, equivalent to `alter session close database link …`.
   `current_is_role_enabled` ☰ Determines if a named role is currently enabled. Compare with `is_role_enabled`
   `free_unused_user_memory` ☰
   `get_package_memory_utilization` ☰
   `is_role_enabled` ☰ *Deprecated* in favor of `current_is_role_enabled` and `session_is_role_enabled`.
   `is_session_alive` ☰
   `list_context` ☰ Lists a session's active namespaces and contexts.
   `modify_package_state` ☰
   `reset_package` ☰
   `session_is_role_enabled` ☰ Determines if a → development/databases/Oracle/security/roles[role] is enabled in the current → development/databases/Oracle/architecture/session.
   `session_trace_disable` ☰ Resets the session-level SQL trace for the session from which it was called.
   `session_trace_enable` ☰ Enables → development/databases/Oracle/architecture/session level → development/databases/Oracle/SQL/statement/execution#ora-sql-exec-tracing[SQL statement execution tracing] for the invoking session.
   `session` ☰
   `set_context` ☰ Sets a value for an attribute in a → development/databases/Oracle/objects/contexts[context].
   `set_edition_deferred` ☰ Requests to switch to an edition.
   → development/databases/Oracle/installed/packages/dbms/session/api/set/identifier[`set_identifier`] ☰
   → development/databases/Oracle/installed/packages/dbms/session/set_nls[`set_nls`] ☰ Equivalent to `alter session set nls-parameter`.
   `set_role` ☰ Sets a → development/databases/Oracle/security/roles[role], equivalent to the `set role …` statement.
   `set_sql_trace` ☰ Turns on or off → development/databases/Oracle/SQL/statement/execution#ora-sql-exec-tracing[SQL statement execution tracing]. Equivalent to `alter session set sql_trace …`
   `sleep` ☰ Suspends a → development/databases/Oracle/architecture/session for the given amount of seconds, compare with → development/databases/Oracle/installed/packages/dbms/lock/sleep[`dbms_lock.sleep`].
   `switch_current_consumer_group` ☰
   `unique_session_id` ☰
  table }

}

sa:

  → development/databases/Oracle/architecture/session

  → development/databases/Oracle/installed/packages/dbms
