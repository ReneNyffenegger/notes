$ Oracle: DBMS_SQL.EXECUTE
@ dbms_sql.execute

`dbms_sql.execute` executes the statement that was parsed with `→ development/databases/Oracle/installed/packages/dbms/sql/parse`.

{ Return value

  For → development/databases/Oracle/SQL/DML, `dbms_sql.execute` returns the number of affected (processed) records.

  For `→ development/databases/Oracle/SQL/select` statements, the returned value is undefined

}

sa:
  `→ development/databases/Oracle/installed/packages/dbms/sql`
