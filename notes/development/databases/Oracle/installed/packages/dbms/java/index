$ dbms_java

In order to have `dbms_java`, Oracle JVM must be installed.

With `dbms_java`, it is possible to use the functionality that is defined in the `DbmsJava` Java class.

{ loadjava #dbms_java-loadjava

  See also the → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/bin/loadjava[`loadjava`] executable (which is found, for example in the
  → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/bin[`$ORACLE_HOME/bin`] directory.

}
