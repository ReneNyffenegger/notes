$ Oracle: DBMS_TF
@ dbms_tf

`dbms_tf` is used for → development/databases/Oracle/PL-SQL/function-procedure/polymorphic-table[polymorphic table functions (PTF)], hence the name (tf).

{ Procedures and functions

  table { ll

  `column_type_name` ☰
  `col_to_char` ☰
  `cstore_exists` ☰
  `cstore_get` ☰
  `get_col` ☰
  `get_env returns env_t` ☰
  `get_row_set`, `put_row_set` ☰ Typically (?) used in the implementing package's `fetch_rows` procedure.
  `get_xid` ☰ This function returns a `varchar2(1024)` and is typically used in `open` calls to allocate and initialize an execution specific state. 
  `put_col` ☰
  `row_replication` ☰
  `row_to_char` ☰
  `supported_type` ☰ Verifies if `dbms_tf` supports a given → development/databases/Oracle/SQL/data-types[data type]
  `trace` ☰
  `xstore_clear` ☰
  `xstore_exists` ☰
  `xstore_get` ☰
  `xstore_remove` ☰
  `xstore_set` ☰

  table }

}

sa:

  → development/databases/Oracle/installed/packages/dbms/tf/introduction[Introduction]

  → development/databases/Oracle/installed/packages/dbms/tf/table_t[`dbms_tf.table_t`]

  → development/databases/Oracle/installed/packages/dbms/tf/add-columns[Add columns to a result set]

  → development/databases/Oracle/installed/packages/dbms

  The package body of `dbms_tf` is found in → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/rdbms/admin/dbmstf_sql[`$ORACLE_HOME/rdbms/admin/dbmstf.sql`]
