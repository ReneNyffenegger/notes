$ Oracle: DBMS_ADVISOR.TUNE_MVIEW

`dbms_advisor.tune_mview` optimizes → development/databases/Oracle/objects/views/materialized[materialized view] creation statemens to achieve `refresh fast` and `enable query rewrite` goals
