$ Oracle: DBMS_STATS_INTERNAL
@ dbms_stats_internal

{ Query hidden parameter value

  Query the session value of a → development/databases/Oracle/adminstration/init-parameters/_hidden:

code {
select
   sys.dbms_stats_internal.get_parameter_val('_adaptive_fetch_enabled')
from
   → development/databases/Oracle/SQL/dual;
code }

  Compare with `sys.kupu$utilities_int.get_parameter_value`.

}

sa:
  Other → development/databases/Oracle/installed/packages/dbms[packages] such as `→ development/databases/Oracle/installed/packages/dbms/stats`.
