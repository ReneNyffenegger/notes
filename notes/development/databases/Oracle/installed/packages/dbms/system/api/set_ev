$ Oracle: DBMS_SYSTEM.SET_EV

This procedure allows to set a *diagnost event* in another → development/databases/Oracle/architecture/session.

In the special case of the diagnostic event 10046, `dbms_support.start_trace` can be used.

code {
dbms_support.set_ev(
  sid,
  serial#,
  event_number,
  event_level,
  event_name
);
code }

sa:

  Starting a 10046 event with → development/databases/Oracle/installed/packages/dbms/support#dbms_support-start-trace[`dbms_support`].

  → development/databases/Oracle/installed/packages/dbms
