$ Oracle DBMS_METADATA: FETCH_* procedures/functions

sa:
  `OPEN`, `CLOSE`

 `dbms_metadata.set_parameter` sets parameters for the FETCH API.

  The maximum number of objects to be retrieved with FETCH calls can be specified with `dbms_metadata.set_count`.
