$ DBMS_METADATA.SET_FILTER

Calling `→ development/databases/Oracle/installed/packages/dbms/metadata[dbms_metadata].set_filter` adds a conditions to the underlying `where` clause with which eventually the set of objects
is fetched.
-
These conditions are added with `and`.

sa:
  → development/databases/Oracle/installed/packages/dbms/metadata/filters[Filters]
