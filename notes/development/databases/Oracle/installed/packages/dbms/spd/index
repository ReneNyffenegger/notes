$ Oracle: DBMS_SPD
@ dbms_spd

Manage → development/databases/Oracle/SQL/statement/execution/plan/directive[SQL plan directives].

{ Functions and procecures

  table { ll
   `alter_sql_plan_directive` ☰
   `create_stgtab_directive` ☰
   `drop_sql_plan_directive` ☰
   `flush_sql_plan_directive` ☰ Transfer directives stored in memory to (persistent) location in → development/databases/Oracle/architecture/database/logical-structures/tablespace/SYSAUX.
   `get_prefs` ☰
   `pack_stgtab_directive` ☰
   `set_prefs` ☰
   `transfer_spd_for_dp` ☰
   `unpack_stgtab_directive` ☰
  table }

}

sa:
  → development/databases/Oracle/installed/packages/dbms
