$ DBMS_RANDOM.STRING


code {
dbms_random.string(opt char, len number).
code }

`→ development/databases/Oracle/installed/packages/dbms/random[dbms_random].string` returns a random string whose set of possible characters is specified by
`opt` and whose length is specified by the parameter `len`.

The (case insensitive) value `opt` specifies the characteristics of the string:
table { ll
  `U` ☰ Uppercase letters only
  `L` ☰ Lowercase letters only
  `A` ☰ Mixed case letters only
  `X` ☰ Uppercase letters and digits (alphanumeric)
  `P` ☰ Printable characters
table }

gh|Oracle-Patterns|/Installed/dbms/random/string.sql||

sa:
  `→ development/databases/Oracle/installed/packages/dbms/random`
