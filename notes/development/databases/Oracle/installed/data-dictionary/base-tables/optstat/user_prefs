$ Oracle: OPTSTAT_USER_PREFS$
@ optstat_user_prefs$

`optstat_user_prefs$` stores statistics preferences for tables (and other objects?).

These preferences are manipulated with the `*prefs` procedures of
`→ development/databases/Oracle/installed/packages/dbms/stats`.

