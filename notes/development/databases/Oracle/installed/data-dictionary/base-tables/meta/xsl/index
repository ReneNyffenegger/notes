$ Oracle: SYS.METAXSL$
@ metaxsl$

{ Column TRANSFORM

  The values found in the column `transform` seem to contain the available tranformations that can be peformed with
 `→ development/databases/Oracle/installed/packages/dbms/metadata`.

 `dbms_metadata` allows to configure transformations with
 → development/databases/Oracle/installed/packages/dbms/metadata/api/add_transform[`dbms_metadata.add_transform`] and
 `dbms_metadata.set_transform_param`.
  

code {
select
-- count(*),
   transform
from
   sys.metaxsl$
group by
   transform
order by
   transform;
--
-- ALTERDDL
-- ALTERXML
-- DDL
-- EDITIONING_VIEW
-- MODIFY
-- MODIFYSTREAMSXML
-- MODIFYSXML
-- PARSE
-- REDEFINITION
-- STREAMSXML
-- SXML
-- SXMLDDL
code }

}
