$ Oracle: DEFAULT_PWD$
@ default_pwd$

`default_pwd$` is used by the *Default Password Scanner tool* to
  • store the verifiers of default passwords and then
  • to generate the list of users still using default passwords (→ development/databases/Oracle/installed/data-dictionary/users/with_defpwd[`SYS.DBA_USERS_WITH_DEFPWD`])

sa:
  → development/databases/Oracle#ora-products[Oracle products] which can be queried from `default_pwd$`.
