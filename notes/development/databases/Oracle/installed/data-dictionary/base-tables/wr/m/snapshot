$ WRM$_SNAPSHOT
@ wrm$_snapshot


{ Column STATUS

  A value of `0` seems to indicate *valid* snapshots (`catawrtvw.sql`).

 `→ development/databases/Oracle/installed/data-dictionary/hist/snapshot` selects only records where `status = 0` while `→ development/databases/Oracle/installed/data-dictionary/hist/ash/snapshot` selects records where `status in (0, 1)`

}

sa:
 `wrm$_snapshot_details`
