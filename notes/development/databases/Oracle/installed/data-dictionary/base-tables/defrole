$ Oracle: DEFROLE$
@ defrole$

`sys.defrole$` stores a → development/databases/Oracle/users-roles/users[user's] default roles.
-
If no entry is found for a user, all roles assigned to that user are default roles (i.e. the efect of `alter user XYZ default roles ALL` is to clear all records for XYZ from `defrole$`.

sa:
 `→ development/databases/Oracle/installed/data-dictionary/base-tables/user`

links:

  → http://yong321.freeshell.org/oranotes/DefaultRole.txt
