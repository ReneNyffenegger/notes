$ Oracle: DBA_PROFILES

Each → development/databases/Oracle/users-roles/users[user] is assigned a → development/databases/Oracle/users-roles/profile which defines some usage limits on
database resources and some constraints related to passwords.

The following (→ development/databases/Oracle/SQL/select/pivot[pivot query]) selects the users's currently assigned profile and the values of each profile:
gh|Oracle-Patterns|/Installed/data-dictionary/profiles/pivot.sql||

sa:
  The currently available profiles and their resource limit values can be queried from → development/databases/Oracle/installed/data-dictionary/profiles[`DBA_PROFILES`].
