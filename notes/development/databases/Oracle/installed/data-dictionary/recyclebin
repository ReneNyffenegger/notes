$ Oracle: DBA_RECYCLEBIN
@ dba_recyclebin

`dba_recyclebin` shows the tables and their associated objects such as indexes, triggers, sequences (in case of identity columns) etc that are in the
→ development/databases/Oracle/installed/data-dictionary/recyclebin.

This view comes in the variants `user_recyclebin`, `dba_recyclebin` and `cdb_recyclebin`.

gh|Oracle-Patterns|/Installed/data-dictionary/recyclebin/example-01.sql||

sa:

  The → development/databases/Oracle/SQL-Plus command `→ development/databases/Oracle/SQL-Plus/show[show] recyclebin`.

  The init parameter `→ development/databases/Oracle/adminstration/init-parameters/recyclebin`.

 `recyclebin` is a → development/databases/Oracle/installed/data-dictionary/_synonyms[special synonym] for `user_recyclebin`.


  → development/databases/Oracle/installed/data-dictionary
