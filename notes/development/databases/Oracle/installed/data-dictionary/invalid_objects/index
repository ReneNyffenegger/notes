$ Oracle: DBA_INVALID_OBJECTS

`dba_invalid_objects` also comes in the variant `cdb_invalid_objects`. Interestingly, there is no `user_invalid_objects` or `all_invalid_objects`.

code {
select
   *
from
   dba_invalid_objects
where
   owner = user;
code }

sa:

  The column `status` of → development/databases/Oracle/installed/data-dictionary/objects[`dba_objects`].

  → development/databases/Oracle/installed/data-dictionary/errors[`dba_errors`]
