$ Oracle: Special synonyms for dome data dictionary views

There are some → development/databases/Oracle/objects/synonyms[synonyms] for a limited set of → development/databases/Oracle/installed/data-dictionary views:
code {
select
   synonym_name,
   table_name
from
   dba_synonyms
where
   table_owner  = 'SYS'          and
   → development/databases/Oracle/SQL/functions/regular_expressions/regexp_like[regexp_like](table_name, '^(USER_|ALL_|DBA_)') and
   table_name <> synonym_name
order by
   synonym_name;
code }
