$ Oracle: DBA_SQL_PROFILES
@ dba_sql_profiles

gh|Oracle-Patterns|/Installed/data-dictionary/sql/profiles/show.sql||

{ CATEGORY

?`category` controls the scope of the SQL Profile, i. e. which user sessions can apply the profile.

  By default, all profiles are created in the `DEFAULT` category.
  -
  So all session where the init paramter of `sqltune_category` is set to `default` can use this profile.

}

sa:

 `→ development/databases/Oracle/installed/packages/dbms/sqltune`

  → development/databases/Oracle/installed/data-dictionary
