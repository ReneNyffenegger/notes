$ Oracle: WHERE clause subqueries

The following → development/databases/Oracle/SQL/clauses/where[`where`] clause → development/databases/Oracle/SQL/select/subquery[subqueries] are possible in Oracle:
table { ll
  Single-row ☰ The subquery returns zero or one record.
  → development/databases/Oracle/SQL/clauses/where/subquery/exists_not-exists`exists`, `not exists`] ☰
  → development/databases/Oracle/SQL/clauses/where/subquery/in_not-in[`in`, `not in`] ☰
  `any`, `all` ☰ 
table }

sa:
  → development/databases/Oracle/SQL/clauses/where/subquery/execution-plan[SQL execution plans] for subqueries in `where` clauses.
