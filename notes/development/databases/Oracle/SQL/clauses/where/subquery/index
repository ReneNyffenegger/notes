$ Oracle: WHERE clause subuqueries

The following → development/databases/Oracle/SQL/clauses/where[`where`] clause → development/databases/Oracle/SQL/select/subquery[subqueries] are possible in Oracle:
table { ll
  Single-row ☰ The subquery returns zero or one record.
  `exists`, `not exists` ☰
  `in`, `not in` ☰
  `any`, `all` ☰ 
table }
