$ Oracle SQL: PURGE
@ purge

code {
purge table      T;
purge index      I;
purge tablespace TS;
code }

Purge the recycle bin of the user executing the command:
code {
purge recyclebin;
code }

With `SYSDBA` or `PURGE DBA_RECYCLEBIN` privileges, the entire recyclebin can be purged:
code {
purge dba_recyclebin;
code }

sa:
  The init parameter `→ development/databases/Oracle/adminstration/init-parameters/recyclebin`.
