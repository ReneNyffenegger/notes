$ Oracle SQL Plan operation: INSERT STATEMENT

The `INSERT STATEMENT` is always a «root» → development/databases/Oracle/SQL/statement/execution/plan/operations[plan operation] which has exactly one child
plan operation (row source). This child is one of
  • → development/databases/Oracle/SQL/statement/execution/plan/operations/load/as-select[`LOAD AS SELECT`]
  • → development/databases/Oracle/SQL/statement/execution/plan/operations/load/table-conventional[`LOAD TABLE CONVENTIONAL`]
  • → development/databases/Oracle/SQL/statement/execution/plan/operations/px/coordinator[`PX COORDINATOR`]
  • `TEMP TABLE TRANSFORMATION`

sa:

  → development/databases/Oracle/SQL/statement/execution/plan/operations[Plan operations]
