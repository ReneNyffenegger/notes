$ Oracle SQL Plan operation PART JOIN FILTER CREATE
@ PART JOIN FILTER CREATE

In the → development/databases/Oracle/SQL/statement/verbs/explain/plan_table[plan table],
the `PART JOIN FILTER CREATE` → development/databases/Oracle/SQL/statement/execution/plan/operations[plan operation] typically has a value like `:BF0000`, `:BF0001` etc for
→ development/databases/Oracle/SQL/statement/verbs/explain/plan_table/columns/object_name-owner[`object_name`]. BF seems to stand for *Bloom Filter*.
-
The value for `object_node` is typically `:Q1001`, `:Q1002` etc.

sa:

  The plan operation `→ development/databases/Oracle/SQL/statement/execution/plan/operations/join-filter/create`.

  → development/databases/Oracle/SQL/statement/execution/plan/operations[Plan operations]
