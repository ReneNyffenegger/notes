$ Oracle SQL Plan operation INDEX RANGE SCAN (MIN/MAX)
@ INDEX RANGE SCAN (MIN/MAX)

The parent operation of `INDEX RANGE SCAN (MIN/MAX)` is always a `→ development/databases/Oracle/SQL/statement/execution/plan/operations/first-row`.

`INDEX RANGE SCAN (MIN/MAX)` has no child row source.

sa:

 `INDEX FULL SCAN (MIN/MAX)`

  → development/databases/Oracle/SQL/statement/execution/plan/operations[Plan operations]
