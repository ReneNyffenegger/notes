$ Oracle: TRUNCATE TABLE
@ truncate table

`truncate table` is a → development/databases/Oracle/SQL/DDL[DDL statement] and can therfore not be → development/databases/Oracle/SQL/TCL/rollback[rolled back].

sa:
  → development/databases/Oracle/SQL/statement/nouns/table
