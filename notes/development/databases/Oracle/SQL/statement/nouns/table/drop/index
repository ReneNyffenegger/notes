$ Oracle: DROP TABLE
@ drop table

sa:

  Starting with → development/databases/Oracle/Releases-and-versions/23c[release 23c], the `drop table` comes with the `if exists` extension: `drop table if exists tab_xyz`.

  → development/databases/Oracle/SQL/statement/nouns/table/drop/cascade-constraints[`drop table … cascade constraint`]

  → development/databases/Oracle/errors/ORA-00942_table-or-view-does-not-exist

  → development/databases/Oracle/SQL/statement/nouns/table
