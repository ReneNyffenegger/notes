$ Oracle SQL: DROP TABLESPACE
@ drop tablespace

By default, `drop tablespace` does not remove the associated → development/databases/Oracle/architecture/database/files/data[data files] in the filesystem.
-
In order to remove them as well, the `including contents and datafiles` clause must be used.

sa:
  
  The SQL noun → development/databases/Oracle/SQL/statement/nouns/tablespace[`tablespace`].
