$ Oracle function USERENV

`userenv(parameter)` returns some information about the current → development/databases/Oracle/architecture/session.

Oracle considers `userenv` a *legacy function* and recommends to use → development/databases/Oracle/SQL/functions/sys_context[`sys_context()`].

sa:
  → development/databases/Oracle/SQL/functions
