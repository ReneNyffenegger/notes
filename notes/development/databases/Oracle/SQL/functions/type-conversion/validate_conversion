$ Oracle SQL: validate_conversion

The `validate_conversion` function is handy to quickly check if a value can be converted to target → development/databases/Oracle/SQL/data-types[data type]: it returns
`1` if it's possible and `0` otherwise.

gh|oracle-patterns|/SQL/functions/conversion/validate_conversion.sql||

Compare with
code {
cast(val as type default def on conversion error)
code }
