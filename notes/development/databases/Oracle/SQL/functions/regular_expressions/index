$ Oracle functions for regular expressions
@ functions for regular expressions

→ development/databases/Oracle/SQL/functions/regular_expressions/regexp_substr[`regexp_substr`] extracts a portion (a substring) from a text that matches a given
→ development/regular-expressions[regular expression].

→ development/databases/Oracle/SQL/functions/regular_expressions/regexp_replace[`regexp_replace`] replaces a portion of a text
that matches a → development/regular-expressions[regular expression] with another text.

→ development/databases/Oracle/SQL/functions/regular_expressions/regexp_like[`regexp_like`] is similar to the sql `like` operator, but searches
for regular expressions rather than substrings.

`regexp_instr` returns the position at which a regular expression matches.

→ development/databases/Oracle/SQL/functions/regular_expressions/regexp_count[`regexp_count`] returns how many times a given regular expression is matched in a string.

{ Example: word_boundaries

  gh|Oracle-Patterns|/SQL/functions/regular_expressions/word_boundaries.sql||

}

sa:

  → development/databases/SQL-Server/T-SQL/functions/regexp

  → development/regular-expressions

  → development/databases/Oracle/SQL/functions
