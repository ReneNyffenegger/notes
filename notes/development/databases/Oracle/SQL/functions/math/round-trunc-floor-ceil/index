$ Oracle SQL: ROUND, ROUND_TIES_TO_EVEN, TRUNC, FLOOR, CEIL

`floor(n)` returns the largest integer equal to or less than `n`; `floor(n) <= n`
-
`ceil(n)` returns the smallest integer that is greater than or equal to `n`; `ceal(n) >= n`


gh|Oracle-patterns|/SQL/functions/math/round-trunc-floor-ceil/select.sql||

sa:
  → development/databases/Oracle/SQL/functions
