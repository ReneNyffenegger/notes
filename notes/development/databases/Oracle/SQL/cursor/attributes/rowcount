$ Oracle: cursor attribute %rowcount

The → development/databases/Oracle/SQL/cursor/attributes[cursor attribute] `%rowcount` stores the amount of records that were affected by the most recent execution of the
cursor on which the attribute is applied.

For *implicit cursors*, `sql%rowcount` can be used.

sa:
  The → development/databases/SQL-Server[SQL Server] equivlent for `%rowcount` seems to be
  → development/databases/SQL-Server/T-SQL/variables/atat#mssql-atat-rowcount[`@@rowcount`].
