$ Oracle SQL: Session and system control statements

`→ development/databases/Oracle/SQL/session-system-control/alter-session` and `set role` are *session control* statements.

`alter system` is the *system control* statement.

sa:
  → development/databases/Oracle/SQL
