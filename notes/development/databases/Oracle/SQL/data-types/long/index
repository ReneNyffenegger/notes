$ Oracle data type: LONG
@ long

sa:

  The → development/databases/Oracle/OraOLEDB/connection-string-attributes[OraOLEDB specific connection string attribute `ChunkSize`]

  → development/databases/Oracle/SQL/functions/lob/to[`to_lob`] converts a `long` or a → development/databases/Oracle/SQL/data-types/raw[`long raw`] to a
  → development/databases/Oracle/SQL/data-types/lob[lob data type]

  → development/databases/Oracle/errors/ORA-00997_illegal-use-of-LONG-datatype and
  → development/databases/Oracle/errors/ORA-00932_inconsistent-datatypes#ora-00932-exp-char-got-long[ORA-00932: inconsistent datatypes: expected CHAR got LONG].

  The → development/databases/Oracle/SQL-Plus setting → development/databases/Oracle/SQL-Plus/set/long[`long`] specifies the number of bytes that are displayed for
  `long` values.

  → development/databases/Oracle/SQL/data-types
