$ Oracle: JOIN TO ONE

`join to one` enfroces *Row Widening Only Queries* (RWOQ) automatically.

code {
select
   ‥
from
   oe.order_items                                                         oi
   JOIN TO ONE (oe.orders o, oe.product_information p, oe.customers c)
code }

`join to one` was introduced in → development/databases/Oracle/Releases-and-versions/23c[26ai] (23.26.2+)
