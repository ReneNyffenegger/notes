$ Oracle SQL MODEL clause: Adding column

code {
select
   dummy,
   added_column
from
   dual
model
   dimension by(dummy)
   measures (sysdate added_column)
   ();
code }

sa:
  
  The → development/databases/Oracle/SQL/select/model[`model`] clause.
