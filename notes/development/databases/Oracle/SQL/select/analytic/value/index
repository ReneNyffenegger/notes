$ Oracle Analytic Functions: FIRST_VALUE, NTH_VALUE etc.

{ NTH_VALUE #ora-nth_value

  gh|Oracle-Patterns|/SQL/select/analytical_functions/nth_value.sql||

}
{ FIRST_VALUE #ora-first_value

 `first_value` (and `last_value`) are really just a special case of `nth_value(n)` where `n` is `1`.

  gh|Oracle-Patterns|/SQL/select/analytical_functions/first_value.sql||

}

sa:

  Compare with → development/databases/Oracle/SQL/select/analytic/lag-lead[`LAG` and `LEAD`]

  → misc/ISO/9075/features[SQL features] T617 and T618.
