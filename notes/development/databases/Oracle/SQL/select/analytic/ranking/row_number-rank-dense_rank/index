$ Oracle SQL: ROW_NUMBER, RANK and DENSE_RANK

gh|Oracle-Patterns|/SQL/select/analytical_functions/row_number-vs-rank-vs-dense_rank.sql||

sa:

  Using `dense_rank` to → development/databases/Oracle/SQL/select/analytic/ranking/row_number-rank-dense_rank/distinct-values-in-group[select the number of distinct values in a group].

  → development/databases/SQL/select/analytic/ranking/row_number-rank-dense_rank[`row_number()` vs `rank()` vs `dense_rank()`]

  → development/databases/Oracle/SQL/select/analytic
