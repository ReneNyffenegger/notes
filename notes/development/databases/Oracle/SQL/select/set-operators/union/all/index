$ Oracle SQL: UNION ALL
@ union all

`union all` is a → development/databases/Oracle/SQL/select/set-operators.

sa:
 `→ development/databases/Oracle/SQL/select/set-operators/union`

  Combining `union all` with a → development/databases/Oracle/SQL/clauses/with#ora-with-union-all[`with` clause].

  → development/databases/SQL/operation-order/union-all_order-by[`union all` and `order by`]
  -
  The error message → development/databases/Oracle/errors/ORA-00904_invalid-identifier as result of combining
  → development/databases/Oracle/errors/ORA-00904_invalid-identifier#ora-00904-union-all-order-by[`union all` with `order by`].

  The *join factorization* → development/databases/Oracle/optimizer#ora-optimizer-qry-tran[query tranformation].

  The → development/databases/Oracle/SQL/statement/execution/plan/operations/union-all[plan operation `UNION ALL`].

  → development/databases/Oracle/SQL/select/set-operators[Set operators]

  → development/databases/Oracle/errors/ORA-01789_query-block-has-incorrect-number-of-result-columns
