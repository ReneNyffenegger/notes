$ Oracle: UNION ALL
@ union all

sa:

  Combining `union all` with a → development/databases/Oracle/SQL/clauses/with#ora-with-union-all[`with` clause].

  → development/databases/SQL/operation-order/union-all_order-by[`union all` and `order by`]

  The *join factorization* → development/databases/Oracle/optimizer#ora-optimizer-qry-tran[query tranformation].
