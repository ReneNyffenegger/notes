$ Oracle SQL: Aggregate functions
@ aggregate function

Aggregate functions are allowed in
  • a → development/databases/Oracle/SQL/select[`select` statement's] *select list*
  • → development/databases/Oracle/SQL/select/aggregate/having[`having`] clause
  • → development/databases/Oracle/SQL/select/order-by[`order by`] clause

{ TODO

  Is → development/databases/Oracle/SQL/functions/json/dataguide[`json_dataguide()`] missing in this table?

}

sa:

  The → development/databases/Oracle/SQL/select/aggregate/null-values[influence of `null` values] in combination with aggregate functions.

  The → development/databases/Oracle/SQL/select/aggregate/median[`median()`]

 `→ development/databases/Oracle/SQL/select/aggregate/stats_mode` returns the value that occurs the most.

  → development/databases/Oracle/SQL/select/aggregate/distinct[`distinct` and `count`]

 `→ development/databases/Oracle/installed/dynamic-performance-views/sqlfn/metadata` can be used to → development/databases/Oracle/SQL/select/aggregate/query-functions[query aggregate functions].

  The `→ development/databases/Oracle/SQL/select/group-by` clause.
  
  → development/databases/Oracle/SQL/select/aggregate/null-values[`NULL` values in aggregate functions]

  → development/databases/Oracle/SQL/select/aggregate/listagg[`listagg`] is an aggregate function that allows to concatenate values of
  different records into *one* record.

  Aggregate functions can be → development/databases/Oracle/SQL/select/aggregate/nested[nested] (`select sum(avg(val)) from tab group by other_val`).

  The `aggregate` clause  allows to define a function that returns a single value (row?) for a set of rows.

  → development/databases/Oracle/Data-Cartridge/interfaces/aggregation[Creating user defined aggregate functions] with → development/databases/Oracle/Data-Cartridge.

  The → development/databases/Oracle/SQL/select/pivot[`pivot` clause] in an → development/databases/Oracle/SQL/select[`select` statement].

  The `approx_for_aggregation` → development/databases/Oracle/adminstration/init-parameters.

  The → development/databases/Oracle/Releases-and-versions/21c#ora-21c-any_value[`any_value` extension] introduced in 21c.
