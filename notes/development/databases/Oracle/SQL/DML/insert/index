$ Oracle: INSERT SQL statement
@ insert

An → development/databases/Oracle/SQL/DML/insert[`insert`] statement is used to add → development/Data to a
→ development/databases/Oracle/objects/tables.

Data is added to table either by an
→ development/databases/Oracle/SQL/DML/insert/paths[conventional insert or a direct path insert] operation.

sa:

  → development/databases/Oracle/SQL/DML/insert/select[`insert into … select from …`]

  → development/databases/Oracle/SQL/DML/insert/log-errors[Log errors] for `insert statements`.

  Using the → development/databases/Oracle/SQL/DML/insert/append-hint[`append` hint] with `insert` statements.
