$ Access functions


  → development/databases/Access/functions/fix[`fix(number)`] returns the integer portion of a number.
  If used on a → development/languages/VBA/language/datatypes/date[date], it returns the day of the date (that is: it behaves as `trunc(date)` in Oracle).

{ nz

  `nz` seems to be the → development/databases/Access[Access] equivalent of Oracle's → development/databases/Oracle/SQL/functions/null-related#nvl.

}
