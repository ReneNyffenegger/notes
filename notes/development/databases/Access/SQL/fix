$ Access SQL: fix

The → development/languages/VBA[VBA] function → development/languages/VBA/functions#vba-fix[`fix(number)`] returns the integer portion of a number.
If used on a → development/languages/VBA/language/datatypes/date[date], it returns the day of the date (that is: it behaves as `trunc(date)` in Oracle).

gh|about-Access|/SQL/fix.bas||

sa:
  → development/databases/Access/SQL[Access SQL]
