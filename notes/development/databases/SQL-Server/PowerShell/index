$ SQL Server: PowerShell module SqlServer

The → Windows/PowerShell/language/module[PowerShell module] *SqlServer* by developers and administrators to automate their daily tasks.
-
The module also provides multidimensional and tabular cube processing.

The module *SqlServer* replaces the older *SQLPS* module.

Tasks with → development/databases/SQL-Server/Reporting-Services[SSRS] and *Power BI Report Server* can be autoated with the *ReportingServicesTools* module which is available from the
→ https://www.powershellgallery.com/packages/ReportingServicesTools[PowerShell Gallery].

{ Misc comamnds

code {
PS C:\> import-module sqlserver
PS C:\> cd sqlserver:
PS SQLSERVER:\>
code }

}
