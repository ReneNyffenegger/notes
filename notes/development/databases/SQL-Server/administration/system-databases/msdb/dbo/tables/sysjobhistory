$ msdb.dbo.sysjobhistory

`msdb.dbo.sysjobhistory` records the execution of → development/databases/SQL-Server/administration/system-databases/msdb/dbo/tables/sysjobsteps[job steps].

The → development/databases/SQL-Server/T-SQL/data-types[data type] of the columns `run_date`, `run_time` and `run_durations` is → development/databases/SQL-Server/T-SQL/data-types/integer[integer] in `HHMMSS` and `YYYYMMSS` format, respectively.
-
`msdb.dbo.agent_datetime(run_date, run_time)` creates a → development/databases/SQL-Server/T-SQL/data-types/datetime from such integers.

gh|about-MSSQL|/administration/system-databases/msdb/tables/sysjobhistory/select.sql||
