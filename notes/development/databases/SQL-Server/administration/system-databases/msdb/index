$ SQL Server: msdb (system database)
@ msdb

The msdb → development/databases/SQL-Server/administration/system-databases[system database] is used by the → development/databases/SQL-Server/architecture/services/agent[SQL Server Agent] to schedule
alerts and jobs.

{ restorehistory

  The table `msdb.dbo.restorehistory` can be used to determine when each → development/databases/SQL-Server/administration/database[database] was restored:
  gh|about-MSSQL|/administration/system-databases/msdb/restore_history-last-restore.sql||

}

sa:
  → development/databases/SQL-Server/administration/system-databases
