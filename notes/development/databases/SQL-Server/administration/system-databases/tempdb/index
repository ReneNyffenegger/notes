$ SQL Server: tempdb (system database)
@ tempdb

*tempdb* is a special → development/databases/SQL-Server/administration/database[database] that the
→ development/databases/SQL-Server/architecture/instance uses for temporary data that is created in operations such as
  • sort operations in query statements
  • database integrity checks
  • index maintenance


The *tempdb* is built every time → development/databases/SQL-Server[SQL Server] is started.

The *tempdb* has the same default *collation* as the
→ development/databases/SQL-Server/administration/system-databases/model[model database].

The *tempdb* stores all *temporary stored procedures* and *temporary → development/databases/SQL-Server/sql/create/table[tables]*.

sa:
  → development/databases/SQL-Server/administration/system-databases
