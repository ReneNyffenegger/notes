$ SQL Server: master (system database)
@ master

The *master database* keeps track of all system-level information for an → development/databases/SQL-Server/architecture/instance.

The master database also contains the → development/databases/SQL-Server/T-SQL/procedures/system/extended[extended system stored procedures] (`xp_…`) in the `dbo` schema.

The master database is owned by → development/databases/SQL-Server/administration/sa. This → development/databases/SQL-Server/administration/database#mssql-db-owner[ownership] cannot be changed.

sa:
  → development/databases/SQL-Server/administration/system-databases
