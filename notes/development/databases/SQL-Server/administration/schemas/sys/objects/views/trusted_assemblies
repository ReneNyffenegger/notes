$ SQL Server: sys.trusted_assemblies
@ sys.trusted_assemblies

{ Definition of the view

  The definition of the view is
code {
→ development/databases/SQL-Server/T-SQL/statements/execute[exec] → development/databases/SQL-Server/administration/schemas/sys/objects/stored-procedures/sp_helptext[sp_helptext]

CREATE VIEW sys.trusted_assemblies AS  
 SELECT *  
 FROM → development/databases/SQL-Server/T-SQL/functions/rowset/openrowset[OpenRowSet](TABLE TRUSTED_ASSEMBLIES)  
code }

}

sa:

  → development/databases/SQL-Server/administration/schemas/sys/objects/stored-procedures/sp_add_trusted_assembly[`sp_add_trusted_assembly`]

  → development/databases/SQL-Server/CLR-Integration

  The → development/databases/SQL-Server/administration/schemas/sys[sys schema]
