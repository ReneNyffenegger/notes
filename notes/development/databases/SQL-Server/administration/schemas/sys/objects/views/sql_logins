$ SQL Server: sys.sql_logins
@ sys.sql_logins

`sys.sql_logins` lists logins that can be authenticated by SQL Server.

The logins listed in `sql_logins` are a subset of those found in → development/databases/SQL-Server/administration/schemas/sys/objects/views/server_principals[`sys.server_principals`].

sa:
  The → development/databases/SQL-Server/administration/schemas/sys[sys schema]
