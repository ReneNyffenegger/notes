$ SQL Server: sp_rename

Chaning the name of views, functions, stored procedures or triggers with `sp_rename` does not change the corresponding object name in the
→ development/databases/SQL-Server/administration/schemas/sys/objects/views/sql_modules[`sys.sys_modules` catalog view.
