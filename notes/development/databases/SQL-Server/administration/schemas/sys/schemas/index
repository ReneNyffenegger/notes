$ SQL Server - sys.schemas
@ sys.schemas

`sys.schemas` lists the available → development/databases/SQL-Server/administration/schemas.

`sys.schemas` can be joined with `sys.database_principals`:
  gh|about-MSSQL|/administration/schemas/sys/schemas/select.sql||

sa:

  → development/databases/SQL-Server/administration/schemas/INFORMATION_SCHEMA/schemata[`INFORMATION_SCHEMA.schemata`].

  The → development/databases/SQL-Server/administration/schemas/sys[sys schema].
