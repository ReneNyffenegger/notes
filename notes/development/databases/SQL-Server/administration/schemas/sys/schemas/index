$ SQL Server - sys.schemas
@ sys.schemas

`sys.schemas` can be joined with `sys.database_principals`:
  gh|about-MSSQL|/administration/schemas/sys/schemas/select.sql||

sa:
  The → development/databases/SQL-Server/administration/schemas/sys[sys schema].
