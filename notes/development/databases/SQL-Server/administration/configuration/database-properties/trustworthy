$ SQL Server: database property TRUSTWORTHY

The database property `trustworty` is used to indicate that an → development/databases/SQL-Server/architecture/instance can trust the contents of a → development/databases/SQL-Server/administration/database.

For example, in a trustworthy database, an → Microsoft/dot-net/assembly can be created with `external_access` or `unsafe` permission.
