$ SQL Server: DML statements
-

  • `bulk insert` (Compare with `insert … select * from openrowset(bulk …)` and the `bcp` command.
  • `delete`
  • `. → development/databases/SQL-Server/sql/DML/insert[insert]`
  • `merge`
  • `readtext`
  ??• `select` (Really ???)
  • → development/databases/SQL-Server/sql/update[`update`]
  • `updatetext`
  • `writetext`
