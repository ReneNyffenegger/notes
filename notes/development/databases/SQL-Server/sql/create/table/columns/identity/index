$ SQL Server: automatically insert primary key id with IDENTITY columns 

A column with the `identity` property does not need to explicitely be filled with a value in insert statements. SQL Server will automatically provide an *increasing* and more importantly *unique*
integer (bigint?) for the insert.
-
Thus, identity-columns are an ideal candiate for primary keys in SQL Server.

gh|about-MSSQL|/sql/create/table/identity.sql||

sa:

  → development/databases/SQL-Server/sql/create/table/columns/default-value-sysdatetime[default values in columns].

  The inserted identity-value can be optained with 
  → development/databases/SQL-Server/sql/create/table/columns/identity[`scope_identity()`, `ident_current` or `@@identity`]

  Compare with → development/databases/Oracle[Oracle's] → development/databases/Oracle/objects/sequences#ora-sequence-auto-increment[auto increment] feature.
