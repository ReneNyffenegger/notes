$ SQL Server: Fill columns values with default values in insert statements

In a → development/databases/SQL-Server/sql/create/table[`create table` statement], a column can be declared with the `default` property which will provide the value for the respective column in
insert statements if no value is provided.

In the following example, the column `dt` is such a default column whose value default to `sysdatetime` if not provided in the insert statement:
gh|about-MSSQL|/sql/create/table/columns/default-values-sysdatetime.sql||

sa:
  The → development/databases/SQL-Server/sql/create/table/columns/identity[`identity` property].
