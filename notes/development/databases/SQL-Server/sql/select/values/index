$ SQL Server: create test data with the values clause

The `values(…)` clause is handy to quickly create a bit of test data without
→ development/databases/SQL-Server/sql/create/table[creating a table].

The following example tests a *left join*:
gh|about-MSSQL|/sql/select/values/left-join.sql||
