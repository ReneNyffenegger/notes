$ SQL Server: create test data with the values clause

The `values(…)` clause is handy to quickly create a bit of → development/Data/test[test data] without
→ development/databases/SQL-Server/sql/create/table[creating a table].

The following example tests a *left join*:
gh|about-MSSQL|/sql/select/values/left-join.sql||

sa:

  → development/databases/SQL-Server/sql/select/values/greatest-least[Mimicking Oracle's `greatest` and `least` functions with the values clause]

  → development/databases/SQL-Server/sql/select[select]
