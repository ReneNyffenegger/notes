$ SQL Server: mimicking Oracle's GREATEST() and LEAST() with the values clause

The → development/databases/SQL-Server/sql/select/values[`values` clause] can be used to mimick → development/databases/Oracle[Oracle's] `greatest` and `least` functions:

gh|about-MSSQL|/sql/select/values/greatest-least.sql||
