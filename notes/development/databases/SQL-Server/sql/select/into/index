$ SQL Server: select … into …

The → development/databases/SQL-Server/sql[SQL statement] `select … into …` can be used to create a new table whose values are populated with the returned values from the select statement;

code {
select *
  into tq84_new_table
  from tq84_old_table
where
  …
code }

sa:
  → development/databases/SQL-Server/sql/create/table[`create table…`]
