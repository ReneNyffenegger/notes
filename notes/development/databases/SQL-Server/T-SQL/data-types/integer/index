$ SQL Server: datatype integer
@ integer

The `integer` data type can be abbreviated with `int`.

sa:
  → development/databases/SQL-Server/sql/select/aggregate/avg/beware-of-integers[Beware of integers in `avq()`]

  → development/databases/SQL-Server/T-SQL/data-types[data types]
