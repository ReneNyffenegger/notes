$ SQL Server: datatype rowversion

The `rowversion` → development/databases/SQL-Server/T-SQL/data-types[data type] is not a → development/databases/SQL-Server/T-SQL/data-types/_date-time[date/time related data type].

A deprecated synonym for `rowversion` is `timestamp`


sa:

  → development/databases/SQL-Server/T-SQL/data-types[data types]

  `rowversion` is not found in → development/databases/SQL-Server/administration/schemas/sys/objects/views/systypes[`sys.systypes`].

