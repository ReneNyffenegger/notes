$ SQL Server: datatype sysname

The `sysname` data type is basically equivalent to a `→ development/databases/SQL-Server/T-SQL/data-types/nvarchar[nvarchar](128) not null`.

sa:
  → development/databases/SQL-Server/T-SQL/data-types[data types]
