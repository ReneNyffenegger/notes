$ SQL Server - table data type

The `table` → development/databases/SQL-Server/T-SQL/data-types[data type] is used, for example, for → development/databases/SQL-Server/T-SQL/user-defined-functions/table-valued[table valued functions].

{ Basic demonstration

  The following basic example tries to demonstrate the basic usage of the table data type.

  As with any other data type, → development/databases/SQL-Server/T-SQL/variables[variables] that store a table need to be → development/databases/SQL-Server/T-SQL/variables/declare[declared].

  After such a declaration, the variable can bu used everywhere where an ordinary table can be used. That is, its possible `insert` or `select` from these tables.

  gh|about-MSSQL|/t-sql/data-types/table/intro.sql||

}

sa:

  → development/databases/SQL-Server/T-SQL/data-types

  → development/databases/SQL-Server/objects/table[SQL tables]

  → development/databases/SQL-Server/sql/select/assign-variables[Assign values to variables in a `select` statement]

  The `table` data type is not found in → development/databases/SQL-Server/administration/schemas/sys/objects/views/systypes[`sys.systypes`].
