$ SQL server: datatype float

`float(n)` is an → development/databases/SQL/data-types/predefined/numeric/approximate[approximate numerical data type].
-
The optional `n` (which must be between 1 and 53) specifies the number of mantissa-bits. If not specified, `n` defaults to 53.

→ development/databases/SQL-Server/T-SQL/data-types/real[`real`] is a synonym for `float(24)`.
-
`double precision` is a synonym for `float(53)`.

sa:

  → development/IEEE/754

  → development/databases/SQL-Server/T-SQL/data-types[data types]
