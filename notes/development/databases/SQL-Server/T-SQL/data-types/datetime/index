$ SQL server: datatype datetime

`datetime` stores a date and time with accuracy of a thousandst of a second *with the last decimal being 0, 3 or 7*.

sa:

  → development/databases/SQL-Server/T-SQL/data-types/datetime2[`datetime2`]

  The → development/databases/Oracle equivalent datatype → development/databases/Oracle/SQL/data-types/date[`date`] which stores the
  a date time as accurate as seconds.

