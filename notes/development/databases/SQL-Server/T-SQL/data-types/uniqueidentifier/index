$ SQL Server: datatype uniqueidentifier

The data type `uniqueidentifier` stores 16-bit → development/UUID-GUID[GUIDs].

{ Creation of values for uniqueidentifier

  A `uniqueidentifier` can be created with
     • `newid()`
     • `newsequentialid()` (however in a default constraint of a table column only)
     •  by assigning a → development/databases/SQL-Server/T-SQL/data-types/varchar[varchar] representation of a GUID to a `uniqueidentifer`

  gh|about-MSSQL|/t-sql/data-types/uniqueidentifier/create.sql||

}

sa:
  → development/databases/SQL-Server/T-SQL/data-types
