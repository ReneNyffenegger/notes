$ SQL Server function: is_srvrolemember
@ is_srvrolemember

`is_srvrolemember` can be used if a given login (default: current execution context) is member of a user defined or fixed server role.

sa:
  → development/databases/SQL-Server/T-SQL/functions/security/is_member[`is_member()`]
