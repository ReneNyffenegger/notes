$ SQL Server function: type_id
@ type_id

`type_id(typename)` returns a → development/databases/SQL-Server/T-SQL/data-types[data type's] id given its name.
code {
select
   tp.name       type_name,
   tp.xusertype,
   type_id(tp.name)
from
   sys.systypes tp
code }
