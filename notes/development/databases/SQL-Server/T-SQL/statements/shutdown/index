$ SQL Server: shutdown
@ shutdown

`shutdown` → development/databases/SQL-Server/architecture/services#mssql-change-service-state[stops a SQL Server service] (which is tied to
an → development/databases/SQL-Server/architecture/instance[instance]).

code {
shutdown
shutdown with nowait
code }
