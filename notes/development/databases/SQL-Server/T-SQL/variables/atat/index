$ SQL Server: @@-variables

{ @@fetch_status

  After fetching from a cursor, `@@fetch_status` is 0 if a record was fetched and non 0 otherwise.

}
{ @@options

  See → development/databases/SQL-Server/T-SQL/variables/atat/options[`@@options`]

}
{ @@rowcount

  `@@rowcount` stores how many recores were affected or read.
code {
→ development/databases/SQL-Server/sql/update[update] tq84_foo set col_1 = @col_val where id = @foo_id;
if @@rowcount = 0
   print 'No rows were updated'
code }

}
{ @@servername

  `@@servername` apparently stores the name of the server.

}
{ @@transcount

  TODO.

}
{ @@version

  gh|about-MSSQL|/t-sql/variables/@@/version.sql||

}

sa:
  → development/databases/SQL-Server/T-SQL/variables
