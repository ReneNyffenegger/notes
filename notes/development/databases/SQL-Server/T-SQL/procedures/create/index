$ T-SQL: create stored procedures

{ create or alter procedure

  The `create or alter` clause corresponds to → development/databases/Oracle[Oracle's] `create or replace` clause: it creates a new procedure if no procedure with the given name
  exists, or replaces the existing procedure with the new one.

}
sa:
  → development/databases/SQL-Server/T-SQL/procedures
