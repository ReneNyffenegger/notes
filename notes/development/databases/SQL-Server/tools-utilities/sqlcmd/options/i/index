$ SQL Server - sqlcmd: specyfing an sql file to be executed

When → development/databases/SQL-Server/tools-utilities/sqlcmd[sqlcmd] is invoked in → Windows/dirs/Windows/System32/cmd_exe[cmd.exe], a file containing SQL commands can be specified with the `-i` option:
code {
C:\> sqlcmd -S Hostname\InstanceName -i f:\project\some\script.sql
code }

Other input options are `-Q` and `-q`.
