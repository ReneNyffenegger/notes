$ SQL Server Agent: database roles

The → development/databases/SQL-Server/administration/system-databases/msdb[msdb] database defines three special roles for
→ development/databases/SQL-Server/architecture/services/agent:
  • SQLAgentUserRole
  • SQLAgentReaderRole
  • SQLAgentOperatorRole

{ Querying the roles

  These three special roles can be queried like so:

}
