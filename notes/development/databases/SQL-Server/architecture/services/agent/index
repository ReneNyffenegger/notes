$ SQL Server Agent (service)

Account name: NT Service\SQLSERVERAGENT.

The SQL Server Agent executes scheduled administrative tasks (known as *jobs* in SQL Server 2017).

The SQL Server Agent uses the → development/databases/SQL-Server/administration/system-databases/msdb → development/databases/SQL-Server/administration/system-databases[system database] to schedule alerts and jobs.

sa:
  → development/databases/SQL-Server/architecture/services
