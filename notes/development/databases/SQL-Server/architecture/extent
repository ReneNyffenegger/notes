$ SQL Server: extent
@ extent

An extent is 8 contiguous → development/databases/SQL-Server/architecture/page[pages].

{ 2 types of extents

  ~Uniform extents~ are owned by a single object.

  ~Mixed extents~ are shared by up to eight objects.

}

sa:
  → development/databases/SQL-Server/architecture
