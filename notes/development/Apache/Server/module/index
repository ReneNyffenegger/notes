$ Apache Modules

The list of modules that are compiled into the server can be displayed with the `-l` command line option of `→ development/Apache/Server/httpd`.
code {
$ apache/bin/httpd -l
Compiled in modules:
  core.c
  mod_win32.c
  mpm_winnt.c
  http_core.c
  mod_so.c
code }

sa:

  Modules can be dynamically loaded with the `LoadModule` directive.

  The → development/web/php function `→ development/web/php/snippets/apache/get_modules` returns an → development/web/php/snippets/array of loaded apache modules.
