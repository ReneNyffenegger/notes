$ Apache Jena

*Apache Jena* is a Java framework for building Semantic Web and → development/Data/linked[Linked Data] applications.

ARQ is a query engine that supports → development/databases/query-languages/SPARQL[SPARQL].

TDB Engine: seems to be an → development/web/RDF store.

→ development/Apache/Jena/Fuseki[Fuseki] is a → development/databases/query-languages/SPARQL server.

`tdbloader` (and `tdbloader2` on Unix only?) is a bulk loader for TDB. After loading, data should be able to be queried with `tdbquery`.

code {
./tdbquery --loc /path/to/database 'select * where { ?s ?p ?o }'
code }
