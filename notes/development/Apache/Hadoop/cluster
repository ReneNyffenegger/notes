$ Hadoop cluster

A *Hadoop cluster* consists of → development/Apache/Hadoop/node[nodes].

There is one → development/Apache/Hadoop/YARN/Resource-manager[resource manager] per cluster.

{ Node failure

  → development/Apache/Hadoop/HDFS is designed to be resilient against node failures in a Hadoop cluster.

}
