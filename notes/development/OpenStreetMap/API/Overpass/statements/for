$ Overpass API: for statement
@ for

code {
[out:csv("name";false)];

area[name="Pfungen"];

way(area)[highway][name];

for (t["name"]) (
   make x name=_.val;
   out; 
);
code }

code {
[out:csv(
   'id', 'key', 'val';
    false;
   '|'
)];

node[name='Freienstein'] -> .nd;

for.nd -> .id (id())  {
  for.id -> .ky (keys()) {    
    for.ky -> .vl(t[ky.val]) {
      make x
          id  = id.val, 
          key = ky.val,
          val = vl.val;
      out;
    }
  }
};
code }

{ for is GROUP BY in disguise

  The `for` statement seems to behave like the → evelopment/databases/SQL/select/group-by[SQL `GROUP BY`] clause:
code {
[ out : csv(
   name,
   total_length,
   cnt_ways,
   min_length,
   max_length,
   avg_length,
   all_lengths
)];

{{geocodeArea: Pfungen }};

way[highway](area);

for (t['name']) {

   make Beispiel
     name         = _.val,
     total_length = sum(length()),                // Aggregate all lenghts
     cnt_ways     = count(ways)  ,
     min_length   = min(length()),
     max_length   = max(length()),
     avg_length   = sum(length()) / count(ways),  // There is no function avg()
     all_lengths  = set(length())                 // Concatenate the individual lengths with semicolons.
   ;

   out;
}
code }

}

sa:
  → development/OpenStreetMap/API/Overpass/statements
