$ Csound opcodes: oscil*
@ oscil

`oscil`

`oscili`: interpolated

`oscil3`: cubic interpolation

`oscilikt`: interpolated, allows to change → Kunst/Musik/Csound/GEN-Routines/function-table[function table] number at k-rate

`poscil`: high precision oscilator (p = precise)

`poscil3`: high precision oscilator with cubic interpolation

code {
aout $OSCIL xamp, xfreq, [func-tab-nr, initial-phase]
code }

If `func-tab-nr` is not stated, it defaults to -1 which is a sine.

sa:
  → Kunst/Musik/Csound/Opcodes
