$ Csound: Score Statement
@ score statements

{ f - function table statement #score-stmt-f

  The → http://www.csounds.com/manual/html/f.html[f statement] causes a
  → Kunst/Musik/Csound/GEN-Routines[GEN routine] to be stored in a → Kunst/Musik/Csound/GEN-Routines/function-table.

  TODO ... → Kunst/Musik/Csound/GEN-Routines/

}
{ i #score-stmt-i

code {
i instr-no t-start t-len …
code }

  
  If `p3` (t-len) is negative, it → Kunst/Musik/Csound/tied-notes[ties the note] to the next `i` statement with the same instrument number.

  Usually, the forth parameter (`p4`) is a *pitch class value* (in the form `octave.note-nr`) which can be converted to a frequency
  with → Kunst/Musik/Csound/Opcodes/cpspch[`cpspch`].

}
