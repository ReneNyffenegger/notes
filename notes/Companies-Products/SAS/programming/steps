$ SAS programs: Steps

SAS programs consist of a sequence of steps.

A step must be completed before the next step is started.

A step starts either with the word → Companies-Products/SAS/programming/data-step[`data`] or `proc` and ends with `run;`.

The `run;` between two steps is optional.
