$ SAS - proc append: bulk load into an Oracle table

The `bulkload=yes` instruction makes `proc append` use Oracle's → development/databases/Oracle/tools/sql-loader/[SQL*Loader] to load data into an Oracle table.

gh|about-SAS|/programming/proc/append/oracle/bulk-load.sas||

sa:

  → Companies-Products/SAS/programming/proc/append/oracle
