$ SAS: proc registry
@ proc registry

Without options, `proc registry` prints a list of options.
gh|about-SAS|/programming/proc/registry/basic.sas||

{ list

  `list` forces the output to be written to the → Companies-Products/SAS/log.

  gh|about-SAS|/programming/proc/registry/list.sas||

}
{ export

  `export` is supposed to write the contents of the → Companies-Products/SAS/registry to the specified file.

  For an unknown reason, the written file is different from and much shorter than the text written to the → Companies-Products/SAS/log.

  gh|about-SAS|/programming/proc/registry/export.sas||

}
{ startat

  gh|about-SAS|/programming/proc/registry/startat.sas||

}

sa:

  → Companies-Products/SAS/programming/proc
