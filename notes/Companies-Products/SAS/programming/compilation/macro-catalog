$ SAS: Program compilation - macro catalog
@ macro catalog

The macro catalog stores → Companies-Products/SAS/programming/macros[macro] definitions (macro programs).

A macro is defined (compiled) with the → Companies-Products/SAS/programming/macros/statements/macro statement. This
puts the macro definition into the macro catalog.

When the macro is executed, its definition is taken from the macro catalog.
