$ SAS: Program compilation - macro symbol table
@ macro table

The *macro symbol table* is also called *macro table*.

The macro table stores → Companies-Products/SAS/programming/macros/variables[macro variables].
