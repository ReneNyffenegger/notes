$ SAS: Program compilation - macro symbol table
@ macro table

The *macro symbol table* is also called *macro table* (and *symbol table*?)

The macro table stores → Companies-Products/SAS/programming/macros/variables[macro variables].

Macro variables are created and/or stored in the macro table with the → Companies-Products/SAS/programming/macros/statements/let/index[%let macro statement].
