$ SAS: informats / formats
@ informats and formats

~Informats~ specify how data is converted when it is read (hence the ~in~ in *informat)

~Formats~ specify how values of variables are formateed when they are printed.

Informats are defined with the `attrib` and `informat` statements, formats with `attrib` and `format` statements.

sa:
  → Companies-Products/SAS/programming/formats-informats/words[`wordsN.`]
