$ SAS macro: shellCmd
@ shellCmd

`tq84_shellCmd` can be used to execute to execute a → Linux/shell/commands[shell command] and print its result in
the → Companies-Products/SAS/log[log file]. 

gh|about-SAS|/macros/shellCmd.sas||


{ Example

  The following example uses → Companies-Products/SAS/macros/opt[`tq84_opt`] to determine the work directory and then lists
  the directories contents with → Linux/shell/commands/ls[`ls -ltr`] (ordering from oldest to newest):
  gh|about-SAS|/macros/shellCmd.sas||

}


sa:


  → Companies-Products/SAS/programming/statements/filename/pipe[`filename pipe`]

  The → Companies-Products/SAS/programming/statements/systask statement might be better suited for such a task.

  → Companies-Products/SAS/macros
