$ SAS Deployment Manager
@ deployment manager

{ Relationship between the deployment manager and wizard

  The deployment manager looks similar to the → Companies-Products/SAS/Deployment/Wizard, but is used for post installation tasks (such as applying hot fixes).
  -
  In fact, the deployment manager  is installed by the deployment wizard under → Linux/fhs/usr/local/SAS in
  → Linux/fhs/usr/local/SAS/SASDeploymentManager/9_x

}
{ Starting the deployment manager

  The deployment manager is started by executing `sasdm.sh` (Windows: `sasdm.exe`), located under → Linux/fhs/usr/local/SAS/SASDeploymentManager/9_x.

}

