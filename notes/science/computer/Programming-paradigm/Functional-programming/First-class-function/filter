$ Common first class function: filter

*filter* takes as its arguments a function that returns a boolean (that is: a predicate) and a list. It then applies the function to each element in the function and constructs a new
list that consists of the elements for which the predicate returned 0.


sa:
  → science/computer/Programming-paradigm/Functional-programming

  → development/languages/Perl[Perl's] implementation of the *filter* function is → development/languages/Perl/functions/grep[`grep`].

  The → Linux/shell[shell] command → Linux/shell/commands/grep[`grep`] and Windows console commands → Windows/dirs/Windows/System32/find_exe[`find.exe`] and
  → Windows/dirs/Windows/System32/findstr_exe[`findstr.exe`] might be considered a sort of a *filter* function (in which each line of a text file is an element of a list).

  The → Windows/PowerShell/language/cmdlet[Powershell cmdlet] → Windows/PowerShell/command-inventory/noun/object/where[`where-object`].
