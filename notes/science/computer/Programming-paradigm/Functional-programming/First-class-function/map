$ Common first class function: map

*map* takes as its arguments a function and a list. It then applies the function to each element of the list and returns the list of the return values of the function:
`map(f, [v1, v2, v3]) == [f(v1), f(v2), f(v3)]`


sa:
  → development/languages/Perl/functions/map[Perl's],
  → development/languages/Python/built-in-functions/map[Python's] and
  → development/vim/script/vimscript/functions/map[Vim script's] implementation of map, the
  roughly equivalent → development/languages/R[R] function → development/languages/R/functions/sapply[`sapply()`] and
  → Windows/PowerShell/command-inventory/noun/object/forEach[PowerShell's `forEach object` cmdLet].

  → science/computer/Programming-paradigm/Functional-programming/MapReduce

  The → Windows/PowerShell/language/cmdlet[Powershell cmdlet] `forEach-object`.
