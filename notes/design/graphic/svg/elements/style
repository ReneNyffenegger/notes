$ SVG: style element

The SVG `<style>` element allows to embedd *style sheets* into an SVG document.
-
The element has the same attributes as the → development/web/HTML/tags/style[HTML `<style>` element].

sa:


  → design/graphic/svg/elements[SVG elements]
