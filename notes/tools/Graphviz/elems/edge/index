$ Graphviz: edge
@ edge

? By default, an *edge* ranks ranks the → tools/Graphviz/elems/node[nodes] it connects. This can be turned off by setting the edge's → tools/Graphviz/attributes[attribute] `constraint` to false.
