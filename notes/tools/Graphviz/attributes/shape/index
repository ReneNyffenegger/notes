$ Graphviz - shape attribute
@ shape

The `shape` → tools/Graphviz/attributes[attribute] specifies the shape for → tools/Graphviz/elems/node[nodes].

{ Main shape types

 Graphviz has three (main?) types of shapes:
  • polygon-based shapes
  • record-based shapes (which are superseeded by *HTML like labels*).
  • user-defined

}

{ Some important shape values

  table { lll
    
          ☰ Comments ☰ synonyms
  `box`   ☰ ☰ `rect`, `rectangle`
  `square ☰ ☰
  `none`  ☰ ☰ `plaintext`
  `plain` ☰ Similar to `none` but enforces `width=0 height=0 margin=0` ☰
  `point` ☰ ☰ 

  table }

}


sa:

  → tools/Graphviz/attributes

  → tools/Graphviz
