$ Graphviz: attribute types

→ tools/Graphviz/attributes[Attributes] are key-value pairs. An attribute key has *one* type. This type specifies the range of values that can be assigned to the key.

table { ll
  addDouble   ☰ A double with an optional prefix `+`
  addPoint    ☰ A point with an optional prefix `+`
  → tools/Graphviz/attributes/types/arrowType  ☰ 
  bool        ☰ 
  clusterMode ☰ `local`, `global`, `none`
  → tools/Graphviz/attributes/types/color ☰ `#rrggbb`, `#rrggbbaa`. `h.hhh s.sss v.vvv`, `red`
  colorList   ☰ A colon separated list of weighted color values
  dirType     ☰ `forward`, `back`, `both` or `none`
  double      ☰ 
  → tools/Graphviz/attributes/types/escString ☰ Replace escape sequences in strings.
  int         ☰ 
  layerList   ☰ 
  layerRange  ☰ 
  → tools/Graphviz/attributes/types/lblString ☰ A `lblString` is either an → tools/Graphviz/attributes/types/escString[`escString`] or an *HTML Label*.
  outputMode  ☰ 
  packMode    ☰ 
  pageDir ☰ 
  point ☰ 
  pointList ☰ 
  portPos ☰ 
  quadType ☰ 
  rankType ☰ 
  rect ☰ 
  shape ☰ `shape` is both, an → tools/Graphviz/attributes/shape[attribute (see here)] and a type.
  smoothType ☰ 
  splineType ☰ 
  startType ☰ 
  → tools/Graphviz/attributes/types/string ☰ 
  viewPort ☰ 
table }
