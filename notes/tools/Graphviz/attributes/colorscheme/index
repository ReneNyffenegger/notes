$ Graphviz - colorscheme attribute
@ colorscheme

The value of `colorscheme` specifies a → tools/Graphviz/attributes/colorscheme/color-scheme-namespace.

The possible values of `colorscheme` seem(!) to be
  • → https://en.wikipedia.org/wiki/X11_color_names[x11]
  • → https://www.december.com/html/spec/colorsvg.html[xvg] (which defines 147 color names - but less colores because some colors go with *…grey* and *…gray* or because fuchsia and magenta refer to the same color).
  • one of the many of the → http://www.graphviz.org/doc/info/colors.html#brewer[brewer color scheme]

sa:

  → tools/Graphviz/attributes/_color

  → tools/Graphviz/attributes
