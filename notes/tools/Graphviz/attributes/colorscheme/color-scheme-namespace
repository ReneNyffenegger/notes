$ Graphviz: color scheme namespace
@ color scheme namespace

The *color scheme namespace* defines how a → tools/Graphviz/attributes/colorscheme/color-name is interpreted.

The *color scheme namespace* can be set with the → tools/Graphviz/attributes/colorscheme[`colorscheme` attribute]

sa:
  The → tools/Graphviz/attributes/types/color[attribute type `color`]
