$ GMT grid file format

By default → tools/GMT[GMT] writes out grid as single precision floats in a
COARDS-complaint → Wissenschaft/misc/CDF[netCDF] file format.

However, GMT is able to produce grid files in other grid file formats.
