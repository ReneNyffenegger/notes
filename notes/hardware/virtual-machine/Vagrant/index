
Vagrant allows to build and manage → hardware/virtual-machine[virtual machines].

Vagrant is a project from HashiCorp, as is also → development/Cloud/Terraform.

The default virtualisation provider for Vagrant is → Companies-Products/Oracle/VM-VirtualBox[Oracle VirtualBox].

sa:

  → hardware/virtual-machine

links:

  → https://app.vagrantup.com/boxes/search[Discover Vagrant Boxes]
