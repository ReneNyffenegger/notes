$ VB Editor Object Model: VBComponents.Add
@ add

`→ Microsoft/Office/Visual-Basic-Editor/Object-Model/VBComponents[vbComponents].add(component)` adds a «component» to the VB project:
-
The value of `component` is a member of the `vbext_ComponentType` enum (but `vbext_ct_ActiveXDesigner` does not seem possible?):
table { lrl
  `vbext_ct_ClassModule` ☰ 2 ☰ A → development/languages/VBA/language/module/class.
  `vbext_ct_MSForm` ☰ 3 ☰ A form
  `vbext_ct_StdModule` ☰ 1 ☰ A standard (procedural) → development/languages/VBA/language/module.
  `vbext_pt_StandAlone` ☰ 100 ☰ A standalone → development/languages/VBA#vba-project[project].
table }


sa:
 `→ Microsoft/Office/Visual-Basic-Editor/Object-Model/VBComponents`
