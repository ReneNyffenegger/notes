$ Examples for VBScript MS-Office App Creator: compileApp

This is an example that demonstrates the function `compileApp` of the → Microsoft/Office/VBScript-App-Creator[VBScript MS-Office App Creator]. This function
compiles a → development/languages/VBA/index#vba-project[VBA project] and returns `false` if it has errors.

{ erroneous-module.vb

  The following → development/languages/VBA/language/module[VBA module] is erroneous: it contains an incorrect `end function` which should be `end sub`.

  gh|VBS-MS-Office-App-Creator|/examples/compileApp/erroneous-module.vb||

}
{ create-and-compile-app.wsf

 `create-and-compile-app.wsf` uses the → Microsoft/Office/VBScript-App-Creator[VBScript MS-Office App Creator] to insert the above module and then compile the → development/languages/VBA/index#vba-project[VBA project].

  Because the module has an error, it reports it so to the command line. Note also that the → Microsoft/Office/Visual-Basic-Editor[VB Editor] displays a message box that contains
  the error message.

  gh|VBS-MS-Office-App-Creator|/examples/compileApp/create-and-compile-app.wsf||

}

