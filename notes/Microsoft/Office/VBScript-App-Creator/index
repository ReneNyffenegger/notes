$ VBScript MS-Office App Creator

{ Subs and Functions

  table { ll

    → #createOfficeApp[`createOfficeApp(prod, fileName)`] ☰ Creates an Office document (`.docm`, `xlsx` or `.accdb`, depending on the value of `prod`)
    `insertModule(app, moduleFilePath, moduleName, moduleType)` ☰ Add a → development/languages/VBA/language/module[VBA module] to the application.
    `addReference(app, guid, major, minor)` ☰ Add a reference to a → Windows/development/COM/type-library[type library] aka typelib.
    `currentDir()` ☰ Returns the path of the current directory with a trailing back slash (`\`)
    `replaceThisWorksheetModuleMOdule(app, moduleFilePath)` ☰ Set the content of an Excel's `ThisWorksheet`

  table }

  { createOfficeApp #createOfficeApp

  `createOfficeApp(prod, fileName)` creates an Office document or an → development/databases/Access database.

   The first parameter (`application`) is a string that determines what is to be created. Its value must be one of `word`, `excel` or `access`.
   -
   The second parameter (`fileName`) controls the filename of the document/database that is created. If the file already exists, it is deleted.

code {
set doc = createOfficeApp("word"  , "p:\ath\to\word\document.docm"   )
set xls = createOfficeApp("excel" , "p:\ath\to\excel\worksheet.xlsm" )
set acc = createOfficeApp("access", "p:\ath\to\access\database.accdb")
code }

  }

}
{ Source code

  This is the source code of `create-MS-Office-app.vbs`:
  gh|VBS-MS-Office-App-Creator|/create-MS-Office-app.vbs||

}
{ Examples

  → Microsoft/Office/VBScript-App-Creator/examples/Access[Access]

  Excel: → Microsoft/Office/VBScript-App-Creator/examples/Excel/simple[(simple)],
  → Microsoft/Office/VBScript-App-Creator/examples/Excel/workbook_open[`workbook_open()`)] and
  → Microsoft/Office/VBScript-App-Creator/examples/Excel/add-module[add/replace modules]

  → Microsoft/Office/VBScript-App-Creator/examples/Word[Word (simple)]

}
