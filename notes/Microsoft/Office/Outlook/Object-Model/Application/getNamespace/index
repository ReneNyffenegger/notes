$ Outlook Object Model: Application.GetNamespace

`→ Microsoft/Office/Outlook/Object-Model/Application[application].getNamespace(type)` returns a → Microsoft/Office/Outlook/Object-Model/Namespace[`namespace`] object. The only supported value for `type` is `"Mapi"`.

sa:

  The `namespace` object that is returned by `application.getNamespace("MAPI")` is the same as that returned by `→ Microsoft/Office/Outlook/Object-Model/Application[application].session`.

