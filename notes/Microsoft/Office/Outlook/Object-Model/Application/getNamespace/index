$ Outlook Object Model: application.getNamespace

`application.getNamespace(type)` returns a → Microsoft/Office/Outlook/Object-Model/Namespace[`namespace`] object. The only supported value for `type` is `"Mapi"`.
