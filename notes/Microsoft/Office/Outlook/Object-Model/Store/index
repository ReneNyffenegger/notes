$ Outlook Object Model: Store

{ Methods and properties

  table { ll

    `application` ☰
    `categories` ☰
    `class` ☰
    `createUnifiedGroup()` ☰
    `deleteUnifiedGroup()` ☰
    `displayName` ☰ The store's *display name*
    `exchangeStoreType` ☰ `olExchangeMailbox`, `olExchangePublicFolder`, `olNotExchange`, `olPrimaryExchangeMailbox` or `olAdditionalExchangeMailbox`
    `filePath` ☰ Readonly: path of the *personal folders file* (→ Microsoft/Office/Outlook#outlook-ost-pst-files[`.pst`]) or the *offline folder file* (→ Microsoft/Office/Outlook#outlook-ost-pst-files[`.ost`])
    `getDefaultFolder()` ☰ returns the store's default → Microsoft/Office/Outlook/Object-Model/Folder[folder].
    `getRootFolder()` ☰ returns the store's root-level → Microsoft/Office/Outlook/Object-Model/Folder[folder].
    `getRules()` ☰
    `getSearchFolders()` ☰ returns a folders collection for the *search folders* that are defined in the store.
    `getSpecialFolder()` ☰ returns a «special» folder
    `isCachedExchange` ☰
    `isConversationEnabled` ☰
    `isDataFileStore` ☰
    `isInstantSearchEnabled` ☰
    `isOpen` ☰
    `parent` ☰
    `propertyAccessor` ☰
    `refreshQuotaDisplay()` ☰
    `session` ☰
    `storeID` ☰ String which identifies the store

  table }

}
