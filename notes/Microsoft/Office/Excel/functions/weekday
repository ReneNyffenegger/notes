$ Excel function: weekday

`weekday(dt)` returns the number of week day for a date.

There seems to be no built-in function that returns the name of a week-day. With `choose()`, it's possible to create such a function:
code {
=weekday(dt, "Su", "Mo", "Tu", "We", "Th", "Fr", "Sa", "Su")
code }

