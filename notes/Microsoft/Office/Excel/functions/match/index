$ Excel function: match

`match(value; array; match_type)` returns the index of `value` in `array`.

{ match_type

  `match_type` is optional and needs to be set to one of the following values:
  table { ll
     `-1` ☰ less than match
     `0` ☰ exact match
     `1` ☰ greater than match
  table }

  The default for `match_type` is `1`.

}

{ Simple example

  In the following simple example, `=index("Baz", a1;a5)` returns `3` because `Bas` is the third element in the range `a1:a5`.

  gh|about-Excel|/functions/match/simple.bas||

  gh|about-Excel|/functions/match/img/simple.png||

  A more elaborate version is → Microsoft/Office/Excel/functions/index-match-lookup-etc/match[here].

}


sa:

  → Microsoft/Office/Excel/functions/lookup[`lookup`] and → Microsoft/Office/Excel/functions/index_[`index`]

  → Microsoft/Office/Excel/functions[Excel functions]
