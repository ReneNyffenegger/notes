$ Excel function: ISREF
@ isref

`isref(arg)` evaluates to `TRUE` if `arg` is a reference, to `FALSE` otherwise.

When running with A1 → Microsoft/Office/Excel#excel-cell-address[reference style], the following evaluates to true:
code {
=isref(C7)
code }

sa:

  Excel's → Microsoft/Office/Excel/functions/is[`is` functions]

  → Microsoft/Office/Excel/functions[Excel functions]
