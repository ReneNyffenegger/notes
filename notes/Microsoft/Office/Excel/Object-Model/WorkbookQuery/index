$ Office Object Model: Excel - WorkbookQuery

A `WorkbookQuery` object represents a query that was crated by → Microsoft/Power-Query. Its `formula` property stores the → Microsoft/Power-Query/M-formula/language[Power Query M formula].

Compare the `workbookQuery` object with the → Microsoft/Office/Excel/Object-Model/QueryTable[`queryTable`] and → Microsoft/Office/Excel/Object-Model/ListObject[`listObject`] objects.

Note that the collection that contains `workbookQuery` objects is named `queries` (not `workbookQueries`!) and is found in the → Microsoft/Office/Excel/Object-Model/Workbook[`workbook`] object.

{ Methods and Properties

  table { ll

   `Application` ☰
   `Creator` ☰
   `Delete()` ☰
   `Description` ☰
   `Formula` ☰ The → Microsoft/Power-Query/M-formula/language[Power Query M formula] that provides the data for the object
   `Name` ☰
   `Parent` ☰

  table }

}

sa:

  → Microsoft/Office/Excel/Object-Model
