$ Excel Object Model: Workbook.runAutoMacros

Calling `→ Microsoft/Office/Excel/Object-Model/Workbook[workbook].runAutoMacros` calls
  • `auto_open`
  • `auto_close`
  • `auto_activate`
  • `auto_deactivate`

The four functions are considered obsolete; the four workkbook events `open`, `activate`, `deactivate` and `close` should be used.
