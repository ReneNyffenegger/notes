$ Excel Object Model: write to multiple worksheets at once

With `→ Microsoft/Office/Excel/Object-Model/Workbook[workbook].worksheets(→ development/languages/VBA/language/arrays[array]( "sheet name 1", …))` it is possible to select multiple sheets which then allows to change
values on them concurrently.

gh|about-MS-Office-object-model|/Excel/Workbook/worksheets/write-to-multiple-sheets.bas||

sa:
  This technique is also used to → Microsoft/Office/Excel/Object-Model/Workbook/exportAsFixedFormat[export a subset of worksheets as PDF].
