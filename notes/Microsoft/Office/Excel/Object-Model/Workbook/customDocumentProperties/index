$ Excel Object Model: workbook.customDocumentProperties

A → Microsoft/Office/Excel/Object-Model/Workbook[workbook's] `.customDocumentProperties` stores all *custom document properties* of the corresponding workbook.

sa:

  Via `.customDocumentProperties`, it is possible to assign an → Microsoft/Azure/products/Information-Protection[Azure Information Protection] label to a workbook,
  → Microsoft/Office/Object-Model/DocumentProperties/set-Azure-Information-Protection-Label[here's an example] that does that with
  → development/languages/VBA[Visual Basic for Applications].

  `workbook.builtinDocumentProperties` stores the collection of *built-in* document properties.
