$ Excel Object Model: Shapes

{ Methods and properties

  table { ll
    → Microsoft/Office/Excel/Object-Model/Shapes/add[`addX()`] ☰ Various `add…` functions allow to add shapes, such as `addPicture()`, `addTextbox()` etc.
   `application` ☰
   `buildFreeform()` ☰ Returns a `freeformBuilder` object 
   `count` ☰
   `creator` ☰
   `item()` ☰
   `parent` ☰
   `range` ☰ Returns a `shapeRange` object
   `selectAll()` ☰
  table }

}

{ AddChart2 #xls-obj-model-shapes-addChart2

code {
AddChart2 (      _
   Style       := … , _
   → Microsoft/Office/Excel/Object-Model/Chart#excel-chart-type[XlChartType] := … , _
   Left        := … , _
   Top         := … , _
   Width       := … , _
   Height      := … , _
   NewLayout   := …
)
code }

}

sa:

  → Microsoft/Office/Excel/Object-Model/Shapes/add[`add…` methods]

  → Microsoft/Office/Excel/Object-Model
