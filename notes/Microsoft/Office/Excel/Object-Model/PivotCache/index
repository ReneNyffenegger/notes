$ Office Object Model: Excel - PivotCache

A pivot cache provides the data for a → Microsoft/Office/Excel/Pivot/Table[PivotTable] report (see also the → Microsoft/Office/Excel/Object-Model/PivotTable[`PivotTable`] object).

? It appears that there is *one* pivot cache per *data source*. This pivot cache can then be used by multiple pivot tables.

The definition of a pivot cache can be stored as a *Office Data Connection file* (→ development/filename-extensions#odc[`.odc`]), see the `SaveAsODC()` method.

{ Properties and methods

  table { ll

    `ADOConnection` ☰ Returns an ADO connection object (if the cache is connected to an OLE DB data source)
    `Application` ☰
    `BackgroundQuery` ☰ A read/write boolean that controls if reports are performed in the background (i.e. asychronously)
    `CommandText` ☰
    `CommandType` ☰ One of the `XlCmdType` enumeration values `xlCmdCube`, `xlCmdDefault`, `xlCmdSql` or `xlCmdTable`
    `Connection` ☰ OLE DB connection string, or ODBC setting, or URL, or path to a text file, or path to a file that specifies a database or a web query
    `CreatePivotChart()` ☰ Creates a standalone `PivotChart`, returns a `Shape` object
    `CreatePivotTable()` ☰ Creates and returns a `PivotTable` object
    `Creator` ☰
    `EnableRefresh` ☰
    `Index` ☰
    `IsConnected` ☰ Compare with `MakeConnection()`
    `LocalConnection` ☰
    `MaintainConnection` ☰
    `MakeConnection()` ☰ Compare with `IsConnected`
    `MemoryUsed` ☰
    `MissingItemsLimit` ☰
    `OLAP` ☰
    `OptimizeCache` ☰
    `Parent` ☰
    `QueryType` ☰ A value of the `XlQueryType` enumeration
    `RecordCount` ☰
    `Recordset` ☰
    `Refresh()` ☰ Redraw(?) the the pivot cache.
    `RefreshDate` ☰
    `RefreshName` ☰
    `RefreshOnFileOpen` ☰
    `RefreshPeriod` ☰
    `ResetTimer()` ☰
    `RobustConnect` ☰
    `SaveAsODC()` ☰
    `SavePassword` ☰
    `SourceConnectionFile` ☰
    `SourceData` ☰
    `SourceDataFile` ☰
    `SourceType` ☰ An element of the `XlPivotTableSourceType` enumeration
    `UpgradeOnRefresh` ☰
    `UseLocalConnection` ☰
    `Version` ☰ An element of the `XlPivotTableVersionList` enumeration
    `WorkbookConnection` ☰

  table }

}

sa:

 → Microsoft/Office/Excel/Object-Model

 `→ Microsoft/Office/Excel/Object-Model/Workbook[workbook].pivotCaches`
