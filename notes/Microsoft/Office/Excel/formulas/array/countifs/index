$ Excel array formulas: countifs

{ Count the number of occurences of specific strings

  In the following example, `countifs()` is used to compare tha values in the range that is filled in `testData` with a contanst array (the curly braces `{"foo", "bar", "baz"}`).

  With this construct, `countifs()` returns an array whose values are filled into the cells in the first row (columns 2 through 4).

  gh|about-Excel|/formulas/array/countifs/foo-bar-baz.bas||

  gh|about-Excel|/formulas/array/countifs/img/foo-bar-baz.png||

}
sa:
  → Microsoft/Office/Excel/functions/countif[`countif`]
