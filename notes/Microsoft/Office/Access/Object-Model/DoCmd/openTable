$ Access: DoCmd.OpenTable

Show the table whose name is `contracts`
code {
DoCmd.OpenTable "contracts"
code }

sa:

 `DoCmd.OpenTable` might be used in conjunction with → Microsoft/Office/Access/Object-Model/DoCmd/applyFilter[`DoCmd.ApplyFilter`].

  There is also `DoCmd.OpenView()` and `DoCmd.OpenQuery()`
  
  The → Microsoft/Office/Access/Object-Model/DoCmd[`DoCmd`] object.
