$ Access Object Model: Database

A `Database` object seems to encapsulate the tables, forms, queries etc. of an Access (`.accdb`) file.

The database object for the currently open `.accdb` is returned by → Microsoft/Office/Access/Object-Model/Application#access-currentDb[`application.currentDb()`].

The `Database` object belongs to DAO rather than Access.


{ name

  The `name` property returns the path of the `accdb` file.

  gh|about-MS-Office-object-model|/Access/Database/name.bas||

}
{ properties

  gh|about-MS-Office-object-model|/Access/Database/properties.bas||

}
sa:
  → Microsoft/Office/Access/Object-Model
