$ Access Object Model: Application.RunCommand

`application.runCommand()` runs a *built-in* menu or toolbar commands. `runCommand` takes a single parameter whose type is the
→ https://docs.microsoft.com/en-us/office/vba/api/access.accommand[`acCommand` enumeration].

`runCommand()` replaces `→ Microsoft/Office/Access/Object-Model/DoCmd[DoCmd].DoMenuItem`.

sa:

  Compare with → Microsoft/Office/Access/Object-Model/DoCmd/runCommand[`DoCmd.RunCommand`].

  The → Microsoft/Office/Access/Object-Model/Application[`Application`] object.
