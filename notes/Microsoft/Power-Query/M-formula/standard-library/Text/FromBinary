$ Power Query M formula language standard library: Text.FromBinary
@ Text.FromBinary

code {
let
   readTextFile = (filename) => Text.FromBinary(→ Microsoft/Power-Query/M-formula/standard-library/File/Contents(filename))
in
   readTextFile
code }

sa:
  The Power Query → Microsoft/Power-Query/M-formula/standard-library
