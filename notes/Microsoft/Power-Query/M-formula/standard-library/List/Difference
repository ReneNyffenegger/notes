$ Power Query M formula language standard library: List.Difference
@ List.Difference

In the following → Microsoft/Power-Query expression, `diff` is evaluated to the list `{"a", "c", "b"}`.
code {
let
   L1 = {"x", "y", "z"},
   L2 = {"a", "y", "c", "x", "b"},
   diff = List.Difference(L2, L1)
in
   diff
code }

sa:

  The Power Query → Microsoft/Power-Query/M-formula/standard-library
