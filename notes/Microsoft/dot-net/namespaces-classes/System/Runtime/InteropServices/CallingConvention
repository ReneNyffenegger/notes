$ System.Runtime.InteropServices.CallingConvention (enum)

The `System.Runtime.InteropServices.CallingConvention` enum specifies a → development/languages/C-C-plus-plus/calling-convention and is used (for example? or exclusively?) in the
field → `CallingConvention` of the → Microsoft/dot-net/namespaces-classes/System/Runtime/InteropServices/DllImportAttribute[`System.Runtime.InteropServices.DllImportAttribute`] class.

This enum has the following fields
  • `Cdecl`
  • `FastCall`
  • `StdCall`
  • `ThisCall`
  • `Winapi` (which uses the default calling convention of the respective platform: `Stdcall` on 32-bit Windows and `Cdecl` on 32-bit Linux)
