$ System.IntPtr (struct)

`System.IntPtr` represents a handle or a pointer.

The size (number of bits) of `System.IntPtr` corresponds to the bitness of the operating system that PowerShell runs on. Thus, it is useful to determine the → Windows/dirs/Windows/System32/cmd_exe/tasks/determine-if-32-or-64-bit-Windows[OS's bitness].
