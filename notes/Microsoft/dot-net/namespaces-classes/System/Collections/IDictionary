$ System.Collection.IDictionary (interface)

`System.Collection.IDictionary` defines an interface that is able to store key/value pairs. These key/value pairs are stored in → Microsoft/dot-net/namespaces-classes/System/Collections/DictionaryEntry[`System.Collections.DictionaryEntry`] objects.

`System.Collection.IDictionary` implements → Microsoft/dot-net/namespaces-classes/System/Collections/ICollection[`System.Collections.ICollection`] and
→ Microsoft/dot-net/namespaces-classes/System/Collections/ICollection[`System.Collections.ICollection`].

{ Iterating over an IDictionary interface

  In → development/languages/C-Sharp[C#], it's possible to iterate over key/value pairs with → development/languages/C-Sharp/language/keywords/statement/iteration/foreach-in[`foreach … in`].
  -
  This is demonstrated in the following example: The method `GetEnvironmentVariables()` method of → Microsoft/dot-net/namespaces-classes/System/Environment[`System.Environment`] returns
  the set of → Windows/development/environment-variables[environment variables] as an `IDictionary`. The `foreach` loop iterates over this set and prints the name of the environment variable (`Key`) and its value (`Value`).

  gh|.NET-API|/System/Collections/IDictionary/iterate-over.cs||

}
