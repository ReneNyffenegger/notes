$ System.Collections.Generic.List<T>

`System.Collections.Generic.List<T>` is the generic (and → https://github.com/dotnet/platform-compat/blob/master/docs/DE0006.md[preferred]) equivalent of the `System.Collections.ArrayList` class.

{ Creating a List<T> in PowerShell

  The following simple example tries to demonstrate how a `List<T>` can be created in → Windows/PowerShell and how elements are added to the list and how the list is iterated over.

  Additionally, it also shows that the elements are inserted as references so that they can be changed outside the list.

  gh|.NET-API|System/Collections/Generic/List-T/create.ps1||

}
