$ System.Collections.Generic.IEnumerable<T> (interface)

`System.Collections.Generic.IEnumerable<T>` is the base interface for collections in the
  `System.Collections.Generic` namespace such as
   • → Microsoft/dot-net/namespaces-classes/System/Collections/Generic/List-T[`List<T>`]
   • → Microsoft/dot-net/namespaces-classes/System/Collections/Generic/Dictionary-TKey-TValue[`Dictionary<TKey,TValue>`]
   • `Stack<T>`

Other generic collections that impllement `IEnumerable<T>` include
  • `ObservableCollection<T>` and
  • `ConcurrentStack<T>`.

A class that implements `IEnumerable<T>` can be enumerated in → development/languages/C-Sharp with the → development/languages/C-Sharp/language/keywords/statement/iteration/foreach-in[`foreach … in`] statement.

sa:

  `IEnumerable<T>` implements → Microsoft/dot-net/namespaces-classes/System/Collections/IEnumerable[`System.Collections.IEnumerable`].

  → Microsoft/dot-net/namespaces-classes/System/Linq/Enumerable[`System.Linq.Enumerable`] provides functionality that uses and enhances that if `IEnumerable<T>`.

  The → development/languages/C-Sharp/language/keywords/contextual/yield[`yield` keyword] in → development/languages/C-Sharp[C#].
