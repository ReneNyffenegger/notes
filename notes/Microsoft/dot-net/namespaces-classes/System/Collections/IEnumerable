$ Systems.Collections.IEnumerable (interface)

`Systems.Collections.IEnumerable` is the interface for all non-generic enumerable collections.
-
This interface is used by the → development/languages/C-Sharp → development/languages/C-Sharp/language/keywords/statement/iteration/foreach-in[`foreach … in`] and the Visual Basic `for each` statement.

`Systems.Collections.IEnumerable` is also the basis for LINQ.

{ Interface

  `System.Collections.IEnumerable` has one method only, `GetEnumerator()`, which returns a → Microsoft/dot-net/namespaces-classes/System/Collections/IEnumerator[`System.Collections.IEnumerator`].

}


sa:

  → Microsoft/dot-net/namespaces-classes/System/Collections/Generic/IEnumerable-T[`Systems.Collections.Generic.IEnumerable<T>`] (which implements `Systems.Collections.IEnumerable`) is the interface for all generic enumerable collections. 

  The → development/languages/C-Sharp/language/keywords/contextual/yield[`yield` keyword] in → development/languages/C-Sharp[C#].

  A type that implements `IEnumerable` and provides an `Add(xy elem)` method can be constructed with a
  → development/languages/C-Sharp/language/classes-structs/initializer/collection[collection initializer].

  The → Windows/registry/tree-structure/default-value of the → Windows/registry/tree-structure/key[registry key]
  → Windows/registry/tree/HKEY_CLASSES_ROOT/Interface/496B0ABE-CDEE-11D3-88E8-00902754C43A[`HKEY_CLASSES_ROOT\Interface\{496B0ABE-CDEE-11D3-88E8-00902754C43A}`] is
  *IEnumerable*.
