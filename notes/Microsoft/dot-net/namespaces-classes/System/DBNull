$ System.DBNull (class)

`System.DBNull` represents a non-existing value.

The concept of non-existing values is most probably borrowed from → development/databases/SQL[SQL's] special → development/databases/SQL/data-types/null[null value/data type].

`DBNull` is a singleton.

`DBNull` and `null` are not the same thing.

{ Equivalence to System.Convert.DBNull #DBNull-equivalency

  `System.DBNull` is equivalent to → Microsoft/dot-net/namespaces-classes/System/Convert/DBNull[`System.Convert.DBNull`], as can be demonstrated with the following
  → Windows/PowerShell statement:
code {
[System.Convert]::DBNull.Equals([System.DBNull]::Value)
code }

}

sa:
  Compare with the → development/languages/VBA/language/null-and-nothing-etc#vba-nothing[VBA `null` value].
