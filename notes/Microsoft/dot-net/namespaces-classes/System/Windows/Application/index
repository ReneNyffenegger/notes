$ System.Windows.Application (class)

A WPF application.

`System.Windows.Application` derives from `System.Windows.Threading.DispatcherObject`.

An `Application` deals with
  • Application's lifetime
  • processing command line parameters
  • unhandled excpetions
  • sharing resources and properties
  • managing windows in standalone applications
  • navigation
