$ System.Security.Permissions.StrongNameIdentityPermission (class)

A `System.Security.Permissions.StrongNameIdentityPermission` defines the identity permissions for → Microsoft/dot-net/assembly/index#assembly-name[strong names].

{ TODO

  Microsoft's documentation contains the following remark:
  *Starting with the .NET Framework 4, identity permissions are not used.*

}
