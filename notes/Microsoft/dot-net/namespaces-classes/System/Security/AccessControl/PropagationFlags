$ System.Security.AccessControl.PropagationFlags (enum)

The value of the `System.Security.AccessControl.PropagationFlags` enum specifies how *access control entries* are propagated to child objects *if*
one of the → Microsoft/dot-net/namespaces-classes/System/Security/AccessControl/InheritanceFlags[`System.Security.AccessControl.InheritanceFlags`] flags is set.

The enum has three values which can be combined:
  • None
  • NoPropagateInherit
  • InheritOnly
