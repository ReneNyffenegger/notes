$ System.IO.Path (class)

sa:
  → Microsoft/dot-net/namespaces-classes/System/IO/Path/IsPathRooted[`IsPathRooted(path)`] returns `true` if `path` is absolute.

  → Microsoft/dot-net/namespaces-classes/System/IO/Path/ChangeExtension[`ChangeExtension()`] modifies a file name so that has
  a different → development/filename-extensions[extension].

