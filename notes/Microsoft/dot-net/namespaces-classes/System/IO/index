$ System.IO (namespace)

The `System.IO` namespace provides classes that are used in relation to files and directories.

links:
  → https://docs.microsoft.com/en-us/dotnet/standard/io/common-i-o-tasks[Common I/O tasks]
