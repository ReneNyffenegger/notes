$ System.Management.Automation.Host.PSHostRawUserInterface (class)

`System.Management.Automation.Host.PSHostRawUserInterface` manages a PowerShell's console (character-) buffer and input from a keyboard.

The characters in the buffer are represented by → Microsoft/dot-net/namespaces-classes/System/Management/Automation/Host/BufferCell[`System.Management.Automation.Host.BufferCell`] structs.

{ Properties and Methods

  `BackgroundColor` ☰
  `BufferSize` ☰ Size of the buffer measured in characters
  `CursorPosition` ☰ Sets or gets the position of the cursor. If the cursor is set, the view onto the buffer is moved so that the new position is visible.
  `CursorSize` ☰ Cursor size as percentage (of what?)
  `FlushInputBuffer()` ☰
  `ForegroundColor` ☰
  `GetBufferContents()` ☰
  `KeyAvailable` ☰ Non-blocking test if a keystroke is wating in the input buffer to be read by → Microsoft/dot-net/namespaces-classes/System/Management/Automation/Host/PSHostRawUserInterface/ReadKey[`ReadKey()`]
  `LengthInBufferCells()` ☰
  `MaxPhysicalWindowSize` ☰
  `MaxWindowSize` ☰
  `NewBufferCellArray()` ☰
  → Microsoft/dot-net/namespaces-classes/System/Management/Automation/Host/PSHostRawUserInterface/ReadKey[`ReadKey()`] ☰
  `ScrollBufferContents()` ☰
  `SetBufferContents()` ☰
  `WindowPosition` ☰
  `WindowSize` ☰
  `WindowTitle` ☰

}

sa:
  → Microsoft/dot-net/namespaces-classes/System/Management/Automation/Host/PSHostUserInterface[`System.Management.Automation.Host.PSHostUserInterface`]
