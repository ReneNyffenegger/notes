$ System.Management.Automation.PSDriveInfo (class)
@ System.Management.Automation.PSDriveInfo

Information about a → Windows/PowerShell/language/provider/drive

{ Properties and Methods

  table { ll
   `CompareTo()` ☰
   `Credential` ☰ A `→ Microsoft/dot-net/namespaces-classes/System/Management/Automation/PSCredential` object that is used to access the drive.
   `CurrentLocation` ☰ A string whose value corresponds to the *current working directory* of the session in the drive
   `Description` ☰
   `DisplayRoot` ☰ If the drive is targeting a network drive, this value (string) corresponds to the drive's UNC path (read/write).
   `Equals()` ☰
   `GetHashCode()` ☰
   `MaximumSize` ☰
   `Name` ☰
   `Provider` ☰ The `→ Microsoft/dot-net/namespaces-classes/System/Management/Automation/ProviderInfo` object that corresponds to the → Windows/PowerShell/language/provider which implements the functionality for the drive.
   `Root` ☰
   `ToString()` ☰
   `VolumeSeparatedByColon` ☰
  table }

}

sa:
  The `Drives` member of the `→ Microsoft/dot-net/namespaces-classes/System/Management/Automation/ProviderInfo` object.
