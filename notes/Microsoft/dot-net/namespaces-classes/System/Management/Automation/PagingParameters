$ System.Management.Automation.PagingParameters (class)

The `System.Management.Automation.PagingParameters` class seems to be related to using → Microsoft/dot-net/namespaces-classes/System/Management/Automation/CmdletBindingAttribute[`System.Management.Automation.CmdletBindingAttribute`]
with the `SupportsPaging` attribute:

code {
function xyx {

   [CmdletBinding(SupportsPaging = $true)]

    …
   $skip  = → Windows/PowerShell/language/variable/automatic/PsCmdLet[$PSCmdlet].PagingParameters.Skip
   $first = → Windows/PowerShell/language/variable/automatic/PsCmdLet[$PSCmdlet].PagingParameters.First
    …
     
}
code }
