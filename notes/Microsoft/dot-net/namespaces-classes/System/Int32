$ System.Int32 (struct)

In → Windows/PowerShell, the → Windows/PowerShell/language/lexical-structure/token/literal#ps-int-literal-datatype[data type suffix for integers] to indicate a `Int32` is `u`:
code {
PS C:\> (42u).GetType().FullName
System.UInt32
code }

sa:

  → Microsoft/dot-net/namespaces-classes/System/_integral-numeric-types

  The → development/databases/SQL-Server/T-SQL/data-types[SQL Server data type] that is suited best to store a `Int32` is
  → development/databases/SQL-Server/T-SQL/data-types/integer[`integer`].

  → Windows/PowerShell has two → Windows/PowerShell/language/object/type/accelerators[type accelerators] that correspond to `System.Int32`: `[int]` and `[int32]`.
