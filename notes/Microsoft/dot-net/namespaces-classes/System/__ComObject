$ System.__ComObject (internal class)

`System.__ComObject` derives from → Microsoft/dot-net/namespaces-classes/System/MarshalByRefObject[`System.MarshalByRefObject`] and
is the root class for all → Windows/development/COM[COM] wrappers.

? `System.__ComObject` wraps an → Windows/development/COM/IUnknown[COM `IUnknown` interface] (and has a (internal) method `GetIUnknown` method).

? Because `System.__ComObject` is an `internal` class (and its constructor is `protected`), a `__ComObject` cannot be directly instantiated.

links:
  → https://referencesource.microsoft.com/#mscorlib/system/__comobject.cs[Reference source]
