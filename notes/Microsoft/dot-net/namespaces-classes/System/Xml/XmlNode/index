$ System.Xml.XmlNode (class)

{ Methods #dotnet-XmlNode-methods

  table { ll

    `AppendChild`, `PrependChild` ☰ Appends the given → development/XML/DOM/nodes (`XmlNode`) to the end/beginning of the object's child nodes. Compare with `InsertAfter` and `InsertBefore`.
    `Clone` ☰
    `CloneNode` ☰ duplicates a → development/XML/DOM/nodes (when overriden is derived class)
    `CreateNavigator` ☰ Creates a → Microsoft/dot-net/namespaces-classes/System/Xml/XPath/XPathNavigator[`System.Xml.XPath.XPathNavigator`] instance to navigate *this* object.
    `GetEnumerator` ☰
    `GetNamespaceOfPrefix` ☰
    `GetPrefixOfNamespace` ☰
    `InsertAfter`, `InsertBefore` ☰ Inserts the given node after/before a given node. Compare with `AppendChild`
    `Normalize` ☰
    `RemoveAll` ☰
    `RemoveChild` ☰
    `ReplaceChild` ☰
    `SelectNodes` ☰ Returns a `System.Xml.XmlNodeList` that contains the nodes that match an → development/XML/XPath expression. This method is a Microsoft extension to the → development/XML/DOM[Document Object Model]
    `SelectSingleNode` ☰ Returns the first node that maches the given XPath expression. Also a Microsoft extension to the DOM. See also → development/languages/VBA/Useful-object-libraries/MSXML/DOM/selectSingleNode[using `selectSingleNode` in VBA].
    `Supports` ☰
    `WriteContentTo` ☰
    `WriteTo` ☰

  table }

}

sa:

  A `System.Xml.XmlNode` instance might be created by `→ Microsoft/dot-net/namespaces-classes/System/Xml/XmlDocument[System.Xml.XmlDocument].CreateNode`.

  → Microsoft/dot-net/namespaces-classes/System/Xml/XmlNode

  The `Node` property of → Microsoft/dot-net/namespaces-classes/Microsoft/PowerShell/Commands/SelectXmlInfo[`Microsoft.PowerShell.Commands.SelectXmlInfo`].
