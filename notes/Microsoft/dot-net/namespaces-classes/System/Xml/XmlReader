$ System.Xml.XmlReader (class)

`System.Xml.XmlReader` allows to read an → development/XML/DOM/nodes/document in forward-only (non-cashed) and read-only fashion.

Because `XmlReader` is non-cached and read-only, it does not allow to modify an → development/XML/DOM/nodes/document.

sa:
  → Microsoft/dot-net/namespaces-classes/System/Xml/XmlWriter[`System.Xml.XmlWriter`]
