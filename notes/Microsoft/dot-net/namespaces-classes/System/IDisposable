$ System.IDisposable (interface)

The interface `System.IDisposable` is typically implemented by managed types that access unmanaged types such as
→ Microsoft/dot-net/namespaces-classes/System/IO/File[`System.IO.File`] or `System.Drawing.Font`.

Implementors of `System.IDisposable` are required to implement one method only: `public void Dispose()`.

sa:
  → Microsoft/dot-net/Common-Language-Runtime/garbage-collector
