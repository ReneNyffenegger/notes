$ System.Nullable<T> (struct)

Allows to declare variables of types that are otherwise not nullable (such as those that derive from → Microsoft/dot-net/namespaces-classes/System/ValueType[`System.ValueType`]?).

In → development/languages/C-Sharp, `T?` is a shorthand notation for `System.Nullable<T>`.

gh|.NET-API|/System/Nullable-T/simple.cs||
