$ System.Net.HttpListenerResponse (class)
@ System.Net.HttpListenerResponse

*Note*: The `ContentLength64` must be set explicitely before writing to the resturned `Stream` object.
-
This seems to be the reason that calling `.Flush()` on `OutputStream` does not work as intended (at
least by me).

{ Properties and methods

  table { ll

    `Abort()` ☰
    `AddHeader` ☰
    `AppendCookie` ☰
    `AppendHeader` ☰
    `Close` ☰
    `ContentEncoding` ☰
    `ContentLength64` ☰
    `ContentType` ☰
    `Cookies` ☰
    `CopyFrom` ☰
    `Headers` ☰
    `KeepAlive` ☰
    `OutputStream` ☰
    `ProtocolVersion` ☰
    `Redirect` ☰
    `RedirectLocation` ☰ The `StatusCode` is not automatically set (to for example 307 or 308) if a value is assigned to this property
    `SendChunked` ☰
    `SetCookie` ☰
    `StatusCode` ☰
    `StatusDescription` ☰

  table }

}

sa:

  The `Response` property of `→ Microsoft/dot-net/namespaces-classes/System/Net/HttpListenerContext`.

  `System.Web.HttpResponse`
