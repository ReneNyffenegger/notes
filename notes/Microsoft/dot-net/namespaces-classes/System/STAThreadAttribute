$ System.STAThreadAttribute (class)

The `System.STAThreadAttribute` → Microsoft/dot-net/namespaces-classes/System/Attribute[attribute] has only an effect if applied to an application's `Main()` method *and* if → Windows/development/COM interop is used.
