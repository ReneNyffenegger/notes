$ System.Data.Common (namespace)

The `System.Data.Common` namespace exhibits the functionality of *.NET data providers*.
-
Such a .NET data provider consists of a set classes that allow to access data sources (typically
→ development/databases[databases]) in the managed space.
