$ System.Data.Common.DbProviderFactory (abstract class)

Installed providers that implement `DbProviderFactory` can be found with
→ Microsoft/dot-net/namespaces-classes/System/Data/Common/DbProviderFactories#DbProviderFactories-GetFactoryClasses[`System.Data.Common.DbProviderFactories.GetFactoryClasses()`].

{ Methods and properties

  table { ll
   `CanCreateCommandBuilder` ☰ A boolean that informs if the class supports `DbCommandBuilder`
   `CanCreateDataAdapter` ☰ A boolean that informs if the class supports → Microsoft/dot-net/namespaces-classes/System/Data/Common/DbDataAdapter[`System.Data.Common.DbDataAdapter`]
   `CanCreateDataSourceEnumerator` ☰ A boolean that informs if the class supports `DbDataSourceEnumerator`
   `CreateCommand()` ☰ Returns an instance of the provider's class that derives from → Microsoft/dot-net/namespaces-classes/System/Data/Common/DbCommand[`System.Data.Common.DbCommand`].
   `CreateCommandBuilder()` ☰ Returns an instance of the provider's class that derives from `System.Data.Common.DbCommandBuilder`.
   `CreateConnection()` ☰ Returns an instance of the provider's class that derives from → Microsoft/dot-net/namespaces-classes/System/Data/Common/DbConnection[`System.Data.Common.DbConnection`].
   `CreateConnectionStringBuilder()` ☰ Returns an instance of the provider's class that derives from `System.Data.Common.DbConnectionStringBuilder`.
   `CreateDataAdapter()` ☰ Returns an instance of the provider's class that derives from → Microsoft/dot-net/namespaces-classes/System/Data/Common/DbDataAdapter[`System.Data.Common.DbDataAdapter`].
   `CreateDataSourceEnumerator()` ☰ Returns an instance of the provider's class that derives from `System.Data.Common.DbDataSourceEnumerator`.
   `CreateParameter()` ☰ Returns an instance of the provider's class that derives from → Microsoft/dot-net/namespaces-classes/System/Data/Common/DbParameter[`System.Data.Common.DbParameter`].
  table }

  { PowerShell script to print a factory's properties

    The following → Windows/PowerShell/script[PowerShell script] iterates over all `DbProviderFactory` instances that are returned by
    → Microsoft/dot-net/namespaces-classes/System/Data/Common/DbProviderFactories#DbProviderFactories-GetFactoryClasses[`DbProviderFactories.GetFactoryClasses()`]
    and prints their name, description and properties.

    gh|.NET-API|/System/Data/Common/DbProviderFactory/properties.ps1||

  }
}

sa:
  → Microsoft/dot-net/namespaces-classes/System/Data/Common/DbProviderFactories[`System.Data.Common.DbProviderFactories`]
