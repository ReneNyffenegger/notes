$ Microsoft.SqlServer.Dts.Runtime.TaskHost (class)
@ Microsoft.SqlServer.Dts.Runtime.TaskHost

`Microsoft.SqlServer.Dts.Runtime.TaskHost` inherits from `→ Microsoft/dot-net/namespaces-classes/Microsoft/SqlServer/Dts/Runtime/EventsProvider`.

`TaskHost` does not have a matching item in the graphical designer toolbox.

{ Methods and properties

  table { ll
     `ApartmentThreaded` ☰
     `ComputeExpressions()` ☰
     `ExecutionValue` ☰
     `ExecValueVariable` ☰
     `GetExecutionPath()` ☰
     `GetExpression()` ☰
     `GetPackagePath()` ☰
     `HasExpressions` ☰
     `HostType` ☰ A member of the `→ Microsoft/dot-net/namespaces-classes/Microsoft/SqlServer/Dts/Runtime/DTSObjectHostType` enumeration.
     `InnerObject` ☰
     `Properties` ☰  A `Microsoft.SqlServer.Dts.Runtime.DtsProperties` object which contains a collection of `→ Microsoft/dot-net/namespaces-classes/Microsoft/SqlServer/Dts/Runtime/DtsProperty` objects. Microsoft recommends to set (an inner object's) properties via `Properties` rather than casting this object to the corresponding type and set the properties using the derived type.
     `SetExpression()` ☰
  table }

}

