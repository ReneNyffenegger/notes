$ Microsoft.SqlServer.Dts.Tasks.ScriptTask.ScriptObjectModel (Class)
@ Microsoft.SqlServer.Dts.Tasks.ScriptTask.ScriptObjectModel

Access the objects in a (dtsx?) package through the global `Dts` object (which is an instance of `Microsoft.SqlServer.Dts.Tasks.ScriptTask.ScriptObjectModel`).

{ Properties and methods

  table { ll

    `Connections` ☰ A `→ Microsoft/dot-net/namespaces-classes/Microsoft/SqlServer/Dts/Runtime/Connections` object which represents the connections that are defined in the package that contains the Script task.
    `Events` ☰
    `ExecutionValue` ☰ A user defined object which allows to return additional information (with respect to `TaskResult`) about the result of a task execution.
    `Log()` ☰ Write an entry to the enabled log providers.
    `TaskResult` ☰ A `→ Microsoft/dot-net/namespaces-classes/Microsoft/SqlServer/Dts/Runtime/DTSxecResult` (enumeration) value. Additional information can be returned with `ExecutionValue`.
    `Transaction` ☰ The transaction associated with the container of the script task. (User defined object?)
    `VariableDispenser` ☰ A `Microsoft.SqlServer.Dts.Runtime.VariableDispenser` object which can be used to work with existing variables.
    `Variables` ☰ A `→ Microsoft/dot-net/namespaces-classes/Microsoft/SqlServer/Dts/Runtime/Variables` object.

  table }

}

sa:
  → development/databases/SQL-Server/Integration-Services

