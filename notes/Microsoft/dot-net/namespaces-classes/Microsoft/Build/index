$ Microsoft.Build (namespace)

The `Microsoft.Build.*` namespaces contain types that provide programmatic access to, and control of, the → Windows/development/Microsoft-Build-Engine[MSBuild] engine.
