$ Oracle.DataAccess.Client (namespace)

The `Oracle.DataAccess.Client` namespace contains → development/databases/Oracle/ODP_NET classes and enumerations for client-side providers.

The → Microsoft/dot-net/assembly is → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/ODP_NET/bin/v/Oracle_DataAccess_dll[`Oracle.DataAccess.dll`]

The classes in `Oracle.DataAccess.Client` inherit from the same classes that also other .NET data providers provide from, thus, they are used in a similar way.
-
For example, the classes of → Microsoft/dot-net/namespaces-classes/System/Data/SqlClient[`System.Data.SqlClient`] (which are used to access → development/databases/SQL-Server) start with the prefix
`Sql` while those for Oracle start with `Oracle`.

sa:
  The → development/databases/Oracle/ODP_NET/drivers/managed[managed] counterpart of `Oracle.DataAccess.Client` is → Microsoft/dot-net/namespaces-classes/Oracle/ManagedDataAccess/Client[`Oracle.ManagedDataAccess.Client`].
