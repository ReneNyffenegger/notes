$ OracleDataReader - GetBytes()

The method `GetBytes()` of the class → Microsoft/dot-net/namespaces-classes/Oracle/DataAccess/Client/OracleDataReader[Oracle.DataAccess.Client.OracleDataReader`] allows to select (read) binary data
that is stored in à → development/databases/Oracle/SQL/data-types/lob[blob] or → development/databases/Oracle/SQL/data-types/raw[long raw].

{ Simple example

  The following example selects binary data from a → development/databases/Oracle/SQL/data-types/lob[blob] or a → development/databases/Oracle/SQL/data-types/raw[long raw] into a
  `→ Microsoft/dot-net/namespaces-classes/System/Byte[Byte][]` and then writes the data into a file.
  gh|.NET-API|/Oracle/DataAccess/Client/OracleDataReader/GetBytes.cs||


  Two similar examples are → Microsoft/dot-net/namespaces-classes/Oracle/DataAccess/Types/OracleBlob[using `OracleBlob`] and
  → Microsoft/dot-net/namespaces-classes/Oracle/DataAccess/Types/OracleBinary[using `OracleBinary`].

}
