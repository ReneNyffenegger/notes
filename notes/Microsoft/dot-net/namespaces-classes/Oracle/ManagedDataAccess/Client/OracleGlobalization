$ Oracle.ManagedDataAccess.Client.OracleGlobalization (class)
@ Oracle.ManagedDataAccess.Client.OracleGlobalization


{ Interesting properties

  table { ll
    `Calendar` ☰ Clendar system
    `ClientCharacterSet` ☰ The client character set. *Not Available in ODP.NET, Managed Driver and ODP.NET Core*
    `Comparison` ☰ Specifies a method of comparison for `where` clauses and comparison in PL/SQL blocks
    `Currency` ☰ Specifies the string to use as a local currency symbol for the `L` number format element.
    `DateFormat` ☰ `→ development/databases/Oracle/adminstration/init-parameters/nls/format/date`
    `DateLanguage` ☰ The language with which day and month names/abbreviations are spelled.
    `DualCurrency` ☰ Specifies the dual currency symbol, such as Euro, for the `U` number format element
    `ISOCurrency` ☰ Specifies the string to use as an international currency symbol for the `C` number format element
    `Language` ☰ The default language of the database
    `LengthSemantics` ☰ Enables creation of `char` and `varchar2` columns using either byte or character (default) length semantics
    `NCharConversionException` ☰ Determines whether or not data loss during an implicit or explicit character type conversion reports an error
    `NumericCharacters` ☰ Specifies the characters used for the decimal character and the group separator character for numeric values in strings
    `Sort` ☰ Specifies the collating sequence for `order by` clause
    `Territory` ☰ Te name of the territory
    `TimeStampFormat` ☰ The string format for TimeStamp types
    `TimeStampTZFormat` ☰ The string format for TimeStampTZ types
    `TimeZone` ☰ The time zone region name
   table }
}
{ PowerShell snippet

code {
→ Windows/PowerShell/command-inventory/noun/type/add -path $pwd\Oracle.ManagedDataAccess.core.23.26.0.dll

$ora = → Windows/PowerShell/command-inventory/noun/object/new Oracle.ManagedDataAccess.Client.OracleConnection('User Id=‥;Password=‥;Data Source=‥')
$ora.Open()

$sessInfo = $ora.GetSessionInfo()
$sessInfo.DateFormat = 'yyyy-mm-dd hh24:mi:ss'
$ora.SetSessionInfo($sessInfo)
code }

}

sa:
  → development/databases/Oracle/adminstration/init-parameters/nls
