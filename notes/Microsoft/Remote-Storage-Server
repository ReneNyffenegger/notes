$ Microsoft Remote Storage Server (RSS)

*Microsoft Remote Storage Server* allows an administrator to define a set of rules which specify how and when infrequently used files are moved to long term storage such as tape or optical media.

RSS relies on → Windows/file-system/reparse-point[reparse points].
