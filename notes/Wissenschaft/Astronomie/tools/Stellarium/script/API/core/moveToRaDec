$ Stellarium API: core.moveToRaDec

`core.moveToRaDec()` look at the specified coordinates (right ascension and declination) in the sky.

gh|about-Stellarium|/API/core/moveToRaDec.ssc||

Apparently, when used in a script, `core.moveToRaDec()` must be preceeded by a call to → Wissenschaft/Astronomie/tools/Stellarium/script/API/core/wait[`core.wait()`] in order to have any effect. The amount
of the time to be waited can be minimal, for example `0.01` seconds.

sa:

  The respective angles of the currently rendered scene in → Wissenschaft/Astronomie/tools/Stellarium can be returned
 `core.getViewRaAngle()` and `core.getViewDecAngle()`, respectively.

  → Wissenschaft/Astronomie/tools/Stellarium/script/API/core/moveToAltAzi[`core.moveToAltAzi`], `core.moveToRaDec2000`

  → Wissenschaft/Astronomie/tools/Stellarium/script/API
