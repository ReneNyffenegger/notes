$ Desktop environment [X]
@ desktop-environment

  Some desktop environments are Enlightenment, XFCE, → Linux/GUI/GNOME[GNOME], → Linux/GUI/KDE, LXE, LXQt, XFce etc.

sa:

  → Linux/fhs/etc/xdg/autostart is a directory that is source by → Linux/X/XDG compliant desktop environments.

  → Linux/X
