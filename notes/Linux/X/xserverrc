
`xserverrc` is a shell script that starts up the X server.
-
It's read by both → Linux/X/startx[`startx`] and → Linux/X/xinit[`xinit`].

The system-wide xserverrc file is → Linux/fhs/etc/X11/xinit/xserverrc, but it's possible to create a personal one in → Linux/fhs/home/username/_xserverrc 
