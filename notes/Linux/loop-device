A *loop device* allows to use a regular file as a → Linux/block-device. «Within» that regular file,
a → Linux/filesystems is created and then → Linux/shell/commands/mount[mounted] just as any other
block device (such as hart drive partitions).
