$ Links between files

{ Permissions of symbolic links

  As per `man → Linux/shell/commands/chmod`, the permissions of symbolic links are never used.

}

sa:

  Links between files are created with → Linux/shell/commands/ln[`ln`]

  → Linux/filesystem
