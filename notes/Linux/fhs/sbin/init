$ /sbin/init

On a machine running → Linux/init/systemd, `/sbin/init` is (usually?) a symlink to → Linux/fhs/lib/systemd/systemd[`/usr/lib/systemd/systemd`].

sa:
  The → Linux/kernel/CONFIG[Linux kernel config] value of `DEFAULT_INIT` (source file `→ Linux/kernel/source/init/Kconfig`).

  The Kernel parameter `init=`
