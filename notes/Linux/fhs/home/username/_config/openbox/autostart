$ ~/.config/openbox/autostart

If present, this file will be executed by → Linux/X/Openbox (`exec openbox-session` in → Linux/fhs/home/username/_xinitrc) at startup.

An ampersand might be needed after the commands that take some time to complete so that they are executed in the background.
