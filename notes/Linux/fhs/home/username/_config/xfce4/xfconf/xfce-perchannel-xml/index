$ ~/.config/xfce4/xfconf/xfce-perchannel-xml

? `~/.config/xfce4/xfconf/xfce-perchannel-xml` to store a user's xfce4 Xfconf database in → development/XML files.

? The frontend for reading and manipulating this database in the GUI is `xfce4-settings-editor` and in the shell `xfconf-query`. But see also `xfce4-settings-manager`.

sa:
?  There is also `xfsettingsd`, a daemon that applies various settings (to the database?)
