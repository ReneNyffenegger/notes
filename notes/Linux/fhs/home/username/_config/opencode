$ ~/.config/opencode/

Files and directories I've found in `~/.config/opencode` include
  • `node_modules/`
  • `.gitignore` (which contains `nodes_modules`, `package.json`, `bun.lock` and `.gitignore`)
  • `bun.lock`
  • `package.json`

Other files that could be located here include
  • `opencode.json`
  • `AGENTS.md`
  • `agents/` (Configuration of agents with markdown files?)
  • `tui.json` (UI theme)
  • `commands/`
  • `modes/`
  • `plugins/`
  • `skills/`
  • `tools/`
  • `themes/`
 

{ opencode.json

code {
{
   "$schema": "→ https://opencode.ai/config.json[https://opencode.ai/config.json]",
   "provider": {
      "anthropic": { ‥ },
       ‥
   },
   "agent": {
      "plan"        : { ‥ },
      "code-reviwer": { ‥ }
   },
   "command": {
      "test"        : { ‥ },
      "build"       : { ‥ }
   },
   "instructions" [
   ],
   "mcp": {
   },
   "plugin": [
   ],
   "formatter": {
   },
   "keybinds": {
   },
   "permission": {
   },
   "compaction": {
   },
   "watcher": {
   },
   "default_agent": "plan",
   "share": "manual",
   "snapshot": false,
   "autoupdate": ‥,
   "experimental": { ‥  },
   "enabled_providers": [ ‥ ],
   "disabled_providers": [ ‥ ]
}
code }

  Text injection into `opencode.json`:
    • Environment variables: `{env:VARIABLE_NAME}`.
    • File content: `{file:~/.secrets/openai-key}`

}


sa:
  `→ Linux/fhs/home/username/_local/share/opencode`
