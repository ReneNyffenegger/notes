$ ~/.ssh/id_rsa

{ Extracting the public key

  The public key can be extracted from `~/.ssh/id_rsa` with `→ development/security/SSH/commands/keygen -y`. The following `diff` command should exit with `0` (i. e. no differences found):
code {
$ diff <(cat ~/.ssh//id_rsa.pub) <(ssh-keygen -y -f ~/.ssh/id_rsa -P passphrase)
$ echo $?
0
code }

}

sa:

 `→ Linux/fhs/home/username/_ssh/id_rsa_pub`

  → Linux/fhs/home/username/_ssh
