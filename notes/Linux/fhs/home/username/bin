$ ~/bin

`~/bin` is sometimes referred to as the user's private `/bin` directory.

If this directory exists, it is added to the `PATH` environment variable by the default `→ Linux/fhs/home/username/_profile` of the
→ Linux/distributions[distribution] I am currently working on

sa:
  The → Linux/fhs/home/username[home directory].
