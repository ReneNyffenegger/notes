$ ~/.Xauthority

`~/.Xauthority` is the default authority file if the → development/environment-variable[environment variable] `XAUTHORITY` is not defined.

sa:
  → Linux/shell/commands/X/xauth[`xauth`]
