$ ~/.conda

`~/.conda/tos` seems to store accepted terms of services (`conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main` etc)

sa:
 `→ Linux/fhs/home/username/miniconda3`
