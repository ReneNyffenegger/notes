$ ~/.openclaw

~/.openclaw` should be readable by the owner only (`700`). Compare with `openclaw doctor`.

{ Files and directories

  Files I'v found under `~/.openclaw` include:
  table { ll
    → Linux/fhs/home/username/_openclaw/openclaw_json[`openclaw.json`] ☰ Should be readable by the owner only (`600`). Compare with `openclaw doctor`
    `openclaw.json.bak` ☰
    `update-check.json` ☰
  table }

  Directories:
  table {
    → Linux/fhs/home/username/_openclaw/agents[`agents`] ☰
    `canvas` ☰
    `cron` ☰
    `devices` ☰
    `identity` ☰
    → Linux/fhs/home/username/_openclaw/workspace[`workspace`] ☰
  table }

  { TODO

    Can/will there also be `~/.openclaw/credentials/` (such as `~/.openclaw/credentials/whatsapp/<accountId>/creds.json`)
    -
    *Legacy OAuth import*: `~/.openclaw/credentials/oauth.json`

    Can/will there also be `~/.openclaw/extensions/<pluginId>` for plugin configuration?
    -
    See also the command `openclaw plugins install <npm-spec>`

    Can/will there also be `~/.openclaw/hooks` directory?

    Can/will there also be `~/.openclaw/logs` directory?

  }

}

sa:
 `~/.openclaw-dev`, `~/.openclaw-<name>`
