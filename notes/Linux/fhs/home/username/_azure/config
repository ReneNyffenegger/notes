$ ~/.azure/config

`~/.azure/config` is the default location for the → Microsoft/Azure/CLI#az-cli-config[Azure CLI configuration] data file in Linux.

The config file is written in INI file format.

The commands to modify the config file are `az init` and `az config`.
