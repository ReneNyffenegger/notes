$ ~/.local

? `~/.local` is commonly used for installing software locally, noteably by `→ development/languages/Python/installation/directory/Scripts/pip` in conjuction with the `--user` command line option.
