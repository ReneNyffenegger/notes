$ ~/.local/share

`$HOME/.local/share` is the default location for `$XDG_DATA_HOME`.

sa:

  `→ Linux/fhs/home/username/_config`,
  `→ Linux/fhs/home/username/_local/state`

  The function `site_data_dir()` and `site_data_path()` of the Python module `→ → development/languages/Python/libraries/platformdirs`.
