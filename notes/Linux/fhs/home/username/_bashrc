$ ~/.bashrc

`~/.bashrc` is executed for interactive non-→ Linux/shell[login shells].

Apparently, it should contain things such as aliases and functions, but no → development/environment-variable[environment variables] (they
should go to → Linux/fhs/home/username/_profile).
