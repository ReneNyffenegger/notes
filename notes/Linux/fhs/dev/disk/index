$ /dev/disk

Directories found under `/dev/disk` include:
  • `by-diskseq/`
  • `by-id/`
  • `by-label/`
  • `by-partlabel/`
  • `by-partuuid/`
  • `by-path/`
  • → Linux/fhs/dev/disk/by-uuid[`by-uuid/`]

The «files» in these directories are symbolic links that point to the «real» devices or partitions under `→ Linux/fhs/dev`

sa:

  The `-j` flag of `→ Linux/shell/commands/iostat`.

  `→ Linux/fhs/dev`
