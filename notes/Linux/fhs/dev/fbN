$ /dev/fbN

`/dev/fbN` (`N` being a number) represents a *frame buffer* device (which is an abstraction for graphics hardware).

{ TODO

code {
# → Linux/shell/commands/dmesg | → Linux/shell/commands/grep fb0
…
[    1.813295] fb0: EFI VGA frame buffer device
code }

}

sa:

  `→ Linux/fhs/dev/ttyN`

  `→ Linux/fhs/sys/class/graphics/fbN`

  `→ Linux/fhs/dev`
