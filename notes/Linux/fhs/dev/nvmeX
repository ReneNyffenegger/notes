$ /dev/nvme…

Devices connected through → https://en.wikipedia.org/wiki/NVMe[NVM Express] (→ hardware#hw-nvme[NVMe]) appear as `/dev/nvme…` files.
-
Such devices have → https://utcc.utoronto.ca/~cks/space/blog/linux/NVMeDeviceNames[three levels] of names:
table { ll
 `/dev/nvme0` ☰ A character device
 `/dev/nvme0n1` ☰ The first «namespace»
 `/dev/nvme0n1p1` ☰ The first partition
 `/dev/nvme0n1p2` ☰ The second partition
table }

sa:
 
  NVMe (Non-Volatile Memory Express) is a specification for accessing solid-state drives (SSDs) that are connected through the PCI Express (PCIe) bus

  The `→ Linux/shell/commands/nvme` command.

  `→ Linux/fhs/dev/[/dev]/ng…`
