$ /dev/urandom

`/dev/urandom` is a faster but less secure random number genrator than `→ Linux/fhs/dev/random`.

Apparently, `/dev/urandom` is created in `→ Linux/kernel/source/drivers/char/mem_c`.

sa:
  `→ Linux/fhs/dev/random`

  `→ Linux/fhs/dev`
