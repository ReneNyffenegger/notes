$ /dev/pts

`/dev/pts` is a directory that contains → Linux/device-file[device files] for slave → development/misc/pseudo-terminal[pseudo terminals].

The file name (e.g. `/dev/pts/4`) of the terminal connected to STDIN can be determined with → Linux/shell/commands/tty[`tty`].

The files uner `/dev/pts` might be symbolically linked from `→ Linux/fhs/proc/pid/fd`.

sa:

  → Linux/filesystem/filesystems#devpts.
