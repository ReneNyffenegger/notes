$ /dev/snd

{ WSL

  → https://stackoverflow.com/a/68316880[There is no] `/dev/snd` or direct audio hardware emulation available in WSL,
  instead, the newest release of WSL achieves audio output by pointing → Linux/sound/PulseAudio clients to an external *PulseAudio server* via a socket in `→ Linux/fhs/mnt/wslg` (`→ Linux/fhs/mnt/wslg/PulseServer`)

}

sa:

  → Linux/sound

 `→ Linux/fhs/dev`
