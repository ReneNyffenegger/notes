$ /dev/kmsg

Writes to `/dev/kmsg` come out as `printk`, reads acccess the buffered `printk` records.

Apparently, `/dev/kmsg` is created in `→ Linux/kernel/source/drivers/char/mem_c`.

{ TODO
 
  Is there a relationship to `→ Linux/fhs/proc/kmsg`

}
