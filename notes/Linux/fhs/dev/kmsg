$ /dev/kmsg

Writes to `/dev/kmsg` come out as `printk`,
-
reads from `/dev/kmsg` acccess the buffered `printk` records.


sa:

? `→ Linux/shell/commands/dmesg` is a shell utility that is able to read and parse messages in `/dev/kmsg`.

 `→ Linux/fhs/proc/kmsg`

  Apparently, `/dev/kmsg` is created in `→ Linux/kernel/source/drivers/char/mem_c`.
