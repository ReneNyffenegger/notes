$ /dev/null

Data sent to `/dev/null` is discarded. If read from `/dev/null`, it returns *EOF*.

Apparently, `/dev/null` is created in → Linux/kernel/source/drivers/char/mem_c.

sa:
  → Linux/fhs/dev/zero
