$ /dev/kvm

code {
$ → Linux/shell/commands/sudo → Linux/shell/commands/cat /dev/kvm
cat: /dev/kvm: Invalid argument
code }

{ kvm API - file descriptors

 `→ Linux/kernel/source/Documentation/virt/kvm/api_rst` says:
 *The kvm API is centered around file descriptors. An initial `open("/dev/kvm")` obtains a handle to the kvm subsystem; this handle can be used to issue system ioctls.*

}

sa:
  → Linux/KVM
