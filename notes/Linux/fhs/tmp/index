$ /tmp

Usually a world writable directory used as a scratch space.

It's often cleared when a system is rebooted. To keep temporary data accross reboots, use → Linux/fhs/var/tmp[`/var/tmp`].

sa:
  → Linux/fhs/dev/shm
