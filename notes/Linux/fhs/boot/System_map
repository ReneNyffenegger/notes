$ /boot/System.map

Although located in the → Linux/fhs/boot/index directory, it is not needed to → Linux/booting/index[boot] Linux.

`System.map` is a *symbol file* for the → Linux/kernel.  It lists function entry points and addresses of kernel data structures of a particular build of a → Linux/kernel.

It is used by `→ Linux/logging/klogd`, ksymoops, OOPS etc. and might generally be useful to investigate kernel problems.

sa:

  `→ Linux/kernel/source/System_map`

   The Debian/APT package `linux-image-$(uname -r)` contains files such as `/boot/System.map-$(uname -r)` etc.

  `→ Linux/fhs/proc/kallsyms`
