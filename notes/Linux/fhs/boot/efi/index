$ /boot/efi

The → hardware/UEFI/ESP[EFI System Partition] is typically mounted on `/boot/efi` in the Ubuntu family of Linux distributions.
-
Other distributions seem to mount the ESP on `/boot` or `/efi`.

sa:
 `→ Linux/fhs/boot`
