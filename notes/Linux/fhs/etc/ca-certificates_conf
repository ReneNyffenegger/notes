$ /etc/ca-certificates.conf

`/etc/ca-certificates.conf` controls which certificates will or won't be installed to `→ Linux/fhs/etc/ssl/certs` when `→ Linux/shell/commands/update-ca-certificates` is executed.

sa:
 `→ Linux/fhs/etc`
