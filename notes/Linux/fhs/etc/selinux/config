$ /etc/selinux/config

`/etc/selinux/config` controls the system's state of *SELinux*.

This file allows to set the two(?) config parameters
  • `SELINUX` (to `enforcing`, `permissive` or `disabled`)
  • `SELINUXTYPE` (to `targeted`, `minimum` or `mls`)

sa:

  → Linux/security/modules/SELinux

? `/usr/sbin/grubby`

 `→ Linux/fhs/etc/sysconfig/selinux`
