$ /etc/cups/cupsd.conf

This file is the configuration file for → Linux/CUPS[CUPS].

The syntax follows → development/Apache/Server[Apache HTTP] server's configurations file.

The configuratino file is read by `cupsd`, the CUPS scheduler.

sa:
  → Linux/fhs/etc/cups
