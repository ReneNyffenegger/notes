$ /etc/skel

Files I found under `/etc/skel` on my → Linux/distributions[distribution] include
  • → Linux/fhs/home/username/_bashrc[`.bashrc`]
  • → Linux/fhs/home/username/_profile[`.profile`]
  • → Linux/fhs/home/username/_bash_logout[`.bash_logout`]

sa:

 → Linux/shell/commands/useradd (option `-k`).

 → Linux/fhs/etc/default/useradd
