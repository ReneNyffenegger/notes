$ /etc/pacman.d/mirrorlist

`/etc/pacman.d/mirrorlist` is the → Linux/distributions/arch[Arch Linux] repository mirrorlist.

{ Updating mirrorlist

code {
pacman -Sy pacman-mirrorlist
code }

}

sa:
  `→ Linux/distributions/arch/pacman`
