$ /Linux/fhs/etc/pacman.d/gnupg

This directory should contain two files:
  • `pubring.gpg` holds the public keys of all packagers.
  • `trustdb.gpg` contains a so-called trust database, which specifies that the keys are authentic and trusted.
