$ /etc/shells

List of valid → Linux/shell/index#login-shell[login shells].

The content of `/etc/shells` might look like
code {
# /etc/shells: valid login shells
/bin/sh
/bin/bash
/bin/rbash
/bin/dash
code }

sa:
  → Linux/fhs/etc
