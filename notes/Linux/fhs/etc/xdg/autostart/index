$ /etc/xdg/autostart

This is the global autostart directory.
-
It contains information about session services that
→ Linux/X/XDG compliant graphical session managers (such as → Linux/GUI/GNOME[GNOME] or → Linux/GUI/KDE) should start automatically when
a user logs in.
