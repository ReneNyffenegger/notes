$ /etc/msystem.d/

The directory `/etc/msystem.d` contains the following files
  • `CLANG64`
  • `CLANGARM64`
  • `MINGW32`
  • `MINGW64`
  • `MSYS`
  • `UCRT64`

Each of these files sets and exports the following environment variables:
table { ll
  `MSYSTEM_PREFIX` ☰
  `MSYSTEM_CARCH` ☰
  `MSYSTEM_CHOST` ☰
  `MINGW_CHOST` ☰
  `MINGW_PREFIX` ☰
  `MINGW_PACHAGE_PREFIX` ☰
table }

One of these files is sourced by `→ Linux/fhs/etc/msystem`, depending on the value of `$MSYSTEM`.
