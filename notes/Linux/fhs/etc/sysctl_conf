$ /etc/sysctl.conf

Configuration file to set system variables (aka *sysctl values*).

The file is read and set by `→ Linux/shell/commands/sysctl`.

code {
# Comments can be introduced with a hash
; or a semicolon, otherwise, lines read
token = value
code }

sa:
  `man sysctl.conf`
