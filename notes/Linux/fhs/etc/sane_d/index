$ /etc/sane.d

  `/etc/sane.d` is the default directory to store configration files for → Linux/misc/SANE[SANE]. The → development/environment-variable[environment variable] `SANE_CONFIG_DIR` allows to specify
  another directory.

  → Linux/fhs/etc/sane_d/dll_conf

  → Linux/fhs/etc/sane_d/net_conf


sa:
  → Linux/fhs/etc
