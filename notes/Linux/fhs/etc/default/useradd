$ /etc/default/useradd

This file defines some defaults for → Linux/shell/commands/useradd, for example:
code {
GROUP=100
HOME=/home
INACTIVE=-1
EXPIRE=
SHELL=/bin/bash
SKEL=/etc/skel
CREATE_MAIL_SPOOL=no
code }
