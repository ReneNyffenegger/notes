$ /etc/NetworkManager/system-connections/

`/etc/NetworkManager/system-connections/` contains files (most of them with a `.nmconnection` extension, but some also without any extension) whose content is something like
code {
[connection]
id=someSSID
uuid=01234567-89ab-cdef-0123-456789abcdef
type=wifi
timestamp=1726994746

[wifi]
mode=infrastructure
ssid=someSSID

[wifi-security]
key-mgmt=wpa-psk
psk=theSecretPassword

[ipv4]
method=auto

[ipv6]
ip6-privacy=2
method=auto

[proxy]
code }

sa:

  → Linux/networking/NetworkManager

  The parent directory `→ Linux/fhs/etc/NetworkManager`
