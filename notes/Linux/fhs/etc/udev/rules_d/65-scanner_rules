$ /etc/udev/rules.d/65-scanner.rules

The file contains → Linux/udev rules for supported → hardware/USB and → hardware/SCSI[SCSI] → Linux/scanner[scanner] devices.

Apparently, this file is created based on description files (*.desc) by `sane-desc` from `sane-backends`.
