$ /etc/pacman.conf

`/etc/pacman.conf` is the → Linux/distributions/arch/pacman[Pacman package manager] configuration file. `pacman` tries to read this file each time it is invoked.

sa:

  `man 5 pacman.conf`
