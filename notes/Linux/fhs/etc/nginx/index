$ /etc/nginx

`/etc/nginx` is the directory in which → web/webserver/nginx is configured.

`→ Linux/fhs/etc/nginx/nginx_conf` is the main configuration file.

links:
 → https://wiki.debian.org/Nginx/DirectoryStructure[Nginx Directory Structure]
