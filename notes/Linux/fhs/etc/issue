$ /etc/issue

Apparently, the content of this file is printed before the login prompt.

`/etc/issue` is normally parsed by the programm `agetty` (the *alternative* `getty`).

sa:
  → Linux/fhs/etc/issue_net, → Linux/fhs/etc
