$ /etc/hostname

sa:

 `→ Linux/shell/commands/uname -n`

 `→ Linux/fhs/proc/sys/kernel/hostname`

  In → Windows/Subsystem-for-Linux, the content of `/etc/hostname` can be overwritten by then entry `hostname` in the `[network]` section of `→ Linux/fhs/etc/wsl_conf`.
