$ /etc/security/pam_env.conf

`/etc/security/pam_env.conf` is the configuration file for `pam_env`, a *PAM module* to load a configura list of environment variables.

sa:

 `→ Linux/fhs/etc/environment`

 `→ Linux/fhs/etc/security`
