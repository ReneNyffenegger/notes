$ /etc/mime.types

→ development/web/Multipurpose-Internet-Mail-Extensions[Media (MIME) types] and the → development/filename-extensions[extensions] that represent them.

sa:

  The parent directory `→ Linux/fhs/etc`

  With APT, `/etc/mime.types` is installed with
code {
$ → Linux/shell/commands/sudo → Linux/package-managers/APT/commands/apt/install -y media-types
code }
