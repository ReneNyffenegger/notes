$ /etc/debian_version

`/etc/debian_version` should be present in every → Linux/distributions/Debian[Debian]-based Linux installation.

The file contains one line:
code {
$ cat /etc/debian_version
11.6
code }

sa:
 `→ Linux/fhs/etc/os-release`
