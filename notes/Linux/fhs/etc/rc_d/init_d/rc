$ /etc/rc.d/init.d/rc

`/etc/rc.d/init.c/rc` ist the *main → Linux/init/System-V#system-v-run-levels[runlevel] control script*. 

The script takes one argument: the new(?) runlevel that the system should go into.

This script is also the first script that is called by → Linux/init/System-V#boot-process[System V's boot process] with the argument `S` (?).

The script sources → Linux/fhs/etc/sysconfig/rc_site to allow for cusomization.

sa:
  → Linux/init/System-V
