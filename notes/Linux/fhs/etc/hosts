$ /etc/hosts

Static table lookup for hostnames.

{ Windows Subsystem for Linux (WSL)

  In → Windows/Subsystem-for-Linux[WSL], `/etc/hosts` inherits the entries
  from → Windows/dirs/Windows/System32/drivers/etc/hosts[`%windir%\system32\drivers\etc\hosts`] (unless `generateHosts` (section `[network]`) is set to `false` in `→ Linux/fhs/etc/wsl_conf`).

}

sa:

 `→ Linux/fhs/etc`
