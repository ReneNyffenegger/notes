$ /etc/letsencrypt/live

`/etc/letsencrypt/live` contains the private keys and the certificates managed by Let's encrypt (actually: certbot).

For each domain, there is a subdirectory (`→ Linux/fhs/etc/letsencrypt/_domain/index`).

sa:

  → https://certbot.eff.org/docs/using.html#where-are-my-certificates[Where are my certificates] in the Certbot User Guide.

 `→ Linux/fhs/etc/letsencrypt`
