$ /etc/localtime

`/etc/localtime` is usually(?) symlinked to a binary file like `/usr/share/zoneinfo/Europe/Zurich`.
The default link seems to be to `/usr/share/zoneinfo/UTC`.

sa:
  → Linux/timezone
