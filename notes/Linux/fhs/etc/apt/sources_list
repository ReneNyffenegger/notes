$ /etc/apt/sources.list

The file contains the locations of → Linux/APT#apt-repositories[apt repositories] to fetch packages from.

Such repositories are added or removed with → Linux/APT/commands/add-apt-repository.
  
sa:
  `→ Linux/shell/commands/man sources.list`
