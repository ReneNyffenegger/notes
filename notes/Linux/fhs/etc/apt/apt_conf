$ /etc/apt/apt.conf

`apt-config` queries → Linux/package-managers/APT#APT-config[APT configuration directives] from `→ Linux/fhs/etc/apt/apt_conf`.


sa:
 `→ Linux/package-managers/APT/commands/apt-config` queries → Linux/package-managers/APT configuration directives from `apt.conf`.

 `/etc/apt/apt.conf.d/`

 `→ Linux/fhs/etc/apt`

  `man apt.conf`
