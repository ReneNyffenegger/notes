$ /etc/apt/apt-file.conf

If `/etc/apt/apt-file.conf` is present, `→ Linux/package-managers/APT/commands/apt-file` will read this file after all
default → Linux/package-managers/APT#APT-config[APT configuration] files.
