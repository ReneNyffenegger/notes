$ /etc/dbus-1/session.conf

Apparently, this configuration file is obsolete in newer versions of D-Bus.

In older versions, this file defined the behaviour of the well-known(?) session bus which in newer
versions is determined in → Linux/fhs/usr/share/dbus-1/session_conf.

sa:
  → Linux/D-Bus
