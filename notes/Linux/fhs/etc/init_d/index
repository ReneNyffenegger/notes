$ /etc/init.d

{ Restarting a service

  `/etc/init.d` contains files that allow to restart a service:

code {
# /etc/init.d/cupsd restart
code }

}

sa:

→ Linux/fhs/etc/init_d/nfs-kernel-server

 `→ Linux/fhs/usr/lib/systemd/system-generators/systemd-sysv-generator` is a generator to create → Linux/init/systemd wrapper services for SysVinit scripts (which are traditionally stored in `/etc/init.d`).
