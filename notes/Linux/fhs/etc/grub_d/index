$ /etc/grub.d

This directory contains template files which are used by `→ Linux/shell/commands/grub-mkconfig` to create `→ Linux/fhs/boot/grub/grub_cfg`.

The grub manual says that `→ Linux/fhs/etc/grub_d/40_custom` is particularly useful.

sa:

 `→ Linux/fhs/boot/grub/grub_cfg`
