$ /etc/mtab

Table of → Linux/shell/commands/mount[mounted] → Linux/filesystem/filesystems[filesystems].

On «modern» systems, `/etc/mtab` is a symbolic link to `→ Linux/fhs/proc/self/mounts`.

sa:

 `→ Linux/fhs/etc/fstab`, `→ Linux/fhs/proc/mount`

 `→ Linux/shell/commands/findmnt`
