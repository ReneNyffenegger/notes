$ /etc/hosts.allow

List of hosts that are allowed to access the system, for example via → Linux/filesystem/NFS[NFS]. (Compare → Linux/fhs/etc/hosts_deny)

sa:

  `man 5 hosts_access`, `man 5 hosts_options`
