$ /etc/group

Defines the → Linux/group[groups] to which a →Linux/user[user] belongs.

A user can be added to a group with → Linux/shell/commands/usermod#add[usermod -aG …]


sa:
  → Linux/fhs/etc/passwd
