$ /etc/shadow

This file should only be accessible to → Linux/root. It contains the password's hashes for the users listed in → Linux/fhs/etc/passwd.

An `/etc/shadow` file can be created from an `/etc/passwd` file with → Linux/shell/commands/pwconv.

sa:
  → Linux/security
