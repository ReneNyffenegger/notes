$ /lib/modules

This directory contains directories with → Linux/kernel/module[kernel modules] needed for the → Linux/kernel.

These directories are created after → Linux/kernel/compilation[building the kernel] with `make modules_install`. 

The name of these directories follows the pattern kernel-version-→Linux/kernel/CONFIG/LOCALVERSION (apparently, that
is equal to → Linux/fhs/lib/modules/VERSION/.

sa:

 `→ Linux/shell/commands/lsmod`, `→ Linux/shell/commands/modprobe`
