$ /lib/firmware

`/lib/firmware` is the directory where the → Linux/kernel[kernel] and kernel drivers look for firmware images.

Most firmware can be found in → https://git.kernel.org/pub/scm/linux/kernel/git/firmware/linux-firmware.git/tree[this github repository]

sa:
  → Linux/fhs/lib
