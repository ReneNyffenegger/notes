$ /usr/bin

`→ Linux/fhs/usr/bin/cpp` is the preprocessor

`→ Linux/fhs/usr/bin/ld` is the linker

`/usr/bin/[` and `→ Linux/fhs/usr/bin/test` are equivalent commands, except that `[` expects a terminating `]`


sa:

 `/usr/xpg4/bin` and `/usr/xpg6/bin`, `/usr/xpg7/bin`

 `→ Linux/fhs/bin`

 `→ Linux/fhs/home/username/_local/bin`
