
`git-shell` is a restricted → Linux/shell#login-shell[login shell] for → development/version-control-systems/git[git]-only → development/security/SSH access.

sa:
  → development/version-control-systems/git/commands/daemon[`git daemon`]
