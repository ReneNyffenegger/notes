$ /usr/bin/ld

The linker. See → Linux/shell/commands/ld[`ld`].

On my current (Debian) system, `/usr/bin/ld` is a symbolic link to `/usr/bin/x86_64-linux-gnu-ld`.

sa:

 `→ Linux/fhs/usr/bin`, `→ Linux/fhs/usr/bin/ld_so`
