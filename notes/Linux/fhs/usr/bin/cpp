$ /usr/bin/cpp

The preprocessor.

`cpp` is not bo be confused with → development/languages/C-C-plus-plus/CPP[c++].

The preprocessor is concerned with things like
  • macro expansion
  • removing comments
  • trigraph conversion
  • escaped newline splicing
  • processing of directives

sa:

  → development/languages/C-C-plus-plus/GCC/options/f/preprocessed[`gcc -fpreprocessed`]

  → Linux/fhs/usr/bin
