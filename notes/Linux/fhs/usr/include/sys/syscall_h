$ /usr/include/sys/syscall.h

`sys/syscall.h` can be included to pull the → Linux/kernel/sys-calls[syscall] numbers. Those constants are defined as macros with a `SYS_` prefix (for example `SYS_exit`).

sa:
  → Linux/kernel/source/arch/x86/entry/syscalls/syscall_64_tbl
