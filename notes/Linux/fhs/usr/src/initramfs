$ /usr/src/initramfs/

The `/usr/src/initramfs` directory is commonly used for building an embedded *initramfs*.

{ TODO

  → https://github.com/torvalds/linux/blob/master/usr/gen_init_cpio.c[`/usr/gen_init_cpio.c`] seems to be the source code for an executable that will be stored in
  `/usr/src/linux/usr/gen_init_cpio` and is used to create the cpio archive of the initramfs.

}
