$ /usr/share/distro-info

The `→ Linux/fhs/usr/share[/usr/share]/distro-info` contains CSV files each of which constains a Linux distribution's «version history».

The fields in the files are
table { ll
  `version` ☰
  `codename` ☰
  `series` ☰
  `created` ☰
  `release` ☰
  `eol` ☰
  `eol-lts` ☰
  `eol-elts` ☰
table }
