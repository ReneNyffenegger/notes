$ /usr/share/misc/pci.ids

*List of PCI IDs*: vendors, devices, classes and subclasses.

sa:

  The `-i` option of `→ Linux/shell/commands/lspci`.

  This list is maintained at → https://pci-ids.ucw.cz/ and can be updated with the `update-pciids` utility.
