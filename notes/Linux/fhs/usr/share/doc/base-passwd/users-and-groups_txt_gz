$ /usr/share/doc/base-passwd/users-and-groups.txt.gz

This document describes the purposes of the → Linux/user[users] and → Linux/group[groups] found in the → Linux/distributions/Debian `base-passwd` package.

sa:

  `→ Linux/fhs/etc/passwd`, `→ Linux/fhs/etc/group`
