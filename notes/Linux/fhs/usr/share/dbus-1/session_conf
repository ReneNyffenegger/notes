$ /usr/share/dbus-1/session.conf

This configuration file controls the per-user-login-session message bus.
-
Add a session-local.conf and edit that rather than changing this file directly.

sa:
  → Linux/D-Bus

  → Linux/fhs/etc/dbus-1/session_conf
