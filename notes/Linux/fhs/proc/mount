$ /proc/mounts

`/proc/mounts` (as also `→ Linux/fhs/etc/mtab`) is (usually) a symbolic link to `→ Linux/fhs/proc/self/mounts`.

sa:

 `→ Linux/fhs/etc/fstab`, `→ Linux/fhs/etc/mtab`

 `→ Linux/shell/commands/mount`

 `→ Linux/fhs/proc`
