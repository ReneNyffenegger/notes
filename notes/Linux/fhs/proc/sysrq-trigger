$ /proc/sysrq-trigger

As per a comment in `→ Linux/kernel/source/drivers/tty/sysrq_c`: Writing `C` to `/proc/sysrq-trigger` is like *sysrq-C`.

As per a comment in `Documentation/arch/x86/earlyprintk.rst`, a harmless kernel message can be provoked by writing `h` into `/sys/sysrq-trigger`.

sa:

  → Linux/kernel/SysRq
