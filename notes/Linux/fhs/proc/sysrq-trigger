$ /proc/sysrq-trigger

As per a comment in `→ Linux/kernel/source/drivers/tty/sysrq_c`: Writing `C` to `/proc/sysrq-trigger` is like *sysrq-C`.

As per a comment in `Documentatino/arch/x86/earlyprintk.rst`, a harmless kernel message can be provoked by wrting `h` into `/sys/sysrq-trigger`.

sa:

  → Linux/kernel/SysRq
