$ /proc/filesystems

`/proc/filesystems` can be used to determine which → Linux/filesystem/filesystems[filesystems] are currently supported by the → Linux/kernel.

{ TODO

code {
$ sudo mount /dev/sdd1 /mnt/d
mount: /mnt/d: unknown filesystem type 'ntfs'.
code }

}

sa:
  → Linux/fhs/proc
