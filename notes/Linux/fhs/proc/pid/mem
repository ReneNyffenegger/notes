$ /proc/$pid/mem

This file allows to access the → Linux/memory[memory] of the → Linux/kernel/process[process] with process id (PID) = `$pid` using standard `read` and `write` operations *if* one has the
necessary rights to do so.

sa:
  → Linux/fhs/proc/pid
