$ /proc/uptime

`/proc/uptime` records (in *one* line):
   • The number of seconds the system has been running up to now
   • The sum of seconds each core has spent idle.

code {  
$ → Linux/shell/commands/cat /proc/uptime
133128.29 912009.71
code }

sa:
 `→ Linux/fhs/proc`
