$ /proc/modules

sa:

 `→ Linux/shell/commands/lsmod` is a → Linux/shell/commands[shell command] that shows loaded modules by formatting the content of `/proc/modules`.

 `→ Linux/fhs/sys/module`
  
 `→ Linux/fhs/proc`
