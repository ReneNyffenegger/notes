$ /proc/config.gz

This file shows the → Linux/kernel/CONFIG[configuration options] with which the currently running → Linux/kernel was built.

This file requires the config option `→ Linux/kernel/CONFIG#config_ikconfig_proc` to be set when the kernel is compiled.

View this file with
code {
$ → Linux/shell/commands/zcat /proc/config.gz
code }

sa:

  The `→ Linux/kernel/source/_config` file stores the config options with which the → Linux/kernel[Kernel] is compiled.

 `→ Linux/kernel/source/kernel/configs_c`

 `→ Linux/fhs/proc`
