$ /proc/config.gz

This file shows the configuration options with which the currently running kernel was built.

view this file with `→ Linux/shell/commands/zcat /proc/config.gz`.

{ See also

  → Linux/kernel/source/_config

}
