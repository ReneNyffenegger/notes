$ /proc/swaps

`/proc/swaps` shows activ → Linux/swap-space[swap spaces]:
  • Filename (which can be a partition, for example → Linux/fhs/dev/sdX[`/dev/sda3`])
  • Type (Partition, …)
  • Size
  • Usage
  • Priority

sa:

 → Linux/shell/commands/swapon[`swapon -s`] is equivalent to `cat /proc/swaps` (See also → Linux/swap-space#display-swap-space[displaying swap space])

 → Linux/swap-space
