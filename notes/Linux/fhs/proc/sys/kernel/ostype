$ /proc/sys/kernel/ostype

`cat /proc/sys/kernel/ostype` might print something like
code {
Linux
code }

sa:

  → Linux/shell/commands/uname[`uname -o`] and the (→ Linux/shell/bash[Bash]) variable → Linux/shell/bash/variables/internal#ostype[`$OSTYPE`].

  → Linux/fhs/proc/version
