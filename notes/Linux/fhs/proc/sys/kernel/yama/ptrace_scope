$ /proc/sys/kernel/yama/ptrace_scope

The default value of 1 blocks non-child *ptrace*.

A value of 0 allows for the more premissive behavior.

sa:

  The `CAP_SYS_PTRACE` → Linux/security/capabilities[capability].

  → Linux/kernel/syscalls/ptrace[`ptrace`],

  → Linux/security/Yama
