$ /proc/bus/input/devices

Each *device driver* must register a request handler using `request_irq()`. This registers a given interrupt handler for
a given interrupt line. `/proc/bus/input/devices` shows the (hardware) input devices.

sa:
  → Linux/fhs/proc/bus/input
