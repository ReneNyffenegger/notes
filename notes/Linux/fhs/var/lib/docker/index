$ /var/lib/docker

→ development/tools/Docker images are, by default, located in `/var/lib/docker`.

When docker is uninstalled, images, volumes, containers and networks stored in `/var/lib/docker` are not automatically removed.
