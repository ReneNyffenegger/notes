$ /var/lib/apt/extended_states

`/var/lib/apt/extended_states` is database that stores a boolean like flag (`0` or `1`) which indicates if a package was installed automatically or manually. (Manually installed packages won't be removed by `apt-get` unless `purge` is used).
