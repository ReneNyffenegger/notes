$ /var/lib/dpkg

`/var/lib/dpkg` contains the dpgk database.

The dpkg database can be queried with `→ Linux/package-managers/dpkg/commands/dpkg-query`.

sa:
  
 `→ Linux/distributions/Debian/dpkg` and its `--admindir` options which alows to use another directory.

 `→ Linux/fhs/var/lib/dpkg/alternatives`

 `→ Linux/fhs/var/lib/dpkg/available`
