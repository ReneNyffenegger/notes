$ /var/lib/dpkg/status

`/var/lib/dpkg/status` lists the `→ Linux/distributions/Debian/dpkg` packages's status.

This file is backed up to `/var/backups/dpkg.status.*[.gz]`.

sa:

 `→ Linux/fhs/var/lib/dpkg/available`
