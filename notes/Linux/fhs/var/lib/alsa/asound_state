$ /var/lib/alsa/asound.state
-

This file seems to store → Linux/sound/ALSA mixer settings.

Apparently, this file should be written with `alsactl store` and loaded with `alsactl restore`.

sa:
  → Linux/sound/ALSA
