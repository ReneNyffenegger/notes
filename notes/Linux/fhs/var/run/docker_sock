$ /var/run/docker.sock

? `/var/run/docker.sock` is the (default) socket for the → development/tools/Docker daemon.

sa:

  The error message → development/tools/Docker#docker-err-perm-denied[Error message: Got permission denied while trying to connect to the Docker daemon socket at unix:///var/run/docker.sock].

  The `-H` option of `→ development/tools/Docker/dockerd`
