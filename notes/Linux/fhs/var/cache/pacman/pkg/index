$ /var/cache/pacman/pkg

The default cache/database directory of → Linux/distributions/arch/pacman[pacman].

Apparently, it can be reduced in size with `sudo → Linux/distributions/arch/pacman[pacman] -Sc` if it becomes to big.

sa:
  → Linux/fhs/var/cache
