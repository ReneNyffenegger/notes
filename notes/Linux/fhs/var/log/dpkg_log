$ /var/log/dpkg.log

`/var/log/dpkg.log` is the default log file for `→ Linux/distributions/Debian/dpkg`.

sa:
  the `log` directive in `→ Linux/fhs/etc/dpkg/dpkg_cfg` and the `--log` command line option.
