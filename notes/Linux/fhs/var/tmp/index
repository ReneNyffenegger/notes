$ /var/tmp

`→ Linux/fhs/var[/var]/tmp` contains temporary files. Unlike `→ Linux/fhs/tmp`, this is directory is not cleaned up at system boot up, so the data stays for an unspecified duration.

sa:

  The sub directory → Linux/fhs/var/tmp/_oracle[`.oracle`].
