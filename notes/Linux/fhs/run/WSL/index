$ /run/WSL

Files found in `/run/WSL` are named like `NN_interop`, where `NN` is a number.

These files seem to be either a symlink to another file under `/run/WSL` or a socket.

sa:

  The → Windows/Subsystem-for-Linux#WSL_INTEROP[`$WSL_INTEROP`] environment variable

  → Windows/Subsystem-for-Linux
