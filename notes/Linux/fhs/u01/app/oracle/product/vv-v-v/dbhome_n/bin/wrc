$ $ORACLE_HOME/bin/wrc

`wrc` is the *workplay replay client*, a multithreaded program where each thread replays the workload thas was captured from Oracle sessions.

{ Misc

? If `wrc` writes a trace file, it is written to the directory specified with `workdir=`.

}
