$ $ORACLE_HOME/rdbms/mesg/oraus.msg

*→ development/databases/Oracle/errors[Error messages] for Oracle*

For errorcode 10000 through 10999 (which are actually *debug event codes*), `oraus.msg` lists several levels that can be set.
