$ catmetinsert.sql

`catmetinsert.sql` inserts data into the Oracle dictionary for use by the DataPump Metadata API.

sa;
  One of the tables that is filled by `catmetinsert.sql` is → development/databases/Oracle/installed/data-dictionary/base-tables/meta/xsl/param[`metaxslparam$`].

