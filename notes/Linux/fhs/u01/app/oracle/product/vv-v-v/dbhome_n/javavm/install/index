$ $ORACLE_HOME/javavm/install

`$ORACLE_HOME/javavm/install` contains a few scripts that are are useful only if the JServer option is installed.
-
Some of them are:
  • `initjvm.sql`: Initializes JServer by installing core Java class libraries and Oracle-specific Java classes.
  • `rmjvm.sql`: to remove all elements of JServer
  • `catjava.sql`: to install Java-related packages and classes
