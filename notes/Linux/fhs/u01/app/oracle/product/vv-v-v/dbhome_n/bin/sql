$ $ORACLE_HOME/bin/sql.bat

`$ORACLE_HOME/bin/sql.bat` is a wrapper that is supposed to execute → development/databases/Oracle/SQLcl.
-
However, because this → Windows/dirs/Windows/System32/cmd_exe/batch-files contains forward slashes rather than backward slashes, it failes with a
*The syntax of the command is incorrect.* error message.
