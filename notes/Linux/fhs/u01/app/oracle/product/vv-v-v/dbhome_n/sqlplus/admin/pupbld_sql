$ $ORACLE_HOME/sqlplus/admin/pupbld.sql

`$ORACLE_HOME/sqlplus/admin/pupbld.sql` is the script that install the
→ development/databases/Oracle/SQL-Plus[SQL*Plus] `PRODUCT_USER_PROFILE` tables.

These tables are only used by SQL*Plus and allow to disable commands per user.

sa:
  → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/sqlplus/admin/index
