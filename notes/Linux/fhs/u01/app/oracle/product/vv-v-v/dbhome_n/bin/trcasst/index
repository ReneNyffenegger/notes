
`trcasst` is the *trace assistant* for → development/databases/Oracle/Net Services trace files.

trcasst only works with level 16 (support) Oracle Net Services trace files and can only be used when the
`diag_adr_enabled` parameter is set to off.

