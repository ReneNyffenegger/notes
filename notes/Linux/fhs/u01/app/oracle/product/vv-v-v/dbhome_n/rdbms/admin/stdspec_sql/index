$ $ORACLE_HOME/rdbms/admin/stdspec.sql

Package `standard`, which is
"
   special in that it is really the root of the PL/SQL dependencies graph;
"

sa:

  `→ development/databases/Oracle/installed/packages/dbms/standard`

  `→ development/databases/Oracle/SQL/cursor/ref/sys_refcursor`

