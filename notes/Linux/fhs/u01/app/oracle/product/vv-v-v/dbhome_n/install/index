$ $ORACLE_HOME/install

{ Files

  Files found in `$ORACLE_HOME/install` may include:

  table { ll
   `envVars.properties` ☰
   `envVars.properties.bak` ☰
   `files.lst` ☰
   `files.lst_….bkp` ☰
   `make.log` ☰
   `modmakedeps.pl` ☰
   `orabasetab` ☰
   `root_$(→ Linux/shell/commands/uname -n)_$(→ Linux/shell/commands/date +%F_%H-%M-%S-%N).log` ☰ The name of this log file is assigned to the variable `LOG` in `$ORACLE_HOME/isntall/utl/rootmacro.sh`.
   → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/install/root_schagent_sh[`root_schagent.sh`] ☰
  table }

  With Oracle 11.1, the file `changePerm.sh` is not available anymore because the permissions under `→ Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n` are set correctly (see MOS Note 834626.1).

}
{ Directories

  Directories found in `$ORACLE_HOME/install` may include:

  table { ll
   `acls` ☰
   `chainedInstall` ☰
   `checkpoints` ☰
   `config` ☰
   `inst_plugin` ☰
   `jlib` ☰
   `mesg` ☰
   `response` ☰
   `sizes` ☰
   `utl` ☰
  table }


}

sa:

  `→ Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/install/response/db_install_rsp`

  `→ Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/install/orabasetab`

  `→ Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/rdbms/install`, `$ORACLE_HOME/javavm/install`, `$ORACLE_HOME/javavm/ojvmwcu/install`, `$ORACLE_HOME/network/install`, `$ORACLE_HOME/crs/install`, `$ORACLE_HOME/ldap/install`.

  `→ Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n`
