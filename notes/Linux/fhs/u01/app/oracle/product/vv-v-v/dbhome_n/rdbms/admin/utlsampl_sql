$ $ORACLE_HOME/rdbms/admin/utlsampl.sql

`$ORACLE_HOME/rdbms/admin/utlsampl.sql` creates → development/databases/Oracle/sample-schemas[sample schemas] (such as `SCOTT`) and demonstration → development/databases/Oracle/objects/tables[tables] and, such as `EMP` and `DEPT`.

