$ $ORACLE_HOME/bin/dbstart

`$ORACLE_HOME/bin/dbstart` is a shell script that starts all databases that are marked for automatic starting in the `→ Linux/fhs/etc/oratab` file (third column = `Y`).

sa:

 `$ORACLE_HOME/bin/dbshut`

  MOS Note 91815.1
