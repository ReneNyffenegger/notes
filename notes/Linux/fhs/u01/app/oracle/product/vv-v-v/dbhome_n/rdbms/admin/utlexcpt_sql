$ $ORACLE_HOME/rdbms/admin/utlexcpt.sql

`utlexcpt.sql` creates a → development/databases/Oracle/objects/tables named `exceptions` (edited for better readability) which can be used for the `→ development/databases/Oracle/objects/tables/constraints/exceptions-into`
clause of an SQL statement.

code {
create table exceptions(
   row_id     → development/databases/Oracle/SQL/data-types/rowid,
   owner      varchar2(128),
   table_name varchar2(128),
   constraint varchar2(128)
);
code }

sa:

  Compare with `utlexpt1.sql`.

  → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/rdbms/admin
