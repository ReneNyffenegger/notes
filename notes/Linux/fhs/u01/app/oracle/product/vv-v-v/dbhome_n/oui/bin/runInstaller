$ $ORACLE_HOME/oui/bin/runInstaller

? The *Oracle Universal Installer* (OUI) is a Java-based GUI with which Oracle components can be installed.

{ Execution modes

  OUI runs in one of three execution modes:
  table { ll
    Interactive ☰ Uses the GUI to solicit input from the user
    Suppressed ☰ Some information is provided by response files, other information from the GUI
    Silent ☰ No GUI is opened, all information is provided by response files
  table }

}

sa:

  The initialization file is `→ Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/oui/oraparam_ini`.

  Log files of installations can be found in
    • `$ORACLE_HOME/cfgtoollogs/oui/oraInstall<timestamp>.{out,err}`
    • `$ORACLE_HOME/cfgtoollogs/oui/installActions<timestamp>.log`

  `→ Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/runInstaller`
