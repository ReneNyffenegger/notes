$ $ORACLE_HOME/rdbms/admin/catexp

`catexp.sql` prepares the database for  the export/import utility:
   • import views in the data dictionary
   • `EXP_FULL_DATABASE` and `IMP_FULL_DATABASE` roles
   • necessary privileges to the `EXP_FULL_DATABASE` and `IMP_FULL_DATABASE` roles
   • `FULL_DATABASE` and `IMP_FULL_DATABASE` to the DBA role
   • Records the version of `catexp.sql` that has been installed
