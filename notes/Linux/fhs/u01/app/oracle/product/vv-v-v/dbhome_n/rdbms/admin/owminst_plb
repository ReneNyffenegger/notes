$ $ORACLE_HOME/rdbms/admin/owminst.plb

`owminst.plb` installs → development/databases/Oracle/Workspace-Manager:

{ Verify installation

  To verify if Worskpace Manger was installed successfully, the following query might be used:
code {
select dbms_wm.getWorkspace from dual;
code }

  This query should return `LIVE`.

}
