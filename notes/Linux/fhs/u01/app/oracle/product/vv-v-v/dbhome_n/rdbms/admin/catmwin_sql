$ $ORACLE_HOME/rdbms/admin/catmwin.sql

`catmwin.sql` defines maintenance window and → development/databases/Oracle/optimizer/statistics[stats] collection job
(`→ development/databases/Oracle/installed/packages/dbms/stats[dbms_stats].gather_database_stats_job_proc`).

This script creates the windows named `MONDAY_WINDOW`, `TUESDAY_WINDOW` etc. and the window group named `MAINTENANCE_WINDOW_GROUP`.

It also creates the programs / jobs
  • `gather_stats_prog`
  • `auto_space_advisor_prog`
  • `dbms_autotask_data`
  • `ORA$AUTOTASK_CLEAN`
  • `HM_CREATE_OFFLINE_DICTIONARY`
  • `DRA_REEVALUATE_OPEN_FAILURES`
