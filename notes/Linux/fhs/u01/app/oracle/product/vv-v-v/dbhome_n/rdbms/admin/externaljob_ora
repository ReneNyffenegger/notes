$ $ORACLE_HOME/rdbms/admin/externaljob.ora
@ externaljob.ora

`externaljob.ora` is a configuration file is used by `→ development/databases/Oracle/installed/packages/dbms/scheduler` when executing external (operating system) → development/databases/Oracle/objects/jobs[jobs].

code {
run_user = nobody
run_group = nobody
code }
