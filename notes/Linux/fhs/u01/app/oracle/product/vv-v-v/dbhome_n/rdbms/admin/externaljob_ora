$ $ORACLE_HOME/rdbms/admin/externaljob.ora
@ externaljob.ora

`externaljob.ora` is a configuration file is used by `→ development/databases/Oracle/installed/packages/dbms/scheduler` 
and specifies the Linux user and group with which external (operating system) → development/databases/Oracle/objects/jobs[jobs] are executed.

code {
run_user = nobody
run_group = nobody
code }
