$ $ORACLE_HOME/bin/dbSetup.pl
@ dbSetup.pl

`$ORACLE_HOME/bin/dbSetup.pl` is a → development/languages/Perl script to launch db setup wizard for configuring Database home image.

The script relies on the «parent class» → Linux/fhs/u01/app/oracle/product/vv-v-v/dbhome_n/bin/CommonSetup_pm[`CommonSetup`].
