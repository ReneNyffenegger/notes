$ $ORACLE_BASE/oraInventory/oraInst.loc

When the Oracle software is installed on a system, the Universal Installer creates the `oraInst.loc` file.

This file identified the name of the *Oracle Inventory* → Linux/group.
-
This is normally `oinstall`.

code {
inventory_loc=/opt/oracle/oraInventory
inst_group=→ development/databases/Oracle/adminstration/unix-users-and-groups#oracle-inventory-group[oinstall]
code }
