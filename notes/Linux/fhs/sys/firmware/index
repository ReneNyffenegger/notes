$ /sys/firmware

`/sys/firmware` contains files that allows to view and manipulate firmware-specific objects and attributes.

Subdirectories that might be found under `/sys/firmware` include
  • `acpi`
  • `dmi`
  • → Linux/fhs/sys/firmware/efi[`efi`]
  • `memmap`

sa:

 `→ Linux/fhs/sys`
