$ /sys/bus/pci

Files and subdirectories found under `/sys/bus/pci` include
table { ll
  → Linux/fhs/sys/bus/pci/devices[`devices/`] ☰
  `drivers/` ☰
  `drivers_autoprobe` ☰
  `drivers_probe` ☰
  `rescan` ☰
  `resource_alignment` ☰
  `slots/` ☰
  → Linux/fhs/sys/bus/pci/uevent[`uevent`] ☰
table }

sa:

 The parent directory `→ Linux/fhs/sys/bus`

 → hardware/PCI
