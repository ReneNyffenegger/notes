$ /sys/class/input

The «files» under `/sys/class/input` are named `inputN`, `mouseN`, `mice` (and others also?).
-
These files are symlinks to files found in the tree under `→ Linux/fhs/sys/devices`.

Some of the file names found in `/sys/class/input` are also present under `→ Linux/fhs/dev/input`.

sa:
  → Linux/kernel/input
