$ /sys/fs/btrfs/{fs-guid}

«Files» and «directories» I've encountered under `/sys/fs/btrfs/{fs-guid}` include:
table { ll
  `allocation` ☰
  `bdi/` ☰ A sym link
  `bg_reclaim_threshold` ☰
  `checksum` ☰ When a filesystem is mounted the implementation backing the checksums is logged
  `clone_alignment` ☰
  `commit_stats` ☰ Number of commits and time related statistics.
  `devices/` ☰
  `devinfo/` ☰
  `discard/` ☰
  `exclusive_operation` ☰
  `features/` ☰
  `generation` ☰
  `label` ☰
  `metadata_uuid` ☰
  `nodesize` ☰
  `quota_override` ☰
  `read_policy` ☰
  `sectorsize` ☰
table }

sa:
  The parent directory `→ Linux/fhs/sys/fs/btrfs`
