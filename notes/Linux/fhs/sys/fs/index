$ /sys/fs

`/sys/fs` contains subdirectories for mounted(?) → Linux/filesystem[filesystems].

Some subdirectories I found under `/sys/fs` include:
table { ll
  `9p` ☰
  `bpf` ☰
  `btrfs` ☰
  → Linux/fhs/sys/fs/cgroup[`cgroup`] ☰ (and filesystem below) expose the → Linux/kernel/cgroup[kernel control group] hierarchy.
  `ext4` ☰
  → Linux/fhs/sys/fs/fuse` ☰
  `nfs` ☰
  → Linux/fhs/sys/fs/selinux` ☰
  `xfs` ☰
table }

sa:
 `→ Linux/fhs/sys`
