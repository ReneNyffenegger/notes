$ /sys/dev

`/sys/dev` contains the directories `→Linux/fhs/sys/dev/char` and `→Linux/fhs/sys/dev/block` which both contain
symlinks in the form `<major>:<minor>` which point to the sysfs directory for the given device.


