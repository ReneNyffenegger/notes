$ /sys/kernel/debug/tracing

sa:

 Because the *tracefs* filesystem is mounted also on `→ Linux/fhs/sys/kernel/tracing`, these directories have the same structure.

`→ Linux/shell/commands/lsof` needs readable access on `/sys/kernel/debug/tracing`.
