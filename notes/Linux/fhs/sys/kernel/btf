$ /sys/kernel/btf/

The files in the `/sys/kernel/btf` directory contain BTF type information and related data

sa:
  The viles `vboxvideo` and `vboxguest` in a → Companies-Products/Oracle/VM-VirtualBox[Virtual Box] guest.
