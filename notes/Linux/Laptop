$ Linux on laptops

{ Battery status #linux-battery-status

code {
$ upower -i $(upower -e | grep BAT) | grep --color=never -E "state|to\ full|to\ empty|percentage"
code }

  Checking the battery status via → Linux/fhs/sys/class/power_supply/BAT0/capacity.

}
