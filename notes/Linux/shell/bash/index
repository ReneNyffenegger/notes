$ bash
abbr: Bourne-Again Shell

With `set -o vi` on (?), the «next» command can be edited in $→ development/environment-variable#editor with `<Esc>Kv`.

{ TODO
 
  { programmable completion

    → Linux/fhs/usr/share/bash-completion/bash-completion

  }

}
