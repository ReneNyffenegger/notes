$ Bash: scripts

{ Count arguments

  The number of arguments with which a script was called is stored in `$#`.

  gh|about-Bash|/scripts/count-arguments||

}
{ Name of the script

  `$0` stores the name of the script.

  gh|about-Bash|/scripts/scriptname-in-dollar-0||

}

sa:

  → Linux/shell/commands/zenity to create message boxes in a shell script.

  → Linux/shell/bash

links:
  → https://vaneyckt.io/posts/safer_bash_scripts_with_set_euxo_pipefail/[Safer bash scripts with `set -euxo pipefail`]
