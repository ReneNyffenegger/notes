$ Bash built-in: command

{ Check if an executable is available

  The following `if` statement verifies that `→ Linux/X/clipboard/xclip` is available. If it is not,
  the script exits with the code 1:
code {
if ! command -v xclip >/dev/null 2>&1; then
  → Linux/shell/bash/built-in/echo xclip is not available
  exit 1
fi
code }

}

sa:
 `→ Linux/shell/bash/built-in/type`
