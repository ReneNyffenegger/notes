$ Shell command: kill
@ kill

`kill` terminats (which is an euphemism) a → Linux/kernel/process.

{ Quickly kill a process

Kill the process whose name contains `CMD`:
code {
$ kill -9  $(ps -ef | grep CMD | grep -v grep | awk '{print $2}')
code }

  But see → Linux/shell/commands/killall for such a thing.

}

sa:


  → Linux/kernel/signals

  → Linux/shell/commands/index[Shell commands]
