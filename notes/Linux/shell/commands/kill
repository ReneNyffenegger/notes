$ Shell command: kill
@ kill

`kill` terminats (which is an euphemism) a → Linux/kernel/process.

{ Quickly kill a process

Kill the process whose name contains `CMD`:
code {
$ kill -9  $(ps -ef | grep CMD | grep -v grep | awk '{print $2}')
code }

  But see also `→ Linux/shell/commands/killall` to kill a process by its name.

}

sa:

  → Linux/kernel/process/signal

  The → Windows/dirs/Windows/System32/cmd_exe[`cmd.exe`] equivalent seems to be → Windows/dirs/Windows/System32/taskkill_exe[`taskkill.exe`].

  → Linux/shell/commands/index[Shell commands]
