$ Shell command: sync
@ sync 

`sync` synchronizes cached writes to persistent storage.

`/bin/sync` is the shell of → Linux/user `sync` so that anyone who knows sync's → development/security/password can sync the system.
code {
$ → Linux/shell/commands/getent passwd sync | awk -F ':' '{print $NF}'
/bin/sync
code }

sa:

  → Linux/shell/commands[Shell commands]
