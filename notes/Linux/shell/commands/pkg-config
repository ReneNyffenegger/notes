$ Shell command: pkg-config
@ pkg-config

`pkg-config` provides information about installed libraries in the system and is useful in combination with compiling C/C++ files(?) and linking the resulting  object files.

sa:

 `→ Linux/fhs/usr/lib/pkgconfig`,
 `→ Linux/fhs/usr/share/pkgconfig`

  → Linux/shell/commands[Shell commands]
