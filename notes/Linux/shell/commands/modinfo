$ Shell command: modinfo
@ modinfo

Show information about a → Linux/kernel/module.

The current list of all → Linux/kernel/module#kernel-module-param[parameters] of a loadable module can be shown with
code {
modinfo -p $modulename
code }


sa:

  → Linux/kernel/module#module-shell-commands[Kernel module shell commands]

  → Linux/shell/commands[Shell commands]
