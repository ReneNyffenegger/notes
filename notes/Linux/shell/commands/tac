$ Shell command: tag
@ tac

`tac` (being → Linux/shell/commands/cat read from right to left) prints the lines of a file in reverse order.

sa:

  → Linux/shell/commands/rev

  → Linux/shell/commands/index[Shell commands]

