$ Shell command: host
@ host

`host` can be used to convert between IP addresses and names using → development/networking/DNS.

sa:

  → Linux/shell/commands/dig[`dig`],

  → Linux/shell/commands/index[Shell commands]
