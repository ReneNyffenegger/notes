$ Shell command: host
@ host

`host` can be used to convert between IP addresses and names using → development/networking/DNS.

code {
host -t A   renenyffenegger.ch
host -t MX  renenyffenegger.ch
host -t ANY renenyffenegger.ch
‥
code }

sa:

 `→ Linux/shell/commands/dig`

  → Linux/shell/commands[Shell commands]
