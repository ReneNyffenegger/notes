$ Shell command: xdg-open
@ xdg-open

`xdg-open` is a wrapper that opens a given file in the «correct» application.

code {
xdg-open some.html
xdg-open fizz-the-roly.png
# etc
code }

sa:
  → Linux/shell/commands/index[Shell commands]

  → Linux/X/XDG
