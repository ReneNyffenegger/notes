$ Shell command: par
@ par

sa:

  → https://github.com/ReneNyffenegger/shell-commands/tree/master/par

  `par` seems to be the *non greedy* equivalent(?) of `→ Linux/shell/commands/fmt`

  → Linux/shell/commands/index[Shell commands]
