$ Shell command: dc
@ dc

`dc` is a *desk calculator* with *arbitrary precision* whose input is formulated in *reverse-polish notation*:
code {
$ → Linux/shell/bash/built-in/echo 98 14 + p | dc
112
code }

sa:
 `→ Linux/shell/commands/bc`
