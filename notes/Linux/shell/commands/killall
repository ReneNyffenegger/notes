$ Shell command: killall
@ killall

`killall` terminates a → Linux/kernel/process by its name (as opposed to its → Linux/kernel/process#linux-pid[PID] as `→ Linux/shell/commands/kill` does).

sa:

  → Linux/shell/commands[Shell commands]
