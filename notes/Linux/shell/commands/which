$ Shell command: which
@ which


`which ${progname}` is equivalent to `→ Linux/shell/commands/type -P ${progname}`.

sa:
  → Linux/shell/commands/index[Shell commands]

  → development/languages/Perl/modules/_scripts_/whichpm[`whichpm`]: a cross-platform script that locates installed → development/languages/Perl/modules[Perl modules] by their package name.

  The Windows executable `→ Windows/dirs/Windows/System32/where_exe` and the PowerShell command `→ Windows/PowerShell/command-inventory/noun/command/get`.
