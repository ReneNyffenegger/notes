$ Shell command: parted
@ parted

`parted` is a program to create or manipulate → hardware/harddisk/partition-table[partition tables].

{ Source code

  The source code is hosted in a git repository: → http://git.savannah.gnu.org/cgit/parted.git

}

sa:

  → Linux/filesystem/shrink-and-grow

  → Linux/shell/commands/index[Shell commands]

  The → Windows command line command → Windows/dirs/Windows/System32/diskpart_exe[`diskpart.exe`].
