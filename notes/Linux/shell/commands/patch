
A patch is a non-binary file, created by → Linux/shell/commands/diff, that can be used to apply a change in another text document (usually source code).


rem {

  The `-s` argument causes the program to be silent except for errors.
 
  patch -sp1 < foo.patch

or
  patch  -p1 < ../patch...

Reversing a patch:
  patch -R -p1 < ../patch...

rem }
