$ Shell command: pstree
@ pstree

`pstree` prints the current → Linux/kernel/process[processes] as a tree (in parent-child relationship).

Compare with
  • → Linux/shell/commands/ps[`ps -f`] (which includes a process' parent id in the output) and → Linux/shell/commands/ps[`ps xaf`]
  • `→ Linux/shell/commands/sudo → Linux/shell/commands/systemctl status`


sa:

  → Linux/shell/commands[Shell commands]

  `pstree.X11`
