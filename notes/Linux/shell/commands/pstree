$ Shell command: pstree
-
@ pstree

`pstree` prints the current → Linux/kernel/process[processes] as a tree (in parent-child relation ship).

Compare with → Linux/shell/commands/ps[`ps f`] or
Compare with → Linux/shell/commands/ps[`ps xaf`].


sa:


  → Linux/shell/commands/index[Shell commands]
