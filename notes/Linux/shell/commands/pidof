$ Shell command: pidof
@ pidof

`pidof` finds a → Linux/kernel/process#linux-pid[process id] from a command name, for example:
code {
pidof systemd
code }

sa:

 `→ Linux/shell/commands/ps -C cmdlist`

  → Linux/shell/commands[Shell commands]
