$ Shell command: chroot
@ chroot

`chroot` is a way to change a process' top directory to another directory than the → Linux/fhs/root-directory.

Apparently, only → Linux/user/root can execute `chroot`, other users get a *cannot change root directory to …: Operation not permitted* error message.

sa:
  → Linux/shell/commands/index[Shell commands]
