$ Shell command: zcat
@ zcat

Get the uncompressed size for a compressed file:
code {
$ zcat file.Z | → Linux/shell/commands/wc -c
code }

`zcat` is a shell script that in its normal operation simply executes `exec → Linux/shell/commands/gzip -cd "$@"`

sa:

  → Linux/shell/commands[Shell commands]
