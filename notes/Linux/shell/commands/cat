$ Shell command: cat
@ cat

`cat` concatenates files and prints them on stdout.

{ Precede every line with the line number:

  Cat precedes every line with its line number when invoked with `-n`.

code {
cat -n file
code }

}

sa:

  Combining `cat` with a → Linux/shell/bash/scripts/here-document[here document] to → Linux/shell/bash/scripts/here-document#here-doc-create-file[create a file].

  → Linux/shell/commands/tac[`tac`]

  → Linux/shell/commands/index[Shell commands]
  
  In → Windows/dirs/Windows/System32/cmd_exe[`cmd.exe`], the contents of a file can be displayed with the
  → Windows/dirs/Windows/System32/cmd_exe/commands/type[`type`] command.
  -
  In → Windows/PowerShell, a file's content is returned by → Windows/PowerShell/command-inventory/noun/content/get[`get-content`].
