$ Shell command: ifup
-
@ ifup

`ifup` brings a network interface up.


`ifup foo` uses → Linux/fhs/etc/sysconfig/ifconfig_NIC[`/etc/sysconfig/ifconfig.foo`] to determine the settings it needs for the interface card.

sa:

  → Linux/fhs/sys/class/net

  → Linux/shell/commands/index[Shell commands]

