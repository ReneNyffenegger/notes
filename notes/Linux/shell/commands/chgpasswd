$ Linux shell command: chgpasswd
@ chgpasswd

`chgpasswd` changes → Linux/group → Linux/security/password[passwords] in batch mode.

sa:

  → Linux/shell/commands[Shell commands] such as `→ Linux/shell/commands/chpasswd`.
