$ Shell command: showkey
@ showkey


Used to find out the →hardware/keyboard#keycode[keycodes] (`showkey --keycode`) and →hardware/keyboard#scancode[scancodes] (`showkey --scancode`) of a key when pressed.

sa:

  →Linux/shell/commands/dumpkeys

  → Linux/shell/commands/evtest

  → Linux/shell/commands/index[Shell commands]
