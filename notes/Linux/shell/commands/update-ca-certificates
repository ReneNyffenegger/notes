$ Shell command: update-ca-certificates
@ update-ca-certificates

`update-ca-certificates` updates `→ Linux/fhs/etc/ssl/certs` with the certificates that are configured in `→ Linux/fhs/etc/ca-certificates_conf`.

sa:

 `→ Linux/shell/commands/certutil`

  → Linux/shell/commands[Shell commands]

? Is `→ Linux/fhs/usr/share/ca-certificates` related to `update-ca-certficates`

? It seems that `update-ca-certificates` is used on Ubuntu, Debian, OpenSUSE, SLES while its counterpart for Fedora, RHEL, CentOS is `update-ca-trust`.
