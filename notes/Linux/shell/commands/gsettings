$ Shell command: gsettings
@ gsettings

Configuration tool for GSettings.

GSettings is a frontend to backends such as *dconf* or *configuration files*. Apparently, GSettings is also capable of modifyint the → Windows/registry[Windows registry].

GSettings is a GLib implementation of DConf.


{ Is there a shortcut to open the terminal?

code {
gsettings list-recursively org.gnome.settings-daemon.plugins.media-keys | grep -i terminal
code }

}

sa:
  `dconf-editor`

  → Linux/shell/commands/index[Shell commands]
