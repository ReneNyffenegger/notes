$ Shell command: localedef
@ localedef

`localedef` compiles → Linux/locale definition files into a binary format which then is used by the C library functions `setlocale`, `localeconf` etc.

sa:

  → Linux/shell/commands[Shell commands] such as `→ Linux/shell/commands/locale-gen`
