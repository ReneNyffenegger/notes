$ Shell command: top
@ top

`top` displays the most CPU intensive → Linux/kernel/process[processes]:

sa:

  → Linux/shell/commands[Shell commands] (such as `→ Linux/shell/commands/ps`)

  `htop` is an *interactive* process viewer.
