$ Shell command: cut
@ cut

`-f` extracts the specified columns from *tab separated columns* file:
gh|shell-commands|/cut/f1,3||

`-d` specifies the delimiter (in the following command: a space):
gh|shell-commands|/cut/d__f1,3||

sa:

 `→ Linux/shell/commands/column` to create tabular data from an input stream.

 `→ development/languages/awk`

  → Linux/shell/commands[Shell commands]
