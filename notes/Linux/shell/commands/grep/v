$ Shell: find lines that don't match a regexp with grep -v

  `→ Linux/shell/commands/grep[grep] -v regexp file` prints all lines in `file` that *don't* match the
  → Linux/shell/bash/variables/regular-expressions[regular expression] `regexp`.

  `grep -v` is the abbreviation for `grep --invert-match`.

sa:
  → Linux/shell/commands/index[Shell commands]
