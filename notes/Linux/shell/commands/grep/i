$ Shell: search for a regex case insensitively with grep -i

`→ Linux/shell/commands/grep[grep] -i` is a → Linux/shell/commands/index[shell command] to *case-insenstively* search for a → Linux/shell/bash/variables/regular-expressions[regular expression].

`grep -i` is the abbreviation for `grep --ignore-case`.
