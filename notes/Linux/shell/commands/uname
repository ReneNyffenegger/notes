$ Shell command: uname
@ uname

`uname` prints information about the system.

{ Operating system

  `uname -o` prints the name of the operation system, for example `GNU/Linux`.

}
{ Host name

  `uname -n` prints the host name.

}
{ Kernel name

  `uname -s` prints the name of the kernel, for example `Linux`.

}
{ Kernel release

  `uname -r` prints the → Linux/kernel/release, for example `4.12.8-2-ARCH`.

}
{ CPU

  `uname -m` prints the processor type, for example `x86_64`.

}

sa:
  → Linux/shell/commands/index[Shell commands]
