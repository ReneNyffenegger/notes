$ Shell command: insmod
@ insmod

`insmod` inserts a → Linux/kernel/module[module] into the → Linux/kernel[kernel].

An more clever alternative to `insmod` that also can handle dependencies is `→ Linux/shell/commands/modprobe`.

sa:

  → Linux/kernel/module/index#module-shell-commands[Kernel module shell commands]
  
  → Linux/shell/commands/index[Shell commands]

