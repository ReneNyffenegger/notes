$ Shell command: arch
@ arch

Print the *machine hardware name*, for example `x86_64`.

`arch` has the same output as → Linux/shell/commands/uname[`uname -m`].

sa:
  → Linux/shell/commands/index[Shell commands]
