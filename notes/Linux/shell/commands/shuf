$ Shell command: shuf
@ shuf

`shuf` permutate lines from → development/stdin-stdout-stderr[stdin] and write result to → development/stdin-stdout-stderr[stdout].

Choose three random numbers between 1 and 10:
code {
$ for i in {1..10} ; do echo $i ; done | shuf -n 3
code }

sa:

  → Linux/shell/commands/index[Shell commands]
