$ Shell command: mcookie
-
@ mcookie

`mcookie` creates a magic cookie, or more precisely a 128 bit random number in hexadecimal representation, for use with → Linux/shell/commands/X/xauth[`xauth`].

sa:
  → Linux/shell/commands/index[Shell commands]
