$ Shell command: xinput
@ xinput

`xinput` is a utility for configuring and testing XInput devices.

`xinput list` (or simply `xinput` ?) lists all input devices. It seems that, at least for touchpoints, the shown values can be used for the `-p` property of `xfconf-query -c pointers -p …`.
