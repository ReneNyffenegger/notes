$ Shell command: udevadm
@ udevadm

A → Linux/udev administration tool that controls the runtime behaviour of the *udevd* daemon, can access the udev database (→ Linux/fhs/etc/udev/hwdb_bin?) monitors uevents and waits for them to finish etc…

sa:

  → Linux/shell/commands/index[Shell commands]
