$ Shell command: lshw
@ lshw

`lshw` lists a system's → hardware configuration.

sa:

 → hardware

 Other → Linux/shell/commands[shell commands] such as `→ Linux/shell/commands/hwinfo`, `→ Linux/shell/commands/lsusb` or `→ Linux/shell/commands/lspci`.
