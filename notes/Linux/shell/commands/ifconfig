$ Shell command: ifconfig
@ ifconfig

`ifconfig` is deprecated in favor of `→ Linux/shell/commands/ip` (?) and `→ Linux/shell/commands/iw`.

sa:

  → Linux/networking[Networking]

  → Linux/shell/commands[Shell commands]
