$ Shell command: grub-install
@ grub-install

`grub-install` installs → Linux/booting/GRUB/index on a → hardware/device.

sa:
  `→ Linux/shell/commands/grub-mkconfig` and other → Linux/shell/commands[shell commands].
