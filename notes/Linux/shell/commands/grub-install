$ grub-install

`grub-install` install → Linux/booting/GRUB/index on a → hardware/device.

`grub-install` is a shell script, the actual work is done by → Linux/shell/commands/grub-mkimage and → Linux/shell/commands/grub-setup.
