$ Shell command: lpinfo
-
@ lpinfo


`lpinfo` lists available devices and drivers that are known to the → Linux/CUPS[CUPS] server.

`lpinfo -v` lists all devices.

`lpinfo -m` lists all drivers.


sa:

  → Linux/shell/commands/lpadmin[`lpadmin`]

  → Linux/shell/commands/index[Shell commands]
