$ Shell command: lsof
@ lsof

`lsof` lists open files

`lsof` needs to be able to read `→ Linux/fhs/sys/kernel/debug/tracing`

sa:
 `→ Linux/shell/commands/fuser`

  → Linux/shell/commands[Shell commands]
