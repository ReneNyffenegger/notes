$ Shell command: lsblk
@ lsblk

`lsblk` list → Linux/devices#block-devices[block devices].

{ -f

  `lsblk -f` reports information about the file system, such as its → Linux/filesystem#determine-fs-type[type].

}

sa:

  → Linux/shell/commands/index[Shell commands]
