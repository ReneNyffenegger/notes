$ Shell command: make-ca.sh
-
@ make-ca.sh

`make-ca.sh` is a script to populate OpenSSL's CApath from a bundle of PEM formatted CAs.

The script requires the file `certdata.txt` to exist in the local directory.

sa:
  → Linux/shell/commands/index[Shell commands]

