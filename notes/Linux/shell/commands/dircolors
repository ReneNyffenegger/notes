$ Shell command: dircolors
@ dircolors

Print `LS_COLORS=…` followed by `export LS_COLORS`, used to be `eval`ed.
code {
$ dircolors -b
code }

Print database:
code {
$ dircolors -p
code }

sa:
  → Linux/shell/commands/ls

  → Linux/shell/commands/index[Shell commands]
