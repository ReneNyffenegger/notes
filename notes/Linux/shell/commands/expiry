$ Shell command: expiry
@ expiry

`expiry` checks an forces the current → Linux/security/password expiration policy.

sa:

  → Linux/shell/commands[Shell commands] such as
 `→ Linux/shell/commands/chage` or `unix_chkpwd`.
