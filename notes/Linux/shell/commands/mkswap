$ Shell command: mkswap
@ mkswap

`mkswap` initializes a swap partition (see → Linux/swap-space[swap space]).

sa:

 `→ Linux/shell/commands/swapon` activates swap space.

  → Linux/shell/commands[Shell commands]
