$ Shell command: vmstat
@ vmstat

`vmstat` reports virtual memory statistics (paging, block I/O, traps, CPU activity etc.)

{ TODO

  The amount of memory set aside for *buffered I/O* is shown under `cache` (heading `memory`) with
code {
vmstat -S m
code }

}

sa:

  → Linux/shell/commands[Shell commands]
