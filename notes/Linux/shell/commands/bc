$ Shell command: bc
@ bc

An arbitrary precision calculator language.

{ Floating point

  Use the `-l` flag (`bc -l`) or the `scale` special variable:

gh|shell-commands|/bc/testfiles/floating-point-yes|

}


sa:
  → Linux/shell/commands/index[Shell commands]
