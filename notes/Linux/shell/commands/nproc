$ Shell command: nproc
@ nproc


Print the number of processing unites (cores, processors).

sa:

  → development/make/index#multiple_processors[make's j flag].

  → Linux/shell/commands/index[Shell commands]
