$ Shell command: nproc
@ nproc

`nproc` reports the number of processing units (cores and/or → hardware/CPU[CPUs]) available to the current → Linux/kernel/process.

sa:

  The number of CPUs might be passed to → development/make#multiple_processors[make's `j` flag] like so:
code {
$ make -j $(nproc --all)
code }

 `→ Linux/shell/commands/lscpu` and other → Linux/shell/commands[Shell commands].
