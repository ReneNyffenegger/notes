$ Shell command: history
@ history

`history` displays the → Linux/shell/bash/history[command line history] of previously typed commands.

Delete a specific command from the history:
code {
history -d 42
code }

sa:
  → Linux/shell/commands[Shell commands]

  → Linux/shell/bash/history
