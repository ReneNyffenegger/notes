$ Shell command: history
@ history



View the → Linux/shell/bash/history[command line history] of previously typed commands.

Delete a specfic command from the history:
code {
history -d 42
code }

sa:
  → Linux/shell/commands/index[Shell commands]

  → Linux/shell/bash/history
