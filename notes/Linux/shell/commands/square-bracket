$ Shell command: [
@ [

`[` is a regular executable (`/usr/bin/[`).

`[ xxx ]` is equivalent to `→Linux/shell/commands/test xxx`.

`[` can can be used, for example, in an → Linux/shell/bash/if[if] statement.


In → Linux/shell/bash/, `[[` is a built-in.

sa:
  → Linux/shell/commands/test

  → Linux/shell/commands/index[Shell commands]
