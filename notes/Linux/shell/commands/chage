$ Shell command: chage
@ chage

`chage` changes a → Linux/user[user's] → Linux/security/password expiry information.

sa:
  → Linux/shell/commands[Shell commands] such as
 `→ Linux/shell/commands/expiry` or `unix_chkpwd`.
