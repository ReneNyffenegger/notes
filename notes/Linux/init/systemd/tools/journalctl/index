
`journalctl` prints log entries from → Linux/init/systemd[systemd's] journal.

Show the execution times of a *one shot* service:
code {
$ → Linux/shell/commands/sudo journalctl -u → development/security/cryptography/SSL-TLS/Lets-Encrypt#certbot[certbot]
code }

sa:

 `→ Linux/fhs/var/log/journal`

  → Linux/shell/commands[Shell commands]
