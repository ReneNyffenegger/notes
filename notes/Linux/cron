
cron: the job scheduler, allows → Linux/user[users] to schedule jobs (comands, shell scripts) so that the run periodically or at given dates.

Often, cron is used for system maintenance/administration purposes.
