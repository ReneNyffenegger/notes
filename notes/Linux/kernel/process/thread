
In Linux, a *thread* is almost the same as a → Linux/kernel/process[process] with the major difference being that threads share the same memory virtual memory address space.

{ TODO

  NPTL: Native → Linux/POSIX Threads Library.

}
