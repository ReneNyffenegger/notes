$ drivers/firmware/efi/libstub/

Directory containing → hardware/UEFI[EFI stub code] shared among architectures.

The `Makefile` in this directory contains the following comment:
"
  The stub may be linked into the kernel proper or into a separate boot binary,
  but in either case, it executes before the kernel does (with MMU disabled) …
"
