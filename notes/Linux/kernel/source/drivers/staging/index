$ drivers/staging

`drivers/staging` contains work-in-progress sources whose finished version is likely to be accepted to the kernel proper.

sa:
  The `STAGING SUBSYSTEM` → Linux/kernel/source/_topics/subsystems[subsystem].
