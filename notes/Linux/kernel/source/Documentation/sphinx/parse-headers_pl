$ Documentation/sphinx/parse-headers.pl

`Documentation/sphinks/parse-headers.pl` convert a C → development/languages/C-C-plus-plus/C/header-files[header] or source file  into *ReStructured Text* (RST) 

code {
$ Documentation/sphinx/parse_headers.pl [<options>] <C_FILE> <OUT_FILE> [<EXCEPTIONS_FILE>]
code }


The scirpt is capable of identifying defines, functions, structs, typedefs,
enums and enum symbols and create cross-references for all of them.
-
It is also capable of distinguish `#define` used for specifying a Linux ioctl.

sa:
  Scripts located under → Linux/kernel/source/scripts[`scripts/`]

links:
  → https://github.com/torvalds/linux/blob/master/Documentation/sphinx/parse-headers.pl
