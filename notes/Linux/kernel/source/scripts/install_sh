$ scripts/install.sh

sa:

 `scripts/install.sh` seems to be called via `→ Linux/kernel/compilation/make/install`

 `→ Linux/fhs/sbin/installkernel`, adapted from code in `arch/i386/boot/Makefile`.

  → Linux/kernel/source/scripts[`scripts/`]
