$ scripts/setlocalversion

`scripts/setlocalversion` adds local version information from the version control systems git, mercurial (hg) and subversion (svn).

sa:

  This script is somehow related to → Linux/kernel/CONFIG/LOCALVERSION_AUTO.

  Other → Linux/kernel/source/scripts[scripts]
