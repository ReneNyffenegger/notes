$ scripts/head-object-list.txt

`scripts/head-object-list.txt` contains the paths and names of object files that will be placed at the head of `→ Linux/kernel/source/vmlinux`.
-
Thus, the content of the file is used when `→ Linux/kernel/source/vmlinux_a` is produced.

sa:
 `→ Linux/kernel/source/scripts`
