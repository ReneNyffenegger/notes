$ scripts/Makefile.build

code {
make -f ./scripts/Makefile.build   obj=…   path/to/target
code }

{ TODO

 `scripts/Kbuild.include` defines the variable → https://github.com/torvalds/linux/blob/ce9ecca0238b140b88f43859b211c9fdfd8e5b70/scripts/Kbuild.include#L114[`build := -f $(srctree)/scripts/Makefile.build obj`] which can be used with
code {
$(Q)$(MAKE) $(build)=dir
code }

  What is the relation of `scripts/Makefile.build` to `tools/build/Makefile.build`

}

sa:
  The directory `→ Linux/kernel/source/scripts`
