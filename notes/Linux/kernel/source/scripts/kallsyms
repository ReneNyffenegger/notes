$ scripts/kallsyms

`scripts/kallsyms` is a binary that creates assembler source code that contains *symbol information* (see also → Linux/kernel/source#linux-kallsyms[kallsyms])

This binary is creted from the source file `→ Linux/kernel/source/scripts[scripts]/kallsyms.c`.

sa:

  The function `kallsyms` in `→ Linux/kernel/source/scripts/link-vmlinux_sh`
