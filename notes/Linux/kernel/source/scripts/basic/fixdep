$ scripts/basic/fixdep

{ Usage

code {
fixdep <depfile> <target> <cmdline>
code }

  The indendended usage is to redirect the program's output which is printed to stdout to `.*.cmd` files

}

sa:

 `fixdep` is created from `→ Linux/kernel/source/scripts/basic/fixdep_c`
