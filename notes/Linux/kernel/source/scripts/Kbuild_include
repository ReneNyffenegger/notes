$ scripts/Kbuild.include

`scripts/Kbuild.include` contains generic definitions to be included in other makefiles (such as *the* `→ Linux/kernel/source/Makefile`).

sa:

  → Linux/kernel/source/scripts[`scripts/`]
