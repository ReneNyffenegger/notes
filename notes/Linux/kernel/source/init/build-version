$ init/build-version

`init/build-version` is a shell script that increases the number found in `→ Linux/kernel/source/_version`.

If `.version` does not exist, the script initializes the content of `.version` to 1.

links:
  → https://github.com/torvalds/linux/blob/master/init/build-version[`init/build-version`]
