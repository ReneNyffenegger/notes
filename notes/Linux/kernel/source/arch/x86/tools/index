$ arch/x86/tools

{ relocs #linux-x86-relocs

? One (or *the*?) tool that is built in `arch/x86/tools` is *relocs* which is used to create  `→ Linux/kernel/source/arch/x86/boot/compressed/vmlinux_relocs` from `→ Linux/kernel/source/vmlinux`.

}

{ TODO

  Apparently, arch/x86/tools/relocs.* contains prototypes for ELF editing.

}

