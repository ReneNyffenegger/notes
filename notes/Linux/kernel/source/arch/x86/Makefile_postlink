$ arch/x86/Makefile.postlink

The purpose of `arch/x86/Makefile.postlink` is to
  • separate relocations from `→ Linux/kernel/source/vmlinux` into `vmlinux.relocs`
  • strip relocations from vmlinux.
