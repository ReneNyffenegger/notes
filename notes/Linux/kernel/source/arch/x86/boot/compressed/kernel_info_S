$ arch/x86/boot/compressed/kernel_info.S

{ kernel_info

 `kernel_info` is a symbol at the start of the following «field»:
  table { ll
    `LToP` ☰ Magic, 4 Bytes (`0x506f544c`)
     Size of `kernel_info` ☰ 4 Bytes
     Maximal allowed type for `setup_data` and `setup_indirect` structs ☰ Value of `SETUP_TYPE_MAX`, 4 Bytes

  table }

}
