$ arch/x86/kernel/irq.c

Common interrupt code for 32 and 64 bit

Defines `do_IRQ` which handles all normal device IRQs. (Special SMP cross-CPU interrupts have their own handlers)


sa:
  → Linux/kernel/interrupts
