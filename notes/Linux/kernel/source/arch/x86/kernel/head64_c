$ arch/x86/kernel/head64.c

Preparation to run common code.

{ x86_64_start_kernel() #linux-axk-x86_64_start_kernel

 `x86_64_start_kernel()` is declared as `__noreturn`.
  -
  The last function called is `x86_64_start_reservations` whose last function called is → Linux/kernel/source/init/main_c#linux_start_kernel[`start_kernel()`] (defined in `→ Linux/kernel/source/init/main_c`)

 `x86_64_start_kernel` also contains some sanity checks on the kernel image and module area mappings which are checked at *build-time* produce no code.


}
