$ .mailmap

sa:

 `.mailmap` is read by `→ Linux/kernel/source/scripts/get_maintainer_pl`

  → development/version-control-systems/git/commands/_options/format-pretty[Git's `--format`] options `%N`, `%E` and `%L` print an authors name, eMail and eMail portion left of the at sign with respect to `.mailmap`.
