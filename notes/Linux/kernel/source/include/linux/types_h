$ include/linux/types.h

{ struct list_head #linux-list_head

code {
struct list_head {
        struct list_head *next, *prev;
};
code }

  Some functions to manipulate a `list_head` seem to be found in `→ Linux/kernel/source/include/linux/list_h`.

  A `list_head` is found, for example, in → Linux/kernel/source/include/linux/kobject_h#linux-kobject[`struct kobject`]
  or → Linux/kernel/source/include/linux/kobject_h#linux-kset[`struct kset`].

}
