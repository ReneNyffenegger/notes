$ vmlinux.a

`vmlinux.a` is created when the kernel is → Linux/kernel/compilation/make[made]. In an x64 architecture, the commands to create `vmlinux.a` is a variation of
code {
→ development/GNU/toolchain/binutils/ar cDPrST vmlinux.a ./built-in.a  lib/lib.a  arch/x86/lib/lib.a
→ development/GNU/toolchain/binutils/ar mPiT $(ar t vmlinux.a | sed -n 1p) vmlinux.a $(ar t vmlinux.a | grep -F -f ./scripts/head-object-list.txt)
code }

`vmlinux.a` is used to produce `→ Linux/kernel/source/vmlinux_o`.

sa:

 `→ Linux/kernel/source/vmlinux`

  → Linux/kernel/source
