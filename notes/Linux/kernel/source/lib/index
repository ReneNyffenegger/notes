$ lib/

{ kstrtox.c : Convert strings to integers (or -E)

 `kstrtoull`, `kstrtoll`, `_kstroul`, `_kstrtol`, `kstrouint`, `kstrtoint`, `kstrtou16`, `kstrtos16`, `kstrtou8`, `kstrtos8`
 `kstrtobool`, `kstrtobool_from_user`

  See also `include/linux/kstrtox.h`

}
{ string_helpers.c : Formatting and printing strings

 `string_get_size` - get the size in the specified units

  …


}
{ vsprintf.c : String conversion functions

 `simple_strtoX` functions.
  -
 `X` = `ull` (`unsigned long long`), `ul` (`unsigned long`), `l` (`signed long`) or `ll` (`signed long long`).
  -
  These functions have caveats, `kstrtoX` should be used.

  `Xprintf` functions.
  `X` = `vsn`, `vscn`, `sn`, `scn`, `vs`, `s`, `vbin_`, `bstr_`, `b`
  
  `Xscanf` functions.
   -
  `X` = `vs`, `s`

}

sa:
 → Linux/kernel/source
