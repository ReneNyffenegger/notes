$ .version

`.version` contains a number that is increased when `init/build-version` is run.

If the file does not exist, `init/build-version` initializes it with 1.

The number in `.version` is reflected in `→ Linux/kernel/source/include/generated/utsversion_h`.
