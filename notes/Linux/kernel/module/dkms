$ Dynamic Kernel Module Support (DKMS)
@ DKMS

Apparently, DKMS is useful and/or needed for proprietary drivers (especially graphic cards, RAID controllers and virtualization).

sa:

  → Linux/fhs/var/lib/dkms

  → Linux/kernel/module
