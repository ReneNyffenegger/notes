$ Kernel compilation: make install
@ install

`→ Linux/kernel/compilation/make install` installs the → Linux/kernel using (your) `~/bin/installkernel` or (distribution) `→ Linux/fhs/sbin/installkernel` or install to `$(INSTALL_PATH)` and run → Linux/booting/LILO.
