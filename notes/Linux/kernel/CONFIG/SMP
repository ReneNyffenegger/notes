$ CONFIG_SMP

Symmetric multi-processing support: used when system has more than one → hardware/CPU.

With more than 8 CPUs, use also → Linux/kernel/CONFIG/X86_BIGSMP.

sa:
  → Linux/kernel/locks/spinlock
