
A *uevent* is a message that is created and sent when a → hardware/device is inserted or removed.

A uevent message is a readable text string. It is sent via → Linux/IPC/Netlink.

Usually, such a uevent is handled by udevd.
