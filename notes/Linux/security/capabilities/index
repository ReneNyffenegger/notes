$ Linux capabilities

{ TODO

code {
$ grep Cap → Linux/fhs/proc/pid/status[/proc/$container_pid/status]
code }

}

sa:

  `setcap`, `getcap`

  `CAP_SYS_PTRACE` and `→ Linux/fhs/proc/sys/kernel/yama/ptrace_scope`.

 `→ development/tools/Docker/commands/run --cap-add`
