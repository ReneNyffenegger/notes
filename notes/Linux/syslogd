
Syslogd is a logging → Linux/daemons[daemon]. It  provices logging of messages received from programs and facilities on the local host as well as from remote hosts. 

sa:

  → Linux/fhs/etc/syslog_conf is the main configuration file for syslogd.

  → Linux/shell/commands/logger

  → Linux/logging/klogd
