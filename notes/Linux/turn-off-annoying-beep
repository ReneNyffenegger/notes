
{ Bash

  Either `set bell-style none` in `→ Linux/fhs/etc/inputrc` or `setterm -blength 0` in `→Linux/fhs/home/username/_bash_profile`.

  I also had some success with `bind 'set bell-style none'`.

}

{ Vim

  `:set t_vb=` and `:set vb`

}

sa:
  → Linux/sound/index#linux-get-rid-beep
