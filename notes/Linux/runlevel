
The *run level* determines which sub systems to run.

The runlevel is changed with `/sbin/telnit`.

The runlevels are: 1) Single-user, 2) ?, 3) Multi-user / no GUI, 4) undefined, 5) Multi-user with GUI, 6) Reboot

{ See also

  → Linux/init

}
