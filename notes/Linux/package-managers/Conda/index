

{ List availabe environments

code {
conda env list
conda info --envs
code }

  It seems that environments are stored under → Linux/fhs/home/username/miniconda3[`~/miniconda3]/envs`, `→ Linux/fhs/home/username/_conda`

}
{ Switching to a given environment

code {
conda activate ‥
code }

}
{ Exiting an environment

code {
(base) $ conda deactivate
$
code }

}
{ Environment variables:

 table { ll
   `CONDA_EXE` ☰ `/home/rene/miniconda3/bin/conda`
   `CONDA_PREFIX` ☰ `/home/rene/miniconda3`
   `CONDA_PROMPT_MODIFIER` ☰ `(base) `
   `_CE_CONDA` ☰
   `CONDA_SHLVL` ☰ `1`
   `CONDA_PYTHON_EXE` ☰ `/home/rene/miniconda3/bin/python`
   `CONDA_DEFAULT_ENV` ☰ `base`
 table }

 Why does conda modify `→ Linux/fhs/home/username/_bashrc`? Is this necessary and/or can it be turned off?

}

sa:
 `→ Linux/fhs/home/username/miniconda3`, `→ Linux/fhs/home/username/_conda`

  → Linux/package-managers
