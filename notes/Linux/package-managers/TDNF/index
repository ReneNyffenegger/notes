$ Tiny dandified

Tiny dandified (tdnf, or also *tiny dandified YUM*?) is a → Linux/package-managers.

This package manager is a C implemenation of → Linux/package-managers/DNF (that implements a subset of the dnf commands) without → development/languages/Python dependencies (DNF, in turn, is based on YUM).

The configuration file is `/etc/tdnf/tdnf.conf`

sa:
  tndf is the package manager for → Linux/distributions/CBL-Mariner.
