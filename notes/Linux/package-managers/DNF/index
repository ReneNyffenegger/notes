
*DNF* = *Dandified YUM* (which is a fancy way to say that DNF is the «next-generation version of the Yellowdog Updater, Modified (yum)» is a
→ Linux/package-managers for RPM based Linux → Linux/distributions[distributions].

DNF has been the default command-line package manager for Fedora since version 22, which was released in May 2015.

sa:

  `→ Linux/fhs/etc/dnf/dnf_conf`

  → Linux/package-managers
