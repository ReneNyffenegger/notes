$ apt build-dep

? `apt build-dep` is used to fetch all dependencies that be needed to build a → Linux/package-managers/APT#apt-pkg-bin-vs-src[source (as opposed to binary) package].

sa:

 `→ Linux/package-managers/APT/commands/apt`, `→ Linux/package-managers/APT/commands/apt-src`
