$ apt list

`→ Linux/package-managers/APT/commands/apt list` finds packages based on package name patterns. (similar to `→ Linux/package-managers/dpkg/commands/dpkg-query --list`).
-
The patterns support globbing.

code {
$ apt list 'apt-*'
code }


sa:
 `→ Linux/package-managers/APT/commands/apt`
