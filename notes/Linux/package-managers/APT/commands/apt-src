$ apt-src

`apt-src` is used to download, install, upgrade and track → Linux/package-managers/APT#apt-pkg-bin-vs-src[source packages].

sa:

 `→ Linux/package-managers/APT/commands/apt/build-dep`

 `dpkg-source`

 `~/.apt-src/status`, `~/.apt-src/config`.
