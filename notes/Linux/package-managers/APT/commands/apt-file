$ apt-file

`apt-file` is a utility to search for → Linux/package-managers/APT#APT-search-pkg[APT packages].

code {
apt-file [options] search pattern
apt-file [options] → #apt-file-show   package
code }

`apt-file list` is an alias for `apt-file show`, `apft-file find` for `apt-file search`.

{ apt-file show #apt-file-show

  `apt-file show` lists the contents of a package in a similar way like `dpgk -L` except that the packae does not be to installed of fetched.

}

sa:

  → Linux/package-managers/APT
