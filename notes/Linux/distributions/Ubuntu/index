
Ubuntu builds on → Linux/distributions/Debian[Debian's] architecture.

{ Checking the Ubuntu version

code {
$ → Linux/shell/commands/lsb_release -a
code }

}

{ TODO

  { Version number LTS

    The first part of the version number indicates the release year. (For example `17.04` was released in 2017).

    LTS stands for *Long Time support*.

  }

  { restart network

code {
sudo service network-manager restart
code }

  }

}
