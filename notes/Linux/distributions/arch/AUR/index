$ Arch Linux User Repository
@ AUR

→ https://aur.archlinux.org/.

Installing AUR packages apparently needs the *base-devel* group to be installed.

rem {

Apparently yaourt is outdated

{ Yaourt

  Apparently, the AUR is interfaced with `yaourt` (Yet AnOther User Repository Tool).

  Yaourt is installed with → Linux/distributions/arch/pacman:
code {
pacman -Sy yaourt
code }

  Sync yaourt with the AUR:
code {
yaourt -Syy
code }

}
rem }

sa:
  → Linux/distributions/arch/pacman

  → Linux/fhs/etc/makepkg_conf
