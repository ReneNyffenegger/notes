abbr: package manager for Debian


{ -L

  `dpkg -L` lists files that are »owned« by a package. Thus, it's possible to find the executables of a package with
code {
$ dpgk -L *package-name* | → Linux/shell/commands/grep[grep] /bin
code }

}

{ TODO

  → Linux/APT/[apt] is a high level user friendly utility to operate with dpkg.

code {
sudo dpkg-reconfigure tzdata
code }

}

sa:
  → Linux/fhs/var/lib/dpkg

  → Linux/deb-rpm
