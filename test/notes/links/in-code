$ Links in code

The following code fragment `→ links/links` as well as this code fragment `→links/links` as well as this code fragment `→links/links -etc abc` as well as
this code fragment `etc →links/links foo` point to the same destination.

code {
Now, let's see if
this link →links/within-stars also
works.
code }

code {
Same thing,
but →links/within-stars[link with brackets].
code }
